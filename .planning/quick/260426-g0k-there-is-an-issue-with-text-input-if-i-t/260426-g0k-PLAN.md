---
status: complete
task: "There is an issue with text input. If I type five characters and backspace to erase the whole string, the first character is always present and impossible to replace, and if I begin typing more characters, that first character gets pushed to the right by the inserted characters"
phase: quick-260426-g0k
plan: 1
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - vendor/raxol/lib/raxol/ui/components/input/text_input.ex
  - test/foglet_bbs/tui/widgets/input/text_input_test.exs
must_haves:
  truths:
    - "Backspace at cursor position 1 removes the first character from the input value."
    - "Deleting all characters via backspace leaves cursor at position 0 and empty value."
    - "Typing after a full erase inserts at position 0 without preserving old characters."
  artifacts:
    - path: vendor/raxol/lib/raxol/ui/components/input/text_input.ex
      provides: "Raxol text input backspace edit logic"
      exports:
        - "handle_backspace_operation/4"
    - path: test/foglet_bbs/tui/widgets/input/text_input_test.exs
      provides: "Regression coverage for backspace-then-retype behavior"
      exports:
        - "backspace to empty then replace preserves replacement text"
key_links:
  - from: vendor/raxol/lib/raxol/ui/components/input/text_input.ex
    to: vendor/raxol/lib/raxol/ui/components/input/text_input.ex
    via: "handle_backspace_operation(true, state, current_pos, current_value)"
    pattern: "String.slice"
---

# Plan: Fix TextInput backspace delete behavior at cursor start

## Goal

Fix single-line text input editing so backspace can delete at cursor position 1 without leaving a phantom leading character, and ensure typing after fully deleting the input inserts at cursor position 0.

## Scope

- Update `RaxolTextInput.EditingHandler` in `vendor/raxol/lib/raxol/ui/components/input/text_input.ex` to use safe prefix slicing for backspace.
- Add a focused regression test in `test/foglet_bbs/tui/widgets/input/text_input_test.exs`.

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Fix backspace prefix slicing</name>
  <files>vendor/raxol/lib/raxol/ui/components/input/text_input.ex</files>
  <action>Change backspace prefix calculation from a range slice (`0..(current_pos - 2)`) to a length slice that safely handles `current_pos == 1`, so the first character can be deleted.</action>
  <verify>None (surgical one-line change).</verify>
  <done>Backspace now handles first-position deletion and no longer preserves a first character when all chars are erased.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression test for erase-and-replace flow</name>
  <files>test/foglet_bbs/tui/widgets/input/text_input_test.exs</files>
  <action>Type five chars, backspace five times, then type a replacement char. Assert final value is replacement-only and cursor ends at 1.</action>
  <verify>mix test test/foglet_bbs/tui/widgets/input/text_input_test.exs</verify>
  <done>New test fails on old behavior and passes after the slice fix.</done>
</task>

## Validation

- `mix test test/foglet_bbs/tui/widgets/input/text_input_test.exs`
