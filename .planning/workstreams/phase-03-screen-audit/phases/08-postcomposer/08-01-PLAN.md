---
phase: 08-postcomposer
plan: 01
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - lib/foglet_bbs/tui/screens/post_composer.ex
  - test/foglet_bbs/tui/screens/post_composer_test.exs
autonomous: true
requirements:
  - COMPOSER-01
  - COMPOSER-02
  - COMPOSER-03
  - COMPOSER-04
  - COMPOSER-05
user_setup: []
must_haves:
  truths:
    - "PostComposer submit flow uses a with chain for create_reply while preserving all existing modal error branches and success behavior."
    - "A source-order warning comment exists above PostComposer handle_key/2 clauses to protect Ctrl+S/Ctrl+C interception order."
    - "Inline `{80, 24}` fallbacks are replaced by `@default_terminal_size` in post_composer.ex."
    - "Compose + MultiLineInput body behavior is unchanged."
    - "mix precommit passes without new suppressions."
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/post_composer.ex"
      provides: "With-chain submit path, source-order warning note, and terminal-size attr cleanup"
      contains: "@default_terminal_size {80, 24}"
    - path: "test/foglet_bbs/tui/screens/post_composer_test.exs"
      provides: "Regression coverage for with-chain branch parity and source-order-sensitive behavior"
      contains: "describe \"do_submit/3 on success (reply-jump)\""
  key_links:
    - from: "lib/foglet_bbs/tui/screens/post_composer.ex"
      to: "lib/foglet_bbs/tui/widgets/compose.ex"
      via: "forwarded body input events"
      pattern: "Compose.translate_key"
    - from: "lib/foglet_bbs/tui/screens/post_composer.ex"
      to: "lib/foglet_bbs/tui/screens/domain.ex"
      via: "posts module resolution"
      pattern: "Domain.get"
    - from: "test/foglet_bbs/tui/screens/post_composer_test.exs"
      to: "lib/foglet_bbs/tui/screens/post_composer.ex"
      via: "submit/cancel parity assertions"
      pattern: "PostComposer.handle_key"
---

<objective>
Deliver Phase 8 by rewriting PostComposer submit pipeline to a `with` chain and adding source-order guard documentation while preserving all current user-visible behavior.

Purpose: complete `COMPOSER-01..05` with a minimal-risk refactor that does not alter compose-body flow or modal semantics.
Output: only `post_composer.ex` and `post_composer_test.exs` are modified; phase grep/rubric gates and `mix precommit` are green.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/phase-03-screen-audit/STATE.md
@.planning/workstreams/phase-03-screen-audit/ROADMAP.md
@.planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
@.planning/workstreams/phase-03-screen-audit/phases/08-postcomposer/08-CONTEXT.md
@.planning/workstreams/phase-03-screen-audit/phases/08-postcomposer/08-RESEARCH.md
@.planning/workstreams/phase-03-screen-audit/phases/08-postcomposer/08-PATTERNS.md
@.planning/workstreams/phase-03-screen-audit/phases/08-postcomposer/08-UI-SPEC.md
@.planning/workstreams/phase-03-screen-audit/phases/08-postcomposer/08-VALIDATION.md
@AGENTS.md

@lib/foglet_bbs/tui/screens/post_composer.ex
@test/foglet_bbs/tui/screens/post_composer_test.exs
@lib/foglet_bbs/tui/widgets/compose.ex
@lib/foglet_bbs/tui/screens/domain.ex
</context>

<interfaces>
PostComposer contracts that must be preserved:

```elixir
@spec init_screen_state(keyword()) :: map()
@spec render(map()) :: any()
@spec handle_key(map(), map()) :: {:update, map(), list()} | :no_match
```

Submit success contract:

```elixir
{:update, new_state, [{:load_posts, thread.id, jump_last: true}]}
```

Required source-order warning text:

```elixir
# NOTE: source order matters for handle_key/2 clauses — do not reorder.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor PostComposer submit path to with-chain and add source-order guard note</name>
  <files>lib/foglet_bbs/tui/screens/post_composer.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/post_composer.ex
    - .planning/workstreams/phase-03-screen-audit/phases/08-postcomposer/08-CONTEXT.md
    - .planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
    - .planning/workstreams/phase-03-screen-audit/phases/07-newthread/07-01-PLAN.md
    - lib/foglet_bbs/tui/widgets/compose.ex
  </read_first>
  <action>
    1. Add `@default_terminal_size {80, 24}` near existing module attributes.
    2. Replace both inline `state.terminal_size || {80, 24}` occurrences with `state.terminal_size || @default_terminal_size`.
    3. Insert a source-order warning comment immediately above the `handle_key/2` clause block describing that `:tab`, `Ctrl+S`, and `Ctrl+C` clauses must remain above fallback forwarding.
    4. Keep existing pre-check modal branches in `submit/1` and logged-in guard in `do_submit/3` unchanged.
    5. Rewrite `submit_reply/4` from nested `case posts_mod.create_reply(...)` to a `with` chain that:
       - binds `{:ok, _post}` success from `posts_mod.create_reply/4`,
       - preserves identical success-state transitions and command payload,
       - preserves identical `{:error, _}` modal branch in the `else`.
    6. Keep `Compose.translate_key/1` and `MultiLineInput.update/2` flow unchanged.
    7. Keep edits scoped to `post_composer.ex` for this task.
  </action>
  <acceptance_criteria>
    - `rg -n '@default_terminal_size \\{80, 24\\}' lib/foglet_bbs/tui/screens/post_composer.ex` returns exactly one match.
    - `rg -n '\\{80, 24\\}' lib/foglet_bbs/tui/screens/post_composer.ex` returns exactly one match (the module attribute).
    - `rg -n 'source order matters for handle_key/2 clauses — do not reorder' lib/foglet_bbs/tui/screens/post_composer.ex` returns exactly one match.
    - `rg -n '^\\s*with\\s+\\{:ok, _post\\}\\s+<-' lib/foglet_bbs/tui/screens/post_composer.ex` returns one match in `submit_reply/4`.
    - `rg -n 'Compose\\.translate_key|MultiLineInput\\.update' lib/foglet_bbs/tui/screens/post_composer.ex` returns expected forwarding matches.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>
      mix compile --warnings-as-errors
      rg -n '@default_terminal_size \\{80, 24\\}|source order matters for handle_key/2 clauses — do not reorder|^\\s*with\\s+\\{:ok, _post\\}\\s+<-' lib/foglet_bbs/tui/screens/post_composer.ex
    </automated>
  </verify>
  <done>
    PostComposer has with-chain submit flow and explicit source-order guard note, with terminal fallback literals normalized to a module attribute.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock branch-parity behavior in tests and run phase quality gates</name>
  <files>test/foglet_bbs/tui/screens/post_composer_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/screens/post_composer_test.exs
    - lib/foglet_bbs/tui/screens/post_composer.ex
    - .planning/workstreams/phase-03-screen-audit/phases/08-postcomposer/08-VALIDATION.md
    - .planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
    - AGENTS.md
  </read_first>
  <action>
    1. Keep and tighten submit tests so all existing modal branches remain asserted:
       - empty body,
       - over-limit body,
       - not logged-in user,
       - domain create failure (`{:error, _}`),
       - success transition to `:post_reader` with composer cleanup and `jump_last: true`.
    2. Add a regression assertion that source-order-sensitive control-key behavior still routes through explicit Ctrl+S/Ctrl+C clauses (not fallback char forwarding).
    3. Keep compose-body forwarding behavior tests unchanged for `Compose` + `MultiLineInput`.
    4. Run and pass:
       - `mix test test/foglet_bbs/tui/screens/post_composer_test.exs`
       - `mix precommit`
    5. Ensure grep/rubric checks for COMPOSER and inherited AUDIT requirements remain green.
  </action>
  <acceptance_criteria>
    - `mix test test/foglet_bbs/tui/screens/post_composer_test.exs` exits 0.
    - `mix precommit` exits 0.
    - `rg -n 'Ctrl\\+S with empty body shows error modal|Ctrl\\+S with body > max_post_length shows error modal|You must be logged in to post|Failed to create post' test/foglet_bbs/tui/screens/post_composer_test.exs` returns matches for all listed branches.
    - `rg -n ':red|:green|:cyan|:yellow|:blue|:magenta|:white|:black|\"#[0-9a-fA-F]{6}\"|\\e\\[|\\x1b|%\\{.*theme.*\\||box style.*border|IO\\.(write|puts|inspect)|\\(Map\\.get\\(state, :session_context\\) \\|\\| %\\{\\}\\) \\|> Map\\.get\\(:theme\\)|get_in\\(ctx, \\[:domain' lib/foglet_bbs/tui/screens/post_composer.ex` returns zero matches.
    - `wc -l lib/foglet_bbs/tui/screens/post_composer.ex` is less than or equal to pre-phase baseline.
  </acceptance_criteria>
  <verify>
    <automated>
      mix test test/foglet_bbs/tui/screens/post_composer_test.exs
      mix precommit
    </automated>
  </verify>
  <done>
    PostComposer tests lock submit branch parity and full quality gates are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal key events -> PostComposer handlers | Untrusted terminal input drives mode toggles, submit, cancel, and body edits. |
| submit path -> posts domain module | User draft body crosses into domain write path via dynamic posts module resolution. |
| post success transition -> post_reader | State handoff controls post reload and selected-post jump behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `handle_key/2` clause ordering | mitigate | Add explicit source-order warning note and keep control-key clauses above fallback forwarding. |
| T-08-02 | Denial of Service | unbounded draft body input | mitigate | Keep max-length enforcement and over-limit modal branch behavior unchanged. |
| T-08-03 | Elevation of Privilege | unauthenticated submit path | mitigate | Keep logged-in guard and explicit modal error for nil user ID. |
| T-08-04 | Repudiation | submit error handling drift during refactor | mitigate | Preserve exact error modal text and branch coverage in tests after with-chain rewrite. |
</threat_model>

<verification>
Run after implementation:

```bash
mix test test/foglet_bbs/tui/screens/post_composer_test.exs
mix precommit
rg -n '@default_terminal_size \{80, 24\}|\{80, 24\}|source order matters for handle_key/2 clauses — do not reorder|^\s*with\s+\{:ok, _post\}\s+<-' lib/foglet_bbs/tui/screens/post_composer.ex
wc -l lib/foglet_bbs/tui/screens/post_composer.ex
```

All submit/cancel branch tests and gate commands must pass, and line count must not increase.
</verification>

<success_criteria>
Phase 8 planning is complete when COMPOSER-01..05 are fully covered, the plan includes concrete executable tasks with verifiable acceptance criteria, a threat model is present, and execution can begin without additional planning artifacts.
</success_criteria>

<output>
After completion, create `.planning/workstreams/phase-03-screen-audit/phases/08-postcomposer/08-01-SUMMARY.md`.
</output>
