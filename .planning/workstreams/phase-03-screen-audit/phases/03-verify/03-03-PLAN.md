---
phase: 03-verify
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - lib/foglet_bbs/tui/screens/verify.ex
autonomous: true
requirements:
  - VERIFY-04
user_setup: []
must_haves:
  truths:
    - "Verify screen file line count is strictly lower than the 271-line pre-phase baseline"
    - "All AUDIT-05 grep gates return zero on verify.ex"
    - "AUDIT-18 canonical section order is satisfied or any deviation is documented in the moduledoc"
    - "AUDIT-19 init_screen_state/1 exists with @spec init_screen_state(keyword()) :: map()"
    - "AUDIT-17 protected region below the Verify status/error line remains empty"
    - "mix precommit exits 0"
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/verify.ex"
      provides: "Final audit-compliant Verify screen"
      max_lines: 270
  key_links:
    - from: "lib/foglet_bbs/tui/screens/verify.ex"
      to: "mix precommit"
      via: "compile, format, credo, sobelow, dialyzer"
      pattern: "mix precommit"
---

<objective>
Wave 2 performs the final audit closure for Verify.

Purpose: Wave 1 migrates behavior and state ownership; Wave 2 proves the screen satisfies AUDIT-05..22, keeps the gateway layout sparse, and passes the full project quality gate.
Output: `verify.ex` finalized under the 271-line baseline with `mix precommit` green.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/phase-03-screen-audit/STATE.md
@.planning/workstreams/phase-03-screen-audit/ROADMAP.md
@.planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
@.planning/workstreams/phase-03-screen-audit/phases/03-verify/03-CONTEXT.md
@.planning/workstreams/phase-03-screen-audit/phases/03-verify/03-RESEARCH.md
@.planning/workstreams/phase-03-screen-audit/phases/03-verify/03-VALIDATION.md
@AGENTS.md
@lib/foglet_bbs/tui/screens/verify.ex
</context>

<interfaces>
AUDIT-05 grep gates for `verify.ex`:

```bash
grep -E ':red|:green|:cyan|:yellow|:blue|:magenta|:white|:black' lib/foglet_bbs/tui/screens/verify.ex
grep -E '"#[0-9a-fA-F]{6}"' lib/foglet_bbs/tui/screens/verify.ex
grep -E '\\e\[|\\x1b' lib/foglet_bbs/tui/screens/verify.ex
grep -E '%\{.*theme.*\|' lib/foglet_bbs/tui/screens/verify.ex
grep -E 'box style.*border' lib/foglet_bbs/tui/screens/verify.ex
grep -E 'IO\.(write|puts|inspect)' lib/foglet_bbs/tui/screens/verify.ex
grep -E '\{80, 24\}' lib/foglet_bbs/tui/screens/verify.ex
grep -E '\(Map\.get\(state, :session_context\) \|\| %\{\}\) \|> Map\.get\(:theme\)' lib/foglet_bbs/tui/screens/verify.ex
grep -E 'get_in\(ctx, \[:domain' lib/foglet_bbs/tui/screens/verify.ex
```

AUDIT-18 expected function order:

```text
init_screen_state/1
render/1
handle_key/2
handle_verify_event/2
private key/event handlers
private render helpers, including pad_buffer_with_cursor/1
private state plumbing, including default_verify_state/0 and get/put/clear helpers
private domain plumbing, including submit_raw/1, resend_code_raw/1, cooldown helpers, maybe_log_verify_code/2
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run Verify audit gates, trim LoC, and pass mix precommit</name>
  <files>lib/foglet_bbs/tui/screens/verify.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/verify.ex (post-Wave-1 state)
    - .planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md (AUDIT-05..22)
    - .planning/workstreams/phase-03-screen-audit/phases/03-verify/03-CONTEXT.md (D-07..D-09 sparseness, D-18/D-19 canonical layout)
    - .planning/workstreams/phase-03-screen-audit/phases/03-verify/03-RESEARCH.md (security domain and common pitfalls)
    - .planning/workstreams/phase-03-screen-audit/phases/03-verify/03-VALIDATION.md (commands and sampling map)
    - AGENTS.md (project precommit and Elixir gotchas)
    - CLAUDE.md (project precommit and Elixir gotchas)
  </read_first>
  <action>
    Complete the audit closure for `verify.ex`.

    Concrete target state:
    - Run all nine AUDIT-05 grep gates listed in `<interfaces>`; each must return zero matches. Fix any match by routing through existing Theme/ScreenFrame patterns or removing invalid direct terminal/color/ANSI usage.
    - Verify `wc -l lib/foglet_bbs/tui/screens/verify.ex` is strictly less than the 271-line pre-phase baseline. If the line count is `271` or higher, trim only redundant whitespace, duplicated helper code, or verbose comments. Do not remove the D-14 hand-rolled-buffer rationale or any safety comments documenting cooldown semantics.
    - Verify no rows were added below the existing instruction line, slot visualization line, and status line. Blocked submit/resend feedback must remain modal-only per D-07 through D-09.
    - Verify canonical function order with `grep -n "^  def\\|^  defp" lib/foglet_bbs/tui/screens/verify.ex`. Move functions only as needed; do not change behavior while reordering.
    - Verify the moduledoc explicitly cites inherited 07 D-02 and states that the 6-character buffer remains hand-rolled. Also ensure it no longer says state lives in `state.verify_state`.
    - Run `mix format lib/foglet_bbs/tui/screens/verify.ex`.
    - Run `mix test test/foglet_bbs/tui/screens/verify_test.exs test/foglet_bbs/tui/layout_smoke_test.exs`.
    - Run `mix test`.
    - Run `mix precommit` and fix every failure without adding `@dialyzer`, `credo:disable`, or `sobelow_skip` suppressions.
  </action>
  <verify>
    <automated>mix precommit</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l lib/foglet_bbs/tui/screens/verify.ex` prints a value less than `271`.
    - All nine AUDIT-05 grep gate commands return zero matches.
    - `rg -n "07 D-02|hand-rolled|TextInput cannot" lib/foglet_bbs/tui/screens/verify.ex` returns matches in the moduledoc.
    - `rg -n "state\\.verify_state|verify_state" lib/foglet_bbs/tui/screens/verify.ex` returns zero.
    - `rg -n "def init_screen_state|@spec init_screen_state\\(keyword\\(\\)\\) :: map\\(\\)" lib/foglet_bbs/tui/screens/verify.ex` returns both matches.
    - `mix test test/foglet_bbs/tui/screens/verify_test.exs test/foglet_bbs/tui/layout_smoke_test.exs` exits 0.
    - `mix test` exits 0.
    - `mix precommit` exits 0.
  </acceptance_criteria>
  <done>Verify screen satisfies AUDIT-05..22, remains sparse, reduces line count, and passes full project precommit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal key event -> Verify render/handler | Untrusted input is rendered and handled in the TUI process. |
| Verify handler -> Accounts | Verification and resend requests cross into domain code. |
| migrated screen_state -> future Verify entries | Cleared or stale state determines whether a new verification flow starts cleanly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | `verify.ex` input and submit paths | mitigate | `mix precommit` plus migrated tests must preserve input filtering and five-attempt lockout. |
| T-03-09 | Denial of Service | `verify.ex` resend path | mitigate | Preserve resend cooldown tests and no inline shortcut that bypasses modal cooldown feedback. |
| T-03-10 | Information Disclosure | Verify render path | accept | The screen shows only existing instruction/slot/status lines and modal copy; no secrets or generated codes are rendered. |
| T-03-11 | Elevation of Privilege | `screen_state[:verify]` lifecycle | mitigate | Grep/test gates confirm no top-level stale state remains and success/escape clear the migrated state. |
</threat_model>

<verification>
Final phase checks:

```bash
mix test test/foglet_bbs/tui/screens/verify_test.exs test/foglet_bbs/tui/layout_smoke_test.exs
mix test
mix precommit
rg -n "verify_state" lib/foglet_bbs/tui/app.ex lib/foglet_bbs/tui/screens/login.ex lib/foglet_bbs/tui/screens/register.ex lib/foglet_bbs/tui/screens/verify.ex test/foglet_bbs/tui/screens/verify_test.exs test/foglet_bbs/tui/layout_smoke_test.exs
wc -l lib/foglet_bbs/tui/screens/verify.ex
```

`rg` must return zero production/test migration matches and `wc -l` must be less than 271.
</verification>

<success_criteria>
Phase 03 is complete when all VERIFY-01..05 requirements are covered, Verify has no top-level state ownership, lockout/resend protections remain tested, and `mix precommit` is green.
</success_criteria>

<output>
After completion, create `.planning/workstreams/phase-03-screen-audit/phases/03-verify/03-03-SUMMARY.md`.
</output>
