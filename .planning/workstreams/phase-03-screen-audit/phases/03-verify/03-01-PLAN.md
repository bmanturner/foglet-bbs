---
phase: 03-verify
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/foglet_bbs/tui/screens/verify_test.exs
  - test/foglet_bbs/tui/layout_smoke_test.exs
autonomous: true
requirements:
  - VERIFY-03
  - VERIFY-05
user_setup: []
must_haves:
  truths:
    - "Verify tests assert migrated state through state.screen_state[:verify], not state.verify_state"
    - "Attempt-lockout tests still cover the 5-failed-attempt cooldown after the state migration"
    - "Resend-cooldown tests still prove resend cooldown and attempt cooldown remain independent"
    - "Layout smoke Verify fixtures include resend_cooldown_until and seed screen_state[:verify]"
  artifacts:
    - path: "test/foglet_bbs/tui/screens/verify_test.exs"
      provides: "Behavior coverage for Verify after migration to screen_state[:verify]"
      contains: "Map.get(new_state.screen_state || %{}, :verify)"
    - path: "test/foglet_bbs/tui/layout_smoke_test.exs"
      provides: "Smoke fixtures for Verify render using migrated screen_state shape"
      contains: "screen_state: %{verify:"
  key_links:
    - from: "test/foglet_bbs/tui/screens/verify_test.exs"
      to: "Foglet.TUI.Screens.Verify"
      via: "Verify.handle_key/2, Verify.handle_verify_event/2, Verify.render/1"
      pattern: "Verify\\.(handle_key|handle_verify_event|render)"
    - from: "test/foglet_bbs/tui/layout_smoke_test.exs"
      to: "Foglet.TUI.Screens.Verify.render/1"
      via: "App struct fixture with screen_state[:verify]"
      pattern: "screen_state: %\\{verify:"
---

<objective>
Wave 0 rewrites Verify tests and layout smoke fixtures to describe the post-Phase-3 ownership model before production code changes.

Purpose: the current tests assert the deprecated top-level `state.verify_state`; leaving them unchanged would mask migration regressions and stale-state leaks.
Output: tests that use `screen_state[:verify]` everywhere. They may fail until Wave 1 removes the old production field.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/phase-03-screen-audit/STATE.md
@.planning/workstreams/phase-03-screen-audit/ROADMAP.md
@.planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
@.planning/workstreams/phase-03-screen-audit/phases/03-verify/03-CONTEXT.md
@.planning/workstreams/phase-03-screen-audit/phases/03-verify/03-RESEARCH.md
@.planning/workstreams/phase-03-screen-audit/phases/03-verify/03-PATTERNS.md
@.planning/workstreams/phase-03-screen-audit/phases/03-verify/03-VALIDATION.md
@.planning/workstreams/phase-03-screen-audit/phases/02-register/02-CONTEXT.md
@AGENTS.md

@test/foglet_bbs/tui/screens/verify_test.exs
@test/foglet_bbs/tui/layout_smoke_test.exs
@lib/foglet_bbs/tui/screens/verify.ex
@lib/foglet_bbs/tui/app.ex
</context>

<interfaces>
Target Verify screen-state shape, per 03-CONTEXT D-04:

```elixir
%{
  buffer: "",
  attempts: 0,
  cooldown_until: nil,
  resend_cooldown_until: nil
}
```

Fixture helper target:

```elixir
defp verify_state(overrides \\ %{}) do
  Map.merge(%{buffer: "", attempts: 0, cooldown_until: nil, resend_cooldown_until: nil}, overrides)
end

defp put_verify_state(state, verify_state) do
  %{state | screen_state: Map.put(state.screen_state || %{}, :verify, verify_state)}
end

defp get_verify_state(state), do: Map.get(state.screen_state || %{}, :verify)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate verify_test.exs fixtures and assertions to screen_state[:verify]</name>
  <files>test/foglet_bbs/tui/screens/verify_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/screens/verify_test.exs
    - .planning/workstreams/phase-03-screen-audit/phases/03-verify/03-CONTEXT.md (D-01..D-17 state ownership, reset semantics, cooldown invariants)
    - .planning/workstreams/phase-03-screen-audit/phases/03-verify/03-PATTERNS.md (verify_test.exs fixture pattern)
    - test/foglet_bbs/tui/screens/register_test.exs (Phase 2 screen_state fixture precedent)
    - lib/foglet_bbs/tui/screens/verify.ex (current public functions under test)
    - AGENTS.md (test guidelines: no Process.sleep/1; deterministic setup only)
  </read_first>
  <action>
    Replace every `verify_state:` fixture seed and every `new_state.verify_state` assertion in `verify_test.exs` with `screen_state[:verify]` helpers.

    Concrete target state:
    - `base_state/1` returns an App-derived map with `screen_state: %{verify: verify_state()}` and no `verify_state:` key.
    - Add private helpers named `verify_state/1`, `put_verify_state/2`, and `get_verify_state/1`.
    - Every test that currently updates `%{state | verify_state: %{...}}` must instead call `put_verify_state(state, verify_state(%{...}))`.
    - Every success, escape, or cancel assertion must check `Map.get(new_state.screen_state || %{}, :verify) == nil`, per D-11 and D-12.
    - Preserve the existing behavioral assertions for uppercase input, max 6 chars, invalid-code attempt increment, 5-attempt lockout, blocked submit modal, resend reset semantics, resend cooldown blocking, and resend-vs-entry independence per D-15 through D-17.
    - Do not add sleeps, new processes, snapshot assertions, new dependencies, or inline countdown UX assertions.
  </action>
  <verify>
    <automated>mix test test/foglet_bbs/tui/screens/verify_test.exs</automated>
    Expected during Wave 0: tests may fail against current production code only because `verify.ex` has not migrated yet. Syntax/compile errors in the test file are not acceptable.
  </verify>
  <acceptance_criteria>
    - `rg -n "verify_state:" test/foglet_bbs/tui/screens/verify_test.exs` returns zero.
    - `rg -n "\\.verify_state" test/foglet_bbs/tui/screens/verify_test.exs` returns zero.
    - `rg -n "screen_state: %\\{verify:" test/foglet_bbs/tui/screens/verify_test.exs` returns at least one match.
    - `rg -n "resend_cooldown_until" test/foglet_bbs/tui/screens/verify_test.exs` still returns matches in resend tests.
    - `rg -n "attempts == 5|cooldown_until != nil|Too many attempts|Please wait to resend" test/foglet_bbs/tui/screens/verify_test.exs` returns matches proving lockout/resend protections remain covered.
  </acceptance_criteria>
  <done>Verify tests describe migrated `screen_state[:verify]` behavior and retain lockout/resend semantic coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate Verify layout smoke fixtures to screen_state[:verify]</name>
  <files>test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/layout_smoke_test.exs
    - .planning/workstreams/phase-03-screen-audit/phases/03-verify/03-CONTEXT.md (D-07..D-09 minimal presentation, no new rows)
    - .planning/workstreams/phase-03-screen-audit/phases/03-verify/03-PATTERNS.md (layout_smoke_test.exs fixture pattern)
    - .planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md (AUDIT-16/AUDIT-17 layout sparseness)
    - lib/foglet_bbs/tui/screens/verify.ex (render contract and slot visualization)
    - AGENTS.md (project test and precommit guidance)
  </read_first>
  <action>
    Update both Verify smoke fixtures in `layout_smoke_test.exs` to seed:

    ```elixir
    screen_state: %{
      verify: %{buffer: "XK7", attempts: 0, cooldown_until: nil, resend_cooldown_until: nil}
    }
    ```

    Remove the old top-level `verify_state:` key from those fixtures. Preserve current assertions around the `[XK7___]`/slot visualization and rendered screen dimensions; do not add an inline resend countdown row or any new persistent Verify content.
  </action>
  <verify>
    <automated>mix test test/foglet_bbs/tui/layout_smoke_test.exs</automated>
    Expected during Wave 0: tests may fail against current production code only because `verify.ex` has not migrated yet. Syntax/compile errors in the smoke file are not acceptable.
  </verify>
  <acceptance_criteria>
    - `rg -n "verify_state:" test/foglet_bbs/tui/layout_smoke_test.exs` returns zero.
    - `rg -n "screen_state: %\\{\\s*verify:" test/foglet_bbs/tui/layout_smoke_test.exs` returns at least one match, or the equivalent multi-line `screen_state` fixture is present.
    - `rg -n "resend_cooldown_until: nil" test/foglet_bbs/tui/layout_smoke_test.exs` returns at least two matches.
    - `rg -n "countdown|resend cooldown|Wait [0-9]" test/foglet_bbs/tui/layout_smoke_test.exs` returns zero new persistent-row assertions.
  </acceptance_criteria>
  <done>Layout smoke tests use migrated Verify state shape and preserve the existing sparse render contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal key event -> Verify screen handler | Untrusted key input mutates in-memory verify buffer and may trigger verification/resend calls. |
| Verify screen handler -> Accounts domain | Verify/resend requests cross from UI state into account verification domain functions. |
| screen_state lifecycle -> App modal/current_screen | Stale in-memory state can persist across screen transitions if not explicitly cleared. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | test/foglet_bbs/tui/screens/verify_test.exs | mitigate | Preserve tests for 6-character input filtering, uppercase normalization, 5-attempt lockout, and blocked-submit modal. |
| T-03-02 | Denial of Service | resend cooldown tests | mitigate | Preserve tests proving `resend_cooldown_until` blocks repeated resend and does not mutate on blocked resend. |
| T-03-03 | Elevation of Privilege | state clearing tests | mitigate | Assert success and escape remove `screen_state[:verify]` so stale buffers/counters cannot carry into a new flow. |
</threat_model>

<verification>
Run after both tasks:

```bash
mix test test/foglet_bbs/tui/screens/verify_test.exs test/foglet_bbs/tui/layout_smoke_test.exs
rg -n "verify_state:|\\.verify_state" test/foglet_bbs/tui/screens/verify_test.exs test/foglet_bbs/tui/layout_smoke_test.exs
```

The `rg` command must return zero matches. The test command may remain red until Wave 1 production migration, but test files must compile.
</verification>

<success_criteria>
Wave 0 succeeds when all Verify test fixtures and assertions target `screen_state[:verify]`, semantic coverage for lockout/resend remains present, and no smoke fixture seeds top-level `verify_state`.
</success_criteria>

<output>
After completion, create `.planning/workstreams/phase-03-screen-audit/phases/03-verify/03-01-SUMMARY.md`.
</output>
