---
phase: 07-newthread
plan: 01
type: execute
wave: 1
depends_on: ["00", "01"]
files_modified:
  - lib/foglet_bbs/tui/screens/new_thread.ex
  - test/foglet_bbs/tui/screens/new_thread_test.exs
autonomous: true
requirements:
  - NEWTHREAD-01
  - NEWTHREAD-02
  - NEWTHREAD-03
  - NEWTHREAD-04
  - NEWTHREAD-05
user_setup: []
must_haves:
  truths:
    - "Title input is migrated from hand-rolled `title_input` string/cursor handling to `Input.TextInput` while preserving NewThread behavior contracts."
    - "Body composer remains `Compose` + `MultiLineInput` with unchanged submit/cancel/tab semantics and unchanged source-order-sensitive key interception."
    - "Inline `{80, 24}` fallbacks are replaced by `@default_terminal_size` in `new_thread.ex`."
    - "The load-bearing source-order warning comment for compose key handling is preserved verbatim and still guards Ctrl+S/Ctrl+C interception above body fallthrough."
    - "`mix precommit` passes with no new suppressions and Phase 7 grep/rubric gates stay green."
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/new_thread.ex"
      provides: "TextInput-backed title entry and audit-compliant defaults/order/guards"
      contains: "@default_terminal_size {80, 24}"
    - path: "test/foglet_bbs/tui/screens/new_thread_test.exs"
      provides: "Regression coverage for title-input migration and compose-path invariants"
      contains: "describe \"compose step\""
  key_links:
    - from: "lib/foglet_bbs/tui/screens/new_thread.ex"
      to: "lib/foglet_bbs/tui/widgets/input/text_input.ex"
      via: "title input rendering and key-event flow"
      pattern: "TextInput"
    - from: "lib/foglet_bbs/tui/screens/new_thread.ex"
      to: "lib/foglet_bbs/tui/widgets/compose.ex"
      via: "body-key translation in focused body mode"
      pattern: "Compose.translate_key"
    - from: "test/foglet_bbs/tui/screens/new_thread_test.exs"
      to: "lib/foglet_bbs/tui/screens/new_thread.ex"
      via: "behavioral parity assertions for compose flow"
      pattern: "NewThread.handle_key"
---

<objective>
Deliver Phase 7 by migrating NewThread title input to `Input.TextInput` while preserving all load-bearing compose behavior and audit guardrails.

Purpose: complete the NewThread-specific screen audit (`NEWTHREAD-01..05`) with zero feature expansion.
Output: only `new_thread.ex` and `new_thread_test.exs` are changed; all phase gates and `mix precommit` are green.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/phase-03-screen-audit/STATE.md
@.planning/workstreams/phase-03-screen-audit/ROADMAP.md
@.planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
@.planning/workstreams/phase-03-screen-audit/phases/07-newthread/07-CONTEXT.md
@.planning/workstreams/phase-03-screen-audit/phases/07-newthread/07-RESEARCH.md
@.planning/workstreams/phase-03-screen-audit/phases/07-newthread/07-PATTERNS.md
@.planning/workstreams/phase-03-screen-audit/phases/07-newthread/07-VALIDATION.md
@AGENTS.md

@lib/foglet_bbs/tui/screens/new_thread.ex
@test/foglet_bbs/tui/screens/new_thread_test.exs
@lib/foglet_bbs/tui/widgets/input/text_input.ex
@lib/foglet_bbs/tui/widgets/compose.ex
</context>

<interfaces>
NewThread contracts that must be preserved:

```elixir
@spec init_screen_state(keyword()) :: map()
@spec render(map()) :: any()
@spec handle_key(map(), map()) :: {:update, map(), list()} | :no_match
```

Load-bearing comment and ordering contract in `new_thread.ex`:

```elixir
# NOTE: The Ctrl+C (cancel) and Ctrl+S (submit) clauses ... MUST remain above this clause in source order.
```

Required grep gate on terminal-size fallback:

```bash
rg -n '\\{80, 24\\}' lib/foglet_bbs/tui/screens/new_thread.ex
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate NewThread title input to Input.TextInput and enforce audit guardrails</name>
  <files>lib/foglet_bbs/tui/screens/new_thread.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/new_thread.ex
    - lib/foglet_bbs/tui/widgets/input/text_input.ex
    - lib/foglet_bbs/tui/widgets/compose.ex
    - .planning/workstreams/phase-03-screen-audit/phases/07-newthread/07-CONTEXT.md
    - .planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
  </read_first>
  <action>
    1. Add `alias Foglet.TUI.Widgets.Input.TextInput` and introduce `@default_terminal_size {80, 24}` in module attributes.
    2. Update NewThread screen state so title input is represented by TextInput-backed state (instead of hand-rolled `title_input` mutation logic), keeping title length cap enforcement behavior intact.
    3. Replace hand-rolled title rendering (`Title: ...█`) with `TextInput.render(...)` using `theme: theme` and a clear title label/context that keeps screen copy sparse.
    4. Preserve body compose path unchanged: keep `Compose.render_input`, `Compose.translate_key`, `MultiLineInput.update`, and body-focused Tab/Ctrl semantics exactly as behaviorally documented.
    5. Replace both inline terminal-size fallbacks with `@default_terminal_size` and keep all Phase-0 helper routing (`Theme.from_state`, `Domain.get`) intact.
    6. Preserve the source-order warning comment and ensure Ctrl+S/Ctrl+C interception clauses remain above body fallthrough in source order.
    7. Keep scope fence: modify only this screen file for Task 1.
  </action>
  <acceptance_criteria>
    - `rg -n '@default_terminal_size \\{80, 24\\}' lib/foglet_bbs/tui/screens/new_thread.ex` returns exactly one match.
    - `rg -n '\\{80, 24\\}' lib/foglet_bbs/tui/screens/new_thread.ex` returns exactly one match (the attribute declaration).
    - `rg -n 'TextInput' lib/foglet_bbs/tui/screens/new_thread.ex` returns render/handler integration matches.
    - `rg -n 'source order matters for handle_key/2 clauses — do not reorder' lib/foglet_bbs/tui/screens/new_thread.ex` returns exactly one match.
    - `rg -n 'Compose\\.translate_key|MultiLineInput\\.update|Compose\\.render_input' lib/foglet_bbs/tui/screens/new_thread.ex` returns expected body-path matches.
    - `rg -n '\\(Map\\.get\\(state, :session_context\\) \\|\\| %\\{\\}\\) \\|> Map\\.get\\(:theme\\)' lib/foglet_bbs/tui/screens/new_thread.ex` returns zero matches.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>
      mix compile --warnings-as-errors
      rg -n '@default_terminal_size \\{80, 24\\}|source order matters for handle_key/2 clauses — do not reorder|TextInput|Compose\\.translate_key|MultiLineInput\\.update' lib/foglet_bbs/tui/screens/new_thread.ex
    </automated>
  </verify>
  <done>
    NewThread title input is TextInput-backed, body compose behavior is unchanged, inline terminal fallback literals are removed, and source-order-sensitive key handling protections remain intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update NewThread tests and run phase quality gates</name>
  <files>test/foglet_bbs/tui/screens/new_thread_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/screens/new_thread_test.exs
    - lib/foglet_bbs/tui/screens/new_thread.ex
    - .planning/workstreams/phase-03-screen-audit/phases/07-newthread/07-VALIDATION.md
    - .planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
    - AGENTS.md
  </read_first>
  <action>
    1. Update tests to reflect title-input migration to TextInput-backed state while preserving existing user behavior expectations.
    2. Keep and/or tighten compose flow tests for Tab behavior, Ctrl+S submit, Ctrl+C/Esc origin-aware cancel, and body input interaction.
    3. Ensure tests still cover title cap behavior and submit validation errors (empty title/body).
    4. Run and pass focused screen tests: `mix test test/foglet_bbs/tui/screens/new_thread_test.exs`.
    5. Run and pass full quality gate: `mix precommit`.
    6. Enforce phase grep/rubric checks for NEWTHREAD + inherited AUDIT IDs.
  </action>
  <acceptance_criteria>
    - `mix test test/foglet_bbs/tui/screens/new_thread_test.exs` exits 0.
    - `mix precommit` exits 0.
    - `rg -n '\\{80, 24\\}' lib/foglet_bbs/tui/screens/new_thread.ex` returns exactly one match.
    - `rg -n ':red|:green|:cyan|:yellow|:blue|:magenta|:white|:black|"#[0-9a-fA-F]{6}"|\\e\\[|\\x1b|%\\{.*theme.*\\||box style.*border|IO\\.(write|puts|inspect)|\\(Map\\.get\\(state, :session_context\\) \\|\\| %\\{\\}\\) \\|> Map\\.get\\(:theme\\)|get_in\\(ctx, \\[:domain' lib/foglet_bbs/tui/screens/new_thread.ex` returns zero matches.
    - `wc -l lib/foglet_bbs/tui/screens/new_thread.ex` is less than or equal to pre-phase baseline.
  </acceptance_criteria>
  <verify>
    <automated>
      mix test test/foglet_bbs/tui/screens/new_thread_test.exs
      mix precommit
      rg -n ':red|:green|:cyan|:yellow|:blue|:magenta|:white|:black|"#[0-9a-fA-F]{6}"|\\e\\[|\\x1b|%\\{.*theme.*\\||box style.*border|IO\\.(write|puts|inspect)|\\(Map\\.get\\(state, :session_context\\) \\|\\| %\\{\\}\\) \\|> Map\\.get\\(:theme\\)|get_in\\(ctx, \\[:domain' lib/foglet_bbs/tui/screens/new_thread.ex
    </automated>
  </verify>
  <done>
    NewThread test coverage locks migration behavior, and all required phase quality gates are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal key events -> NewThread handlers | Untrusted terminal input drives title/body state transitions and submit/cancel actions. |
| NewThread submit path -> `Foglet.Threads.create_thread/3` | User-provided title/body crosses into domain thread creation logic. |
| NewThread -> ThreadList navigation | State handoff after success determines what board/thread content is loaded next. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `handle_compose_key/3` ordering | mitigate | Preserve source-order-sensitive Ctrl+S/Ctrl+C clauses and assert behavior via tests. |
| T-07-02 | Denial of Service | title/body input handling | mitigate | Keep bounded title-length checks and existing body input contracts; reject invalid input paths without crash. |
| T-07-03 | Information Disclosure | render and error surfaces | accept | Preserve existing sparse copy/error behavior; no new sensitive state surfaced in UI. |
| T-07-04 | Elevation of Privilege | thread creation path | mitigate | Preserve current-user guard and existing submit validation before domain call. |
</threat_model>

<verification>
Run after implementation:

```bash
mix test test/foglet_bbs/tui/screens/new_thread_test.exs
mix precommit
rg -n ':red|:green|:cyan|:yellow|:blue|:magenta|:white|:black|"#[0-9a-fA-F]{6}"|\e\[|\x1b|%\{.*theme.*\||box style.*border|IO\.(write|puts|inspect)|\{80, 24\}|\(Map\.get\(state, :session_context\) \|\| %\{\}\) \|> Map\.get\(:theme\)|get_in\(ctx, \[:domain' lib/foglet_bbs/tui/screens/new_thread.ex
rg -n 'source order matters for handle_key/2 clauses — do not reorder' lib/foglet_bbs/tui/screens/new_thread.ex
wc -l lib/foglet_bbs/tui/screens/new_thread.ex
```

All grep gates must pass, source-order note must remain present, and line count must not increase.
</verification>

<success_criteria>
Phase 7 planning is complete when NEWTHREAD-01..05 are fully covered, the plan includes explicit executable tasks with concrete acceptance criteria, a mandatory threat model is present, and execution can proceed with no additional planning artifacts required.
</success_criteria>

<output>
After completion, create `.planning/workstreams/phase-03-screen-audit/phases/07-newthread/07-01-SUMMARY.md`.
</output>
