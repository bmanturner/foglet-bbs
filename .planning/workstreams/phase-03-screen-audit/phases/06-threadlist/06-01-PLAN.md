---
phase: 06-threadlist
plan: 01
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - lib/foglet_bbs/tui/screens/thread_list.ex
  - test/foglet_bbs/tui/screens/thread_list_test.exs
  - test/foglet_bbs/threads_test.exs
autonomous: true
requirements:
  - THREADS-01
  - THREADS-02
  - THREADS-03
  - THREADS-04
  - THREADS-05
  - THREADS-06
  - THREADS-07
user_setup: []
must_haves:
  truths:
    - "ThreadList keeps creator handle, post-count, and time-ago metadata on every row"
    - "Sticky-then-recency ordering remains unchanged and load-bearing"
    - "ThreadList.load_threads/2 guards module probes with Code.ensure_loaded/1 before function_exported?/3"
    - "ThreadList exposes init_screen_state/1 and removes inline %{selected_index: 0} defaults"
    - "Threads.list_threads/2 coverage locks created_by preload contract for row rendering"
    - "Dead-code seam posture for load_threads/2 is explicit and documented"
    - "mix precommit exits 0"
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/thread_list.ex"
      provides: "Audited ThreadList with explicit initializer, safe module-probe dispatch, and in-flight loading affordance"
      max_lines: 240
    - path: "test/foglet_bbs/tui/screens/thread_list_test.exs"
      provides: "ThreadList behavior tests for metadata, sort invariants, loading branch, and dispatch correctness"
    - path: "test/foglet_bbs/threads_test.exs"
      provides: "Threads context contract tests proving created_by preload availability on list_threads/2"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/thread_list.ex"
      to: "Foglet.Threads.list_threads/2"
      via: "domain dispatch with user-aware arity"
      pattern: "function_exported?(threads_mod, :list_threads, 2)"
    - from: "lib/foglet_bbs/tui/screens/thread_list.ex"
      to: "Foglet.TUI.App.do_update({:load_threads, board_id}, state)"
      via: "documented seam ownership"
      pattern: "@doc false"
---

<objective>
Deliver Phase 6 ThreadList scope in one bounded execution plan.

Purpose: preserve user-visible thread browsing behavior while closing the module-probe correctness gap, initializer audit gap, loading-affordance decision, and created_by preload coverage gap.
Output: only `thread_list.ex`, `thread_list_test.exs`, and `threads_test.exs` change; all quality gates pass.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/phase-03-screen-audit/STATE.md
@.planning/workstreams/phase-03-screen-audit/ROADMAP.md
@.planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
@.planning/workstreams/phase-03-screen-audit/phases/06-threadlist/06-CONTEXT.md
@.planning/workstreams/phase-03-screen-audit/phases/06-threadlist/06-RESEARCH.md
@.planning/workstreams/phase-03-screen-audit/phases/06-threadlist/06-PATTERNS.md
@.planning/workstreams/phase-03-screen-audit/phases/06-threadlist/06-VALIDATION.md
@AGENTS.md

@lib/foglet_bbs/tui/screens/thread_list.ex
@test/foglet_bbs/tui/screens/thread_list_test.exs
@test/foglet_bbs/threads_test.exs
@lib/foglet_bbs/threads.ex
@lib/foglet_bbs/tui/app.ex
@lib/foglet_bbs/tui/widgets/progress/spinner.ex
</context>

<interfaces>
Target interfaces for this phase:

```elixir
@spec init_screen_state(keyword()) :: map()
def init_screen_state(opts \\ [])

@spec load_threads(map(), String.t()) :: {map(), list()}
def load_threads(state, board_id)
```

Dead-code audit command (must be captured in summary evidence):

```bash
rg -n "load_threads" lib test
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update ThreadList for guarded dispatch, initializer adoption, and loading affordance</name>
  <files>lib/foglet_bbs/tui/screens/thread_list.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/thread_list.ex
    - lib/foglet_bbs/tui/app.ex
    - lib/foglet_bbs/tui/widgets/progress/spinner.ex
    - .planning/workstreams/phase-03-screen-audit/phases/06-threadlist/06-CONTEXT.md
    - .planning/workstreams/phase-03-screen-audit/phases/06-threadlist/06-RESEARCH.md
    - .planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
  </read_first>
  <action>
    Apply these exact code changes in `thread_list.ex`:

    1. Add public `init_screen_state/1` returning `%{selected_index: 0}` and replace every inline fallback `|| %{selected_index: 0}` with `|| init_screen_state()` in `render/1`, `handle_key/2`, and `move_selection/2`.
    2. In `load_threads/2`, compute a local boolean `loaded?` from `Code.ensure_loaded(threads_mod)` and require `loaded?` for both `function_exported?(threads_mod, :list_threads, 2)` and `(…, 1)` branches.
    3. Change `load_threads/2` docs to `@doc false` and add a comment stating production ownership is in `Foglet.TUI.App.do_update({:load_threads, board_id}, state)`.
    4. Keep two-pass sort helpers unchanged (`sort_threads/1` + `sort_by_recency/1`) and preserve row metadata formatting shape.
    5. Replace static `"Loading..."` text with spinner-backed loading row when and only when `state.current_thread_list == nil`; keep empty-board state message unchanged.

    Do not modify files outside `thread_list.ex` in this task.
  </action>
  <acceptance_criteria>
    - `rg -n "def init_screen_state|@spec init_screen_state\(keyword\(\)\) :: map\(\)" lib/foglet_bbs/tui/screens/thread_list.ex` returns matches.
    - `rg -n "Code.ensure_loaded\(|loaded\? and function_exported\?\(threads_mod, :list_threads, 2\)|loaded\? and function_exported\?\(threads_mod, :list_threads, 1\)" lib/foglet_bbs/tui/screens/thread_list.ex` returns matches.
    - `rg -n "@doc false|App.do_update\(\{:load_threads, board_id\}, state\)" lib/foglet_bbs/tui/screens/thread_list.ex` returns matches.
    - `rg -n "Spinner.render|Loading…" lib/foglet_bbs/tui/screens/thread_list.ex` returns matches.
    - `rg -n "sort_threads\(|sort_by_recency\(" lib/foglet_bbs/tui/screens/thread_list.ex` returns matches.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>
      mix compile --warnings-as-errors
      mix test test/foglet_bbs/tui/screens/thread_list_test.exs
    </automated>
  </verify>
  <done>ThreadList keeps existing UX contracts while landing guarded dispatch, explicit state initializer, and loading-affordance policy.</done>
</task>

<task type="auto">
  <name>Task 2: Extend test coverage for dispatch correctness and created_by preload contract; run full gate</name>
  <files>test/foglet_bbs/tui/screens/thread_list_test.exs, test/foglet_bbs/threads_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/screens/thread_list_test.exs
    - test/foglet_bbs/threads_test.exs
    - lib/foglet_bbs/tui/screens/thread_list.ex
    - lib/foglet_bbs/threads.ex
    - .planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
    - AGENTS.md
  </read_first>
  <action>
    Apply these exact test updates:

    1. In `thread_list_test.exs`, add a test asserting `ThreadList.init_screen_state() == %{selected_index: 0}`.
    2. Add a loading-state render test where `current_thread_list: nil` and assert render succeeds and includes loading affordance output.
    3. Keep existing sort and metadata tests unchanged and add/adjust dispatch test assertions so the 2-arity path is explicitly validated under guarded module-load logic.
    4. In `threads_test.exs`, add a focused `list_threads/2` test for a seeded board/user asserting each returned row has `created_by` map and non-empty `created_by.handle`.
    5. Run and pass:
       - `mix test test/foglet_bbs/tui/screens/thread_list_test.exs`
       - `mix test test/foglet_bbs/threads_test.exs`
       - `mix precommit`

    If any gate fails, fix root cause without suppressions.
  </action>
  <acceptance_criteria>
    - `rg -n "init_screen_state\(\) == %\{selected_index: 0\}" test/foglet_bbs/tui/screens/thread_list_test.exs` returns one match.
    - `rg -n "current_thread_list: nil|Loading" test/foglet_bbs/tui/screens/thread_list_test.exs` returns a loading-path test match.
    - `rg -n "created_by|handle" test/foglet_bbs/threads_test.exs` returns the new preload-contract assertions.
    - `mix test test/foglet_bbs/tui/screens/thread_list_test.exs` exits 0.
    - `mix test test/foglet_bbs/threads_test.exs` exits 0.
    - `mix precommit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>
      mix test test/foglet_bbs/tui/screens/thread_list_test.exs
      mix test test/foglet_bbs/threads_test.exs
      mix precommit
    </automated>
  </verify>
  <done>Tests lock the corrected dispatch and preload contracts; all quality gates are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| key events -> ThreadList handlers | Untrusted terminal input drives list selection and route commands. |
| domain module resolution -> load_threads/2 dispatch | Session-injected domain module can vary by runtime/test context. |
| thread query rows -> row metadata renderer | Renderer assumes `created_by` and metadata fields are well-formed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | dynamic domain adapter dispatch | mitigate | Require `Code.ensure_loaded/1` before arity probes and gate both exports with `loaded?`. |
| T-06-02 | Denial of Service | loading render branch | mitigate | Spinner used only for in-flight async state; no blocking loops in render path. |
| T-06-03 | Information Disclosure | thread row metadata | accept | Keep existing metadata only (handle/post-count/time-ago), no extra gutter fields. |
| T-06-04 | Repudiation | dead-code seam ambiguity | mitigate | Mark seam `@doc false` and document App ownership in code + phase summary evidence. |
</threat_model>

<verification>
Run after implementation:

```bash
mix test test/foglet_bbs/tui/screens/thread_list_test.exs
mix test test/foglet_bbs/threads_test.exs
mix precommit
rg -n "load_threads" lib test
wc -l lib/foglet_bbs/tui/screens/thread_list.ex
```

The sticky-then-recency behavior and row metadata assertions must remain green, and line count must remain less than or equal to pre-phase baseline.
</verification>

<success_criteria>
Phase 06 planning is complete when THREADS-01..THREADS-07 are fully covered, task actions are concrete and verifiable, security/threat requirements are captured, and execution can proceed without additional planning artifacts.
</success_criteria>

<output>
After completion, create `.planning/workstreams/phase-03-screen-audit/phases/06-threadlist/06-01-SUMMARY.md`.
</output>
