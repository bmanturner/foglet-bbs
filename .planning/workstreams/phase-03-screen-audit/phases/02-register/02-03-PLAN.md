---
phase: 02-register
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - lib/foglet_bbs/tui/screens/register.ex
autonomous: false
requirements:
  - REGISTER-05
user_setup: []
must_haves:
  truths:
    - "lib/foglet_bbs/tui/screens/register.ex line count is strictly less than 294 (AUDIT-16)"
    - "All 9 AUDIT-05 grep gates return zero matches on register.ex"
    - "AUDIT-18 canonical section order is satisfied (functions appear in §3 → §4 → §5 → §6 → §7 → §8 → §9 → §10 order)"
    - "AUDIT-19 init_screen_state/1 is present and has @spec init_screen_state(keyword()) :: map()"
    - "AUDIT-17 protected-region: no content renders below the error line on :invite_code or :combined steps (no new rows added)"
    - "mix precommit exits 0 (compile --warnings-as-errors + format + credo --strict + sobelow + dialyzer all green)"
    - "REGISTER-04 apply/3 + credo:disable-for-next-line appears exactly once — the sole documented AUDIT-15 exception"
    - "Developer visually verifies register screen renders correctly via SSH smoke test (KeyBar hints, focus highlighting, error rendering, password masking)"
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/register.ex"
      provides: "Final AUDIT-compliant register.ex — rubric items AUDIT-05..22 all satisfied"
      max_lines: 293
  key_links:
    - from: "lib/foglet_bbs/tui/screens/register.ex"
      to: "mix precommit toolchain"
      via: "compile --warnings-as-errors + format + credo --strict + sobelow + dialyzer"
      pattern: "mix precommit"
---

<objective>
Wave 2 — final AUDIT rubric sweep. Verify every rubric item AUDIT-05..22 passes on `register.ex`
post-Wave-1, compress LoC if needed to land strictly under 294, and run the `mix precommit`
toolchain end-to-end. Includes one human-verify checkpoint for visual smoke testing over SSH.

Purpose: Wave 1 lands the structural refactor but does not itself guarantee every AUDIT rubric
item. This plan is the gate: grep gates, section order, line count, precommit, and a human
render check together close REGISTER-05.

Output: `register.ex` at final committed state — AUDIT-compliant, precommit-green, visually
verified.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
@.planning/workstreams/phase-03-screen-audit/ROADMAP.md
@.planning/workstreams/phase-03-screen-audit/phases/02-register/02-CONTEXT.md
@.planning/workstreams/phase-03-screen-audit/phases/02-register/02-RESEARCH.md
@.planning/workstreams/phase-03-screen-audit/phases/02-register/02-VALIDATION.md

@lib/foglet_bbs/tui/screens/register.ex
</context>

<interfaces>
<!-- Rubric gates (from REQUIREMENTS.md §Audit-wide AUDIT-05..22, scoped to register.ex) -->

### AUDIT-05 grep gates (9 patterns, all must return zero on register.ex)

| # | Pattern | Command |
|---|---------|---------|
| 1 | Named color atoms | `grep -E ':red\|:green\|:cyan\|:yellow\|:blue\|:magenta\|:white\|:black' lib/foglet_bbs/tui/screens/register.ex` |
| 2 | Hex literals | `grep -E '"#[0-9a-fA-F]{6}"' lib/foglet_bbs/tui/screens/register.ex` |
| 3 | Raw ANSI | `grep -E '\\\\e\\\[\|\\\\x1b' lib/foglet_bbs/tui/screens/register.ex` |
| 4 | Theme struct mutation | `grep -E '%\{.*theme.*\|' lib/foglet_bbs/tui/screens/register.ex` |
| 5 | Nested box border | `grep -E 'box style.*border' lib/foglet_bbs/tui/screens/register.ex` |
| 6 | Direct terminal writes | `grep -E 'IO\.(write\|puts\|inspect)' lib/foglet_bbs/tui/screens/register.ex` |
| 7 | `{80, 24}` inline | `grep -E '\{80, 24\}' lib/foglet_bbs/tui/screens/register.ex` |
| 8 | Inlined theme extraction | `grep -E '\(Map\.get\(state, :session_context\) \|\| %\{\}\) \|> Map\.get\(:theme\)' lib/foglet_bbs/tui/screens/register.ex` |
| 9 | Inlined domain lookup | `grep -E 'get_in\(ctx, \[:domain' lib/foglet_bbs/tui/screens/register.ex` |

### AUDIT-15 exception (preserved)

`grep -c "credo:disable-for-next-line" lib/foglet_bbs/tui/screens/register.ex` must return
exactly `1` — the documented exception for `valid_invite_code?/1`'s `apply/3`.

### AUDIT-16 size gate

`wc -l lib/foglet_bbs/tui/screens/register.ex` must output a number strictly less than `294`.

### AUDIT-18 canonical section order — smoke check

```bash
grep -n "^  def \|^  defp " lib/foglet_bbs/tui/screens/register.ex
```

Expected sequence by section:
- §3 `def init_screen_state(`
- §4 `def render(`
- §5 `def handle_key(`  (multiple clauses)
- §6 `def handle_wizard_event(`  (multiple clauses)
- §7 `defp handle_invite_key(`, `defp handle_combined_key(`, `defp validate_and_submit(`, `defp next_field(`, `defp focused_input(`, `defp update_focused_input(`, `defp input_key(`
- §8 `defp render_invite_step(`, `defp render_combined_step(`, `defp keys_for(`
- §9 `defp get_register_ss(`, `defp put_register_ss(`, `defp clear_register_ss(`, `defp init_screen_state_for(`
- §10 `defp submit(`, `defp maybe_build_verify_code(`, `defp handle_register_success(`, `defp valid_invite_code?(`, `defp changeset_error_text(`, `defp registration_mode(`, `defp session_ctx(`, `defp maybe_log_verify_code(`

### AUDIT-19 presence

`grep -c "def init_screen_state" lib/foglet_bbs/tui/screens/register.ex` must return `1`.

### AUDIT-17 protected-region: register screen

REQUIREMENTS.md AUDIT-17 row: `login / register / verify` — "Below error line (gateways stay
minimal forever)". Both `render_invite_step/2` and `render_combined_step/2` must end with
`rows ++ error_items` where `error_items` is either `[]` or
`[text(""), text(reg.error, ...)]`. No row appears AFTER the error row.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: AUDIT-05..22 rubric sweep + LoC trim + mix precommit</name>
  <files>lib/foglet_bbs/tui/screens/register.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/register.ex (final post-Wave-1 state)
    - .planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md (AUDIT-05..22 rubric — full set)
    - .planning/workstreams/phase-03-screen-audit/phases/02-register/02-RESEARCH.md (AUDIT Rubric Assessment section — per-gate status summary)
    - .planning/workstreams/phase-03-screen-audit/phases/02-register/02-CONTEXT.md (D-10 Config.get render-path safety — document in moduledoc if not already)
    - CLAUDE.md (mix precommit is trusted to catch formatting + credo + sobelow + dialyzer — run it, fix what it reports)
  </read_first>
  <action>
    ### Step 1 — Run all 9 AUDIT-05 grep gates

    Run each grep command from the `<interfaces>` AUDIT-05 table above. Each must return zero
    matches. If any returns non-zero, fix the offending line (likely a stray inline theme read
    or color atom reintroduced by Wave 1). Re-run until all gates are zero.

    ### Step 2 — Verify AUDIT-15 preservation

    ```bash
    grep -c "credo:disable-for-next-line" lib/foglet_bbs/tui/screens/register.ex
    ```

    Must output exactly `1`. If `0`, the REGISTER-04 preservation was broken — restore the
    `valid_invite_code?/1` block from pre-Phase-2 (02-RESEARCH.md §Functions to PRESERVE
    VERBATIM has the exact code). If `>1`, some new suppression crept in — delete the extras;
    no new suppressions are allowed.

    ### Step 3 — AUDIT-16 line count gate

    ```bash
    wc -l lib/foglet_bbs/tui/screens/register.ex
    ```

    Must output a number strictly less than `294`. Pre-phase baseline: 294.

    If the count is `≥ 294`:

    - Compress `render_combined_step/2` further. The `fields = [{:handle, "...", reg.handle_input}, ...]`
      + `Enum.map` pattern is already the compressed form (02-PATTERNS.md); check whether any
      redundant whitespace or verbose clause can shrink.
    - Collapse similar module attributes if feasible.
    - Review for duplicate helpers (e.g. two blank-line gaps between functions can be reduced
      to one in some Elixir formatter configs — check `mix format` output is respected).
    - If compression below 294 is genuinely impossible without sacrificing readability, STOP
      and escalate to the user: "AUDIT-16 size gate cannot be met at N LoC vs 294 baseline —
      roadmap discussion required per AUDIT-16 policy 'Any increase triggers a roadmap
      discussion; default is rollback to scope.'" Do NOT silently commit a regression.

    ### Step 4 — AUDIT-17 protected-region verification

    Read `render_invite_step/2` and `render_combined_step/2`. Confirm the last expression in
    each `column` block is `rows ++ error_items` (or equivalent) with no rows appended AFTER
    `error_items`. If any decorative row was added below the error line during Wave 1 (e.g.
    a "Press Tab to continue" hint or spinner), remove it.

    ### Step 5 — AUDIT-18 canonical section order

    Run:
    ```bash
    grep -n "^  def \|^  defp " lib/foglet_bbs/tui/screens/register.ex
    ```

    Read the output line-by-line and confirm the function definitions appear in the order
    specified in `<interfaces>` §18. If any function is out of order, move it. Most common
    slip: `handle_wizard_event/2` landing in §5 alongside `handle_key/2` instead of its §6
    slot, or `maybe_build_verify_code/2` drifting into §9 instead of §10.

    If the section order is already correct, no edit is needed.

    ### Step 6 — AUDIT-19 presence

    ```bash
    grep -c "def init_screen_state" lib/foglet_bbs/tui/screens/register.ex
    grep -c "@spec init_screen_state(keyword()) :: map()" lib/foglet_bbs/tui/screens/register.ex
    ```

    Both must output `1`. If either is `0`, restore per §3 template in 02-02-PLAN.md Task 2.

    ### Step 7 — Moduledoc documents D-10 Config.get safety

    Verify the moduledoc (top of the file) includes language acknowledging that
    `Config.get("registration_mode", "open")` on the render path is safe because
    `Foglet.Config` is ETS read-through cached. If not present, append a paragraph to the
    moduledoc:

    ```elixir
    @moduledoc """
    ...existing moduledoc...

    ## Config.get safety (D-10)

    `Foglet.Config.get/2` calls in `registration_mode/1` are safe on the render path —
    `Foglet.Config` is ETS read-through cached, so repeated reads are constant-time. No
    render-path change required.
    """
    ```

    (This matches the Phase 1 precedent in login.ex's moduledoc per LOGIN-05.)

    ### Step 8 — mix precommit end-to-end

    ```bash
    mix precommit
    ```

    Must exit 0. `mix precommit` runs:
    1. `mix compile --warnings-as-errors`
    2. `mix format --check-formatted`
    3. `mix credo --strict`
    4. `mix sobelow`
    5. `mix dialyzer`

    If any stage fails:
    - **compile:** unresolved symbol or type mismatch — most likely an interim Wave 1 artifact
      that didn't get cleaned. Fix and re-run.
    - **format:** run `mix format lib/foglet_bbs/tui/screens/register.ex` and re-run precommit.
    - **credo:** typical offenders are: `apply/3` usage outside the documented exception
      (delete it), predicate function not ending in `?` (rename), long function (re-split).
      Fix and re-run.
    - **sobelow:** register.ex does not use Plug directly — a flag here usually means a string
      concatenation or `Code.eval_string` crept in; delete it.
    - **dialyzer:** spec mismatch on init_screen_state/1 or handle_wizard_event/2 return types
      — reconcile the spec. Most likely culprit: `handle_wizard_event/2` returning a 3-tuple
      `{:update, state, cmds}` instead of the required bare `{state, cmds}` — fix in Wave 1
      terms (this would indicate a Task 2 regression escaped earlier gates).

    After fixing each stage, re-run `mix precommit` from scratch — do not assume earlier
    stages still pass.

    ### Step 9 — Final sanity run of test suite

    ```bash
    mix test
    ```

    Must exit 0. This is a broader gate than the plan-level `mix test test/.../register_test.exs`
    because it exercises every consumer of `Foglet.TUI.App` struct — any place that pattern-
    matched on `register_wizard:` would blow up here and not in the screen-local tests.

    If the full suite has failures outside `test/foglet_bbs/tui/screens/`, investigate: another
    module may have been reading `state.register_wizard` (e.g. a supervisor, a session handler,
    a test helper). These are out-of-scope for Phase 2 per AUDIT-13(b) — if found, STOP and
    escalate. The most likely culprit is a test helper file that constructs an App struct with
    explicit `register_wizard: nil`; those just need the key removed.

    ### Step 10 — Grep-verified project-wide `register_wizard` sweep

    ```bash
    grep -rn "register_wizard" lib/ test/
    ```

    Expected matches:
    - `lib/foglet_bbs/tui/app.ex`: exactly one match — the `do_update({:register_wizard, event}, state)` clause at ~line 354
    - `lib/foglet_bbs/tui/screens/register.ex`: one match inside `handle_invite_key/2` — the emitted command `{:register_wizard, {:submit_step, :invite_code, value}}` (this is the dispatch tag, not a field reference; it is correct)
    - Nowhere else

    If extra matches exist in `lib/` or `test/`, they are leftover references to the removed
    field and must be cleaned. In `test/`, this usually means a test helper or a fixture
    builder in another screen's test file — remove those references.
  </action>
  <acceptance_criteria>
    - All 9 AUDIT-05 grep gates return `0` matches on `lib/foglet_bbs/tui/screens/register.ex`
    - `grep -c "credo:disable-for-next-line" lib/foglet_bbs/tui/screens/register.ex` returns exactly `1`
    - `wc -l lib/foglet_bbs/tui/screens/register.ex | awk '{print $1}'` outputs a number `<` `294` (strict)
    - `grep -c "def init_screen_state" lib/foglet_bbs/tui/screens/register.ex` returns `1`
    - `grep -c "@spec init_screen_state(keyword()) :: map()" lib/foglet_bbs/tui/screens/register.ex` returns `1`
    - `grep -c "ETS read-through cached\|Config.get safety" lib/foglet_bbs/tui/screens/register.ex` returns at least `1` (D-10 documented in moduledoc)
    - `mix precommit` exits `0`
    - `mix test` exits `0` (full suite, not just register tests)
    - `grep -rn "register_wizard" lib/ test/` returns exactly 2 meaningful matches: the dispatch clause in `lib/foglet_bbs/tui/app.ex` and the command tuple in `lib/foglet_bbs/tui/screens/register.ex`. Any other match indicates leftover field references.
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "credo:disable-for-next-line" lib/foglet_bbs/tui/screens/register.ex
      wc -l lib/foglet_bbs/tui/screens/register.ex
      grep -c "def init_screen_state" lib/foglet_bbs/tui/screens/register.ex
      grep -rn "register_wizard" lib/ test/
      mix precommit
    </automated>
  </verify>
  <done>
    All AUDIT-05 grep gates zero; AUDIT-15 exception preserved at exactly 1 occurrence;
    AUDIT-16 LoC strictly under 294; AUDIT-17 protected-region empty (no row below error
    line); AUDIT-18 canonical section order confirmed; AUDIT-19 init_screen_state/1 present
    with correct spec; D-10 Config.get safety documented in moduledoc; `mix precommit` exits 0;
    full `mix test` suite green; project-wide `register_wizard` references contain only the 2
    expected dispatch-tag matches.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — SSH smoke test of two-step register flow</name>
  <what-built>
    Wave 1 rewrote `register.ex` end-to-end (two-step wizard over `screen_state[:register]`,
    four TextInput structs on the :combined step, `with`-chain submit, AUDIT-18 canonical
    section order). Wave 2 Task 1 verified every grep-checkable AUDIT rubric item and `mix
    precommit` green. The automated gates cover source-level correctness, but AUDIT-17's
    protected-region invariant and the actual rendered KeyBar / focus highlighting / error
    message placement require a real SSH session — these are pixel-visible behaviors that no
    test assertion meaningfully exercises.

    The `mix precommit` toolchain caught: format, warnings, credo strictness, sobelow
    security patterns, dialyzer spec mismatches. `mix test` caught: every assertion in the
    Wave 0 test rewrite (init_screen_state/1, Tab cycling, Enter advance/submit, confirm-
    password mismatch, invite_code step round-trip, cancel flow).
  </what-built>
  <how-to-verify>
    Run the BBS locally and SSH in:

    ```bash
    # Terminal 1: start the server
    mix phx.server

    # Terminal 2: SSH into the TUI (adjust port if your config differs)
    ssh -p 2222 localhost
    ```

    Then walk through each of these scenarios and confirm visible behavior:

    **Scenario A — open-mode registration, happy path:**
    1. From the login screen, press `R` to enter register.
    2. You should see FOUR input rows stacked:
       ```
       Handle:           _
       Email:
       Password:
       Confirm password:
       ```
       The `Handle:` label is bold + accent-colored (focused); others are dim.
       The `_` represents the TextInput cursor on the focused field.
    3. Type `alice` — characters appear in the handle row.
    4. Press Tab — focus jumps to `Email:` (label becomes bold + accent).
    5. Type `alice@example.com`, Tab to Password, type `sekret01`, Tab to Confirm, type `sekret01`.
    6. Press Enter — registration proceeds (either to `:verify` screen or `:main_menu` depending
       on `require_email_verification` config). No error row appears; no "Passwords do not
       match." message shown.
    7. Press Escape at any point during data entry — returns to login screen; re-entering
       register shows a fresh empty form (screen_state[:register] cleared).

    **Scenario B — confirm-password mismatch:**
    1. From login, press `R`. Type handle + email, advance to Password.
    2. Type `sekret01` in Password, Tab, type `different` in Confirm.
    3. Press Enter — you should see the error row appear BELOW the confirm field:
       ```
       Confirm password: different_
       <blank row>
       Passwords do not match.       (bold, error-colored)
       ```
       Focus STAYS on `Confirm password:` (label still bold + accent). No screen transition.
    4. Tab — focus moves to Handle (cycling), error row disappears.
    5. Verify: NO content renders below the "Passwords do not match." row (AUDIT-17 gateway
       rule — below error line is protected).

    **Scenario C — invite_only mode, valid + invalid codes:**
    1. Set `registration_mode` config to `"invite_only"`
       (likely via `Foglet.Config.put("registration_mode", "invite_only")` in an IEx session,
       or via whatever config dashboard the app exposes).
    2. From login, press `R`. You should see ONE input row:
       ```
       Invite code: _
       ```
       (not the four-field :combined step).
    3. Type `no` (too short), press Enter — you should see:
       ```
       Invite code: no
       <blank row>
       Invalid code.                  (bold, error-colored)
       ```
       Field stays on :invite_code step.
    4. Backspace to clear, type `VALIDCODE1`, press Enter — transition to :combined step;
       four input rows appear with focus on Handle.
    5. Complete registration as in Scenario A.

    **Scenario D — sysop_approved mode:**
    1. Set `registration_mode` to `"sysop_approved"`.
    2. From login, press `R`. You should see the four-field :combined step directly (no
       invite_code step).
    3. Complete registration — expect a modal appearing with "Account Pending" title and
       message about sysop approval. Modal closes → session terminates.

    **Scenario E — KeyBar hints:**
    1. On :invite_code step: KeyBar (bottom of the screen) shows `Enter Submit · Esc Cancel`.
    2. On :combined step: KeyBar shows `Tab Switch field · Enter Next/Submit · Esc Cancel`.

    **Scenario F — password masking:**
    1. On :combined step, type in Password: you should see `*` characters, not the raw
       password.
    2. Same for Confirm password.
    3. Handle and Email fields are NOT masked (visible text).

    If ANY scenario fails or produces visible regressions (wrong colors, fields rendering
    outside the frame, error row showing in the wrong place, KeyBar missing keys), describe
    the issue. Otherwise type "approved".
  </how-to-verify>
  <resume-signal>
    Type "approved" if all six scenarios render correctly. If any scenario fails, describe the
    failing behavior in detail — an executor will open a gap-closure plan via `/gsd-plan-phase
    02 --gaps`.
  </resume-signal>
  <files>lib/foglet_bbs/tui/screens/register.ex (read-only — no edits during this checkpoint)</files>
  <action>
    Execute the six-scenario SSH smoke test described in `<how-to-verify>`. This task does not
    modify code. It pauses for the developer to connect via SSH, walk through all six scenarios,
    and either type "approved" or describe a failing behavior that triggers a gap-closure plan.
  </action>
  <verify>
    <automated>echo "MANUAL — checkpoint requires developer SSH smoke test; no automated gate."</automated>
  </verify>
  <done>
    Developer types "approved" on all six SSH-rendered scenarios (open happy path, confirm
    mismatch, invite_only valid + invalid, sysop_approved, KeyBar hints, password masking).
    If any scenario fails, a gap-closure plan is opened via `/gsd-plan-phase 02 --gaps`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer terminal → live BBS via SSH | Real session; exercises production code paths with test credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | Human-verify checkpoint exposes test credentials over SSH | accept | SSH to localhost on dev machine; credentials used ("alice" / "sekret01" / "VALIDCODE1") are explicit test values documented in the scenario. Scoped to developer's local machine. |
| T-02-03-02 | Tampering | Dev accidentally commits a debug patch during rubric sweep | mitigate | Task 1 acceptance criteria run `mix precommit` as the final gate. `mix precommit` includes `mix format --check-formatted` (catches stray whitespace) and `mix credo --strict` (catches stray `IO.inspect` calls or debug shims). Developer must resolve `mix precommit` before Task 2 resume-signal. |
| T-02-03-03 | Elevation of Privilege | AUDIT-15 exception accidentally expanded | mitigate | Task 1 acceptance criteria require `grep -c "credo:disable-for-next-line"` to return EXACTLY `1`. Any additional suppression fails the gate. Prevents gradual erosion of the single documented exception. |

**Note:** Wave 2 is verification-only; no new production code paths are added. Security posture
is inherited from Wave 1.
</threat_model>

<verification>
## Overall Plan Checks

1. **AUDIT-05 gates:** All 9 grep patterns return zero on register.ex.
2. **AUDIT-15 exception:** Exactly 1 occurrence of `credo:disable-for-next-line` (the
   `valid_invite_code?/1` block).
3. **AUDIT-16 size gate:** `wc -l` output strictly less than 294.
4. **AUDIT-17 protected region:** No row rendered below error line on either wizard step
   (verified via SSH smoke test Scenario B Step 5).
5. **AUDIT-18 canonical section order:** Function definitions appear in §3 → §4 → §5 → §6 →
   §7 → §8 → §9 → §10 order (grep verified).
6. **AUDIT-19 init_screen_state/1:** Present with `@spec init_screen_state(keyword()) ::
   map()`.
7. **`mix precommit`:** Green end-to-end.
8. **`mix test`:** Green full suite (not just register tests).
9. **Project-wide register_wizard sweep:** Exactly 2 meaningful matches (dispatch clause +
   command tag).
10. **Human SSH smoke test:** All six scenarios render correctly; "approved" received.
</verification>

<success_criteria>
1. All AUDIT-05..22 rubric items pass for register.ex (grep gates zero, AUDIT-15 exception
   preserved, AUDIT-16 under 294, AUDIT-17 no protected-region fill, AUDIT-18 section order,
   AUDIT-19 init_screen_state/1 present).
2. `mix precommit` exits 0 (compile + format + credo + sobelow + dialyzer all green).
3. `mix test` exits 0 (full suite, not just register tests).
4. Human-verify SSH smoke test passes all six scenarios (open happy path, confirm mismatch,
   invite_only valid + invalid, sysop_approved, KeyBar hints, password masking).
5. Phase 2 requirements REGISTER-01..06 all delivered and verifiable.
</success_criteria>

<output>
After completion, create `.planning/workstreams/phase-03-screen-audit/phases/02-register/02-03-SUMMARY.md`
</output>
