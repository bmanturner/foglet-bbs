---
phase: 05-boardlist
plan: 01
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - lib/foglet_bbs/tui/screens/board_list.ex
  - test/foglet_bbs/tui/screens/board_list_test.exs
autonomous: true
requirements:
  - BOARDS-01
  - BOARDS-02
  - BOARDS-03
  - BOARDS-04
  - BOARDS-05
user_setup: []
must_haves:
  truths:
    - "BoardList continues to render board rows via SelectionList + ListRow with unchanged j/k/Enter behavior"
    - "BoardList exposes init_screen_state/1 and removes repeated inline %{selected_index: 0} fallbacks"
    - "Loading state uses a spinner-backed row only while board load is in flight (board_list == nil)"
    - "BoardList.load_boards/1 remains as a documented test seam with @doc false; production ownership stays in App.do_update({:load_boards}, state)"
    - "BoardList row gutters stay unfilled (no additional row metadata/presence/badge content)"
    - "AUDIT-05 grep gates pass and mix precommit exits 0"
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/board_list.ex"
      provides: "Audit-compliant BoardList with initializer, spinner loading affordance, and documented load seam"
      max_lines: 130
    - path: "test/foglet_bbs/tui/screens/board_list_test.exs"
      provides: "BoardList behavior tests covering initializer, loading path, and navigation invariants"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/board_list.ex"
      to: "Foglet.TUI.Widgets.Progress.Spinner"
      via: "loading branch when state.board_list is nil"
      pattern: "Spinner.render"
    - from: "lib/foglet_bbs/tui/screens/board_list.ex"
      to: "Foglet.TUI.App.do_update({:load_boards}, state)"
      via: "load_boards/1 doc seam comment"
      pattern: "@doc false"
---

<objective>
Deliver Phase 5 BoardList planning scope in one bounded execution plan.

Purpose: preserve existing board-list behavior while closing the remaining audit deltas for state initializer adoption, async loading affordance, and dead-code-seam documentation.
Output: only `board_list.ex` and `board_list_test.exs` change; full quality gates pass.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/phase-03-screen-audit/STATE.md
@.planning/workstreams/phase-03-screen-audit/ROADMAP.md
@.planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
@.planning/workstreams/phase-03-screen-audit/phases/05-boardlist/05-CONTEXT.md
@.planning/workstreams/phase-03-screen-audit/phases/05-boardlist/05-RESEARCH.md
@.planning/workstreams/phase-03-screen-audit/phases/05-boardlist/05-PATTERNS.md
@.planning/workstreams/phase-03-screen-audit/phases/05-boardlist/05-VALIDATION.md
@AGENTS.md

@lib/foglet_bbs/tui/screens/board_list.ex
@test/foglet_bbs/tui/screens/board_list_test.exs
@lib/foglet_bbs/tui/app.ex
@lib/foglet_bbs/tui/widgets/progress/spinner.ex
</context>

<interfaces>
Target interfaces for this phase:

```elixir
@spec init_screen_state(keyword()) :: map()
def init_screen_state(opts \\ [])

@spec load_boards(map()) :: {map(), list()}
def load_boards(state)
```

Dead-code audit command (must be captured in summary evidence):

```bash
rg -n "load_boards" lib test
```

AUDIT-05 gate command for BoardList file:

```bash
rg -n ':red|:green|:cyan|:yellow|:blue|:magenta|:white|:black|"#[0-9a-fA-F]{6}"|\\e\[|\\x1b|%\{.*theme.*\||box style.*border|IO\.(write|puts|inspect)|\{80, 24\}|\(Map\.get\(state, :session_context\) \|\| %\{\}\) \|> Map\.get\(:theme\)|get_in\(ctx, \[:domain' lib/foglet_bbs/tui/screens/board_list.ex
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update BoardList screen for AUDIT-19 initializer + spinner loading + load seam docs</name>
  <files>lib/foglet_bbs/tui/screens/board_list.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/board_list.ex
    - lib/foglet_bbs/tui/app.ex (do_update({:load_boards}, state) ownership)
    - lib/foglet_bbs/tui/widgets/progress/spinner.ex
    - .planning/workstreams/phase-03-screen-audit/phases/05-boardlist/05-CONTEXT.md
    - .planning/workstreams/phase-03-screen-audit/phases/05-boardlist/05-RESEARCH.md
    - .planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
  </read_first>
  <action>
    Apply the following exact screen changes:

    1. Add public `init_screen_state/1` returning `%{selected_index: 0}` and replace all inline default fallbacks with that initializer in `render/1`, `handle_key/2`, and `move_selection/2`.
    2. Add `alias Foglet.TUI.Widgets.Progress.Spinner` and replace static loading text branch (`"Loading..."`) with a spinner-backed row that renders:
       - `Spinner.render(frame, style: :line, theme: theme)`
       - `text("Loading…", fg: theme.dim.fg)`
       where `frame` is derived from monotonic milliseconds divided by `Spinner.frame_duration_ms/0`.
    3. Keep empty-state and loaded-list rendering contracts unchanged (still `SelectionList` + `ListRow`; no additional board-row fields).
    4. Change `load_boards/1` doc to `@doc false` and add a comment stating this function is retained as a test seam while production loading is owned by `Foglet.TUI.App.do_update({:load_boards}, state)`.
    5. Keep the `Domain.get/2` fallback behavior unchanged.

    Do not modify any file outside `board_list.ex` for this task.
  </action>
  <acceptance_criteria>
    - `rg -n "def init_screen_state|@spec init_screen_state\\(keyword\\(\\)\\) :: map\\(\\)" lib/foglet_bbs/tui/screens/board_list.ex` returns matches.
    - `rg -n "Spinner.render|Spinner.frame_duration_ms|Loading…" lib/foglet_bbs/tui/screens/board_list.ex` returns matches.
    - `rg -n "SelectionList.render|ListRow.render" lib/foglet_bbs/tui/screens/board_list.ex` returns matches.
    - `rg -n "@doc false|test seam|App.do_update\\(\\{:load_boards\\}, state\\)" lib/foglet_bbs/tui/screens/board_list.ex` returns matches.
    - `rg -n "Loading\.\.\." lib/foglet_bbs/tui/screens/board_list.ex` returns zero matches.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>
      mix compile --warnings-as-errors
      mix test test/foglet_bbs/tui/screens/board_list_test.exs
    </automated>
  </verify>
  <done>BoardList has explicit initializer ownership, spinner-backed loading affordance, and documented load seam without behavior drift.</done>
</task>

<task type="auto">
  <name>Task 2: Update BoardList tests and run full quality gates</name>
  <files>test/foglet_bbs/tui/screens/board_list_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/screens/board_list_test.exs
    - lib/foglet_bbs/tui/screens/board_list.ex
    - .planning/workstreams/phase-03-screen-audit/REQUIREMENTS.md
    - .planning/workstreams/phase-03-screen-audit/phases/05-boardlist/05-VALIDATION.md
    - AGENTS.md
  </read_first>
  <action>
    Update tests to lock Phase 5 behavior while preserving existing route coverage:

    1. Add a test asserting `BoardList.init_screen_state() == %{selected_index: 0}`.
    2. Keep direct `BoardList.load_boards/1` tests (test seam retained by design).
    3. Add a render smoke test for loading state with `board_list: nil` to ensure spinner branch renders without crash.
    4. Keep existing navigation tests (`j`/`k`, Enter, `Q`) and their expectations unchanged.
    5. Run and pass:
       - `mix test test/foglet_bbs/tui/screens/board_list_test.exs`
       - `mix precommit`

    If precommit fails, fix issues without introducing new suppressions.
  </action>
  <acceptance_criteria>
    - `rg -n "init_screen_state\(\) == %\{selected_index: 0\}" test/foglet_bbs/tui/screens/board_list_test.exs` returns one match.
    - `rg -n "board_list: nil|Loading" test/foglet_bbs/tui/screens/board_list_test.exs` returns a loading-branch test match.
    - `mix test test/foglet_bbs/tui/screens/board_list_test.exs` exits 0.
    - `mix precommit` exits 0.
    - `git diff --name-only` for this phase only lists `lib/foglet_bbs/tui/screens/board_list.ex` and `test/foglet_bbs/tui/screens/board_list_test.exs` as code changes.
  </acceptance_criteria>
  <verify>
    <automated>
      mix test test/foglet_bbs/tui/screens/board_list_test.exs
      mix precommit
    </automated>
  </verify>
  <done>BoardList tests enforce initializer/loading behavior and all phase quality gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| key events -> BoardList handlers | Untrusted terminal input drives selection and navigation state. |
| board load command -> App async task | Screen rendering depends on asynchronously loaded domain data. |
| test seam function -> future contributors | Public helper misuse could bypass intended App ownership path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `board_list` selection state | mitigate | Keep bounded index math and initializer defaults; preserve `Enum.at` nil guard path. |
| T-05-02 | Denial of Service | loading affordance branch | mitigate | Spinner remains stateless and render-only; no blocking loop or long-running logic added in render. |
| T-05-03 | Information Disclosure | board row render | accept | Phase preserves existing board name + unread exposure, with no new metadata columns. |
| T-05-04 | Elevation of Privilege | `load_boards/1` seam ambiguity | mitigate | Mark seam `@doc false` and document App ownership to prevent accidental production coupling. |
</threat_model>

<verification>
Run after implementation:

```bash
mix test test/foglet_bbs/tui/screens/board_list_test.exs
mix precommit
rg -n "load_boards" lib test
rg -n ':red|:green|:cyan|:yellow|:blue|:magenta|:white|:black|"#[0-9a-fA-F]{6}"|\\e\[|\\x1b|%\{.*theme.*\||box style.*border|IO\.(write|puts|inspect)|\{80, 24\}|\(Map\.get\(state, :session_context\) \|\| %\{\}\) \|> Map\.get\(:theme\)|get_in\(ctx, \[:domain' lib/foglet_bbs/tui/screens/board_list.ex
wc -l lib/foglet_bbs/tui/screens/board_list.ex
```

The AUDIT-05 grep gate must return no matches, and `wc -l` must be less than or equal to the pre-phase baseline (`120`).
</verification>

<success_criteria>
Phase 05 planning is complete when BOARDS-01..05 are fully covered in this plan, all task actions are concrete and verifiable, security/threat requirements are captured, and execution can proceed with no additional planning artifacts required.
</success_criteria>

<output>
After completion, create `.planning/workstreams/phase-03-screen-audit/phases/05-boardlist/05-01-SUMMARY.md`.
</output>
