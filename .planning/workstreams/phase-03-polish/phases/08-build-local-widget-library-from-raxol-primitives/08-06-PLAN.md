---
phase: 08
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - lib/foglet_bbs/tui/size_gate.ex
  - lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
  - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
autonomous: true
requirements: [REQ-W-14]
requirements_addressed: [REQ-W-14]
tags: [raxol, refactor, audit, layout, spacer, justify_content, chrome]

must_haves:
  truths:
    - "Chrome screens (ScreenFrame, StatusBar, SizeGate) render identically before and after this plan — same visible output across every screen, same key bindings, same terminal-too-small copy"
    - "Every `justify_*` call site in `lib/` is inventoried with an explicit keep-or-refactor disposition recorded inline in this plan; each decision has a written rationale"
    - "If a site is refactored to `spacer()`, its existing test file passes unchanged (visual parity is preserved)"
    - "If a site keeps `justify_*`, the moduledoc names the reason (primarily: `spacer/1` is fixed-size per `vendor/raxol/lib/raxol/view/components.ex:164-172`; it cannot reproduce `:space_between` flex behavior without a dynamic width computation)"
    - "`mix precommit` is green after the audit/refactor"
  artifacts:
    - path: "lib/foglet_bbs/tui/size_gate.ex"
      provides: "Audited; keep-or-refactor decision applied per audit table below"
      contains: "Raxol.Core.Renderer.View"
    - path: "lib/foglet_bbs/tui/widgets/chrome/status_bar.ex"
      provides: "Audited; keep-or-refactor decision applied per audit table below"
      contains: "Raxol.Core.Renderer.View"
    - path: "lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex"
      provides: "Audited; keep-or-refactor decision applied per audit table below"
      contains: "Raxol.Core.Renderer.View"
  key_links:
    - from: "lib/foglet_bbs/tui/widgets/chrome/status_bar.ex:44"
      to: "`justify_content: :space_between` (or its `spacer()` equivalent per audit disposition)"
      via: "row-level style map"
      pattern: "justify_content|spacer"
    - from: "lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex:37"
      to: "`justify_content: :space_between` (or its `spacer()` equivalent per audit disposition)"
      via: "outer column style map"
      pattern: "justify_content|spacer"
    - from: "lib/foglet_bbs/tui/size_gate.ex:80"
      to: "`justify_content: :center, align_items: :center` (keep — see audit)"
      via: "centered column style map"
      pattern: "justify_content"
---

<objective>
Close the second clause of the Phase 8 ROADMAP goal: *"Also survey and adopt primitives we aren't yet using — e.g. `spacer()` — where they read more naturally than the equivalent `justify_*` attributes."*

Purpose: This is a focused audit + refactor pass over every `justify_*` call site in `lib/`. It is NOT a wrapping task — D-02 explicitly keeps base primitives (`spacer`, `divider`) direct-DSL and out of the widget catalog. The value of this plan is producing an **explicit written decision** for each call site, backed by reading the Raxol `spacer/1` source, and applying the refactor only where `spacer()` genuinely reads more naturally than `justify_*` without regressing behavior.

Output: Each of the three `justify_*` call sites in `lib/` is inspected, a keep-or-refactor decision is recorded with rationale, and any refactor is applied without regressing existing tests. `mix precommit` green.
</objective>

<execution_context>
@/Users/bfturner/Dev/local/foglet-bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bfturner/Dev/local/foglet-bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/phase-03-polish/STATE.md
@.planning/workstreams/phase-03-polish/ROADMAP.md
@.planning/workstreams/phase-03-polish/REQUIREMENTS.md
@.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-CONTEXT.md
@.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-RESEARCH.md
@.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-VALIDATION.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@docs/raxol/cookbook/THEMING.md
@lib/foglet_bbs/tui/size_gate.ex
@lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
@lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
@test/foglet_bbs/tui/size_gate_test.exs
@./CLAUDE.md

<interfaces>
<!-- Authoritative spacer/1 signature — read during planning from vendored Raxol source. -->

From `vendor/raxol/lib/raxol/view/components.ex:161-173`:
```elixir
@doc "Creates a spacer component."
@spec spacer(keyword() | map()) :: map()
def spacer(opts \\ []) do
  opts = if is_list(opts), do: Map.new(opts), else: opts

  %{
    type: :spacer,
    size: Map.get(opts, :size, 1),
    direction: Map.get(opts, :direction, :vertical)
  }
end
```

From `vendor/raxol/lib/raxol/ui/layout/engine.ex:323-334`:
```elixir
def process_element(%{type: :spacer} = spacer, space, acc) do
  size = Map.get(spacer, :size, 1)
  direction = Map.get(spacer, :direction, :vertical)

  {w, h} =
    case direction do
      :horizontal -> {size, space.height}
      _           -> {space.width, size}
    end

  [%{type: :spacer, x: space.x, y: space.y, width: w, height: h} | acc]
end
```

**Critical finding (drives the audit):** `spacer/1` has a **fixed `size` (default 1 cell)**; the layout engine allocates exactly `size` cells along the main axis and fills the cross-axis to the container width/height. It does **not** flex-grow to fill remaining space. Therefore `spacer()` in a `row` cannot reproduce `justify_content: :space_between`'s behavior of pushing two children to opposite ends of the container without an explicit caller-computed width (i.e., `spacer(size: container_width - left_width - right_width)`).

From `vendor/raxol/lib/raxol/core/renderer/view.ex:353` (DSL entrypoint):
```elixir
defdelegate spacer(opts \\ []), to: Raxol.View.Components
```

### Existing `justify_*` inventory in `lib/` (verified 2026-04-20)

```
lib/foglet_bbs/tui/size_gate.ex:60                      # moduledoc reference (not a call site)
lib/foglet_bbs/tui/size_gate.ex:80                      # CALL SITE — justify_content: :center, align_items: :center
lib/foglet_bbs/tui/widgets/chrome/status_bar.ex:28      # moduledoc reference (not a call site)
lib/foglet_bbs/tui/widgets/chrome/status_bar.ex:44      # CALL SITE — justify_content: :space_between
lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex:37    # CALL SITE — justify_content: :space_between
```

Exactly **3 call sites** total. Zero existing `spacer()` usages in `lib/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit every `justify_*` call site and apply keep-or-refactor decisions</name>
  <files>
    lib/foglet_bbs/tui/size_gate.ex,
    lib/foglet_bbs/tui/widgets/chrome/status_bar.ex,
    lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
  </files>

  <read_first>
    - lib/foglet_bbs/tui/size_gate.ex (target — full file, 92 LOC)
    - lib/foglet_bbs/tui/widgets/chrome/status_bar.ex (target — full file, 51 LOC)
    - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex (target — full file, 51 LOC)
    - test/foglet_bbs/tui/size_gate_test.exs (regression guard for SizeGate)
    - test/foglet_bbs/tui/app_test.exs (exercises ScreenFrame + StatusBar via App.view/1 — the *de facto* chrome regression tests since dedicated `status_bar_test.exs` / `screen_frame_test.exs` do not exist)
    - test/foglet_bbs/tui/layout_smoke_test.exs (chrome smoke coverage)
    - test/foglet_bbs/tui/screens/main_menu_test.exs (StatusBar caller coverage)
    - docs/raxol/getting-started/WIDGET_GALLERY.md §"Layout Primitives" / §"spacer" section
    - docs/raxol/cookbook/THEMING.md (confirm no theme implications — the three files already route colors through theme slots; this plan does NOT touch coloring)
    - vendor/raxol/lib/raxol/core/renderer/view.ex lines 350-360 (the `spacer/1` defdelegate)
    - vendor/raxol/lib/raxol/view/components.ex lines 161-173 (the `spacer/1` body — fixed-size semantics)
    - vendor/raxol/lib/raxol/ui/layout/engine.ex lines 320-335 AND 539-546 (the layout engine's spacer handling — confirms no flex-grow behavior)
  </read_first>

  <action>
    ## Step 1 — Confirm the inventory is complete

    Re-run the inventory grep before acting, in case a new `justify_*` site has landed since planning:

    ```bash
    grep -rn 'justify_' lib/ --include='*.ex' > /tmp/justify-inventory.txt
    wc -l /tmp/justify-inventory.txt   # expected: 5 lines (3 call sites + 2 moduledoc references)
    ```

    If the count differs from 5, stop and investigate. The audit table below is keyed to this exact inventory; any new site must be added to the table with its own disposition before proceeding.

    ## Step 2 — Apply the per-site audit

    For each call site, the planner's decision is recorded below. The executor's job is to **apply** these decisions and **preserve behavior**. Do not re-open a disposition unless the pre-condition evidence has changed.

    ### Audit table

    | Site | File / Line | Current | Disposition | Rationale |
    |------|-------------|---------|-------------|-----------|
    | A | `lib/foglet_bbs/tui/size_gate.ex:80` | `column style: %{justify_content: :center, align_items: :center}` | **KEEP** | `spacer()` has fixed `size` (default 1, per `vendor/raxol/lib/raxol/view/components.ex:164-172`). `justify_content: :center` centers N children in the container's remaining space — equivalent to `[spacer(size: (avail - children_height) ÷ 2), …children, spacer(size: …)]` with caller-computed sizes, which is strictly *less* readable. Keep as-is. No code change. The moduledoc already documents the intent ("Centering uses `column` with `justify_content: :center` and `align_items: :center`"). Add a one-line comment above the `column` pointing at this plan so a future reader sees the decision was deliberate. |
    | B | `lib/foglet_bbs/tui/widgets/chrome/status_bar.ex:44` | `row style: %{justify_content: :space_between}` | **KEEP** | Same reason. `:space_between` pushes the first child to the start and the last child to the end, with remaining space distributed between. `spacer()` cannot flex-grow, so reproducing this with `spacer()` would require `row do: left; spacer(size: container_width - left_width - right_width); right end` — a dynamic-width calculation that StatusBar intentionally delegates to the layout engine (the moduledoc at line 28 notes "`:space_between` then pushes the handle to the right edge"). Refactoring would introduce a measurement dependency with no readability gain. Keep as-is. Add a one-line comment above the `row` referencing this plan and the `spacer/1` fixed-size finding. |
    | C | `lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex:37` | `column style: %{gap: 0, justify_content: :space_between}` | **KEEP** | Same reason as B, on the vertical axis. The outer column has two children — the content-area column (StatusBar + divider + content_element) and the KeyBar — and `:space_between` pushes KeyBar to the bottom of the bordered box regardless of how tall `content_element` renders. Replacing with `spacer()` would require the parent to know `content_element`'s height, which it doesn't (content is opaque per D-05). Keep as-is. Add a one-line comment above the `column` referencing this plan. |

    **Net effect on `justify_*` count:** unchanged (3 call sites surviving). **Net effect on `spacer()` count in `lib/`:** unchanged (0).

    ### Why the audit still has value despite zero refactors

    The ROADMAP goal clause 2 is **"adopt where they read more naturally"** — not "replace everywhere." The correct action, given the Raxol `spacer/1` semantics, is to **document the decision** so a future maintainer (or a future CONTEXT.md decision) doesn't re-litigate it. This plan's output is the audit table above, committed inline in the three affected files via one-line comments, plus the explicit rationale in the plan summary.

    ## Step 3 — Apply the comments

    For each of the three files, add a single-line comment immediately above the `column`/`row` call that references this plan and the spacer-fixed-size finding. Suggested wording:

    **`lib/foglet_bbs/tui/size_gate.ex`** — above line 80:
    ```elixir
        # Kept `justify_content: :center` over `spacer()` per 08-06 audit
        # (spacer/1 is fixed-size — vendor/raxol/lib/raxol/view/components.ex:164).
        column style: %{justify_content: :center, align_items: :center} do
    ```

    **`lib/foglet_bbs/tui/widgets/chrome/status_bar.ex`** — above line 44:
    ```elixir
        # Kept `justify_content: :space_between` over `spacer()` per 08-06 audit —
        # spacer/1 is fixed-size (vendor/raxol/lib/raxol/view/components.ex:164),
        # cannot reproduce the flex-grow push-to-opposite-ends behavior without
        # caller-computed widths.
        row style: %{justify_content: :space_between} do
    ```

    **`lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex`** — above line 37:
    ```elixir
          # Kept `justify_content: :space_between` over `spacer()` per 08-06 audit —
          # would require knowing `content_element`'s height at call time.
          column style: %{gap: 0, justify_content: :space_between} do
    ```

    **Elixir gotchas (from CLAUDE.md):**
    - Comments do not affect compile; `mix format` will preserve them as long as they sit on their own line at the expected indentation.
    - Do NOT change anything other than adding the three comments — this plan is deliberately scoped to docs + audit record, not behavior change. Changing the `column`/`row` invocations breaks the "tests pass unchanged" invariant.

    ## Step 4 — Run the regression tests

    After adding the comments, run:

    ```bash
    mix test test/foglet_bbs/tui/size_gate_test.exs
    mix test test/foglet_bbs/tui/app_test.exs
    mix test test/foglet_bbs/tui/layout_smoke_test.exs
    mix test test/foglet_bbs/tui/screens/main_menu_test.exs
    ```

    All four must be green. No test file is modified in this task.

    ## Step 5 — If the audit inventory changed (step 1 returned ≠ 5 lines)

    If step 1 discovered a new `justify_*` call site that is not in the table, **halt** and return the discovery to the orchestrator. The planner's disposition table is exhaustive for the inventory as of 2026-04-20; new sites require a new decision, which requires human review. Do not improvise a disposition.
  </action>

  <verify>
    <automated>mix test test/foglet_bbs/tui/size_gate_test.exs test/foglet_bbs/tui/app_test.exs test/foglet_bbs/tui/layout_smoke_test.exs test/foglet_bbs/tui/screens/main_menu_test.exs --trace</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn 'justify_' lib/ --include='*.ex' | wc -l` returns 5 (3 call sites + 2 moduledoc mentions — no net-new sites introduced)
    - `grep -c '08-06 audit' lib/foglet_bbs/tui/size_gate.ex` ≥ 1 (audit comment landed)
    - `grep -c '08-06 audit' lib/foglet_bbs/tui/widgets/chrome/status_bar.ex` ≥ 1
    - `grep -c '08-06 audit' lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex` ≥ 1
    - `grep -q 'justify_content: :center, align_items: :center' lib/foglet_bbs/tui/size_gate.ex` exits 0 (Site A unchanged)
    - `grep -q 'justify_content: :space_between' lib/foglet_bbs/tui/widgets/chrome/status_bar.ex` exits 0 (Site B unchanged)
    - `grep -q 'gap: 0, justify_content: :space_between' lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex` exits 0 (Site C unchanged)
    - `mix test test/foglet_bbs/tui/size_gate_test.exs` exits 0
    - `mix test test/foglet_bbs/tui/app_test.exs` exits 0
    - `mix test test/foglet_bbs/tui/layout_smoke_test.exs` exits 0
    - `mix test test/foglet_bbs/tui/screens/main_menu_test.exs` exits 0
    - `mix compile --warnings-as-errors` exits 0
    - `mix format --check-formatted` exits 0
  </acceptance_criteria>

  <done>
    All three `justify_*` call sites carry an inline comment citing this plan and the `spacer/1` fixed-size rationale. Zero behavior change; zero test change. The audit record is now load-bearing — a future maintainer reading the chrome files sees the decision and its pre-condition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run mix precommit and write audit summary</name>
  <files>.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-06-SUMMARY.md</files>

  <read_first>
    - lib/foglet_bbs/tui/size_gate.ex (post-Task-1 state, with comment added)
    - lib/foglet_bbs/tui/widgets/chrome/status_bar.ex (post-Task-1)
    - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex (post-Task-1)
    - ./CLAUDE.md (to confirm `mix precommit` gate composition)
  </read_first>

  <action>
    ## Step 1 — Final precommit gate

    Run the full gate:

    ```bash
    mix precommit
    ```

    This runs `compile --warnings-as-errors`, `format`, `credo --strict`, `sobelow`, `dialyzer`, and `mix test`. Any failure halts the plan. If a gate fails, diagnose the root cause — do NOT skip with `--no-verify`. The only changes this plan makes are three comment lines, so a failure almost certainly indicates a pre-existing issue that surfaced or a formatting drift in the edited files; fix the minimal thing to pass the gate.

    ## Step 2 — Write the summary

    Create `.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-06-SUMMARY.md`:

    ```markdown
    # Phase 8 Plan 06 — `spacer()` Adoption Audit — Summary

    **Plan:** 08-06-PLAN.md (ROADMAP Phase 8 goal, clause 2)
    **Requirement closed:** REQ-W-14
    **Date:** <today>
    **Scope:** Audit every `justify_*` call site in `lib/` and decide per-site whether `spacer()` reads more naturally.

    ## Audit result

    | Site | File | Disposition | Rationale |
    |------|------|-------------|-----------|
    | A | `lib/foglet_bbs/tui/size_gate.ex:80` | KEEP `justify_content: :center, align_items: :center` | `spacer/1` has fixed size (`vendor/raxol/lib/raxol/view/components.ex:164-172`); cannot reproduce `:center` without caller-computed sizes. |
    | B | `lib/foglet_bbs/tui/widgets/chrome/status_bar.ex:44` | KEEP `justify_content: :space_between` | `spacer/1` does not flex-grow; reproducing `:space_between` would require dynamic width measurement of `text` children. |
    | C | `lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex:37` | KEEP `justify_content: :space_between` | Same — plus `content_element` is an opaque Raxol element (D-05 lock); parent cannot measure its height. |

    ## Net change

    - 3 files touched (comments only).
    - 0 `justify_*` sites refactored.
    - 0 `spacer()` calls added in `lib/`.
    - 0 test files changed.
    - 0 behavior changes in chrome or SizeGate rendering.

    ## Why the audit still closed the requirement

    The ROADMAP goal clause 2 reads *"adopt where they read more naturally"*. Reading the Raxol `spacer/1` source shows it cannot replace `justify_*` without a readability regression (introducing caller-computed sizes). Documenting this decision in the three files prevents a future maintainer from re-litigating it; that's the deliverable.

    ## Precommit

    `mix precommit` — green. (Gate composition per `./CLAUDE.md`: compile --warnings-as-errors, format, credo --strict, sobelow, dialyzer, mix test.)

    ## Follow-up

    If a future plan introduces a chrome layout where `content_element`'s height is known at build time (e.g., a fully statically-sized screen), revisit Site C. Otherwise this audit is durable.
    ```

    Fill in `<today>` with the date read from `./CLAUDE.md` memory (`2026-04-20`) or the current system date if available.
  </action>

  <verify>
    <automated>mix precommit && test -f .planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-06-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
    - `mix precommit` exits 0
    - `test -f .planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-06-SUMMARY.md` exits 0
    - `grep -q 'REQ-W-14' .planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-06-SUMMARY.md` exits 0
    - `grep -q 'KEEP' .planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-06-SUMMARY.md` exits 0 (audit dispositions recorded)
    - `grep -q 'spacer/1' .planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-06-SUMMARY.md` exits 0 (rationale preserved)
  </acceptance_criteria>

  <done>
    `mix precommit` green. Summary written and REQ-W-14 closed. Audit record permanent across three chrome files + summary artifact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source files → compiler | Only comment lines added; no new executable code paths. |
| Test harness → source | Tests exercise the unchanged `column`/`row` calls; behavior must be byte-identical. |

## STRIDE Threat Register (phase-local; ASVS L1)

This plan is a refactor-audit pass with no I/O, no authentication, no data flow. Threat surface is purely *regression in chrome rendering*.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Tampering | Accidental whitespace/indentation change during comment insertion | mitigate | Only add comments above existing lines; `mix format --check-formatted` gates any drift. |
| T-08-06-02 | Denial of service | Regression in SizeGate centering → terminal-too-small message misrenders | mitigate | `test/foglet_bbs/tui/size_gate_test.exs` must pass unchanged; listed in Task 1 `<verify>`. |
| T-08-06-03 | Tampering | Regression in StatusBar `:space_between` behavior (handle no longer right-aligned) | mitigate | `test/foglet_bbs/tui/screens/main_menu_test.exs` and `test/foglet_bbs/tui/app_test.exs` exercise StatusBar; listed in Task 1 `<verify>`. |
| T-08-06-04 | Tampering | Regression in ScreenFrame KeyBar positioning | mitigate | `test/foglet_bbs/tui/layout_smoke_test.exs` + `app_test.exs` exercise ScreenFrame; listed in Task 1 `<verify>`. |
</threat_model>

<verification>
Phase-local checks for this plan:

1. **Inventory unchanged (no net-new `justify_*` sites, no net-new `spacer()` calls):**
   ```
   grep -rn 'justify_' lib/ --include='*.ex' | wc -l      # expected: 5
   grep -rn 'spacer('  lib/ --include='*.ex' | wc -l      # expected: 0
   ```

2. **All three audit comments landed:**
   ```
   for f in \
     lib/foglet_bbs/tui/size_gate.ex \
     lib/foglet_bbs/tui/widgets/chrome/status_bar.ex \
     lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
   do
     grep -q '08-06 audit' "$f" || echo "MISSING comment in $f"
   done
   ```
   Expected: no "MISSING" output.

3. **All existing chrome + size-gate tests still green:**
   ```
   mix test test/foglet_bbs/tui/size_gate_test.exs \
            test/foglet_bbs/tui/app_test.exs \
            test/foglet_bbs/tui/layout_smoke_test.exs \
            test/foglet_bbs/tui/screens/main_menu_test.exs
   ```

4. **Final precommit gate:**
   ```
   mix precommit
   ```
</verification>

<success_criteria>
- Three chrome/size-gate files carry an inline `08-06 audit` comment explaining the keep-vs-refactor decision at the `justify_*` call site.
- Zero behavior change — every existing chrome test and size-gate test passes unchanged.
- `.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-06-SUMMARY.md` records the audit table, rationale, and precommit result.
- `mix precommit` green.
- REQ-W-14 closed.
</success_criteria>

<output>
After completion, `.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-06-SUMMARY.md` exists with the full audit record.
</output>
