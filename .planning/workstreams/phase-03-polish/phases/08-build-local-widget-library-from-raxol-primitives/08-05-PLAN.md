---
phase: 08
plan: 05
type: execute
wave: 2
depends_on: ["08-01", "08-02", "08-03", "08-04"]
files_modified:
  - lib/foglet_bbs/tui/widgets/README.md
  - test/foglet_bbs/tui/widgets/catalog_smoke_test.exs
autonomous: true
requirements: [REQ-W-12, REQ-W-13]
requirements_addressed: [REQ-W-12, REQ-W-13]
tags: [raxol, widget, docs, readme, catalog, smoke, precommit]

must_haves:
  truths:
    - "A developer landing in `lib/foglet_bbs/tui/widgets/` reads `README.md` and can name the file + module for every D-02 widget plus the unchanged neighbors"
    - "A cross-bucket smoke test renders one widget from each bucket (Input stateless, Input stateful, Display stateful, Display stateless, Progress, List) through a common theme fixture and refutes any hardcoded color atom across the combined output"
    - "`mix precommit` is green — every gate (compile --warnings-as-errors, format, credo --strict, sobelow, dialyzer, mix test) passes across the full Phase 8 widget catalog"
    - "Every widget listed in the README corresponds to an actual file in `lib/foglet_bbs/tui/widgets/`"
  artifacts:
    - path: "lib/foglet_bbs/tui/widgets/README.md"
      provides: "D-12 discoverability: one-line entry per module with link to file"
      contains: "SmartList"
    - path: "test/foglet_bbs/tui/widgets/catalog_smoke_test.exs"
      provides: "Cross-bucket integration smoke; ensures theme hygiene holds in combined render"
      contains: "defmodule Foglet.TUI.Widgets.CatalogSmokeTest"
  key_links:
    - from: "lib/foglet_bbs/tui/widgets/README.md"
      to: "every widget .ex file in the catalog"
      via: "Markdown relative link `[file](path)`"
      pattern: "\\]\\(.*\\.ex\\)"
    - from: "test/foglet_bbs/tui/widgets/catalog_smoke_test.exs"
      to: "one widget from each bucket"
      via: "single test case that renders Button + TextInput + Table + Tree + Progress + Spinner + SmartList + RadioGroup"
      pattern: "Foglet.TUI.Widgets\\.(Input|Display|Progress|List)\\."
---

<objective>
Lock in the Phase 8 catalog's discoverability and cross-bucket theme-hygiene story.

Purpose: Two things that only make sense AFTER all 11 widgets exist — (a) a `README.md` index that a developer can grep-then-open without hunting through directories (D-12), and (b) a cross-bucket smoke test that renders one widget from each bucket and proves the theme-hygiene invariant holds across the combined render tree (not just per-widget). This catches any regression where per-widget hygiene passes in isolation but a slot gets leaked during composition.

Output: 1 `README.md` + 1 cross-bucket smoke test file. `mix precommit` green — the final phase gate.
</objective>

<execution_context>
@/Users/bfturner/Dev/local/foglet-bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bfturner/Dev/local/foglet-bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/phase-03-polish/STATE.md
@.planning/workstreams/phase-03-polish/ROADMAP.md
@.planning/workstreams/phase-03-polish/REQUIREMENTS.md
@.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-CONTEXT.md
@.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-RESEARCH.md
@.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-PATTERNS.md
@.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-VALIDATION.md
@.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-01-SUMMARY.md
@.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-02-SUMMARY.md
@.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-03-SUMMARY.md
@.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-04-SUMMARY.md
@lib/foglet_bbs/tui/theme.ex
@test/foglet_bbs/tui/widgets/modal_test.exs
@test/foglet_bbs/tui/widgets/list/list_row_test.exs
@./CLAUDE.md

<interfaces>
<!-- Expected module names (every file was created in 08-01 through 08-04). -->

Phase 8 widgets (D-02):
  Foglet.TUI.Widgets.Input.Button
  Foglet.TUI.Widgets.Input.Checkbox
  Foglet.TUI.Widgets.Input.RadioGroup
  Foglet.TUI.Widgets.Input.TextInput
  Foglet.TUI.Widgets.Input.Tabs
  Foglet.TUI.Widgets.Input.Menu
  Foglet.TUI.Widgets.Display.Table
  Foglet.TUI.Widgets.Display.Tree
  Foglet.TUI.Widgets.Display.Progress
  Foglet.TUI.Widgets.Progress.Spinner
  Foglet.TUI.Widgets.List.SmartList

Pre-existing widgets (must also appear in README per D-12):
  Foglet.TUI.Widgets.Chrome.{ScreenFrame, StatusBar, KeyBar}
  Foglet.TUI.Widgets.Compose
  Foglet.TUI.Widgets.Modal
  Foglet.TUI.Widgets.Post.{MarkdownBody, PostCard}
  Foglet.TUI.Widgets.List.{SelectionList, ListRow}

Theme fixtures for cross-bucket test:
  Foglet.TUI.Theme.default()       # returns resolve(:gray)
  Foglet.TUI.Theme.resolve(:danger) # alternate palette
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write lib/foglet_bbs/tui/widgets/README.md (D-12 discoverability index)</name>
  <files>lib/foglet_bbs/tui/widgets/README.md</files>

  <read_first>
    - .planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-PATTERNS.md §"Plan 08-05 §README + integration" (the specified Markdown shape — verbatim copy is acceptable)
    - .planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-CONTEXT.md §D-12
    - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex (pre-existing, unchanged — confirm it exists)
    - lib/foglet_bbs/tui/widgets/chrome/status_bar.ex (pre-existing)
    - lib/foglet_bbs/tui/widgets/chrome/key_bar.ex (pre-existing)
    - lib/foglet_bbs/tui/widgets/compose.ex (pre-existing, flat per D-11)
    - lib/foglet_bbs/tui/widgets/modal.ex (pre-existing, flat per D-11)
    - lib/foglet_bbs/tui/widgets/post/markdown_body.ex (pre-existing)
    - lib/foglet_bbs/tui/widgets/post/post_card.ex (pre-existing)
    - lib/foglet_bbs/tui/widgets/list/selection_list.ex (pre-existing, D-03 lean)
    - lib/foglet_bbs/tui/widgets/list/list_row.ex (pre-existing)
    - lib/foglet_bbs/tui/widgets/input/button.ex (landed in 08-01)
    - lib/foglet_bbs/tui/widgets/input/checkbox.ex (landed in 08-01)
    - lib/foglet_bbs/tui/widgets/input/radio_group.ex (landed in 08-01)
    - lib/foglet_bbs/tui/widgets/input/text_input.ex (landed in 08-02)
    - lib/foglet_bbs/tui/widgets/input/tabs.ex (landed in 08-02)
    - lib/foglet_bbs/tui/widgets/input/menu.ex (landed in 08-02)
    - lib/foglet_bbs/tui/widgets/display/table.ex (landed in 08-03)
    - lib/foglet_bbs/tui/widgets/display/tree.ex (landed in 08-03)
    - lib/foglet_bbs/tui/widgets/display/progress.ex (landed in 08-03)
    - lib/foglet_bbs/tui/widgets/progress/spinner.ex (landed in 08-03)
    - lib/foglet_bbs/tui/widgets/list/smart_list.ex (landed in 08-04)
  </read_first>

  <action>
    ## Step 1 — Write `lib/foglet_bbs/tui/widgets/README.md`

    Use the verbatim shape specified in 08-PATTERNS.md §"Plan 08-05: README + integration" §"Specified shape". The full Markdown:

    ```markdown
    # Foglet.TUI.Widgets — Widget Catalog Index

    Every widget in this directory routes colors/styles through
    `Foglet.TUI.Theme` (D-07, D-09) and accepts the theme as an explicit
    `theme:` keyword argument (D-13). Stateful widgets expose the
    `init/1` + `handle_event/2` + `render/2` triplet (D-14); stateless
    widgets expose `render/*` only (D-16).

    New widgets should:

    - Live under `lib/foglet_bbs/tui/widgets/<bucket>/<name>.ex` (D-10).
    - Cite the decision IDs they honour in the `@moduledoc` (`D-07`, `D-09`, `D-13`, `D-14` or `D-16`).
    - Route colors through `Foglet.TUI.Theme` slots — never hardcoded
      `:red`/`:green`/`:cyan`/`:yellow`/`:blue`/`:magenta`/`:white`/`:black` atoms (D-07, D-09).
    - Declare module-constant defaults as `@default_*` / `@on_marker` / etc. at the
      top of the file (D-08).
    - Ship with a D-18 test (theme hygiene + smoke render) in `test/foglet_bbs/tui/widgets/<bucket>/`.

    ## Chrome (Phase 1, unchanged)

    | Module | File | Description |
    |---|---|---|
    | `Chrome.ScreenFrame` | [`chrome/screen_frame.ex`](chrome/screen_frame.ex) | Outer frame wrapping every screen (FRAME-01) |
    | `Chrome.StatusBar`   | [`chrome/status_bar.ex`](chrome/status_bar.ex)   | Top-of-screen title + handle bar (FRAME-02) |
    | `Chrome.KeyBar`      | [`chrome/key_bar.ex`](chrome/key_bar.ex)         | Bottom-of-screen key hints |

    ## Compose / Modal (Phase 4 / 7, flat — unchanged per D-11)

    | Module | File | Description |
    |---|---|---|
    | `Compose` | [`compose.ex`](compose.ex) | Shared plumbing for post/thread composers (COMPOSE-01/02) |
    | `Modal`   | [`modal.ex`](modal.ex)     | Modal body (info/error/warning/confirm) — thin adapter (D-20, Phase 7) |

    ## Post (Phase 1–3, unchanged)

    | Module | File | Description |
    |---|---|---|
    | `Post.MarkdownBody` | [`post/markdown_body.ex`](post/markdown_body.ex) | Themed markdown renderer (RENDER-01, RENDER-02) |
    | `Post.PostCard`     | [`post/post_card.ex`](post/post_card.ex)         | Per-post card (header + body) |

    ## List

    | Module | File | Description |
    |---|---|---|
    | `List.SelectionList` | [`list/selection_list.ex`](list/selection_list.ex) | Stateless selection list — caller owns `selected_index` (D-03) |
    | `List.ListRow`       | [`list/list_row.ex`](list/list_row.ex)             | Single list row with optional metadata (LIST-03) |
    | `List.SmartList`     | [`list/smart_list.ex`](list/smart_list.ex)         | Stateful — search + pagination + multi-select (D-02, Phase 8) |

    ## Input (Phase 8)

    | Module | File | Description |
    |---|---|---|
    | `Input.Button`     | [`input/button.ex`](input/button.ex)             | Themed button with `:role` (primary/secondary/danger/success) |
    | `Input.Checkbox`   | [`input/checkbox.ex`](input/checkbox.ex)         | Toggle with `checked?` + `disabled` |
    | `Input.RadioGroup` | [`input/radio_group.ex`](input/radio_group.ex)   | Single-choice selector (DSL-composed from `text/2`) |
    | `Input.TextInput`  | [`input/text_input.ex`](input/text_input.ex)     | Single-line input with validator/mask/max_length |
    | `Input.Tabs`       | [`input/tabs.ex`](input/tabs.ex)                 | Tab bar with Left/Right/1–9 nav |
    | `Input.Menu`       | [`input/menu.ex`](input/menu.ex)                 | Nested dropdown / context menu |

    ## Display (Phase 8)

    | Module | File | Description |
    |---|---|---|
    | `Display.Table`    | [`display/table.ex`](display/table.ex)       | Sortable / filterable / selectable table |
    | `Display.Tree`     | [`display/tree.ex`](display/tree.ex)         | Hierarchical tree with expand/collapse |
    | `Display.Progress` | [`display/progress.ex`](display/progress.ex) | Animated progress bar (stateless) |

    ## Progress (Phase 8)

    | Module | File | Description |
    |---|---|---|
    | `Progress.Spinner` | [`progress/spinner.ex`](progress/spinner.ex) | Indeterminate spinner (stateless, frame-index-driven) |

    ## Further reading

    - **Research:** [`.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-RESEARCH.md`](../../../../.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-RESEARCH.md) — Pattern 1 / Pattern 2 / Pattern 3 templates, pitfalls, architecture map.
    - **Raxol docs:** [`docs/raxol/getting-started/WIDGET_GALLERY.md`](../../../../docs/raxol/getting-started/WIDGET_GALLERY.md) — source gallery; every Phase 8 widget maps to a section here.
    - **Theming contract:** [`docs/raxol/cookbook/THEMING.md`](../../../../docs/raxol/cookbook/THEMING.md).
    ```

    **Implementation notes:**
    - Verify every file referenced (`chrome/screen_frame.ex`, `post/post_card.ex`, `list/smart_list.ex`, etc.) actually exists in the repo. If any is missing at this stage, it's a Phase 8 bug — fail fast.
    - Relative paths in the "Further reading" section are from `lib/foglet_bbs/tui/widgets/README.md` up four levels (`../../../../`).
  </action>

  <verify>
    <automated>test -f lib/foglet_bbs/tui/widgets/README.md && grep -c 'SmartList\|Table\|Tree\|Progress\|Spinner\|TextInput\|Button\|Checkbox\|RadioGroup\|Tabs\|Menu' lib/foglet_bbs/tui/widgets/README.md | awk '{ if ($1 >= 11) exit 0; else { print "Expected ≥11 widget mentions, got " $1; exit 1 } }'</automated>
  </verify>

  <acceptance_criteria>
    - `test -f lib/foglet_bbs/tui/widgets/README.md` exits 0
    - `grep -c 'SmartList\\|Display.Table\\|Display.Tree\\|Display.Progress\\|Progress.Spinner\\|TextInput\\|Input.Button\\|Input.Checkbox\\|RadioGroup\\|Input.Tabs\\|Input.Menu' lib/foglet_bbs/tui/widgets/README.md` ≥ 11
    - `grep -q 'Chrome.ScreenFrame' lib/foglet_bbs/tui/widgets/README.md` exits 0 (pre-existing widgets also listed per D-12)
    - `grep -q 'List.SelectionList' lib/foglet_bbs/tui/widgets/README.md` exits 0
    - `grep -q 'Post.MarkdownBody' lib/foglet_bbs/tui/widgets/README.md` exits 0
    - `grep -q 'D-07\\|D-09\\|D-13\\|D-14\\|D-16' lib/foglet_bbs/tui/widgets/README.md` exits 0
    - `grep -qE '\\]\\(chrome/screen_frame.ex\\)' lib/foglet_bbs/tui/widgets/README.md` exits 0 (relative link format)
    - `grep -qE '\\]\\(input/button.ex\\)' lib/foglet_bbs/tui/widgets/README.md` exits 0
    - `grep -qE '\\]\\(list/smart_list.ex\\)' lib/foglet_bbs/tui/widgets/README.md` exits 0
    - Every `.ex` file linked from the README actually exists in `lib/foglet_bbs/tui/widgets/` (manual executor check — if any link 404s, fail the task and investigate)
  </acceptance_criteria>

  <done>
    README.md is committed and indexes all D-02 widgets (11) plus the 9 pre-existing unchanged widgets (total 20 entries). Every linked file exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write cross-bucket smoke test + final mix precommit gate</name>
  <files>test/foglet_bbs/tui/widgets/catalog_smoke_test.exs</files>

  <read_first>
    - test/foglet_bbs/tui/widgets/modal_test.exs (theme-hygiene refute pattern lines 145-161)
    - test/foglet_bbs/tui/widgets/list/list_row_test.exs (flatten_text helpers lines 9-24)
    - lib/foglet_bbs/tui/theme.ex (Theme.default/0 + Theme.resolve/1)
    - All 11 Phase 8 widget source files (`lib/foglet_bbs/tui/widgets/{input,display,progress,list}/*.ex`) — to get the actual `render/*` signatures right
  </read_first>

  <behavior>
    Single-file integration test that renders one widget from each bucket into a combined tree and asserts:

    - Test 1 (Input stateless — Button): `Input.Button.render("Save", role: :primary, theme: t)` renders without raising.
    - Test 2 (Input stateless — Checkbox): `Input.Checkbox.render("Agree", checked?: true, theme: t)` renders without raising.
    - Test 3 (Input stateless — RadioGroup): `Input.RadioGroup.render(["A", "B"], 0, theme: t)` renders without raising.
    - Test 4 (Input stateful — TextInput): `TextInput.init(value: "hello") |> TextInput.render(theme: t)` renders without raising.
    - Test 5 (Input stateful — Tabs): `Tabs.init(tabs: ["Home", "Posts"]) |> Tabs.render(theme: t)` renders without raising.
    - Test 6 (Input stateful — Menu): `Menu.init(items: [%{label: "File", children: []}]) |> Menu.render(theme: t)` renders without raising.
    - Test 7 (Display stateful — Table): `Table.init(columns: [%{key: :name, label: "Name"}], rows: [%{name: "x"}]) |> Table.render(theme: t)` renders without raising.
    - Test 8 (Display stateful — Tree): `Tree.init(nodes: [%{id: :root, label: "R", children: []}]) |> Tree.render(theme: t)` renders without raising.
    - Test 9 (Display stateless — Progress): `Progress.render(0.5, theme: t)` renders without raising.
    - Test 10 (Progress — Spinner): `Spinner.render(0, theme: t)` renders without raising.
    - Test 11 (List stateful — SmartList): `SmartList.init(options: [{"A", 1}]) |> SmartList.render(theme: t)` renders without raising.
    - Test 12 (**Combined render — cross-bucket theme hygiene**): collect all 11 rendered trees into one list, serialize the whole list via `inspect/1`, and refute `:red`, `:green`, `:cyan`, `:yellow`, `:blue`, `:magenta`, `:white`, `:black`.
    - Test 13 (**Combined render — alt-theme differential**): same as 12 but assert the default-theme combined inspect differs from the `:danger`-theme combined inspect.
  </behavior>

  <action>
    ## Step 1 — Write `test/foglet_bbs/tui/widgets/catalog_smoke_test.exs`

    ```elixir
    defmodule Foglet.TUI.Widgets.CatalogSmokeTest do
      @moduledoc """
      Cross-bucket smoke + theme-hygiene test (Phase 8, REQ-W-13).

      Renders one widget from each bucket through a common theme and
      asserts (a) every render returns non-nil and (b) the combined
      rendered tree contains no hardcoded color atom. Catches the
      regression where per-widget hygiene passes in isolation but a
      slot gets leaked during composition (e.g., a new widget added
      without a proper `build_*_theme/1` helper).
      """

      use ExUnit.Case, async: true

      alias Foglet.TUI.Theme

      alias Foglet.TUI.Widgets.Display.{Progress, Table, Tree}
      alias Foglet.TUI.Widgets.Input.{Button, Checkbox, Menu, RadioGroup, Tabs, TextInput}
      alias Foglet.TUI.Widgets.List.SmartList
      alias Foglet.TUI.Widgets.Progress.Spinner

      defp theme, do: Theme.default()
      defp alt_theme, do: Theme.resolve(:danger)

      defp render_catalog(t) do
        [
          Button.render("Save", role: :primary, theme: t),
          Checkbox.render("Agree", checked?: true, theme: t),
          RadioGroup.render(["A", "B"], 0, theme: t),
          TextInput.render(TextInput.init(value: "hello"), theme: t),
          Tabs.render(Tabs.init(tabs: ["Home", "Posts"]), theme: t),
          Menu.render(Menu.init(items: [%{label: "File", children: []}]), theme: t),
          Table.render(Table.init(columns: [%{key: :name, label: "Name"}], rows: [%{name: "x"}]), theme: t),
          Tree.render(Tree.init(nodes: [%{id: :root, label: "R", children: []}]), theme: t),
          Progress.render(0.5, theme: t),
          Spinner.render(0, theme: t),
          SmartList.render(SmartList.init(options: [{"A", 1}]), theme: t)
        ]
      end

      describe "per-widget smoke renders (D-17, D-18)" do
        setup do
          {:ok, t: theme()}
        end

        test "Input.Button renders", %{t: t} do
          refute is_nil(Button.render("Save", role: :primary, theme: t))
        end

        test "Input.Checkbox renders", %{t: t} do
          refute is_nil(Checkbox.render("Agree", checked?: true, theme: t))
        end

        test "Input.RadioGroup renders", %{t: t} do
          refute is_nil(RadioGroup.render(["A", "B"], 0, theme: t))
        end

        test "Input.TextInput renders", %{t: t} do
          state = TextInput.init(value: "hello")
          refute is_nil(TextInput.render(state, theme: t))
        end

        test "Input.Tabs renders", %{t: t} do
          state = Tabs.init(tabs: ["Home", "Posts"])
          refute is_nil(Tabs.render(state, theme: t))
        end

        test "Input.Menu renders", %{t: t} do
          state = Menu.init(items: [%{label: "File", children: []}])
          refute is_nil(Menu.render(state, theme: t))
        end

        test "Display.Table renders", %{t: t} do
          state = Table.init(columns: [%{key: :name, label: "Name"}], rows: [%{name: "x"}])
          refute is_nil(Table.render(state, theme: t))
        end

        test "Display.Tree renders", %{t: t} do
          state = Tree.init(nodes: [%{id: :root, label: "R", children: []}])
          refute is_nil(Tree.render(state, theme: t))
        end

        test "Display.Progress renders", %{t: t} do
          refute is_nil(Progress.render(0.5, theme: t))
        end

        test "Progress.Spinner renders", %{t: t} do
          refute is_nil(Spinner.render(0, theme: t))
        end

        test "List.SmartList renders", %{t: t} do
          state = SmartList.init(options: [{"A", 1}])
          refute is_nil(SmartList.render(state, theme: t))
        end
      end

      describe "cross-bucket combined render (D-18, REQ-W-13)" do
        test "no hardcoded color atoms leak in the combined tree" do
          trees = render_catalog(theme())
          serialized = inspect(trees, printable_limit: :infinity, limit: :infinity)

          for atom <- ~w(:red :green :cyan :yellow :blue :magenta :white :black) do
            refute serialized =~ atom,
                   "combined catalog render leaked #{atom}: serialized tree contained the atom"
          end
        end

        test "alt-theme produces a different combined tree" do
          default_out =
            theme()
            |> render_catalog()
            |> inspect(printable_limit: :infinity, limit: :infinity)

          alt_out =
            alt_theme()
            |> render_catalog()
            |> inspect(printable_limit: :infinity, limit: :infinity)

          refute default_out == alt_out,
                 "switching the theme must produce a different combined render"
        end
      end
    end
    ```

    **Implementation notes:**
    - `render_catalog/1` uses ALL 11 widget module aliases and calls the real `init/1` + `render/2` shapes that 08-01 through 08-04 landed. If any signature diverges from the plans (e.g., because the implementer chose a slightly different option key), update the call site to match.
    - The ~w`(:red :green ...)` sigil produces a list of strings. `serialized =~ ":red"` uses the binary-contains regex form.
    - The combined `inspect/1` may be large — `printable_limit: :infinity, limit: :infinity` is required to actually catch leaks (default truncation hides the tail of the tree).

    ## Step 2 — Final precommit gate

    After the test file compiles and passes, run `mix precommit` to prove the whole Phase 8 catalog is green. Fix any lingering:
    - Format issues (`mix format`)
    - Credo warnings (`mix credo --strict`) — especially `String.to_atom` misuse, predicate naming (`?` suffix on private fns), list-Access warnings
    - Sobelow findings — unlikely in widget code, but check for any untrusted-input-to-atom flows
    - Dialyzer warnings — defstruct types, `@spec` alignment
    - Warnings-as-errors — any `Kernel.inspect/2` options the widget passed that Raxol rejects

    If any gate fails in a way that requires changes to the per-widget `.ex` files from earlier plans, make the minimal fix and note the divergence in the plan summary. Do NOT skip a gate.
  </action>

  <verify>
    <automated>mix test test/foglet_bbs/tui/widgets/catalog_smoke_test.exs --trace && mix precommit</automated>
  </verify>

  <acceptance_criteria>
    - `test -f test/foglet_bbs/tui/widgets/catalog_smoke_test.exs` exits 0
    - `grep -q 'defmodule Foglet.TUI.Widgets.CatalogSmokeTest' test/foglet_bbs/tui/widgets/catalog_smoke_test.exs` exits 0
    - `grep -q 'render_catalog' test/foglet_bbs/tui/widgets/catalog_smoke_test.exs` exits 0
    - `grep -c 'Foglet.TUI.Widgets\\.(Input\\|Display\\|Progress\\|List)\\.' test/foglet_bbs/tui/widgets/catalog_smoke_test.exs` ≥ 11 (all 11 widgets referenced)
    - `grep -q 'refute.*:red\\|refute.*:green\\|refute.*:cyan' test/foglet_bbs/tui/widgets/catalog_smoke_test.exs` exits 0
    - `mix test test/foglet_bbs/tui/widgets/catalog_smoke_test.exs` exits 0 with ≥13 tests passing
    - `mix test test/foglet_bbs/tui/widgets/` exits 0 — full widget layer green (all pre-existing + Phase 8)
    - `mix precommit` exits 0 — compile warnings-as-errors, format, credo --strict, sobelow, dialyzer, tests all green
  </acceptance_criteria>

  <done>
    Catalog smoke test committed; `mix test test/foglet_bbs/tui/widgets/` all green; `mix precommit` is green. Phase 8 is shippable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → developer | Markdown rendered by GitHub/IDE; no execution. Plain docs. |
| Smoke test → widget modules | Tests call widget APIs with hard-coded fixtures (no user input); purely internal. |
| `mix precommit` → source files | Static analysis (credo, sobelow, dialyzer) and tests — no runtime risk. |

## STRIDE Threat Register (phase-local; ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Information disclosure | README leaks internal-only module paths | accept | Every module listed is already part of the public `Foglet.TUI.Widgets.*` surface; no secrets. |
| T-08-05-02 | Tampering | Markdown XSS in rendered GitHub view | accept | Markdown rendered by GitHub/IDE only; no app-side parser. |
| T-08-05-03 | Denial of service | Combined smoke-test `inspect/1` OOM on huge trees | mitigate | Test fixtures are tiny (1–2 options per widget); `printable_limit: :infinity` is safe given bounded input. |
</threat_model>

<verification>
Phase-level checks:

1. **README completeness (REQ-W-12):**
   ```
   grep -c 'SmartList\|Display.Table\|Display.Tree\|Display.Progress\|Progress.Spinner\|TextInput\|Input.Button\|Input.Checkbox\|RadioGroup\|Input.Tabs\|Input.Menu' lib/foglet_bbs/tui/widgets/README.md
   ```
   Expected: ≥ 11.

2. **Every linked file exists:**
   ```
   for link in $(grep -oE '\]\([^)]+\.ex\)' lib/foglet_bbs/tui/widgets/README.md | sed 's/](\(.*\))/\1/'); do
     test -f "lib/foglet_bbs/tui/widgets/$link" || echo "MISSING: $link"
   done
   ```
   Expected: no "MISSING" output.

3. **Catalog smoke test references all 11 widgets:**
   ```
   grep -c 'Foglet.TUI.Widgets\.(Input\|Display\|Progress\|List)\.' test/foglet_bbs/tui/widgets/catalog_smoke_test.exs
   ```
   Expected: ≥ 11 (each of the 11 widgets referenced at least once).

4. **Full widget test suite green:**
   ```
   mix test test/foglet_bbs/tui/widgets/
   ```

5. **Final precommit gate (REQ-W-13):**
   ```
   mix precommit
   ```
   Expected: green.
</verification>

<success_criteria>
- `lib/foglet_bbs/tui/widgets/README.md` exists and lists every widget (Phase 1–8) with a relative link to the file + one-line description (D-12).
- `test/foglet_bbs/tui/widgets/catalog_smoke_test.exs` renders one widget from each bucket and asserts combined-tree theme hygiene + alt-theme differential.
- `mix test test/foglet_bbs/tui/widgets/` green across the full widget layer.
- `mix precommit` green — compile --warnings-as-errors, format, credo --strict, sobelow, dialyzer, mix test all pass.
- REQ-W-12 and REQ-W-13 closed; all 13 phase-local REQ-W-* IDs now accounted for across 08-01 through 08-05.
</success_criteria>

<output>
After completion, create `.planning/workstreams/phase-03-polish/phases/08-build-local-widget-library-from-raxol-primitives/08-05-SUMMARY.md` capturing:
- Files created (README.md + catalog_smoke_test.exs)
- REQ-W-12 and REQ-W-13 closed
- Full phase test count (sum across all five plans)
- Any divergences discovered in the cross-bucket test that required tweaking the 08-01/02/03/04 widgets
- Phase 8 completion note — `mix precommit` green confirmation
</output>
