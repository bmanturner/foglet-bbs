---
id: 22-02
phase: 22
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - lib/foglet_bbs/tui/screens/post_reader.ex
  - test/foglet_bbs/tui/screens/post_reader_test.exs
autonomous: true
requirements: [READER-01, READER-02, READER-03, READER-04]
tags: [tui, screen, post-reader, viewport, render-cache, read-pointers]

must_haves:
  truths:
    - "D-01/D-02/D-04: `PostReader` delegates selected-post visual assembly to `Foglet.TUI.Widgets.Post.PostCard` reader helper."
    - "D-03/D-13: Header and progress render outside `Viewport`; only guttered body rows are assigned to `Viewport.children`."
    - "D-04/D-05/D-06: Screen render includes compact `Post X of N • #message_number • @handle • age ago` metadata using theme-routed widget output."
    - "D-07/D-08/D-09: Rendered body rows include a lightweight gutter and preserve existing markdown rendering behavior."
    - "D-10: `PostReader.State.viewport`, `advance_post/2`, `scroll_post/2`, `warm_cache/4`, `warm_viewport/4`, and `prepare_after_load/3` keep their behavioral roles."
    - "D-11/D-12/D-13: Compact `Posts X/N` progress is always visible for selected post and remains outside scrollable body rows."
    - "D-14: Focused `post_reader_test.exs` assertions cover header, message number, handle, age, progress, gutter, and existing navigation/cache/read-pointer behavior."
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/post_reader.ex"
      provides: "Reader composition via PostCard reader parts and existing Viewport ownership"
      contains: "PostCard.reader"
    - path: "test/foglet_bbs/tui/screens/post_reader_test.exs"
      provides: "Focused render and behavior tests for Phase 22"
      contains: "Posts 3/12"
  key_links:
    - from: "PostReader.render_post_content/5"
      to: "PostCard.reader_parts/5"
      via: "cached tuples + width + theme + index/total"
      pattern: "PostCard\\.reader"
    - from: "PostReader.warm_viewport/4"
      to: "PostCard reader body-line path"
      via: "same helper/body-line function as render path"
      pattern: "render_body_lines"
---

<objective>
Integrate the Phase 22 `PostCard` reader helper into `Foglet.TUI.Screens.PostReader`. Replace the current loose two-line header/divider/body composition with a compact BBS-native header, guttered body rows, and compact progress while preserving viewport scroll ownership, navigation, reply/back transitions, render-cache behavior, and read-pointer flushing.

Purpose: This is the user-facing reader facelift. The screen remains the coordinator for selected post, cache lookup, viewport state, and chrome, while `PostCard` owns post-unit visual assembly.

Implements requirements: READER-01, READER-02, READER-03, READER-04.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-post-reader-facelift/22-CONTEXT.md
@.planning/phases/22-post-reader-facelift/22-SPEC.md
@.planning/phases/22-post-reader-facelift/22-RESEARCH.md
@.planning/phases/22-post-reader-facelift/22-VALIDATION.md
@.planning/phases/22-post-reader-facelift/22-01-PLAN.md
@SCREENS.md
@lib/foglet_bbs/tui/screens/post_reader.ex
@lib/foglet_bbs/tui/screens/post_reader/state.ex
@lib/foglet_bbs/tui/widgets/post/post_card.ex
@vendor/raxol/lib/raxol/ui/components/display/viewport.ex
@test/foglet_bbs/tui/screens/post_reader_test.exs
</context>

<threat_model>
| Ref | Threat | Severity | Mitigation |
|-----|--------|----------|------------|
| T-22-01 | Header/progress placed inside `Viewport.children` can scroll away and spoof the selected post context. | medium | `render_post_content/5` must compose `[parts.header, parts.progress, Viewport.render(...)]`; tests assert body lines do not include header/progress text. |
| T-22-02 | Divergent render and warm paths can produce incorrect scroll bounds. | medium | `render_post_content/5` and `warm_viewport/4` must use the same PostCard body-line helper for the selected post. |
| T-22-03 | Read-pointer or navigation regression from cosmetic refactor. | high | Preserve existing behavior tests and avoid changing `advance_post/2`, `scroll_post/2`, `flush_read_pointers/2` semantics except body-line warm shape. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add focused Phase 22 PostReader render tests</name>
  <files>test/foglet_bbs/tui/screens/post_reader_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/screens/post_reader_test.exs
    - lib/foglet_bbs/tui/screens/post_reader.ex
    - lib/foglet_bbs/tui/widgets/post/post_card.ex
    - .planning/phases/22-post-reader-facelift/22-CONTEXT.md
    - .planning/phases/22-post-reader-facelift/22-RESEARCH.md
  </read_first>
  <action>
    Extend `test/foglet_bbs/tui/screens/post_reader_test.exs` with a new `describe "render/1 - Phase 22 reader facelift"` block.

    Add focused tests:
    - A fully populated one-post state renders flattened text containing `Post 1 of 1`, `#42`, `@mina`, and an age token ending in `ago`. Use a post fixture with `id: "p42"`, `message_number: 42`, `user: %{handle: "mina"}`, and an inserted_at value close enough that `Foglet.TimeAgo.format/1` returns a short token. This covers D-04/D-05 and READER-01.
    - A longer 12-post state with `selected_post_index: 2` renders `Posts 3/12`. This covers D-11 and READER-03.
    - Rendered flattened text includes the chosen gutter glyph `│` or `|` and the selected body text. This covers D-07/D-08 and READER-02.
    - Markdown remains delegated: rendering `Hello **world**` does not include raw `**world**` in serialized output and does include `world`. This covers D-09.
    - Source-static assertion: `post_reader.ex` contains `PostCard.reader` (or chosen helper name) and does not build the compact header directly with all four required atoms in screen-local code. This covers D-01/D-02/D-04/READER-04.

    Preserve all existing navigation, scroll, reply, back, cache, load-absorb, and read-pointer tests unchanged.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/post_reader_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/foglet_bbs/tui/screens/post_reader_test.exs` contains `Phase 22 reader facelift`.
    - `test/foglet_bbs/tui/screens/post_reader_test.exs` contains `Post 1 of 1`, `#42`, `@mina`, and `Posts 3/12`.
    - `test/foglet_bbs/tui/screens/post_reader_test.exs` contains a source assertion for `PostCard.reader` or the chosen helper name.
    - `rtk mix test test/foglet_bbs/tui/screens/post_reader_test.exs` exits non-zero before Task 2 and exits 0 after Task 2.
  </acceptance_criteria>
  <done>
    Screen tests lock the visible Phase 22 reader contract before implementation changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compose PostReader from PostCard reader parts while preserving Viewport ownership</name>
  <files>lib/foglet_bbs/tui/screens/post_reader.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/post_reader.ex
    - lib/foglet_bbs/tui/screens/post_reader/state.ex
    - lib/foglet_bbs/tui/widgets/post/post_card.ex
    - vendor/raxol/lib/raxol/ui/components/display/viewport.ex
    - test/foglet_bbs/tui/screens/post_reader_test.exs
    - .planning/phases/22-post-reader-facelift/22-CONTEXT.md
    - .planning/phases/22-post-reader-facelift/22-RESEARCH.md
  </read_first>
  <action>
    Update `lib/foglet_bbs/tui/screens/post_reader.ex` in the render and warm paths only:

    - In `render_post_content/5`, after retrieving cached tuples, call the new PostCard reader helper with the selected post, cached tuples, terminal width, theme, `index: idx`, and `total: total`.
    - Replace the old loose header lines:
      - `text("Post #{idx + 1} of #{total}", fg: theme.dim.fg)`
      - `text(PostCard.author_line(post), fg: theme.dim.fg)`
      - `divider(char: "─", style: %{fg: theme.border.fg})`
      with the helper-returned compact `parts.header` and `parts.progress`.
    - Set `Viewport.children` from `parts.body_lines`, not from a screen-local `PostCard.render_body_lines(tuples, w, theme)` call unless that call is wrapped in a private helper shared by render and warm paths.
    - Keep `Viewport.update({:set_visible_height, available_height}, ss.viewport)` and `Viewport.update({:set_children, body_lines}, vp)` ownership exactly in `PostReader`. Header/progress must not enter `Viewport.children`.
    - Recompute `available_height` conservatively for the extra fixed progress row. The existing baseline is `max(h - 10, 5)`; choose `max(h - 11, 5)` only if needed to prevent command-bar collision at 64x22. The layout smoke plan will verify this.
    - In `warm_viewport/4`, use the same body-line helper/path as render so `scroll_post/2` and render agree on content height.
    - Do not change `handle_key/2`, `advance_post/2`, `scroll_post/2`, `load_posts/2`, `flush_read_pointers/2`, `prepare_after_load/3`, or read-position data shape except where body-line warm calls must use the new helper.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/post_reader_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/screens/post_reader.ex` contains `PostCard.reader` or the chosen helper name.
    - `lib/foglet_bbs/tui/screens/post_reader.ex` no longer contains `PostCard.author_line(post)` in `render_post_content/5`.
    - `lib/foglet_bbs/tui/screens/post_reader.ex` contains `parts.body_lines` or an equivalent body-lines variable from the reader helper.
    - `lib/foglet_bbs/tui/screens/post_reader.ex` still contains `Viewport.update({:set_children` and `Viewport.render(vp, %{})`.
    - `rtk mix test test/foglet_bbs/tui/screens/post_reader_test.exs` exits 0.
    - Existing tests for `n`, `p`, `j`, `k`, `R`, `Q`, `render_cache`, and `flush_read_pointers/2` still pass.
  </acceptance_criteria>
  <done>
    `PostReader` renders the compact metadata header, guttered markdown body, and compact progress while preserving existing viewport, navigation, cache, reply/back, and read-pointer behavior.
  </done>
</task>

</tasks>

<verification>
Run:

```bash
rtk mix test test/foglet_bbs/tui/screens/post_reader_test.exs
rtk mix test test/foglet_bbs/tui/widgets/post/post_card_test.exs
```
</verification>

<success_criteria>
- READER-01: Users see compact post position, stable message number, author, and age.
- READER-02: Body rendering has a gutter and keeps markdown output intact.
- READER-03: Longer-thread progress appears without breaking scroll, reply, previous/next, or back navigation.
- READER-04: `PostReader` delegates post-unit rendering to shared `PostCard` helper(s).
</success_criteria>
