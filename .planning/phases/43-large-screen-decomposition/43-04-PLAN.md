---
phase: 43
plan: 43-04
type: execute
wave: 3
depends_on: [43-01, 43-02, 43-03]
autonomous: true
requirements: [TUI-05]
requirements_addressed: [TUI-05]
files_modified:
  - lib/foglet_bbs/tui/screens/post_reader.ex
  - lib/foglet_bbs/tui/screens/post_reader/render.ex
  - test/foglet_bbs/tui/screens/post_reader_test.exs
---

<objective>
Extract `PostReader.Render` last, moving display-only reader assembly while preserving PostReader reducer logic, subscriptions, loading, navigation, read-pointer flushing, viewport warming, and Phase 44 cache/query boundaries.
</objective>

<must_haves>
<truths>
- TUI-05: PostReader must expose clearer reducer/state/render boundaries while preserving current behavior.
- D-01: PostReader render entry point is `Foglet.TUI.Screens.PostReader.Render`.
- D-02: Top-level `PostReader` keeps `init/1`, `update/3`, `subscriptions/2`, task effects, navigation, read-pointer flushing, and public non-render test seams.
- D-03: `PostReader.render/2` delegates to the sibling render entry point and no longer contains detailed post/body/frame render helper families.
- D-04: `PostReader.Render` owns frame/content/keybar assembly and detailed loaded/loading/error/empty rendering.
- D-05: `PostReader.Render` must not perform Repo calls, PubSub subscription changes, task starts, durable domain writes, or screen-state mutations.
- D-06: Styling remains routed through `Foglet.TUI.Theme` and existing widgets, especially `ScreenFrame`, `PostCard`, and `Spinner`.
- D-07: Extract render helpers only; leave loading, navigation, read-pointer flushing, viewport/cache warming, and `render_cache` mutation plumbing on the reducer/state side. PostReader eager loading and resize cache eviction remain Phase 44 scope.
- D-10: PostReader is planned last because it has the highest coupling between display, cache warming, and read-pointer behavior.
- D-12: Preserve reducer/effect tests through direct `PostReader.update/3`.
- D-13: Render verification must use existing smoke helpers or `rtk mix foglet.tui.render` evidence for PostReader.
- D-14: Add focused structural render-module tests only; no pure text-presence tests.
</truths>
</must_haves>

<tasks>
<task id="43-04-01" type="execute">
<read_first>
- `.planning/phases/43-large-screen-decomposition/43-CONTEXT.md`
- `.planning/phases/43-large-screen-decomposition/43-RESEARCH.md`
- `.planning/phases/43-large-screen-decomposition/43-PATTERNS.md`
- `lib/foglet_bbs/tui/screens/post_reader.ex`
- `lib/foglet_bbs/tui/screens/post_reader/state.ex`
- `lib/foglet_bbs/tui/widgets/post/post_card.ex`
- `lib/foglet_bbs/tui/widgets/progress/spinner.ex`
- `test/foglet_bbs/tui/screens/post_reader_test.exs`
</read_first>
<action>
Create `lib/foglet_bbs/tui/screens/post_reader/render.ex` defining `defmodule Foglet.TUI.Screens.PostReader.Render`.

Move PostReader display-only helpers from `post_reader.ex` into the render module:

- body/frame assembly currently inside `render/2`
- `render_post_content/5`
- `render_local_post_content/5`
- `render_loading/1`
- display-only empty/error helpers
- display-only post card/body rendering helpers
- `frame_state/2` only if it does not mutate or depend on reducer-side cache updates

Keep these in `PostReader`:

- `init/1`
- all `update/3` clauses
- `subscriptions/2`
- `load_posts/2`
- `flush_read_pointers/2`
- `warm_viewport/4`
- `parse_body/2` if it is part of `warm_viewport/4` or `render_cache` mutation plumbing
- `render_cache` mutation and pending read-pointer plumbing
- navigation effect creation
- Phase 44 deferred eager-loading/cache eviction behavior unchanged

If a helper is both display-related and cache-mutating, keep it in `PostReader` and pass an already-prepared render model to `PostReader.Render`. Do not move state mutation into the render module.

Make top-level `render/2` delegate:

```elixir
@impl true
@spec render(State.t(), Context.t()) :: any()
def render(%State{} = state, %Context{} = context), do: Render.render(state, context)
```
</action>
<acceptance_criteria>
- `rtk rg -n "defmodule Foglet\\.TUI\\.Screens\\.PostReader\\.Render" lib/foglet_bbs/tui/screens/post_reader/render.ex` finds the new render module.
- `rtk rg -n "Render\\.render\\(state, context\\)" lib/foglet_bbs/tui/screens/post_reader.ex` finds top-level delegation.
- `rtk rg -n "defp (render_post_content|render_local_post_content|render_loading)\\(" lib/foglet_bbs/tui/screens/post_reader.ex` exits with no matches.
- `rtk rg -n "def (load_posts|flush_read_pointers)\\(" lib/foglet_bbs/tui/screens/post_reader.ex` confirms public non-render seams remain.
- `rtk rg -n "defp warm_viewport\\(" lib/foglet_bbs/tui/screens/post_reader.ex` confirms viewport/cache warming remains out of the render module.
- `rtk rg -n "Effect\\.|Repo\\.|PubSub|Config\\.put|Effect\\.task" lib/foglet_bbs/tui/screens/post_reader/render.ex` exits with no matches.
- `rtk mix test test/foglet_bbs/tui/screens/post_reader_test.exs` exits 0.
</acceptance_criteria>
</task>

<task id="43-04-02" type="execute">
<read_first>
- `test/foglet_bbs/tui/screens/post_reader_test.exs`
- `lib/foglet_bbs/tui/screens/post_reader.ex`
- `lib/foglet_bbs/tui/screens/post_reader/render.ex`
</read_first>
<action>
Add focused PostReader decomposition contract coverage.

The tests must prove:

- `Foglet.TUI.Screens.PostReader.Render` is loaded and exports `render/2`.
- `PostReader.load_posts/2`, `PostReader.flush_read_pointers/2`, and `PostReader.subscriptions/2` remain available on the reducer-facing module.
- existing PostReader reducer tests still cover loading, thread activity, navigation, read-pointer flushing, and subscriptions through `PostReader.update/3`.

Do not add UI text-presence-only assertions.
</action>
<acceptance_criteria>
- `rtk rg -n "PostReader\\.Render|function_exported\\?.*:render, 2|function_exported\\?.*:subscriptions, 2" test/foglet_bbs/tui/screens/post_reader_test.exs` finds decomposition contract coverage.
- `rtk mix test test/foglet_bbs/tui/screens/post_reader_test.exs` exits 0.
- `rtk mix compile --warnings-as-errors` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `rtk mix test test/foglet_bbs/tui/screens/post_reader_test.exs`
- `rtk mix compile --warnings-as-errors`
</verification>

<success_criteria>
- PostReader has a sibling render entry point and top-level render delegation.
- PostReader render extraction does not move load/read-pointer/subscription/cache-warming behavior into render code.
- Phase 44 PostReader eager-loading and cache-eviction scope remains untouched.
</success_criteria>

