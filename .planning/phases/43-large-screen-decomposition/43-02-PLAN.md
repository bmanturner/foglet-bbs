---
phase: 43
plan: 43-02
type: execute
wave: 2
depends_on: [43-01]
autonomous: true
requirements: [TUI-05]
requirements_addressed: [TUI-05]
files_modified:
  - lib/foglet_bbs/tui/screens/main_menu.ex
  - lib/foglet_bbs/tui/screens/main_menu/render.ex
  - test/foglet_bbs/tui/screens/main_menu_test.exs
---

<objective>
Extract `MainMenu.Render` while preserving command descriptors, role visibility helpers, oneliner reducers, modal submit handling, and public non-render seams in the top-level MainMenu screen module.
</objective>

<must_haves>
<truths>
- TUI-05: MainMenu must be easier to modify through separated reducer/state/render boundaries.
- D-01: MainMenu render entry point is `Foglet.TUI.Screens.MainMenu.Render` in `lib/foglet_bbs/tui/screens/main_menu/render.ex`.
- D-02: `MainMenu` keeps reducer-facing callbacks, modal submit handling, task effect creation, and public non-render seams.
- D-03: `MainMenu.render/2` delegates to the sibling render entry point and no longer contains detailed panel helper families.
- D-04: `MainMenu.Render` owns frame/content/keybar assembly and detailed navigation/oneliner panel rendering.
- D-05: `MainMenu.Render` must not perform Repo calls, PubSub subscription changes, task starts, durable domain writes, or screen-state mutations.
- D-06: Styling remains routed through `Foglet.TUI.Theme` and existing Raxol/widgets.
- D-09: MainMenu menu/panel rendering moves to the render module while oneliner actions, modal submit flow, and domain-module resolution remain in reducer-facing code.
- D-10: This follows the smaller-screen extractions and keeps one screen complete in a single plan.
- D-12: Preserve reducer/effect tests through direct `MainMenu.update/3`.
- D-14: Do not add text-presence-only tests.
</truths>
</must_haves>

<tasks>
<task id="43-02-01" type="execute">
<read_first>
- `.planning/phases/43-large-screen-decomposition/43-CONTEXT.md`
- `.planning/phases/43-large-screen-decomposition/43-RESEARCH.md`
- `.planning/phases/43-large-screen-decomposition/43-PATTERNS.md`
- `lib/foglet_bbs/tui/screens/main_menu.ex`
- `lib/foglet_bbs/tui/screens/main_menu/state.ex`
- `test/foglet_bbs/tui/screens/main_menu_test.exs`
</read_first>
<action>
Create `lib/foglet_bbs/tui/screens/main_menu/render.ex` defining `defmodule Foglet.TUI.Screens.MainMenu.Render`.

Move MainMenu display-only helpers from `main_menu.ex` into the render module:

- body/content assembly currently inside `render/2`
- `nav_panel/3`
- `oneliners_panel/2`
- render-only row/panel/body formatting helpers
- display-only width helpers only if they are not public non-render seams

Keep these in `MainMenu`:

- `@main_menu_commands`
- `visible_destinations/1`
- `visible_actions/1`
- `__nav_panel_inner_width__/1`
- key handling for destinations/actions
- oneliner load/submit/hide task effects
- modal creation and modal submit reducers
- authorization visibility checks used to decide available commands

If `MainMenu.Render` needs visible destinations/actions, call `MainMenu.visible_destinations/1` and `MainMenu.visible_actions/1` or pass their computed values through a render model. Do not duplicate role visibility logic in the render module.

Make top-level `render/2` delegate:

```elixir
@impl true
@spec render(State.t() | nil, Context.t()) :: any()
def render(local_state, %Context{} = context), do: Render.render(normalize_state(local_state, context), context)
```

The exact normalization helper may stay private in `MainMenu`; the important target state is that render-specific panel helpers no longer live in `main_menu.ex`.
</action>
<acceptance_criteria>
- `rtk rg -n "defmodule Foglet\\.TUI\\.Screens\\.MainMenu\\.Render" lib/foglet_bbs/tui/screens/main_menu/render.ex` finds the new render module.
- `rtk rg -n "Render\\.render\\(" lib/foglet_bbs/tui/screens/main_menu.ex` finds top-level delegation.
- `rtk rg -n "defp (nav_panel|oneliners_panel)\\(" lib/foglet_bbs/tui/screens/main_menu.ex` exits with no matches.
- `rtk rg -n "def (visible_destinations|visible_actions)\\(" lib/foglet_bbs/tui/screens/main_menu.ex` confirms public non-render seams remain.
- `rtk rg -n "Effect\\.|Repo\\.|PubSub|Config\\.put|Effect\\.task" lib/foglet_bbs/tui/screens/main_menu/render.ex` exits with no matches.
- `rtk mix test test/foglet_bbs/tui/screens/main_menu_test.exs` exits 0.
</acceptance_criteria>
</task>

<task id="43-02-02" type="execute">
<read_first>
- `test/foglet_bbs/tui/screens/main_menu_test.exs`
- `lib/foglet_bbs/tui/screens/main_menu.ex`
- `lib/foglet_bbs/tui/screens/main_menu/render.ex`
</read_first>
<action>
Add focused MainMenu decomposition contract coverage.

The tests must prove:

- `Foglet.TUI.Screens.MainMenu.Render` is loaded and exports `render/2`.
- `MainMenu.visible_destinations/1` and `MainMenu.visible_actions/1` still return role/state-filtered command data used by existing reducer and render behavior.
- existing `MainMenu.update/3` tests still cover navigation, oneliner loading, modal submit, and hide flows.

Do not add assertions that only check for static rendered text. Prefer function exports, command data shape, state updates, effects, and existing structural helpers.
</action>
<acceptance_criteria>
- `rtk rg -n "MainMenu\\.Render|function_exported\\?.*:render, 2" test/foglet_bbs/tui/screens/main_menu_test.exs` finds decomposition contract coverage.
- `rtk mix test test/foglet_bbs/tui/screens/main_menu_test.exs` exits 0.
- `rtk mix compile --warnings-as-errors` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `rtk mix test test/foglet_bbs/tui/screens/main_menu_test.exs`
- `rtk mix compile --warnings-as-errors`
</verification>

<success_criteria>
- MainMenu has a sibling render entry point and top-level render delegation.
- MainMenu command/visibility behavior remains in reducer-facing code and covered by tests.
- MainMenu panel rendering helpers no longer live in the top-level module.
</success_criteria>

