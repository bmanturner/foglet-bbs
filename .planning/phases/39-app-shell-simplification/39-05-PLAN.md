---
phase: 39-app-shell-simplification
plan: 05
type: execute
wave: 2
depends_on: [39-03, 39-04]
files_modified:
  - lib/foglet_bbs/tui/app.ex
autonomous: true
requirements: [STATE-04, APP-01, APP-02, APP-03]
must_haves:
  truths:
    - "App.subscribe/1 derives screen-specific topics by calling subscriptions/2 on the active screen module via function_exported?/3, not by pattern-matching current_screen."
    - "App contains no function clause matching screen against any production-screen atom for entry-dispatch, broadcast routing, initial-state seeding, or topic derivation."
    - "Three App-side decoder helpers (post_reader_state_thread_id/1, post_composer_state_thread_id/1, thread_list_state_board_id/1) are deleted; six related helpers (routed_thread_topic/1, routed_thread_id/1, thread_list_board_topic/1, plus their callees) are deleted."
    - "{:set_user, …} and {:promote_session, …} navigate to MainMenu via Effect.navigate(:main_menu, %{}); first-load runs through MainMenu's :on_route_enter clause."
    - "{:board_activity, …} and {:thread_activity, …} route through the generic active-screen update path; no current_screen == gate remains."
    - "maybe_init_initial_screen_state/1 has only the generic clause; the :main_menu special case is gone."
  artifacts:
    - path: "lib/foglet_bbs/tui/app.ex"
      provides: "Runtime shell post-cleanup: build_pubsub_topics calls subscriptions/2 generically; entry-dispatch is one clause; broadcast handlers are screen-agnostic; the :main_menu special-case in maybe_init_initial_screen_state is gone; set_user/promote_session use Effect.navigate"
      contains: "screen_declared_topics"
  key_links:
    - from: "App.build_pubsub_topics/1"
      to: "screen_module.subscriptions/2"
      via: "function_exported?(module, :subscriptions, 2)"
      pattern: "function_exported?.*:subscriptions"
    - from: "App.maybe_dispatch_route_entry/3"
      to: "route_screen_update(state, screen_key(screen), :on_route_enter)"
      via: "single screen-agnostic clause"
      pattern: ":on_route_enter"
    - from: "App.do_update({:set_user, …}, …)"
      to: "Effect.navigate(:main_menu, %{})"
      via: "apply_effect/2"
      pattern: "Effect.navigate"
---

<objective>
Replace App's screen-specific machinery with the generic mechanisms the screen-side groundwork (Plans 39-03, 39-04) made possible:
1. Rewrite `build_pubsub_topics/1` to invoke `subscriptions/2` via `function_exported?/3`. Delete six helpers: `routed_thread_topic/1`, `routed_thread_id/1`, `post_reader_state_thread_id/1`, `post_composer_state_thread_id/1`, `thread_list_board_topic/1`, `thread_list_state_board_id/1`. Delete the `:board_list`-aggregate special case (BoardList.subscriptions/2 supplies it now).
2. Collapse the five per-screen `maybe_dispatch_route_entry/3` clauses (`:main_menu`, `:moderation`, `:sysop`, `:thread_list`, `:post_reader`) into one screen-agnostic clause that always sends `:on_route_enter`.
3. Rewrite `do_update({:set_user, user}, state)` and `do_update({:promote_session, user}, state)` to use `Effect.navigate(:main_menu, %{})` instead of hardcoded `route_screen_update(state, :main_menu, :load_oneliners)`.
4. Remove the `:main_menu`-specific clause in `maybe_init_initial_screen_state/1`.
5. Remove `current_screen ==` gates from `do_update({:board_activity, …}, state)` and `do_update({:thread_activity, …}, state)`; route generically.
6. Remove the no-op `maybe_seed_legacy_route_context/3` function and its call site.
7. Remove now-unused aliases (`PostComposer`, `PostReader`, `ThreadList`) per Credo `:UnusedAliasWarning`.

Note: This plan does NOT delete the seven legacy struct fields (Plan 39-07 owns that) and does NOT modify `breadcrumb_bar.ex` (Plan 39-06 owns that). Per Pitfall 1, the legacy callback bodies that read `state.posts` etc. must still compile, so the struct cleanup is deferred until after Plan 39-06.

Purpose: D-01, D-02, D-04, D-06, D-07, D-13, D-14, D-15. Closes APP-01 (App = runtime shell), APP-02 (no screen-specific result handlers), APP-03 (PubSub from screen-declared interests), STATE-04 (App stores screen states by key, no individual struct manipulation).

Output: `lib/foglet_bbs/tui/app.ex` materially smaller; specific line ranges deleted; new generic functions inserted; precommit and tests green.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-app-shell-simplification/39-CONTEXT.md
@.planning/phases/39-app-shell-simplification/39-SPEC.md
@.planning/phases/39-app-shell-simplification/39-PATTERNS.md
@AGENTS.md

<interfaces>
Existing public/private APIs that the rewrite uses (already in `app.ex`):
- `route_screen_update(%__MODULE__{} = state, key, message)` (`app.ex:851-865`) — generic dispatch with `function_exported?(module, :update, 3)` guard.
- `screen_module_for(state, key)` and `screen_state_for(state, key)` — screen module / local state lookup.
- `build_context(state)` (`app.ex:128-144`) — builds `Context.t()`.
- `screen_key(screen)` — atom-passthrough (used by existing routing).
- `current_route(state)` or equivalent — confirm by Grep; if no helper exists, `state.current_screen` is the right read.
- `apply_effect(state, %Effect{})` (`app.ex:152-160` and surrounding) — interpreter entry.
- `Effect.navigate/2` constructor.
- `init_route_screen_state(state, screen, params)` (`app.ex:777-794`) — already handles MainMenu via `function_exported?(module, :init, 1)`.

Existing optional-callback dispatch idiom (from PATTERNS.md "Shared Patterns" — `app.ex:854, 870, 964`):
```elixir
if Code.ensure_loaded?(module) and function_exported?(module, :update, 3) do
  ...
end
```

Pre-cleanup line ranges to delete (from CONTEXT.md and PATTERNS.md):
- `app.ex:417-526`: `build_pubsub_topics/1` body + `routed_thread_topic/1`, `routed_thread_id/1`, `post_reader_state_thread_id/1`, `post_composer_state_thread_id/1`, `thread_list_board_topic/1`, `thread_list_state_board_id/1`. (Replace `build_pubsub_topics`, delete the rest.)
- `app.ex:552-563`: `do_update({:set_user, user}, state)` body — replace.
- `app.ex:648-661`: `do_update({:board_activity, …}, …)` and `do_update({:thread_activity, …}, …)` clauses — replace.
- `app.ex:699-714`: `do_update({:promote_session, user}, state)` body — replace.
- `app.ex:808`: `maybe_seed_legacy_route_context/3` no-op definition + call site at `app.ex:157` (or wherever it's called from `apply_effect/2`).
- `app.ex:810-845`: five `maybe_dispatch_route_entry/3` per-screen clauses — replace with one.
- `app.ex:884-893`: `:main_menu` special case in `maybe_init_initial_screen_state/1` — delete.

Aliases at `app.ex:26-28` (`PostComposer`, `PostReader`, `ThreadList`) — verify each is unused after the deletes (Credo `:UnusedAliasWarning` will flag), then delete.

Test file:
- `test/foglet_bbs/tui/app_test.exs` lines 1483-1607 carry subscription regression tests; D-16 says preserve cases at 1483-1530, 1553-1585 (BoardList, ThreadList from route, ThreadList from local state, PostReader from route, PostReader from local state) verbatim. Plan 39-07 deletes the two pin-tests at 1531-1551 and 1587-1607 — DO NOT delete them in this plan; the seven struct fields still exist at this commit, so those tests still compile.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite build_pubsub_topics/1 + delete six topic-decoder helpers + delete maybe_seed_legacy_route_context/3</name>
  <files>lib/foglet_bbs/tui/app.ex</files>
  <behavior>
    - `App.subscribe/1` for an authenticated MainMenu state produces topics `["user:<id>"]` (matches D-18 pin).
    - `App.subscribe/1` for `:board_list` produces topics including `"boards"` (now sourced from `BoardList.subscriptions/2`).
    - `App.subscribe/1` for `:thread_list` with `state.screen_state[:thread_list]` having `board_id: "b-77"` produces topics including `"board:b-77"` (now sourced from `ThreadList.subscriptions/2`).
    - `App.subscribe/1` for `:post_reader` with `state.screen_state[:post_reader]` having `thread_id: "t-99"` produces topics including `"thread:t-99"` (now sourced from `PostReader.subscriptions/2`).
    - `App.subscribe/1` for `:thread_list` with `route_params: %{board_id: "b-route"}` and no local state produces topics including `"board:b-route"`.
    - All existing tests in `test/foglet_bbs/tui/app_test.exs` `subscribe/1` describe block (lines 1483-1607) pass except the two D-17 pin tests at 1531-1551 and 1587-1607 — those will be deleted in Plan 39-07. They CONTINUE to pass at this commit because the struct fields they reference still exist.
    - The Wave 0 D-18 pin (currently `@tag :phase39_target`) passes when run with `--include phase39_target`; this plan does NOT remove that tag (Plan 39-07 owns the wave 0 cleanup).
  </behavior>
  <read_first>
    lib/foglet_bbs/tui/app.ex (Read lines 1-30 for module attributes/aliases; lines 100-160 for subscribe/build_context shape; lines 410-530 for the entire build_pubsub_topics + helper block; lines 800-870 for route_screen_update / screen_module_for plumbing)
    lib/foglet_bbs/tui/screens/post_reader.ex (the new subscriptions/2 — confirm signature)
    lib/foglet_bbs/tui/screens/thread_list.ex (the new subscriptions/2 — confirm signature)
    lib/foglet_bbs/tui/screens/board_list.ex (the new subscriptions/2 — confirm signature)
    .planning/phases/39-app-shell-simplification/39-PATTERNS.md (lines 257-351 for the build_pubsub_topics rewrite section)
  </read_first>
  <action>
    **Step 1 — Replace `build_pubsub_topics/1` body and delete 6 helpers (`app.ex:417-526` range).**

    Find the existing `defp build_pubsub_topics(state) do ... end` definition. Replace its body so it only returns user-level topics + screen-declared topics:

    ```elixir
    defp build_pubsub_topics(%__MODULE__{} = state) do
      user_topics =
        if state.current_user do
          [PubSub.user_topic(state.current_user.id)]
        else
          []
        end

      user_topics ++ screen_declared_topics(state)
    end

    defp screen_declared_topics(%__MODULE__{} = state) do
      key = screen_key(state.current_screen)
      module = screen_module_for(state, key)

      if Code.ensure_loaded?(module) and function_exported?(module, :subscriptions, 2) do
        module.subscriptions(screen_state_for(state, key), build_context(state))
      else
        []
      end
    end
    ```

    Notes:
    - The `if state.current_screen in [:board_list], do: [PubSub.boards_aggregate() | topics]` branch is GONE — `BoardList.subscriptions/2` (Plan 39-03 Task 3) now supplies `["boards"]` whenever BoardList is the active screen.
    - Use `state.current_screen` directly (or `current_route(state)` if such a helper exists in this file — confirm by grep). PATTERNS.md uses `current_route(state)`; if that helper doesn't exist, `state.current_screen` is the right reference.
    - Use `Code.ensure_loaded?/1` AND `function_exported?/3` paired (matching the existing idiom at lines 854, 870, 964).

    DELETE the following six helper functions in their entirety (find by exact name with grep then delete the full `defp ... end` block for each):
    - `routed_thread_topic/1` (around `app.ex:463-473`)
    - `routed_thread_id/1` (around `app.ex:475-481`)
    - `post_reader_state_thread_id/1` (around `app.ex:483-488`)
    - `post_composer_state_thread_id/1` (around `app.ex:490-501`)
    - `thread_list_board_topic/1` (around `app.ex:503-512`)
    - `thread_list_state_board_id/1` (around `app.ex:521-526`)

    **Step 2 — Delete `maybe_seed_legacy_route_context/3` and its call site.**

    The function (around `app.ex:808`) is currently `defp maybe_seed_legacy_route_context(state, _screen, _params), do: state` (a no-op). Delete the function body. Then find its call site (Grep for `maybe_seed_legacy_route_context`) — likely inside the navigate-effect interpreter near `app.ex:157`. Remove the call so the data flow goes directly to the next pipeline step. Test by running `rtk mix compile --warnings-as-errors`.

    **Step 3 — Drop unused aliases.**

    After Step 1's deletions, `PostComposer`, `PostReader`, and `ThreadList` aliases at `app.ex:26-28` are likely unused (Credo `:UnusedAliasWarning` will fire on `mix precommit`). Run `rtk mix compile --warnings-as-errors` after Step 1 — if Credo or the compiler flag any of those aliases as unused, delete them. KEEP any alias that is still referenced (e.g. by a subscription pin-test setup helper).

    **Step 4 — Format and run.**

    `rtk mix format lib/foglet_bbs/tui/app.ex`
  </action>
  <verify>
    <automated>rtk mix compile --warnings-as-errors &amp;&amp; rtk mix test test/foglet_bbs/tui/app_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -nE 'routed_thread_topic|routed_thread_id|post_reader_state_thread_id|post_composer_state_thread_id|thread_list_board_topic|thread_list_state_board_id' lib/foglet_bbs/tui/app.ex` exits 0 (none of the six helpers remain).
    - `! grep -n 'maybe_seed_legacy_route_context' lib/foglet_bbs/tui/app.ex` exits 0.
    - `grep -c 'screen_declared_topics' lib/foglet_bbs/tui/app.ex` returns at least 2 (one definition, one call from `build_pubsub_topics/1`).
    - `grep -c 'function_exported?(.*:subscriptions, 2)' lib/foglet_bbs/tui/app.ex` returns at least 1.
    - `! grep -nE 'state\.current_screen in \[:board_list\]|state\.current_screen == :board_list' lib/foglet_bbs/tui/app.ex` exits 0 (the `:board_list` aggregate special case is gone).
    - `! grep -nE 'state\.current_screen == :thread_list|state\.current_screen == :post_reader' lib/foglet_bbs/tui/app.ex` (excluding comments — pipe through `grep -v '^[[:space:]]*#'` if needed) exits 0.
    - `rtk mix test test/foglet_bbs/tui/app_test.exs --only describe:"subscribe/1"` exits 0 (assuming a subscribe describe block exists; otherwise run the full file).
    - `rtk mix precommit` exits 0.
  </acceptance_criteria>
  <done>
    `build_pubsub_topics/1` is rewritten to delegate to `subscriptions/2`; six helpers and `maybe_seed_legacy_route_context/3` are deleted; unused aliases pruned; existing subscription regression tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collapse maybe_dispatch_route_entry/3 + remove :main_menu special-case in maybe_init_initial_screen_state/1</name>
  <files>lib/foglet_bbs/tui/app.ex</files>
  <behavior>
    - `App.maybe_dispatch_route_entry/3` has exactly ONE clause: a screen-agnostic delegation to `route_screen_update(state, screen_key(screen), :on_route_enter)`.
    - The five per-screen clauses (`:main_menu`, `:moderation`, `:sysop`, `:thread_list`, `:post_reader`) are deleted.
    - `App.maybe_init_initial_screen_state/1` has exactly ONE clause: the generic `init_route_screen_state(state, state.current_screen, state.route_params)` delegation. The `:main_menu`-special clause is gone.
    - First-load behavior for MainMenu, Moderation, Sysop, ThreadList, PostReader continues to work because Plan 39-04 added `:on_route_enter` clauses to each.
  </behavior>
  <read_first>
    lib/foglet_bbs/tui/app.ex (Read lines 805-900 for the entry-dispatch + initial-state region; verify the existing function names match)
    .planning/phases/39-app-shell-simplification/39-PATTERNS.md (lines 174-256 for the route-entry section, lines 473-528 for maybe_init_initial_screen_state section)
    .planning/phases/39-app-shell-simplification/39-04-SUMMARY.md (Plan 39-04 wrote the `:on_route_enter` clauses — confirm they exist)
  </read_first>
  <action>
    **Step 1 — Replace the five `maybe_dispatch_route_entry/3` clauses with one.**

    Find the block at `app.ex:810-845` (five clauses for `:main_menu`, `:moderation`, `:sysop`, `:thread_list`, `:post_reader` plus a final catch-all `(state, _screen, _params), do: {state, []}`). Delete the entire block and replace with one screen-agnostic clause:

    ```elixir
    defp maybe_dispatch_route_entry(%__MODULE__{} = state, screen, _params) do
      route_screen_update(state, screen_key(screen), :on_route_enter)
    end
    ```

    Reasoning:
    - `route_screen_update/3` (`app.ex:851-865`) already has the `function_exported?(module, :update, 3)` guard, so screens that don't implement `:on_route_enter` hit their `update(_message, state, _ctx)` catch-all and become no-ops.
    - Plan 39-04 added `:on_route_enter` clauses to all five screens that need first-load behavior. The semantics (user-conditional for MainMenu/Moderation/Sysop, unconditional for ThreadList, thread_id-gated for PostReader) now live IN the screens.

    DO NOT pattern-match `screen` against any production-screen atom. SPEC R4 acceptance test: `! grep -nE 'defp maybe_dispatch_route_entry.*:[a-z_]+ ?,' lib/foglet_bbs/tui/app.ex` exits 0 (the only clause matches a variable, not an atom).

    **Step 2 — Replace `maybe_init_initial_screen_state/1`.**

    Find the function at `app.ex:884-893`. The current shape:
    ```elixir
    defp maybe_init_initial_screen_state(%{current_screen: :main_menu, current_user: user} = state)
         when not is_nil(user) do
      main_menu_state = state |> build_context() |> Screens.MainMenu.init() |> Map.put(:oneliner_status, :idle)
      put_screen_state(state, :main_menu, main_menu_state)
    end

    defp maybe_init_initial_screen_state(%__MODULE__{} = state) do
      init_route_screen_state(state, state.current_screen, state.route_params)
    end
    ```

    DELETE the first clause entirely. Keep only the second (generic) clause. Reasoning per D-15: `init_route_screen_state/3` at lines 777-794 already handles MainMenu via the `function_exported?(module, :init, 1)` branch, and MainMenu's `:on_route_enter` clause (Plan 39-04 Task 1) sets `oneliner_status: :loading` before the task fires — the `:idle` shim is unnecessary.

    **Step 3 — Format and verify.**

    `rtk mix format lib/foglet_bbs/tui/app.ex`
  </action>
  <verify>
    <automated>rtk mix compile --warnings-as-errors &amp;&amp; rtk mix test test/foglet_bbs/tui/app_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'defp maybe_dispatch_route_entry' lib/foglet_bbs/tui/app.ex` returns exactly 1.
    - The single clause uses a variable for the screen argument, not an atom: `! grep -E 'defp maybe_dispatch_route_entry\(.*, :(login|register|verify|main_menu|board_list|thread_list|post_reader|post_composer|new_thread|account|moderation|sysop)' lib/foglet_bbs/tui/app.ex` exits 0.
    - `grep -c ':on_route_enter' lib/foglet_bbs/tui/app.ex` returns at least 1 (the new dispatch references it).
    - `grep -c 'defp maybe_init_initial_screen_state' lib/foglet_bbs/tui/app.ex` returns exactly 1.
    - `! grep -E 'current_screen: :main_menu' lib/foglet_bbs/tui/app.ex` exits 0 (the special case is gone).
    - `rtk mix test test/foglet_bbs/tui/app_test.exs` exits 0; full screen reducer suite (`rtk mix test test/foglet_bbs/tui/screens/`) exits 0.
    - `rtk mix precommit` exits 0.
  </acceptance_criteria>
  <done>
    `maybe_dispatch_route_entry/3` is one screen-agnostic clause; `maybe_init_initial_screen_state/1` is one generic clause; first-load behavior is preserved through the screens' new `:on_route_enter` clauses.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite set_user / promote_session to use Effect.navigate; remove broadcast :board_activity / :thread_activity gates</name>
  <files>lib/foglet_bbs/tui/app.ex</files>
  <behavior>
    - `do_update({:set_user, user}, state)` returns the result of `apply_effect(%{state | current_user: user}, Effect.navigate(:main_menu, %{}))`. Through the existing navigate interpretation chain, this hits `init_route_screen_state` → `maybe_dispatch_route_entry` (now generic) → `MainMenu.update(:on_route_enter, …)` → emits the load_oneliners task effect.
    - `do_update({:promote_session, user}, state)` does the same, plus the existing `Foglet.Sessions.Supervisor.promote_guest_session/2` side effect when `state.session_pid` is a pid.
    - `do_update({:board_activity, _, _} = msg, state)` routes `msg` to the active screen via `route_screen_update(state, screen_key(state.current_screen), msg)`. BoardList's existing `update/3` (or a new clause added in this task) handles it; other screens no-op.
    - `do_update({:thread_activity, _, _} = msg, state)` routes the same way. PostReader already handles `{:thread_activity, …}` (see PostReader update clause around `post_reader.ex:106-120` per PATTERNS.md), so no new screen change is needed there.
    - The `current_screen ==` gates at `app.ex:648-661` are gone.
    - BoardList NEEDS a new `update({:board_activity, …}, state, ctx)` clause — add it as a tiny piggy-back step (it's small and naturally pairs with the App-side change).
    - All existing `app_test.exs` tests pass; `board_list_test.exs` and `post_reader_test.exs` pass.
  </behavior>
  <read_first>
    lib/foglet_bbs/tui/app.ex (Read lines 545-720 for set_user / promote_session / board_activity / thread_activity clauses)
    lib/foglet_bbs/tui/screens/post_reader.ex (Grep for `:thread_activity` to confirm it's already handled)
    lib/foglet_bbs/tui/screens/board_list.ex (Grep for `:board_activity` — likely NOT yet handled; we'll add a tiny clause)
    .planning/phases/39-app-shell-simplification/39-PATTERNS.md (lines 415-470 for set_user/promote_session, lines 354-413 for broadcast routing, lines 880-902 for BoardList :board_activity clause)
  </read_first>
  <action>
    **Step 1 — Rewrite `do_update({:set_user, user}, state)` (around `app.ex:552-563`).**

    Replace the existing body:
    ```elixir
    defp do_update({:set_user, user}, state) do
      apply_effect(%{state | current_user: user}, Effect.navigate(:main_menu, %{}))
    end
    ```

    Reasoning per D-14: `Effect.navigate/2` interpretation already runs `init_route_screen_state` then `maybe_dispatch_route_entry` (`app.ex:152-160`); the new generic `:on_route_enter` dispatch (Task 2) reaches MainMenu, which delegates to `:load_oneliners`. The hardcoded `route_screen_update(state, :main_menu, :load_oneliners)` is no longer needed.

    Note: SPEC R4 carve-out — `Effect.navigate(:main_menu, %{})` is a literal data argument, not a screen-specific pattern match for entry-message dispatch. This is allowed.

    **Step 2 — Rewrite `do_update({:promote_session, user}, state)` (around `app.ex:699-714`).**

    ```elixir
    defp do_update({:promote_session, user}, state) do
      if is_pid(state.session_pid) do
        Foglet.Sessions.Supervisor.promote_guest_session(state.session_pid, user)
      end

      apply_effect(%{state | current_user: user}, Effect.navigate(:main_menu, %{}))
    end
    ```

    Preserve the `Foglet.Sessions.Supervisor.promote_guest_session/2` side effect — the pre-cleanup behavior promoted the guest session before navigating.

    **Step 3 — Replace the broadcast clauses (around `app.ex:648-661`).**

    Delete the four-clause block (gated and catch-all variants for `{:board_activity, …}` and `{:thread_activity, …}`). Replace with two screen-agnostic clauses:

    ```elixir
    defp do_update({:board_activity, _board_id, _event} = msg, state) do
      route_screen_update(state, screen_key(state.current_screen), msg)
    end

    defp do_update({:thread_activity, _thread_id, _event} = msg, state) do
      route_screen_update(state, screen_key(state.current_screen), msg)
    end
    ```

    `route_screen_update/3`'s existing `function_exported?(module, :update, 3)` guard ensures screens that don't care no-op cleanly.

    **Step 4 — Add `BoardList.update({:board_activity, …}, …)` clause.**

    Locate `lib/foglet_bbs/tui/screens/board_list.ex`. Add a new `update/3` clause adjacent to the existing `update(:load, …)` clause (or insert above it):

    ```elixir
    def update({:board_activity, _board_id, _event}, local_state, %Context{} = context) do
      update(:load, local_state, context)
    end
    ```

    Reasoning: today, App's `{:board_activity, _, _}` clause fires `route_screen_update(:board_list, :load)`. After Task 3's App-side change, the message arrives at BoardList directly — so BoardList must internalize the reload semantics. (PostReader already has its own `:thread_activity` clause per PATTERNS.md line 401-403; verify by `grep -n ':thread_activity' lib/foglet_bbs/tui/screens/post_reader.ex`.)

    **Step 5 — Format and verify.**

    `rtk mix format lib/foglet_bbs/tui/app.ex lib/foglet_bbs/tui/screens/board_list.ex`
  </action>
  <verify>
    <automated>rtk mix compile --warnings-as-errors &amp;&amp; rtk mix test test/foglet_bbs/tui/app_test.exs test/foglet_bbs/tui/screens/board_list_test.exs test/foglet_bbs/tui/screens/post_reader_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -nE 'state\.current_screen == :board_list|state\.current_screen == :post_reader' lib/foglet_bbs/tui/app.ex | grep -v '^[[:space:]]*#'` exits 0 (no `current_screen ==` production gates remain — comments excluded).
    - `! grep -E 'when state\.current_screen ==' lib/foglet_bbs/tui/app.ex` exits 0.
    - `grep -c 'route_screen_update(state, :main_menu, :load_oneliners)' lib/foglet_bbs/tui/app.ex` returns 0 (the hardcoded calls are gone).
    - `grep -cE 'Effect\.navigate\(:main_menu' lib/foglet_bbs/tui/app.ex` returns at least 2 (one in set_user, one in promote_session).
    - `grep -c ':board_activity' lib/foglet_bbs/tui/screens/board_list.ex` returns at least 1.
    - `grep -c 'Foglet.Sessions.Supervisor.promote_guest_session' lib/foglet_bbs/tui/app.ex` returns at least 1 (side effect preserved).
    - `rtk mix test test/foglet_bbs/tui/app_test.exs` exits 0.
    - `rtk mix precommit` exits 0.
  </acceptance_criteria>
  <done>
    `set_user` / `promote_session` use `Effect.navigate(:main_menu, %{})`; broadcast clauses route generically; BoardList handles `:board_activity` directly; all tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ screen module | App invokes `module.subscriptions/2` and `module.update/3` via `function_exported?/3`. Module identity is resolved through `screen_module_for/2`, which uses an internal `screen_modules` allow-list (Phase 34 D-09). No untrusted module can be invoked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08 | E (elevation of privilege) | Generic `:on_route_enter` dispatch could be triggered with arbitrary screen | mitigate | Per Phase 34 D-09, `screen_module_for/2` is allow-listed via `session_context.domain.screen_modules`. An attacker cannot inject a non-Foglet module. |
| T-39-09 | T (tampering) | Generic `{:board_activity, …}` / `{:thread_activity, …}` route to active screen | accept | These messages arrive via PubSub from `Foglet.Boards`/`Foglet.Threads` contexts (authoritative writers). The active screen is set by user navigation effects only. No new attack surface. |
| T-39-10 | R (repudiation) | `set_user` / `promote_session` side-effect (`Sessions.Supervisor.promote_guest_session/2`) | mitigate | Side effect preserved exactly as before; `is_pid/1` guard unchanged. |
</threat_model>

<verification>
- App's PubSub subscription path uses `subscriptions/2` exclusively for screen-specific topics.
- App's entry-dispatch is one clause; broadcast routing is generic.
- All existing `app_test.exs` tests pass (the two D-17 pin tests at 1531-1551 and 1587-1607 are still green at this commit because the legacy struct fields still exist; Plan 39-07 deletes them).
- `rtk mix precommit` exits 0; `rtk mix test` exits 0.
- BoardList handles `:board_activity` directly.
</verification>

<success_criteria>
- [ ] All three tasks complete; all line-range deletions done.
- [ ] App contains no `current_screen` pattern match against any production atom for entry-dispatch, broadcast, initial-state, or topic derivation.
- [ ] Six topic-decoder helpers and `maybe_seed_legacy_route_context/3` are deleted.
- [ ] `screen_declared_topics/1` exists and is the only screen-specific topic source.
- [ ] First-load behavior for MainMenu/Moderation/Sysop/ThreadList/PostReader still works (verified by their reducer tests).
- [ ] BoardList handles `:board_activity`; PostReader continues to handle `:thread_activity`.
- [ ] `rtk mix precommit` exits 0; `rtk mix test` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/39-app-shell-simplification/39-05-SUMMARY.md` recording: line-count delta on `app.ex` (for the eventual non-gating SPEC R10 report), names of all deleted helpers, and confirmation that the two D-17 pin tests at `app_test.exs:1531-1551` and `:1587-1607` still compile and pass at this commit (they will be deleted in Plan 39-07).
</output>
