# Checkpoint — Plan 39-08 Task 2 (SPEC R10 Human-Verify)

**Status:** AWAITING HUMAN APPROVAL
**Type:** checkpoint:human-verify (gate=blocking)
**Plan:** 39-app-shell-simplification / 08
**Spawned:** sequential executor on main working tree

## What was done (Task 1 — autonomous, committed)

- Ran all 12 SPEC §Acceptance Criteria checks; captured verbatim commands, exit codes, and outputs.
- Captured render byte-equivalence diffs for the 5 tracked screens (`main_menu`, `board_list`, `thread_list`, `post_reader`, `account`).
- Recorded line-count delta for `app.ex` (-145), `breadcrumb_bar.ex` (-89), `render_fixtures.ex` (-6).
- Wrote `39-SUMMARY.md` (the phase-level closeout artifact) and `39-08-SUMMARY.md` (per-plan record).

## What is awaiting approval (Task 2 — human-verify)

**SPEC R10:** "Every function in `lib/foglet_bbs/tui/app.ex` (957 lines, post-Phase-39) is attributable to one of the runtime-shell categories: Raxol callbacks, message normalization, route storage, screen-state storage, context construction, effect interpretation, modal/SizeGate, session runtime hooks, PubSub plumbing, rendering dispatch."

The full function-by-function classification table is in `39-SUMMARY.md` under "## Qualitative App-Shell Review (SPEC R10 attestation)". 44 functions classified across 11 categories.

**Executor verdict:** PASS — every function maps to exactly one runtime-shell category; no function exists solely to handle one specific migrated production screen.

## Reviewer instructions

Open `lib/foglet_bbs/tui/app.ex` end-to-end. Spot-check any function in the classification table against its category. If you find a function whose category is wrong, OR a function that exists solely to handle one specific production screen (e.g., a clause that pattern-matches `:main_menu` and does main-menu-only work), report it as a finding.

Resume signal:
- **`approved`** — appends final commit with STATE/ROADMAP/REQUIREMENTS update; phase closes.
- **`fail: <function-name> — <reason>`** — orchestrator opens a follow-up plan or amends 39-SUMMARY.md with the finding before close.

## Two important caveats reported in 39-SUMMARY.md

These are NOT regressions — they are baseline conditions that predate Phase 39 and are tracked in `deferred-items.md`:

1. **`mix precommit` exits 2** on two pre-existing dialyzer warnings (`board_list.ex:161` pattern_match_cov, `sysop.ex:823` pattern_match). Phase 39 introduced ZERO new precommit failures and resolved the third pre-existing warning (`app.ex:63 ThreadEntry.t/0`).
2. **Render byte-equivalence diff has wall-clock-time deltas on all 5 screens** (the right-edge `HH:MM` slot in the chrome) plus **breadcrumb-only deltas on 3 screens** (`main_menu`, `board_list`, `account` — the screens NOT yet migrated to explicit `breadcrumb_parts` per Plan 39-06). These are explicitly permitted by SPEC line 120's exception clause.

## Files written by Task 1

- `.planning/phases/39-app-shell-simplification/39-SUMMARY.md` — phase-level closeout (substantive artifact).
- `.planning/phases/39-app-shell-simplification/39-08-SUMMARY.md` — per-plan record (orchestration meta).
- `.planning/phases/39-app-shell-simplification/.checkpoint-39-08.md` — this file (checkpoint marker).

No source code is modified by this plan.
