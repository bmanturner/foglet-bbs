---
phase: 47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - lib/foglet_bbs/tui/screens/board_list.ex
  - lib/foglet_bbs/tui/screens/moderation.ex
  - lib/foglet_bbs/tui/screens/account/render.ex
  - lib/foglet_bbs/tui/screens/post_reader/render.ex
  - lib/foglet_bbs/tui/screens/thread_list.ex
  - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
  - lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
  - lib/foglet_bbs/tui/widgets/chrome/key_bar.ex
  - lib/foglet_bbs/tui/widgets/chrome/normalizer.ex
autonomous: true
requirements: [R5]
must_haves:
  truths:
    - "All screen call sites under lib/foglet_bbs/tui/screens/ emit V2 grouped command bars"
    - "Chrome KeyBar, ScreenFrame, and StatusBar contain no V1 code paths"
    - "Foglet.TUI.Widgets.Chrome.Normalizer module file no longer exists"
    - "Foglet.TUI.Widgets.Chrome.KeyBar module file no longer exists (it was a Normalizer→CommandBar shim)"
    - "All current TUI screen tests pass; V1 fixture tests are deleted (not skipped)"
  artifacts:
    - path: "lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex"
      provides: "V2-only chrome rendering — no Normalizer alias, no legacy-title clause, no V1 fallback branch"
      forbids: "alias.*Normalizer"
    - path: "lib/foglet_bbs/tui/widgets/chrome/status_bar.ex"
      provides: "V2-only — legacy-title arity at :37 removed"
  key_links:
    - from: "Five migrated screen call sites"
      to: "Foglet.TUI.Widgets.Chrome.CommandBar.normalize_groups/1"
      via: "V2 grouped command bar emission"
      pattern: "%\\{label:"
---

<objective>
Drop all Chrome V1 compatibility shims. Migrate the five surviving V1 call
sites in `lib/foglet_bbs/tui/screens/` (board_list, moderation, account,
post_reader/render, thread_list) to V2 grouped command bars, then remove
the V1 branches from `KeyBar`, `ScreenFrame`, and `StatusBar`, and delete
`KeyBar` and `Normalizer` entirely.

Purpose: closes SPEC R5. CONTEXT D-10 fixes a five-step deletion order
that preserves a green compile at every step — this plan executes that
order as five sequential tasks within the plan.

Output: V2-only chrome surface; V1 fixture tests deleted (D-25); SPEC R5
grep gate clean.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-SPEC.md
@.planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-CONTEXT.md
@.planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-PATTERNS.md
@AGENTS.md

<interfaces>
V2 grouped command bar shape (canonical example — lib/foglet_bbs/tui/screens/sysop/render.ex:52-79):

```elixir
defp sysop_commands(ss, jump_hint) do
  base = [
    %{
      label: "System",
      commands: [%{key: "Q", label: "Back", priority: 0}]
    },
    %{
      label: "Tabs",
      commands: [
        %{key: "←/→", label: "Tab", priority: 10},
        %{key: jump_hint, label: "Jump", priority: 10}
      ]
    }
  ]
  # ...
end
```

Priority tier conventions (from sysop/render.ex):
  - System group commands: priority: 0
  - Tabs / Navigate: priority: 10
  - Actions: priority: 30 (or 5 for conditional commands like Revoke/Retry)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate five V1 screen call sites to V2 grouped command bars (D-10 step 1)</name>
  <files>lib/foglet_bbs/tui/screens/board_list.ex, lib/foglet_bbs/tui/screens/moderation.ex, lib/foglet_bbs/tui/screens/account/render.ex, lib/foglet_bbs/tui/screens/post_reader/render.ex, lib/foglet_bbs/tui/screens/thread_list.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/sysop/render.ex (read :52-79 — canonical V2 grouped emission with priority tiers)
    - lib/foglet_bbs/tui/screens/board_list.ex (read :226-231 — current V1 emission)
    - lib/foglet_bbs/tui/screens/thread_list.ex (read :132-137 — current V1 emission)
    - lib/foglet_bbs/tui/screens/moderation.ex (read :175 and :201-207 — two V1 sites)
    - lib/foglet_bbs/tui/screens/account/render.ex (read :43-52 — `key_bar/1` V1 emission)
    - lib/foglet_bbs/tui/screens/post_reader/render.ex (read :31-38 — V1 emission)
    - .planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-CONTEXT.md (D-10 step 1, D-11 — `@key_hints` text affordances are NOT V1 chrome and are out of scope, D-12 — grep gate after step 1)
    - .planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-PATTERNS.md (Migration table at "Five screen call sites — V1 → V2 grouped command bars")
  </read_first>
  <action>
For each of the five sites below, replace the flat `[{key, description}, ...]`
tuple list with the V2 grouped shape `[%{label: "<group>", commands: [%{key: "<key>", label: "<label>", priority: <n>}, ...]}, ...]`.

Use the priority tier conventions from `sysop/render.ex` (System=0,
Tabs/Navigate=10, Actions=30) consistently across all five sites — this
keeps the chrome vocabulary uniform.

**Site 1 — `board_list.ex:226-231`**

Current V1:
```elixir
[
  {"j/k", "Select"},
  {"←/→", "Collapse/Expand"},
  {"Enter", "Open"},
  {"s/u", "Subscribe/Unsubscribe"},
  {"Q", "Back"}
]
```

V2 replacement:
```elixir
[
  %{
    label: "Navigate",
    commands: [
      %{key: "j/k", label: "Select", priority: 10},
      %{key: "←/→", label: "Collapse/Expand", priority: 10},
      %{key: "Enter", label: "Open", priority: 10}
    ]
  },
  %{
    label: "Actions",
    commands: [%{key: "s/u", label: "Subscribe/Unsubscribe", priority: 30}]
  },
  %{
    label: "System",
    commands: [%{key: "Q", label: "Back", priority: 0}]
  }
]
```

**Site 2 — `thread_list.ex:132-137`**

Current V1:
```elixir
[
  {"j/k", "Select"},
  {"Enter", "Open"},
  {"C", "Compose"},
  {"Q", "Back"}
]
```

V2 replacement: Navigate group (`j/k`, `Enter`) at priority 10 → Actions
group (`C`/Compose) at priority 30 → System group (`Q`/Back) at priority 0.

**Site 3 — `moderation.ex:175`**

Current V1: `[{"Q", "Back"}]`

V2 replacement:
```elixir
[%{label: "System", commands: [%{key: "Q", label: "Back", priority: 0}]}]
```

**Site 4 — `moderation.ex:201-207` (`key_list/1`)**

Current V1: `[{"←/→", "Tab"}, {jump_hint, "Jump"}, {"Q", "Back"}]`

V2 replacement: mirror the sysop pattern at `sysop/render.ex:67-73` exactly
(Tabs group with `←/→`/Tab and `jump_hint`/Jump at priority 10, System
group with `Q`/Back at priority 0). The `jump_hint` variable carries through
unchanged.

**Site 5 — `account/render.ex:43-52` (`key_bar/1`)**

Current V1: `[{"←/→", "Tab"}, {jump_hint, "Jump"}, {"Tab", "Field"}, {"Enter", "Save"}, {"Esc", "Cancel"}, {"Ctrl+Q", "Back"}]`

V2 replacement: Tabs group (`←/→`, `jump_hint`) priority 10 → Field group
(`Tab`/Field) priority 10 → Actions group (`Enter`/Save, `Esc`/Cancel)
priority 30 → System group (`Ctrl+Q`/Back) priority 0.

**Site 6 — `post_reader/render.ex:31-38`**

Current V1: `[{"N", "Next"}, {"P", "Prev"}, {"J", "Scroll ↓"}, {"K", "Scroll ↑"}, {"R", "Reply"}, {"Q", "Back"}]`

V2 replacement: Navigate group (N, P, J, K) priority 10 → Actions group
(R/Reply) priority 30 → System group (Q/Back) priority 0.

**Important — D-11:** Do NOT touch `@key_hints` text affordances in
`invites_surface.ex` or `account/ssh_keys_surface.ex`. Those render via
`text(@key_hints, ...)` inside the screen body, not in chrome — they are
explicitly out of scope per SPEC R5.

After migration, run the SPEC R5 grep gate:

```
rtk grep -rn "{[^,]\+, *\"[^\"]\+\"}" lib/foglet_bbs/tui/screens/
```

Expected: zero hits matching the legacy keybar tuple shape (D-12). Filter
out non-keybar tuple matches (e.g., `{:ok, "..."}` style tuples) by visual
inspection — the grep is the chrome-shape canary, not a generic Elixir
tuple grep.

If any V1 chrome shape appears that wasn't surveyed in PATTERNS.md
("file not on the migration list but still emits keybar tuples"), migrate
it using the same Navigate/Actions/System group vocabulary.

Update or delete tests that asserted V1 tuple shapes for these screens —
per D-25, V1 fixture tests are DELETED (not migrated, not skipped).
Identify candidate tests via:

```
rtk grep -rn "key_bar\|command_bar\|keybar" test/foglet_bbs/tui/screens/
```

For each test that asserts a V1 `{key, description}` tuple shape: if the
test's only behavior assertion is the tuple shape, delete the test. If
the test exercises broader render behavior and incidentally asserts the
V1 shape, migrate the assertion to the V2 grouped shape (this is rare;
when in doubt, delete and trust the screen-level smoke tests).
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/</automated>
  </verify>
  <acceptance_criteria>
    - `rtk grep -rn "{[^,]\\+, *\"[^\"]\\+\"}" lib/foglet_bbs/tui/screens/` returns zero hits matching the legacy keybar tuple shape (SPEC R5 acceptance, D-12)
    - All five migrated files contain at least one `%{label:` literal (V2 grouped emission)
    - `rtk grep -n "@key_hints" lib/foglet_bbs/tui/screens/invites_surface.ex` and `lib/foglet_bbs/tui/screens/account/ssh_keys_surface.ex` still exist (D-11 — out of scope, untouched)
    - `rtk mix test test/foglet_bbs/tui/screens/` is green
    - `rtk mix compile --warnings-as-errors` succeeds (chrome surface still consumes both V1 and V2 — V1 paths still exist in chrome modules at this step)
  </acceptance_criteria>
  <done>
    All five V1 screen call sites emit V2 grouped command bars; SPEC R5
    grep gate is clean for `lib/foglet_bbs/tui/screens/`; V1 fixture tests
    asserting tuple shapes are deleted; chrome modules still carry V1
    branches (next task removes them).
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip V1 paths from chrome widgets and delete KeyBar + Normalizer (D-10 steps 2-5)</name>
  <files>lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex, lib/foglet_bbs/tui/widgets/chrome/status_bar.ex, lib/foglet_bbs/tui/widgets/chrome/key_bar.ex, lib/foglet_bbs/tui/widgets/chrome/normalizer.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex (read :27 alias, :185-189 V2 map clause to preserve, :191-196 legacy-title clause to delete, :198-204 Normalizer fallback to delete)
    - lib/foglet_bbs/tui/widgets/chrome/status_bar.ex (read :37 dispatch, :71-79 status_atoms preserve, :91-99 legacy fallthroughs, :101 legacy-title clause)
    - lib/foglet_bbs/tui/widgets/chrome/key_bar.ex (read all 26 lines — file is deleted)
    - lib/foglet_bbs/tui/widgets/chrome/normalizer.ex (read all 141 lines — file is deleted)
    - .planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-CONTEXT.md (D-10 — five-step deletion order; this task does steps 2-5)
    - test/ chrome-widget tests (run `rtk grep -l "Normalizer\\|KeyBar\\|legacy_title" test/` to find tests that may reference these — per D-25 those tests are DELETED, not migrated)
  </read_first>
  <action>
Execute D-10's five-step deletion order. Step 1 was completed in Task 1.
Steps 2-5 happen here, in order — verify a green compile (or at minimum
no new compile errors beyond what the next step will fix) at each step.

**Step 2 — Remove `Normalizer.commands/1` fallback in `screen_frame.ex`**

In `lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex`:

1. Delete the `alias Foglet.TUI.Widgets.Chrome.Normalizer` line at `:27`.
2. Delete the `Normalizer.commands/1` fallback branch in `command_groups/1`
   at `:198-204`. After deletion, `command_groups/1` reduces to:

   ```elixir
   defp command_groups(commands) do
     if grouped_commands?(commands) do
       CommandBar.normalize_groups(commands)
     else
       []
     end
   end
   ```

   Or, if all callers now guarantee grouped shape, simplify to a direct
   `CommandBar.normalize_groups(commands)` call. Verify by inspecting all
   `command_groups/1` callers in the same file.

After Step 2, `screen_frame.ex` no longer references `Normalizer` at all,
but the legacy-title clause at `:191-196` still exists (Step 5 deletes it).

**Step 3 — Delete `lib/foglet_bbs/tui/widgets/chrome/key_bar.ex`**

After Step 2, `KeyBar` is unreferenced (its body at `:23-25` is just a
`Normalizer → CommandBar` shim). Verify with:

```
rtk grep -rn "Chrome.KeyBar\|Widgets.Chrome.KeyBar" lib/ test/
```

If non-zero hits remain, migrate those references (likely tests asserting
V1 keybar shapes — DELETE per D-25) before deleting the file.

Delete the file:

```
rm lib/foglet_bbs/tui/widgets/chrome/key_bar.ex
```

**Step 4 — Delete `lib/foglet_bbs/tui/widgets/chrome/normalizer.ex`**

After Steps 2 and 3, no consumers remain. Verify with:

```
rtk grep -rn "Chrome.Normalizer\|Widgets.Chrome.Normalizer" lib/ test/
```

If non-zero hits remain, those tests are V1 fixture tests — DELETE per
D-25, then delete the file:

```
rm lib/foglet_bbs/tui/widgets/chrome/normalizer.ex
```

**Step 5 — Remove the V1 "legacy title string" branch in `screen_frame.ex` and `status_bar.ex`**

In `screen_frame.ex`:
- Delete `defp normalize_chrome(_legacy_title, state)` clause at `:191-196`.
- Preserve the `defp normalize_chrome(%{} = chrome, state)` map clause at
  `:185-189` (V2 path).

In `status_bar.ex`:
- Delete the `legacy_title/1` clause and its fallthroughs (`:91-99` and
  `:101`).
- If `def render(state, title), do: render(state, title, [])` at `:37` is
  the legacy 2-arity dispatch with no V2 callers, remove it. Verify with:

  ```
  rtk grep -rn "StatusBar.render(.*,.*)" lib/ test/
  ```

  Confirm all surviving callers use `render/3`. If any 2-arity caller
  remains in `lib/`, that's a missed V1 site — migrate it to the 3-arity
  V2 form before removing the dispatch.

- Preserve `status_atoms/1` (`:71-79`) — used by `ScreenFrame`, unrelated
  to V1.

**Test cleanup**

Per D-25, run a final test scan:

```
rtk grep -rn "legacy_title\|legacy_chrome\|{[^,]\\+, *\"[^\"]\\+\"}" test/foglet_bbs/tui/widgets/chrome/
```

For each match: if the test asserts V1 behavior, DELETE the test (do not
skip, do not migrate). The V1 contract no longer exists; tests asserting
it are dead code.
  </action>
  <verify>
    <automated>rtk mix precommit</automated>
  </verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/widgets/chrome/normalizer.ex` does not exist (SPEC R5 acceptance)
    - `lib/foglet_bbs/tui/widgets/chrome/key_bar.ex` does not exist
    - `rtk grep -rn "Chrome.Normalizer\\|Widgets.Chrome.Normalizer" lib/ test/` returns zero hits
    - `rtk grep -rn "Chrome.KeyBar\\|Widgets.Chrome.KeyBar" lib/ test/` returns zero hits
    - `rtk grep -n "Normalizer" lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex` returns zero hits
    - `rtk grep -n "legacy_title" lib/foglet_bbs/tui/widgets/chrome/` returns zero hits
    - `rtk grep -n "defp normalize_chrome" lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex` returns exactly 1 hit (the V2 map clause only)
    - `rtk mix precommit` is clean (compile-with-warnings-as-errors, formatter, Credo, Sobelow, Dialyzer all pass)
    - Full test suite green; no skipped tests added (D-25)
  </acceptance_criteria>
  <done>
    All four chrome widget files are V2-only or deleted; SPEC R5 fully
    closed; `mix precommit` green.
  </done>
</task>

</tasks>

<verification>
- `rtk mix precommit` passes
- SPEC R5 grep gates clean
- Test suite green; V1 chrome tests deleted (not skipped) per D-25
</verification>

<success_criteria>
- [ ] All five V1 screen call sites emit V2 grouped command bars (R5)
- [ ] `lib/foglet_bbs/tui/widgets/chrome/normalizer.ex` deleted (R5)
- [ ] `lib/foglet_bbs/tui/widgets/chrome/key_bar.ex` deleted (R5 — KeyBar was a Normalizer→CommandBar shim, unreferenced after step 2)
- [ ] `screen_frame.ex` has no `Normalizer` alias and no legacy-title clause (R5)
- [ ] `status_bar.ex` has no legacy-title arity (R5)
- [ ] `@key_hints` text affordances in `invites_surface.ex` / `account/ssh_keys_surface.ex` unchanged (D-11)
- [ ] V1 fixture tests deleted, not skipped (D-25)
- [ ] `rtk mix precommit` green
</success_criteria>

<output>
After completion, create `.planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-03-SUMMARY.md`
</output>
