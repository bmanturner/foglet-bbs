---
phase: 47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/foglet_bbs/tui/screens/post_reader.ex
  - lib/foglet_bbs/posts.ex
  - test/foglet_bbs/tui/screens/post_reader_test.exs
  - test/foglet_bbs/tui/app_test.exs
  - test/foglet_bbs/posts_test.exs
autonomous: true
requirements: [R1, R2]
must_haves:
  truths:
    - "PostReader entry on a 200-post thread with read pointer at post 150 lands at the read pointer"
    - "PostReader entry on a 5-post thread (smaller than the window) still works correctly"
    - "Phase 44 render-purity guard test for PostReader continues to pass"
    - "Foglet.Posts.list_posts/1 no longer exists in lib/ or test/"
    - "Jump-to-post (load_intent: :jump_last) entry continues to function via direction: :last"
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/post_reader.ex"
      provides: "load_posts/2 calls list_reader_window/2 with anchor mapping per D-02"
      contains: "list_reader_window"
    - path: "lib/foglet_bbs/posts.ex"
      provides: "list_reader_window/2 only — list_posts/1 removed"
      forbids: "def list_posts"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/post_reader.ex"
      to: "Foglet.Posts.list_reader_window/2"
      via: "posts_mod.list_reader_window(thread_id, opts)"
      pattern: "list_reader_window"
    - from: "post_reader.ex load_posts/2"
      to: "selected_index_after_window_load/3"
      via: "reuse Phase 44 helper to land selection at read pointer"
      pattern: "selected_index_after_window_load"
---

<objective>
Migrate `Foglet.TUI.Screens.PostReader.load_posts/2` from the eager unbounded
`Foglet.Posts.list_posts/1` to the windowed `Foglet.Posts.list_reader_window/2`,
anchored at the user's read pointer (or `:initial` / `:last` based on
`load_intent`). After the call site is migrated, delete `list_posts/1`
entirely from `Foglet.Posts` and remove every fixture/test that references
it.

Purpose: closes SPEC R1 (deletion) and R2 (migration). R1 cannot succeed
until R2 ships — `BoundedFakePosts.list_posts/1` (a regression-guard raise)
itself contains the `.list_posts` token and would fail R1's grep gate.

Output: `lib/foglet_bbs/posts.ex` with no `list_posts/1`; `post_reader.ex`
calls `list_reader_window/2`; fixture mods migrated; SPEC R1 grep gate clean.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-SPEC.md
@.planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-CONTEXT.md
@.planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-PATTERNS.md
@AGENTS.md

<interfaces>
From lib/foglet_bbs/posts.ex (Phase 44 — already shipped):

```elixir
@spec list_reader_window(String.t(), keyword()) :: ReaderWindow.t()
def list_reader_window(thread_id, opts \\ [])
# opts:
#   :limit (default @reader_window_size)
#   :direction — :initial | :last | :around | :next | :previous
#   :around_message_number — required when direction == :around
#   :anchor_message_number — required for :next / :previous
```

From lib/foglet_bbs/tui/screens/post_reader.ex (existing helpers to REUSE):

```elixir
# :567-583 — direction picker for new windowed loads
def load_direction(state)

# :585-609 — lands the selected index back on read-pointer message_number
# AFTER the windowed load completes
def selected_index_after_window_load(state, posts, anchor)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate PostReader.load_posts/2 to list_reader_window/2 (R2)</name>
  <files>lib/foglet_bbs/tui/screens/post_reader.ex, test/foglet_bbs/tui/screens/post_reader_test.exs</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/post_reader.ex (read :298-337 for current `load_posts/2` body; :567-609 for the window-anchor helpers `load_direction/1` and `selected_index_after_window_load/3`)
    - lib/foglet_bbs/posts.ex (read :99-153 — `list_reader_window/2` API surface and `:around` direction handling)
    - .planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-CONTEXT.md (D-01 through D-04 — anchor mapping rules)
    - .planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-SPEC.md (R2 acceptance: 200-post + pointer-at-150 test, 5-post small-thread test, render-purity guard preservation)
    - test/foglet_bbs/tui/screens/post_reader_test.exs (read top-of-file fixture mods at :11, :62, :78, :114 to understand `BoundedFakePosts` / fixture-mod indirection used by SPEC R2 acceptance)
  </read_first>
  <behavior>
    - Test 1 (R2 acceptance — read-pointer anchor): seed a thread with 200 posts and a read pointer at message_number 150; call `PostReader.load_posts/2` via the existing fixture-mod indirection; assert the fixture mod's `list_reader_window/2` was invoked with `direction: :around, around_message_number: 150` and the post-load screen state's selected index lands on message_number 150 (NOT post #1).
    - Test 2 (R2 acceptance — small-thread fallthrough): seed a thread with 5 posts (smaller than the reader window default); assert `load_posts/2` still works, the fixture mod's `list_reader_window/2` was called, and the resulting screen state contains all 5 posts with no crash.
    - Test 3 (R2 acceptance — jump_last anchor): with `state.load_intent == :jump_last`, assert `list_reader_window/2` is called with `direction: :last` (NOT `:around`).
    - Test 4 (R2 acceptance — no read pointer): with no read-pointer present and `load_intent != :jump_last`, assert `list_reader_window/2` is called with `direction: :initial`.
    - The existing Phase 44 render-purity guard test for PostReader (currently in `post_reader_test.exs`) must continue to pass without modification.
  </behavior>
  <action>
Replace the body of `Foglet.TUI.Screens.PostReader.load_posts/2` (currently at
`post_reader.ex:298-337`) so it calls `posts_mod.list_reader_window/2`
instead of `posts_mod.list_posts/1`.

Concrete anchor mapping (from CONTEXT D-02, do NOT add a new
`:read_pointer` direction keyword per D-03):

```elixir
opts =
  cond do
    is_integer(read_pointer_message_number) ->
      [direction: :around, around_message_number: read_pointer_message_number]

    state.load_intent == :jump_last ->
      [direction: :last]

    true ->
      [direction: :initial]
  end

window = posts_mod.list_reader_window(load_thread_id, opts)
posts = window.posts
```

The read-pointer lookup MUST stay where it currently lives in the load path
(do NOT have the screen call `Foglet.Threads.get_thread_read_pointer/2`
itself — D-03). The fixture-mod indirection is the seam; tests inject a
fake `posts_mod` that records its calls.

After the windowed load returns, reuse `selected_index_after_window_load/3`
(already present at `:585-609`) to land the selected index on the read
pointer's message_number — this is the helper that makes the
"200 posts + pointer at 150" assertion pass (D-04).

Update the seed-read-position, render-cache warmup, and viewport warmup
paths in `load_posts/2` to consume `window.posts` instead of the previous
flat post list. The downstream pipeline already handles bounded windows
because Phase 44 wired the rest of `post_reader.ex` to `list_reader_window/2`
— `load_posts/2` is the last residual.

In `post_reader_test.exs`:
- Migrate every fixture mod (search for `def list_posts(_thread_id)` in the
  file — current locations cited in PATTERNS: lines 11, 62, 78) so they
  implement `list_reader_window/2` only. They already implement it from
  Phase 44; the deletion is what's needed.
- Add the four new behavior tests above. Use the existing fixture-mod
  pattern; assert call recording via the same mechanism the file already
  uses for `list_reader_window/2` recording.
- Use `BoundedFakePosts` carefully: its current `def list_posts(_), do: raise(...)` regression
  guard at line ~114 is itself a `.list_posts` token that fails SPEC R1's
  grep — DELETE that clause in this task. The regression-guard semantic
  is preserved because `list_posts/1` no longer exists in `Foglet.Posts`
  itself (next task).

Per CONTEXT D-22, this is a deletion-only fixture migration; do not add
new fixture surface area beyond the four tests above.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/post_reader_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rtk mix test test/foglet_bbs/tui/screens/post_reader_test.exs` is green
    - `rtk grep -n "list_posts" lib/foglet_bbs/tui/screens/post_reader.ex` returns zero hits (post_reader no longer calls list_posts)
    - `rtk grep -c "def list_posts" test/foglet_bbs/tui/screens/post_reader_test.exs` returns 0 (fixture mods migrated; D-22)
    - `rtk grep -n "selected_index_after_window_load" lib/foglet_bbs/tui/screens/post_reader.ex` returns at least one hit inside `load_posts/2` body (D-04 reuse)
    - `rtk grep -n "around_message_number" lib/foglet_bbs/tui/screens/post_reader.ex` returns a hit inside `load_posts/2` (D-02 anchor mapping)
    - Phase 44 render-purity guard test (search for `render_purity` or similar in post_reader_test.exs) is present and passing
  </acceptance_criteria>
  <done>
    PostReader.load_posts/2 routes through list_reader_window/2; the four R2
    acceptance behaviors are covered by tests; fixture mods no longer
    implement list_posts/1; render-purity guard from Phase 44 still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete Posts.list_posts/1 and remove all remaining call sites (R1)</name>
  <files>lib/foglet_bbs/posts.ex, test/foglet_bbs/tui/app_test.exs, test/foglet_bbs/posts_test.exs</files>
  <read_first>
    - lib/foglet_bbs/posts.ex (read :84-92 — current `list_posts/1` body to be deleted)
    - .planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-SPEC.md (R1: full deletion; grep gate `grep -rn "\.list_posts\b" lib/ test/` returns zero)
    - .planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-CONTEXT.md (D-22, D-23 — test deletion rules: app_test.exs:55-89 fixture impls deleted; posts_test.exs:410-450 tombstone-semantics tests deleted, NOT migrated)
    - test/foglet_bbs/tui/app_test.exs (read :55-89 to confirm fixture-mod impls)
    - test/foglet_bbs/posts_test.exs (read :410-450 to confirm tombstone tests; D-23 says delete outright because Phase 44 D-13/D-14 already covers tombstone behavior through list_reader_window/2)
  </read_first>
  <action>
Step A — Delete `Foglet.Posts.list_posts/1` from `lib/foglet_bbs/posts.ex`
(currently lines 84-92, including its `@spec` and any module-doc reference
to it). Do not deprecate — full deletion per SPEC R1.

Step B — Verify no remaining call sites in `lib/`. Run:

```
rtk grep -rn "\.list_posts\b" lib/
```

Expected output: zero hits. If any hit appears, migrate that call site to
`list_reader_window/2` with appropriate direction (mirroring Task 1's
anchor-mapping conditional). The pre-task survey from PATTERNS.md says
PostReader is the only `lib/` consumer; this is a defensive check.

Step C — Test deletions per D-22 and D-23:

1. `test/foglet_bbs/tui/app_test.exs` — delete the `list_posts/1`
   implementations at `:55-89` (D-22). The host fixture mod stays; only
   the `def list_posts(...)` clauses are deleted. If the fixture mod also
   implements `list_reader_window/2`, that stays.

2. `test/foglet_bbs/posts_test.exs:410-450` — delete the tombstone-semantics
   tests outright (D-23). Phase 44 D-13/D-14 already covers tombstone
   behavior through `list_reader_window/2`. Do NOT migrate.

Step D — Final grep gate:

```
rtk grep -rn "\.list_posts\b" lib/ test/
```

Expected output: zero hits. This is SPEC R1's locked acceptance gate.

If any test/ hit appears that wasn't covered by D-22/D-23, investigate the
file — likely an additional fixture mod missed in the survey. Delete the
clause (do NOT skip; D-25 makes the rule explicit for V1 chrome but the
pattern is the same here: deletions, not skips).
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/posts_test.exs test/foglet_bbs/tui/app_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rtk grep -rn "\.list_posts\b" lib/ test/` returns zero hits (SPEC R1 locked gate)
    - `rtk grep -c "def list_posts" lib/foglet_bbs/posts.ex` returns 0
    - `rtk mix compile --warnings-as-errors` succeeds
    - `rtk mix test test/foglet_bbs/posts_test.exs test/foglet_bbs/tui/app_test.exs` is green
    - File `test/foglet_bbs/posts_test.exs` no longer contains the `list_posts` tombstone tests (D-23: deletion, not migration)
  </acceptance_criteria>
  <done>
    Foglet.Posts.list_posts/1 no longer exists; SPEC R1 grep gate is clean
    across lib/ and test/; the test suite still compiles and passes.
  </done>
</task>

</tasks>

<verification>
- `rtk mix precommit` passes (compile-with-warnings-as-errors, formatter, Credo, Sobelow, Dialyzer all green)
- SPEC R1 grep gate: `rtk grep -rn "\.list_posts\b" lib/ test/` returns zero
- SPEC R2 acceptance: the four behavior tests in post_reader_test.exs pass
- Phase 44 render-purity guard test still passes
</verification>

<success_criteria>
- [ ] `Foglet.Posts.list_posts/1` deleted (R1)
- [ ] No `.list_posts` token in lib/ or test/ (R1 grep)
- [ ] `PostReader.load_posts/2` calls `list_reader_window/2` with anchor mapping per D-02 (R2)
- [ ] 200-posts + pointer-at-150 acceptance test passes — selected index lands on read pointer (R2)
- [ ] 5-post small-thread acceptance test passes (R2)
- [ ] jump_last → `direction: :last`; no-pointer → `direction: :initial` (D-02)
- [ ] Phase 44 render-purity guard preserved (SPEC R2)
- [ ] `rtk mix precommit` green
</success_criteria>

<output>
After completion, create `.planning/phases/47-bound-unbounded-list-queries-drop-chrome-v1-shims-and-reduce/47-01-SUMMARY.md`
</output>
