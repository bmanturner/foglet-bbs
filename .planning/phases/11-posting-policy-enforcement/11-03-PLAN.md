---
phase: 11-posting-policy-enforcement
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - lib/foglet_bbs/tui/screens/new_thread.ex
  - lib/foglet_bbs/tui/screens/post_composer.ex
  - test/foglet_bbs/tui/screens/new_thread_test.exs
  - test/foglet_bbs/tui/screens/post_composer_test.exs
autonomous: true
requirements:
  - POST-04
must_haves:
  truths:
    - "New-thread posting-policy denial keeps the user in the composing flow and shows `You are not allowed to post on this board.`"
    - "Reply posting-policy denial keeps the user in the post composer and shows `You are not allowed to post on this board.`"
    - "Reply locked-thread denial keeps the user in the post composer and shows exactly `This thread is locked`."
    - "Successful allowed reply submissions still navigate to post reader and issue `{:load_posts, thread.id, jump_last: true}`."
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/new_thread.ex"
      provides: "Structured thread-create error formatting"
      contains: "You are not allowed to post on this board."
    - path: "lib/foglet_bbs/tui/screens/post_composer.ex"
      provides: "Structured reply-create error formatting"
      contains: "This thread is locked"
    - path: "test/foglet_bbs/tui/screens/new_thread_test.exs"
      provides: "New-thread POST-04 TUI regression coverage"
      contains: "POST-04"
    - path: "test/foglet_bbs/tui/screens/post_composer_test.exs"
      provides: "Reply POST-04 TUI regression coverage"
      contains: "This thread is locked"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/new_thread.ex"
      to: "Foglet.Threads.create_thread/3"
      via: "format_error(:posting_not_allowed)"
      pattern: "format_error\\(:posting_not_allowed\\)"
    - from: "lib/foglet_bbs/tui/screens/post_composer.ex"
      to: "Foglet.Posts.create_reply/4"
      via: "format_error(:thread_locked)"
      pattern: "format_error\\(:thread_locked\\)"
---

<objective>
Map structured posting-policy and locked-thread errors to clear SSH/TUI terminal copy.

Purpose: POST-04 requires users to receive clear terminal feedback instead of generic or inspect-form errors when context preflight rejects a submission.
Output: NewThread and PostComposer error formatting plus screen tests proving copy and no-success-navigation behavior.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-posting-policy-enforcement/11-CONTEXT.md
@.planning/phases/11-posting-policy-enforcement/11-SPEC.md
@.planning/phases/11-posting-policy-enforcement/11-RESEARCH.md
@.planning/phases/11-posting-policy-enforcement/11-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/11-posting-policy-enforcement/11-01-SUMMARY.md
@.planning/phases/11-posting-policy-enforcement/11-02-SUMMARY.md

<interfaces>
Structured errors produced by plans 11-01 and 11-02:

```elixir
{:error, :posting_not_allowed}
{:error, :thread_locked}
```

Exact terminal copy:

```text
You are not allowed to post on this board.
This thread is locked
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TUI rejection-copy tests</name>
  <files>test/foglet_bbs/tui/screens/new_thread_test.exs, test/foglet_bbs/tui/screens/post_composer_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/screens/new_thread_test.exs
    - test/foglet_bbs/tui/screens/post_composer_test.exs
    - lib/foglet_bbs/tui/screens/new_thread.ex
    - lib/foglet_bbs/tui/screens/post_composer.ex
    - .planning/phases/11-posting-policy-enforcement/11-CONTEXT.md
    - .planning/phases/11-posting-policy-enforcement/11-SPEC.md
    - .planning/phases/11-posting-policy-enforcement/11-VALIDATION.md
  </read_first>
  <behavior>
    - Fake `Threads.create_thread/3` returning `{:error, :posting_not_allowed}` leaves `current_screen == :new_thread`, keeps `screen_state.new_thread`, and sets `screen_state.new_thread.error == "You are not allowed to post on this board."`.
    - Fake `Posts.create_reply/4` returning `{:error, :posting_not_allowed}` leaves `current_screen == :post_composer`, keeps `screen_state.post_composer`, and sets `modal.message == "You are not allowed to post on this board."`.
    - Fake `Posts.create_reply/4` returning `{:error, :thread_locked}` leaves `current_screen == :post_composer`, keeps `screen_state.post_composer`, and sets `modal.message == "This thread is locked"`.
    - Existing successful reply test still asserts transition to `:post_reader` and `{:load_posts, "t1", jump_last: true}` compatibility.
  </behavior>
  <action>Update fake domain modules in both screen test files to return exact structured atoms for `"policy"` and `"locked"` body/title cases. Replace the existing PostComposer generic failure expectation `Failed to create post.` with structured-error expectations. Add POST-04 labels to the relevant describe/test names. Do not add visual/browser tests; keep these as pure `ExUnit.Case` screen tests using fake domains.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg "POST-04" test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs` returns at least 2 matches.
    - `rg "You are not allowed to post on this board\\." test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs` returns at least 2 matches.
    - `rg "This thread is locked" test/foglet_bbs/tui/screens/post_composer_test.exs` returns a match.
    - `rg "Failed to create post\\." test/foglet_bbs/tui/screens/post_composer_test.exs` returns no matches.
    - `rtk mix test test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <done>Screen tests prove posting-policy and locked-thread denials render clear terminal copy and do not navigate as if submission succeeded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Format structured posting errors in TUI screens</name>
  <files>lib/foglet_bbs/tui/screens/new_thread.ex, lib/foglet_bbs/tui/screens/post_composer.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/new_thread.ex
    - lib/foglet_bbs/tui/screens/post_composer.ex
    - lib/foglet_bbs/tui/modal.ex
    - .planning/phases/11-posting-policy-enforcement/11-CONTEXT.md
    - .planning/phases/11-posting-policy-enforcement/11-RESEARCH.md
  </read_first>
  <action>Update `Foglet.TUI.Screens.NewThread.format_error/1` with exact clauses: `defp format_error(:posting_not_allowed), do: "You are not allowed to post on this board."` and `defp format_error(:thread_locked), do: "This thread is locked"` before the binary/inspect fallback. Update `Foglet.TUI.Screens.PostComposer` so the `submit_reply/4` error branch preserves the structured reason: `{:error, reason} -> %{state | modal: %Foglet.TUI.Modal{type: :error, message: format_error(reason)}}`. Add private `format_error/1` clauses in PostComposer for `:posting_not_allowed`, `:thread_locked`, `%Ecto.Changeset{}`, binary, and fallback inspect. Locked-thread copy must be exactly `This thread is locked`; posting-policy copy must be exactly `You are not allowed to post on this board.`. Keep success navigation and `{:load_posts, thread.id, jump_last: true}` unchanged per D-14.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg "format_error\\(:posting_not_allowed\\), do: \"You are not allowed to post on this board\\.\"" lib/foglet_bbs/tui/screens/new_thread.ex lib/foglet_bbs/tui/screens/post_composer.ex` returns 2 matches.
    - `rg "format_error\\(:thread_locked\\), do: \"This thread is locked\"" lib/foglet_bbs/tui/screens/new_thread.ex lib/foglet_bbs/tui/screens/post_composer.ex` returns 2 matches.
    - `rg "Failed to create post\\." lib/foglet_bbs/tui/screens/post_composer.ex` returns no matches.
    - `rg "format_error\\(reason\\)" lib/foglet_bbs/tui/screens/post_composer.ex` returns a match.
    - `rtk mix test test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs` passes.
  </acceptance_criteria>
  <done>TUI screens map structured posting errors to exact terminal copy while preserving successful submission behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 11 quick validation</name>
  <files>lib/foglet_bbs/tui/screens/new_thread.ex, lib/foglet_bbs/tui/screens/post_composer.ex, test/foglet_bbs/tui/screens/new_thread_test.exs, test/foglet_bbs/tui/screens/post_composer_test.exs</files>
  <read_first>
    - .planning/phases/11-posting-policy-enforcement/11-VALIDATION.md
    - .planning/phases/11-posting-policy-enforcement/11-SPEC.md
    - .planning/phases/11-posting-policy-enforcement/11-CONTEXT.md
  </read_first>
  <action>Run the Phase 11 quick command exactly: `rtk mix test test/foglet_bbs/threads/threads_test.exs test/foglet_bbs/posts/posts_test.exs test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs`. If failures are caused by the Phase 11 changes, fix them in the files owned by this plan or, if the failure belongs to Plan 11-01/11-02 files, stop and report the failing test and owner plan in the summary. Do not run watch mode.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/threads/threads_test.exs test/foglet_bbs/posts/posts_test.exs test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The quick command exits with status 0.
    - The command output includes `0 failures`.
  </acceptance_criteria>
  <done>All Phase 11 targeted context and TUI tests pass together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain context -> TUI screen | Structured domain errors cross into terminal presentation. |
| TUI screen -> user | Terminal copy determines whether users understand denial versus validation failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Repudiation | TUI error copy | mitigate | Map `:posting_not_allowed` and `:thread_locked` to distinct fixed strings so denials are auditable in tests and not hidden behind generic failures. |
| T-11-11 | Tampering | Reply navigation state | mitigate | Tests assert rejected replies keep `current_screen == :post_composer` and preserve composer state instead of navigating like success. |
| T-11-12 | Information Disclosure | Terminal errors | accept | Denial copy discloses only that posting is not allowed or thread is locked; no role, scope, or private policy internals are exposed. |
| T-11-13 | Elevation of Privilege | TUI-only enforcement | mitigate | Plan only formats context-returned errors; it does not add UI-only authorization decisions or bypass context checks. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/threads/threads_test.exs test/foglet_bbs/posts/posts_test.exs test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs`.
</verification>

<success_criteria>
POST-04 is covered by tests and implementation. New-thread and reply submissions show `You are not allowed to post on this board.` for posting-policy denial, reply submissions show exactly `This thread is locked` for locked-thread denial, and rejected submissions do not navigate as success.
</success_criteria>

<output>
After completion, create `.planning/phases/11-posting-policy-enforcement/11-03-SUMMARY.md`
</output>
