---
phase: 46
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - lib/foglet_bbs/boards/server.ex
autonomous: true
requirements:
  - DOM-02

must_haves:
  truths:
    - "A reviewer reading lib/foglet_bbs/boards/server.ex top-to-bottom encounters an explicit rationale for Multi+Repo.transaction usage BEFORE reaching the first |> Repo.transaction() call at line ~154"
    - "The moduledoc explicitly states the divergence from project-wide Repo.transact/1 is intentional and locked"
    - "Multi step labels :post and :thread_update remain unchanged (load-bearing for handle_call pattern matches)"
    - "Module observable behavior unchanged: success returns {:ok, post} / {:ok, %{thread, post}}, error returns {:error, reason}"
  artifacts:
    - path: "lib/foglet_bbs/boards/server.ex"
      provides: "Per-board GenServer with documented Multi+Repo.transaction rationale"
      contains: "## Transaction strategy"
      grep_must_match: "Multi step labels :post / :thread_update are load-bearing"
  key_links:
    - from: "lib/foglet_bbs/boards/server.ex moduledoc"
      to: "lib/foglet_bbs/boards/server.ex inline comments above lines 154 and 196"
      via: "single-source-of-truth doc reference (inline comment points back to @moduledoc)"
      pattern: "see @moduledoc"
---

<objective>
Document — and keep — `Foglet.Boards.Server`'s direct `Repo.transaction/1 + Ecto.Multi` usage at `lib/foglet_bbs/boards/server.ex:154,196` as an intentional, locked deviation from the project-wide `Repo.transact/1` convention.

Purpose: A future maintainer reading `boards/server.ex` top-to-bottom must encounter the rationale BEFORE the first `|> Repo.transaction()` call. The Multi step labels `:post` and `:thread_update` are load-bearing (consumed by `handle_call` pattern matches at `:86-93,:102-108`), and converting to `Repo.transact/1` would require manually rebuilding the result map at every call site without changing observed behavior. Closes CONCERNS.md Security Considerations entry at line 151.

Output: Updated `@moduledoc` with a `## Transaction strategy` subheader paragraph (parallel to the existing `## Message number allocation` and `## Crash recovery (D-05)` subheaders) plus two one-line inline pointer comments above lines 154 and 196. ZERO behavior change.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-SPEC.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-RESEARCH.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-PATTERNS.md
@AGENTS.md

<interfaces>
Existing moduledoc structure of lib/foglet_bbs/boards/server.ex (lines 1-27):
  - line 1: defmodule Foglet.Boards.Server do
  - lines 2-27: @moduledoc """ … """
    - "## Message number allocation" (line 8 area) — KEEP
    - "## Crash recovery (D-05)" (line 21 area) — KEEP
    - new "## Transaction strategy" subheader inserts immediately before the closing `"""` at line 27 — ADD

Load-bearing pattern matches (DO NOT MODIFY):
  - server.ex:87  — `{:ok, %{post: post}} ->` (consumes Multi step :post defined at line 128)
  - server.ex:103 — `{:ok, %{thread_update: thread, post: post}} ->` (consumes Multi step :thread_update defined at line 184)

Inline comment insertion sites:
  - line 154 — end of run_post_insert_multi/5, contains `|> Repo.transaction()` — comment goes IMMEDIATELY ABOVE this line
  - line 196 — end of run_thread_create_multi/4, contains `|> Repo.transaction()` — comment goes IMMEDIATELY ABOVE this line

Single-source-of-truth pattern: inline comments POINT BACK to @moduledoc, do not re-state rationale.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `## Transaction strategy` subheader paragraph to @moduledoc (D-01)</name>
  <read_first>
    - lib/foglet_bbs/boards/server.ex (lines 1-30 — full @moduledoc; lines 80-110 — handle_call clauses with load-bearing pattern matches; lines 120-200 — both run_*_multi functions with Multi step definitions)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md (D-01 — exact required content)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-PATTERNS.md (search for "DOM-02 moduledoc + inline comments" — heading style, parallel structure, required content list)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-RESEARCH.md (search for "DOM-02: moduledoc anchor" and "load-bearing pattern matches")
    - .planning/codebase/CONVENTIONS.md (project-wide Repo.transact/1 convention being deviated from)
  </read_first>
  <action>
    Open `lib/foglet_bbs/boards/server.ex`. Inside the existing `@moduledoc """ … """` block, immediately before the closing `"""` (currently at ~line 27), insert a new `## Transaction strategy` subheader section parallel in structure to `## Message number allocation` and `## Crash recovery (D-05)`.

    REQUIRED CONTENT (per D-01, must be present verbatim or as close as the prose allows — these phrases are grep-anchors):
    - The string "## Transaction strategy" as a level-2 markdown heading inside the docstring.
    - The word "intentional" applied to the `Repo.transaction/1 + Ecto.Multi` choice.
    - The phrase "locked deviation from `Repo.transact/1`" (with backticks around `Repo.transact/1`).
    - Explicit citation of step labels `:post` and `:thread_update` as load-bearing.
    - Reference to the `handle_call` clauses at `server.ex:86-93,102-108` (or `lines 86-93 and 102-108`) that depend on those labels for success-side extraction.

    Suggested paragraph (Claude's Discretion — match existing tone; this is a guide, refine wording for parallel structure with the other two subheaders):

    ```
    ## Transaction strategy

    This module is the intentional, locked exception to the project-wide
    `Repo.transact/1` convention. The two write paths,
    `run_post_insert_multi/5` and `run_thread_create_multi/4`, end with
    `|> Repo.transaction()` directly so the success result preserves the
    `Ecto.Multi` step map.

    The Multi step labels `:post` and `:thread_update` are load-bearing.
    The `handle_call` clauses at `lines 86-93` and `lines 102-108`
    pattern-match on `{:ok, %{post: post}}` and
    `{:ok, %{thread_update: thread, post: post}}` to extract the
    success-side values; renaming or restructuring these labels would
    silently break message-number allocation. Converting to
    `Repo.transact/1` would require manually rebuilding the result map at
    every call site without changing observed behavior, and the
    `GenServer` reply contract is locked, so this divergence stays.
    ```

    Style rules (from PATTERNS.md):
    - Level-2 `##` heading (NOT `###` or `#`) — same as the other two subheaders.
    - Single-backtick-wrap module/function names: `Repo.transaction()`, `Repo.transact/1`, `Ecto.Multi`, `handle_call`, `:post`, `:thread_update`.
    - Blank line after the heading; blank line at end of section before closing `"""`.
    - Do NOT invent a fake `(D-XX)` suffix on the subheader (the existing `(D-05)` is a real backreference to a prior CONTEXT decision, not a stylistic suffix).

    DO NOT modify anything outside the moduledoc: no `@spec` changes, no Multi step renames, no behavior changes. The `:call_without_opaque` Dialyzer fix is a Plan 03 (QUAL-01) concern, NOT this plan.
  </action>
  <verify>
    <automated>grep -n "## Transaction strategy" lib/foglet_bbs/boards/server.ex &amp;&amp; grep -n "locked deviation from" lib/foglet_bbs/boards/server.ex &amp;&amp; grep -nE ":post|:thread_update" lib/foglet_bbs/boards/server.ex | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "## Transaction strategy" lib/foglet_bbs/boards/server.ex` returns exactly 1 match inside the @moduledoc.
    - `grep -n "intentional" lib/foglet_bbs/boards/server.ex` returns at least 1 match inside the @moduledoc (line < 30).
    - `grep -n "locked deviation from" lib/foglet_bbs/boards/server.ex` returns at least 1 match.
    - `grep -nE "Repo\\.transact/1" lib/foglet_bbs/boards/server.ex` returns at least 1 match (the divergence target named explicitly).
    - `grep -n ":post" lib/foglet_bbs/boards/server.ex` and `:thread_update` both still appear at their original definition lines (Multi step labels NOT renamed).
    - `grep -nE "(86-93|102-108|lines 86|lines 102)" lib/foglet_bbs/boards/server.ex` returns at least 1 match (handle_call clause references cited).
    - `rtk mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    `## Transaction strategy` subheader added to @moduledoc with all D-01 required content; module compiles clean; Multi step labels and handle_call pattern matches unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add inline pointer comments above lines 154 and 196 (D-02)</name>
  <read_first>
    - lib/foglet_bbs/boards/server.ex (lines 145-160 — end of run_post_insert_multi/5; lines 188-200 — end of run_thread_create_multi/4)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md (D-02 — exact comment text)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-PATTERNS.md (search for "Inline pointer comment sites" — exact indentation and form)
  </read_first>
  <action>
    Per D-02, insert ONE single-line `#` comment immediately above each `|> Repo.transaction()` call (at indented position 4 — same column as the pipe operator).

    Site 1 — at `lib/foglet_bbs/boards/server.ex:154` (end of `run_post_insert_multi/5`).
    Site 2 — at `lib/foglet_bbs/boards/server.ex:196` (end of `run_thread_create_multi/4`).

    Exact comment text (per D-02 — verbatim or close paraphrase; aims for a grep-anchor):
    ```
        # Multi step labels :post / :thread_update are load-bearing — see @moduledoc
        |> Repo.transaction()
    ```

    Indentation: 4 spaces from column 0 (matches the pipeline indent — see PATTERNS.md "Inline pointer comment sites").

    Note: Inserting these comments will shift subsequent line numbers by +1 each. The lines that read `|> Repo.transaction()` will move from lines 154/196 to lines 155/198 (approximately). This is expected and benign — no other artifact references those line numbers post-insert (CONCERNS.md/SUMMARY references describe the pre-insert state and are point-in-time records).

    DO NOT add any other text or modify the function bodies. The comment is a pointer to the moduledoc explanation, not a duplicate of it (per Pattern: "Single-source-of-truth doc references" in PATTERNS.md).

    DO NOT modify the `Multi.run` callback signatures, the `@spec`s, or any other content. Spec narrowing for the Dialyzer `:call_without_opaque` fix is Plan 03's job.
  </action>
  <verify>
    <automated>test "$(grep -c 'Multi step labels :post / :thread_update are load-bearing' lib/foglet_bbs/boards/server.ex)" -eq 2 &amp;&amp; rtk mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Multi step labels :post / :thread_update are load-bearing" lib/foglet_bbs/boards/server.ex` equals exactly 2 (one above each `|> Repo.transaction()`).
    - `grep -c "see @moduledoc" lib/foglet_bbs/boards/server.ex` equals at least 2.
    - `grep -c "|> Repo.transaction()" lib/foglet_bbs/boards/server.ex` equals exactly 2 (no `Repo.transaction` calls added or removed; pipeline structure preserved).
    - Each `|> Repo.transaction()` call has a `# Multi step labels …` line on the immediately preceding source line (verified by quick visual or `grep -B1`).
    - `rtk mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    Two one-line inline pointer comments inserted above the two `|> Repo.transaction()` sites; comments point readers to the @moduledoc rationale; no behavior change.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run precommit + test floor (D-14 cadence gate)</name>
  <read_first>
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md (D-14)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-VALIDATION.md (per-task verification map row 46-02-*)
  </read_first>
  <action>
    Run the full cadence gate after Tasks 1 and 2 are committed:
    1. `rtk mix precommit`
    2. `rtk mix test`

    Per D-14, both MUST exit 0; test summary MUST report ≥ 1 property + 2161 tests, 0 failures.

    Specifically watch for:
    - `format` step: the new moduledoc paragraph and inline comments must be format-clean (no trailing whitespace, no missing newlines at EOF).
    - `dialyzer` step: must NOT introduce new warnings. Adding moduledoc text and `#` comments cannot change types, but if Dialyzer surfaces anything new, STOP — there is a deeper issue.
    - `credo --strict`: docstring-style and comment-style rules pass.

    Record observed test summary in plan SUMMARY.
  </action>
  <verify>
    <automated>rtk mix precommit &amp;&amp; rtk mix test</automated>
  </verify>
  <acceptance_criteria>
    - `rtk mix precommit` exits 0.
    - `rtk mix test` exits 0.
    - Test summary line shows ≥ 1 property and ≥ 2161 tests with 0 failures.
    - No new dialyzer warnings, no new credo violations, no sobelow findings.
  </acceptance_criteria>
  <done>
    Phase-46 plan-02 cadence gate green; module behavior unchanged (no test regression); ready to hand off to plan 03 (QUAL-01).
  </done>
</task>

</tasks>

<verification>
- `grep -c "## Transaction strategy" lib/foglet_bbs/boards/server.ex` equals 1.
- `grep -c "Multi step labels :post / :thread_update are load-bearing" lib/foglet_bbs/boards/server.ex` equals 2.
- `grep -nE "{:ok, %\\{post: post\\}}" lib/foglet_bbs/boards/server.ex` still matches the existing handle_call clause at line ~87 (load-bearing pattern preserved).
- `grep -nE "{:ok, %\\{thread_update:" lib/foglet_bbs/boards/server.ex` still matches the existing handle_call clause at line ~103.
- `rtk mix precommit` exits 0.
- `rtk mix test` exits 0 with ≥ baseline test count.
</verification>

<success_criteria>
- DOM-02 acceptance criteria from SPEC §Requirement 2 all met: top-to-bottom reader encounters rationale before any `Repo.transaction()` call.
- Behavior parity: success-side `{:ok, post}` / `{:ok, %{thread, post}}` and error-side `{:error, reason}` unchanged.
- v2.0 test baseline preserved.
- No new dialyzer warnings (the QUAL-01 plan handles `:call_without_opaque`).
</success_criteria>

<output>
After completion, create `.planning/phases/46-domain-cleanup-and-final-quality-gate/46-02-SUMMARY.md` per template, recording:
- Final moduledoc subheader text (verbatim).
- The two inline comment lines and their post-insert line numbers.
- `rtk mix precommit` and `rtk mix test` outputs (test summary line).
- Pointer note: "Closes CONCERNS.md Security Considerations heading at line 151 — disposition will be applied in plan 04 (QUAL-03)."
</output>
