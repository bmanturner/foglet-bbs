---
phase: 46
plan: 04
type: execute
wave: 4
depends_on: [46-01, 46-02, 46-03]
files_modified:
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements:
  - QUAL-03

must_haves:
  truths:
    - "For every line in .planning/codebase/CONCERNS.md matching ^### , a Disposition line exists within the same section before the next ### or ## heading"
    - "grep -c '^### ' .planning/codebase/CONCERNS.md (= 17) equals grep -c '^- Disposition' .planning/codebase/CONCERNS.md after the bold marker is normalized"
    - "Every Disposition value is exactly one of three: 'Fixed in Phase NN', 'Intentionally retained', or 'Covered by ...'"
    - "The intro paragraph references the v2.1 close pass on 2026-04-29 and asserts every section now carries a disposition"
    - "Original concern text bullets (- Issue:, - Files:, - Impact:, - Fix approach:) are preserved verbatim — only Disposition lines and intro note are additions"
  artifacts:
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Inline disposition register on every concern entry"
      grep_must_match: "v2.1 close pass"
      grep_count_match:
        pattern: "^- "
        contains: "Disposition:"
        equals: 17
  key_links:
    - from: ".planning/codebase/CONCERNS.md disposition lines"
      to: ".planning/phases/41-.../41-XX-SUMMARY.md through 46-.../46-XX-SUMMARY.md"
      via: "Fixed in Phase NN pointers (file paths or SUMMARY anchors)"
      pattern: "Fixed in Phase 4[1-6]"
---

<objective>
Add an inline Disposition line (in bold-colon form) to every ### heading in .planning/codebase/CONCERNS.md (Tech Debt -> Known Bugs -> Security Considerations -> Performance Bottlenecks -> Fragile Areas -> Test Coverage Gaps), classified as one of three values: "Fixed in Phase NN", "Intentionally retained", or "Covered by ...". Update the intro paragraph to reference the v2.1 close pass.

Purpose: The milestone-close gate needs a single objective signal that every concern has been classified. Without per-item disposition, the gate has no falsifiable test. This plan is the v2.1 capstone audit; it benefits from plans 01-03 already being committed because three of the dispositions cite results from those plans (DOM-01 closes the supervisor stub heading at line 89; DOM-02 closes the Plain Repo.transaction heading at line 151; QUAL-01 closes the Pre-existing Dialyzer warnings heading at line 103).

Output: .planning/codebase/CONCERNS.md with 17 new Disposition lines (one per ### heading), each pointing to a phase SUMMARY anchor or backlog rationale, plus an updated intro paragraph noting the v2.1 close pass. ZERO modification to original concern text.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-SPEC.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-RESEARCH.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-PATTERNS.md
@.planning/codebase/CONCERNS.md
@AGENTS.md

<interfaces>
17 ### headings to annotate, in file order (verified by `grep -n '^### ' .planning/codebase/CONCERNS.md`):

  ## Tech Debt (line 11) — 6 headings
    line 13:  ### TUI: Bounded compatibility callback surface still attached to `Foglet.TUI.Screen`
    line 33:  ### TUI: Process-dictionary modal-submit handoff (`take_screen_modal_submit`)
    line 55:  ### TUI: `Foglet.TUI.App` is still 1006 lines and concentrates routing/effects
    line 70:  ### Domain: Screen modules are large and concentrate complexity
    line 89:  ### Boards.Supervisor.boot_board_servers/0 stub still present
    line 103: ### Pre-existing Dialyzer warnings ignored, not fixed

  ## Known Bugs (line 118) — 0 sub-headings (intro-text only — leave untouched)

  ## Security Considerations (line 125) — 2 headings
    line 127: ### SSH key correlation via ETS pubkey stash
    line 151: ### Plain `Repo.transaction` skips Repo.transact ergonomics in board server

  ## Performance Bottlenecks (line 165) — 2 headings
    line 167: ### `Foglet.Posts.list_posts/1` loads every post in a thread, no pagination
    line 179: ### PostReader render cache keyed by `{post_id, terminal_width}`

  ## Fragile Areas (line 194) — 4 headings
    line 196: ### Session replacement race window (`replace_then_promote`)
    line 216: ### `Foglet.SSH.CLIHandler` global connection counter
    line 232: ### Alt-screen escape sequences scattered across CLIHandler termination paths
    line 244: ### Render-path purity invariant on PostReader is enforced by convention only

  ## Test Coverage Gaps (line 258) — 3 headings
    line 260: ### App-shell modal-submit handoff is tested through reducer paths only
    line 275: ### `replace_then_promote/3` 2s-timeout fallback path
    line 287: ### Soft-delete-aware list paths

Disposition vocabulary (D-10): EXACTLY three values:
  - "Fixed in Phase NN" (with one-line pointer: file path, function, or SUMMARY anchor)
  - "Intentionally retained" (with rationale and, when applicable, "see ROADMAP.md backlog")
  - "Covered by ..." (with the test, doc, or SUMMARY artifact named)

Insertion format (PATTERNS.md "Disposition line format — locked by SPEC Requirement 4"):
  Insert as a top-level bullet sibling of `- Issue:`, `- Files:`, `- Impact:`, `- Fix approach:`, AFTER the existing bullets, BEFORE the next ### or ## heading. Form (using bold markdown for the label):

      - **Disposition:** Fixed in Phase NN — `lib/path/to/file.ex` `function/arity` (or `NN-XX-SUMMARY.md` anchor).

Original concern text (`- Issue:`, `- Files:`, `- Impact:`, `- Fix approach:`) MUST be preserved VERBATIM (D-12, SPEC Boundaries).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify cross-reference pointers from phase 41-46 SUMMARYs (read-only)</name>
  <read_first>
    - .planning/codebase/CONCERNS.md (full file — read every section to internalize the original concern text being annotated)
    - .planning/phases/41-tui-contract-and-modal-effects/ (list all *-SUMMARY.md files)
    - .planning/phases/42-app-decomposition/ (list all *-SUMMARY.md files; exact slug may differ — use `ls .planning/phases/42-*/`)
    - .planning/phases/43-large-screen-decomposition/ (or actual slug — `ls .planning/phases/43-*/`)
    - .planning/phases/44-postreader-and-content-query-hardening/ (or actual slug)
    - .planning/phases/45-ssh-and-session-runtime-hardening/ (or actual slug)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-01-SUMMARY.md (DOM-01 pointer)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-02-SUMMARY.md (DOM-02 pointer)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-03-SUMMARY.md (QUAL-01 pointer)
  </read_first>
  <action>
    Read-only verification step. Per Assumption A1 in RESEARCH ("the exact dispositions proposed for each ### heading accurately reflect what those phases shipped"), spot-check the proposed disposition pointers in RESEARCH "CONCERNS.md Heading Inventory" against actual SUMMARY.md content for each cited phase.

    For each of the 17 headings, confirm or refine the proposed disposition wording (see RESEARCH.md "CONCERNS.md Heading Inventory" — proposed dispositions are listed in the table column "Proposed Disposition" for every heading line):

    1. Line 13 (TUI Bounded compatibility): Fixed in Phase 41 — pointers to 41-01-SUMMARY.md (canonical screen contract cleanup) and 41-02-SUMMARY.md (test/contract migration). Verify SUMMARY frontmatter `provides:` mentions screen contract / Screen module cleanup.

    2. Line 33 (Process-dictionary modal-submit): Fixed in Phase 41 — 41-03-SUMMARY.md (first-class modal-submit effect) and 41-04-SUMMARY.md (consumer migration + SubmitStash removal). Verify.

    3. Line 55 (TUI.App 1006 lines): Fixed in Phase 42 — 42-01 through 42-05 SUMMARYs (App.Routing, App.Modal, App.Effects, App.Subscriptions extraction). Verify count by `ls .planning/phases/42-*/*-SUMMARY.md`.

    4. Line 70 (Screen modules large): Fixed in Phase 43 — PostReader, MainMenu, Sysop, Login, NewThread, Account decompositions (43-01 through 43-06). Verify count.

    5. Line 89 (Boards.Supervisor stub): Fixed in Phase 46 — `lib/foglet_bbs/boards/supervisor.ex` stub removed (DOM-01); see 46-01-SUMMARY.md.

    6. Line 103 (Dialyzer warnings ignored): Fixed in Phase 46 — `.dialyzer_ignore.exs` baseline reduced (QUAL-01); see 46-03-SUMMARY.md.

    7. Line 127 (SSH key correlation): Fixed in Phase 45 — `lib/foglet_bbs/ssh/pubkey_stash.ex:21,109` (TTL + sweep); see 45-01-SUMMARY.md. Audit-row recommendation in 45-02-SUMMARY.md. Verify by reading 45-01 frontmatter.

    8. Line 151 (Plain Repo.transaction): Fixed in Phase 46 — documented as intentional, locked deviation (DOM-02); see `lib/foglet_bbs/boards/server.ex` `## Transaction strategy` moduledoc and 46-02-SUMMARY.md.

    9. Line 167 (list_posts/1 no pagination): Intentionally retained — partially addressed by Phase 44 PostReader bounded reader-window; full cursor-pagination of `list_posts/1` deferred at v2.1 kickoff. See ROADMAP.md backlog. Verify by reading 44-01/44-02 SUMMARYs.

    10. Line 179 (PostReader render cache stale widths): Per RESEARCH Open Questions Q1, read body of 44-03-SUMMARY.md to confirm whether resize-eviction was shipped fully and whether width-LRU was in scope. If resize-eviction shipped fully -> "Fixed in Phase 44 (resize eviction); width-LRU intentionally retained — see ROADMAP.md backlog." If only partial -> "Intentionally retained — partial fix in Phase 44 (resize eviction); width-LRU deferred. See ROADMAP.md backlog."

    11. Line 196 (Session replacement race): Covered by 45-03-SUMMARY.md (unified SSH cleanup helper + connection-counter lifecycle proof). Verify.

    12. Line 216 (CLIHandler connection counter): Fixed in Phase 45 — 45-03-SUMMARY.md (idempotent cleanup via `cleanup_done?` + `counter_counted?` state flags).

    13. Line 232 (Alt-screen escape sequences): Fixed in Phase 45 — 45-03-SUMMARY.md (single cleanup helper covers alt-screen leave, lifecycle stop, session stop, optional channel close, counter decrement).

    14. Line 244 (Render-path purity): Covered by 44-03-SUMMARY.md (render-purity guard hardening) and 43-04-SUMMARY.md (PostReader decomposition).

    15. Line 260 (App-shell modal-submit handoff tested via reducer): Covered by 41-03-SUMMARY.md and 41-04-SUMMARY.md (first-class modal-submit effect; SubmitStash removed).

    16. Line 275 (replace_then_promote 2s-timeout fallback path): Per RESEARCH Open Questions Q1, read 45-03-SUMMARY.md body to confirm the 2s-timeout path is exercised. If yes -> "Covered by 45-03-SUMMARY.md (lifecycle proof tests)." If not -> "Intentionally retained — low-priority backlog item. See ROADMAP.md backlog."

    17. Line 287 (Soft-delete-aware list paths): Covered by 44-04-SUMMARY.md (soft-delete reader/list query policy coverage). Verify.

    Discrepancy handling: if a SUMMARY does NOT confirm the proposed disposition, refine the wording to match what the SUMMARY actually delivered, OR escalate to "Intentionally retained" with backlog rationale. The acceptance criterion is that EVERY heading carries a disposition with a phase pointer, NOT that the proposed wording is preserved verbatim.

    Output of this task: a finalized 17-row mapping (heading line -> disposition value -> pointer) recorded as a working note for Task 2. NO file modifications in this task.
  </action>
  <verify>
    <automated>test "$(grep -c '^### ' .planning/codebase/CONCERNS.md)" -eq 17 &amp;&amp; ls .planning/phases/41-*/*-SUMMARY.md .planning/phases/42-*/*-SUMMARY.md .planning/phases/43-*/*-SUMMARY.md .planning/phases/44-*/*-SUMMARY.md .planning/phases/45-*/*-SUMMARY.md .planning/phases/46-*/*-SUMMARY.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 17 dispositions mapped (one per ### heading), each with finalized wording.
    - For every "Fixed in Phase NN" disposition, the cited SUMMARY.md exists and its frontmatter `provides:` / body content supports the claim.
    - For every "Covered by ..." disposition, the cited artifact (SUMMARY.md or test file) exists.
    - For every "Intentionally retained" disposition, a rationale phrase and (when applicable) a backlog pointer are written.
    - Heading 179 disposition decided based on actual 44-03-SUMMARY.md body.
    - Heading 275 disposition decided based on actual 45-03-SUMMARY.md body.
  </acceptance_criteria>
  <done>
    17-disposition mapping finalized; ready to apply to CONCERNS.md in Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert 17 Disposition lines into CONCERNS.md and update the intro paragraph</name>
  <read_first>
    - .planning/codebase/CONCERNS.md (full file — internalize current section structure and bullet style)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md (D-09, D-10, D-11, D-12)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-PATTERNS.md (".planning/codebase/CONCERNS.md — QUAL-03 disposition lines" — exact format and placement rules)
    - Task 1's finalized 17-row mapping (working note from previous task)
  </read_first>
  <action>
    Walk .planning/codebase/CONCERNS.md top-to-bottom in file order. For each ### heading (17 total), insert a single new bullet line of the form (bold markdown for the label):

        - **Disposition:** <value>

    Where <value> is one of (per D-10):
    - "Fixed in Phase NN — <pointer>."
    - "Intentionally retained — <rationale>; see ROADMAP.md backlog." (backlog pointer only when applicable)
    - "Covered by <artifact>."

    Placement rules (per D-09 and PATTERNS.md):
    - Insert AFTER the existing bullets in the heading's section (`- Issue:`, `- Files:`, `- Impact:`, `- Fix approach:`).
    - Insert BEFORE the next ### or ## heading.
    - Treat the new line as a top-level bullet sibling — same indentation as `- Issue:`.
    - Use bold `**Disposition:**` form (matches the existing bold-label style like `**Analysis Date:**` in the intro).

    Apply the 17 finalized dispositions from Task 1, in file order. The wording for headings 5, 6, and 8 is locked because those are the dispositions THIS phase produces:

    5. Line 89 (Boards.Supervisor stub): "Fixed in Phase 46 — `lib/foglet_bbs/boards/supervisor.ex` stub removed (DOM-01); see 46-01-SUMMARY.md."

    6. Line 103 (Dialyzer warnings ignored): "Fixed in Phase 46 — `.dialyzer_ignore.exs` baseline reduced from 54 to <final> lines (QUAL-01); see 46-03-SUMMARY.md." (Plug in the actual final line count from 46-03-SUMMARY.md.)

    8. Line 151 (Plain Repo.transaction): "Fixed in Phase 46 — documented as intentional, locked deviation from `Repo.transact/1` (DOM-02); see `lib/foglet_bbs/boards/server.ex` `## Transaction strategy` moduledoc and 46-02-SUMMARY.md."

    For headings 1, 2, 3, 4, 7, 9, 10, 11, 12, 13, 14, 15, 16, 17 use the wording finalized in Task 1.

    `## Known Bugs` section (line 118): zero ### sub-headings; LEAVE THIS SECTION UNTOUCHED per RESEARCH inventory.

    Update the intro paragraph (D-12). Currently lines 5-9 read (paraphrasing): "This document inventories tech debt ... as of v2.0 close (Phase 40 complete). The post-Phase-40 codebase passes rtk mix precommit and the full rtk mix test suite (1 property + 2161 tests, 0 failures), so most concerns below are latent risks ..."

    APPEND (do NOT replace; preserve v2.0-close framing per D-12) a paragraph noting:
    - The v2.1 close pass on 2026-04-29 (or actual phase-46 completion date if different).
    - Every section now carries a Disposition line on each heading.
    - The three disposition values: "Fixed in Phase NN", "Intentionally retained", "Covered by ...".

    Suggested intro append (Claude's Discretion — refine to match existing tone):

        **v2.1 close pass (2026-04-29):** Phase 46 added an inline `**Disposition:**` line to every `### ` heading in this file, classifying each concern as `Fixed in Phase NN` (with a SUMMARY anchor or file pointer), `Intentionally retained` (with rationale and, where applicable, a `ROADMAP.md` backlog pointer), or `Covered by ...` (with the test or doc artifact named). The original v2.0 concern text is preserved verbatim; this pass adds annotations only.

    DO NOT modify the original concern text bullets (`- Issue:`, `- Files:`, `- Impact:`, `- Fix approach:`) anywhere in the file. Per D-12 and SPEC Boundaries, "Original concern text is preserved verbatim; this phase only adds annotations and the intro note." Only additions allowed are: 17 disposition bullets and the one intro paragraph append.

    DO NOT add a Disposition line to the `## Known Bugs` section (no ### sub-headings exist there).

    Section ordering: file's actual structure is `## Tech Debt` -> `## Known Bugs` -> `## Security Considerations` -> `## Performance Bottlenecks` -> `## Fragile Areas` -> `## Test Coverage Gaps`. Walk in this order.
  </action>
  <verify>
    <automated>HEAD=$(grep -c '^### ' .planning/codebase/CONCERNS.md); DISP=$(grep -c '^- \*\*Disposition:\*\*' .planning/codebase/CONCERNS.md); echo "headings=$HEAD dispositions=$DISP"; test "$HEAD" -eq 17 &amp;&amp; test "$DISP" -eq 17 &amp;&amp; grep -q 'v2.1 close pass' .planning/codebase/CONCERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### ' .planning/codebase/CONCERNS.md` returns 17.
    - `grep -c '^- \*\*Disposition:\*\*' .planning/codebase/CONCERNS.md` returns exactly 17 (matches heading count).
    - Awk/sequential-grep walk: for every ### heading, a `- **Disposition:**` line exists in that heading's section before the next ### or ## heading. (Manual or scripted spot-check.)
    - Every Disposition line value starts with one of: "Fixed in Phase ", "Intentionally retained", "Covered by ".
    - `grep -n 'v2.1 close pass' .planning/codebase/CONCERNS.md` returns at least 1 match (intro paragraph appended).
    - The four original-concern-text bullet labels remain countable and identical to pre-phase: `grep -c '^- Issue:' .planning/codebase/CONCERNS.md`, `grep -c '^- Files:' .planning/codebase/CONCERNS.md`, `grep -c '^- Impact:' .planning/codebase/CONCERNS.md`, `grep -c '^- Fix approach:' .planning/codebase/CONCERNS.md` — counts MUST be unchanged from pre-phase baseline (record pre-phase counts before editing for the comparison).
    - The `## Known Bugs` section is unchanged (no Disposition added there).
  </acceptance_criteria>
  <done>
    17 Disposition lines inserted, one per ### heading, in correct sections; intro paragraph appended with v2.1 close pass note; original concern text preserved verbatim.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run precommit + test floor (D-14 cadence gate) — phase close</name>
  <read_first>
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md (D-14)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-VALIDATION.md (per-task verification map row 46-04-* and Manual-Only Verifications table)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-SPEC.md (Acceptance Criteria — full phase gate)
  </read_first>
  <action>
    This is the final cadence gate for plan 04 AND for the phase as a whole. After Tasks 1 and 2 are committed:

    1. `rtk mix precommit` (full pipeline). Phase 46 only modifies a planning artifact in this plan, but precommit still runs against the full repo (compile, format, credo, sobelow, dialyzer). All MUST pass.

    2. `rtk mix test`. MUST report >= 1 property + 2161 tests, 0 failures.

    3. Phase-gate grep checks (full SPEC Acceptance Criteria block — re-run all 9 in sequence to confirm phase close):
       - `! grep -rn 'def boot_board_servers' lib/foglet_bbs/boards/supervisor.ex` (DOM-01).
       - `grep -n '## Transaction strategy' lib/foglet_bbs/boards/server.ex` returns 1 match (DOM-02).
       - `grep -c 'Multi step labels :post / :thread_update are load-bearing' lib/foglet_bbs/boards/server.ex` equals 2 (DOM-02).
       - `! grep -n ':call_without_opaque' .dialyzer_ignore.exs` (QUAL-01).
       - `wc -l .dialyzer_ignore.exs` is strictly less than 54 (QUAL-01).
       - Every remaining ignore entry has a comment within 5 lines above (QUAL-01) — manual or scripted walk.
       - `grep -c '^### ' .planning/codebase/CONCERNS.md` equals `grep -c '^- \*\*Disposition:\*\*' .planning/codebase/CONCERNS.md` (both = 17) (QUAL-03).
       - `grep -n 'v2.1 close pass' .planning/codebase/CONCERNS.md` returns >= 1 (QUAL-03).
       - `rtk mix precommit` exits 0.
       - `rtk mix test` exits 0 with >= baseline test count.

    Per D-14, any new dialyzer warning, Credo violation, Sobelow finding, or test regression is a hard stop.

    Record observed test summary line and final dialyzer ignore line count in the SUMMARY.
  </action>
  <verify>
    <automated>rtk mix precommit &amp;&amp; rtk mix test &amp;&amp; test "$(grep -c '^### ' .planning/codebase/CONCERNS.md)" -eq "$(grep -c '^- \*\*Disposition:\*\*' .planning/codebase/CONCERNS.md)" &amp;&amp; ! grep -n ':call_without_opaque' .dialyzer_ignore.exs &amp;&amp; test "$(wc -l &lt; .dialyzer_ignore.exs)" -lt 54 &amp;&amp; ! grep -n 'def boot_board_servers' lib/foglet_bbs/boards/supervisor.ex &amp;&amp; grep -q '## Transaction strategy' lib/foglet_bbs/boards/server.ex &amp;&amp; grep -q 'v2.1 close pass' .planning/codebase/CONCERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - All 9 SPEC Acceptance Criteria checks pass (listed in the action body above).
    - `rtk mix precommit` exits 0.
    - `rtk mix test` exits 0 with >= 1 property + 2161 tests, 0 failures.
    - Phase-46 SUMMARY records: final `wc -l .dialyzer_ignore.exs` value, final test summary line, and the 17 Disposition lines applied.
  </acceptance_criteria>
  <done>
    Phase 46 closed: all four requirements (DOM-01, DOM-02, QUAL-01, QUAL-03) shipped; phase gate green; v2.1 milestone close gate satisfied.
  </done>
</task>

</tasks>

<verification>
- `grep -c '^### ' .planning/codebase/CONCERNS.md` returns 17.
- `grep -c '^- \*\*Disposition:\*\*' .planning/codebase/CONCERNS.md` returns 17.
- `grep -n 'v2.1 close pass' .planning/codebase/CONCERNS.md` returns >= 1.
- Every Disposition value starts with one of: "Fixed in Phase ", "Intentionally retained", "Covered by ".
- Original concern text bullet counts unchanged (- Issue:, - Files:, - Impact:, - Fix approach: each preserved at pre-phase count).
- `## Known Bugs` section unchanged.
- All 9 SPEC Acceptance Criteria pass.
- `rtk mix precommit` exits 0; `rtk mix test` exits 0 with >= baseline.
</verification>

<success_criteria>
- QUAL-03 acceptance criteria from SPEC Requirement 4 all met.
- Full phase 46 SPEC Acceptance Criteria block all green (DOM-01, DOM-02, QUAL-01, QUAL-03 combined).
- v2.0 test baseline preserved.
- v2.1 milestone close gate satisfied: every CONCERNS.md heading classified, dialyzer baseline reduced, supervisor stub gone, board-server transaction strategy documented.
</success_criteria>

<output>
After completion, create `.planning/phases/46-domain-cleanup-and-final-quality-gate/46-04-SUMMARY.md` per template, recording:
- All 17 Disposition lines applied (verbatim copy of each, with line numbers in CONCERNS.md).
- The intro-paragraph append text (verbatim).
- Pre-phase counts vs. post-phase counts of: ### headings (17), `- **Disposition:**` lines (17), `- Issue:` (unchanged), `- Files:` (unchanged), `- Impact:` (unchanged), `- Fix approach:` (unchanged).
- `rtk mix precommit` and `rtk mix test` outputs (test summary line).
- Phase-gate confirmation: all 9 SPEC Acceptance Criteria checks green.
- Closing note: "Phase 46 complete; v2.1 milestone close gate satisfied."
</output>
