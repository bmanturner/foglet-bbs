---
phase: 46
plan: 03
type: execute
wave: 3
depends_on: [46-01, 46-02]
files_modified:
  - .dialyzer_ignore.exs
  - lib/foglet_bbs/boards/server.ex
  - lib/foglet_bbs/tui/screens/account.ex
  - lib/foglet_bbs/tui/screens/board_list.ex
  - lib/foglet_bbs/tui/screens/login.ex
  - lib/foglet_bbs/tui/screens/login/state.ex
  - lib/foglet_bbs/tui/screens/main_menu.ex
  - lib/foglet_bbs/tui/screens/moderation.ex
  - lib/foglet_bbs/tui/screens/new_thread.ex
  - lib/foglet_bbs/tui/screens/sysop.ex
  - lib/foglet_bbs/tui/screens/post_composer.ex
  - lib/foglet_bbs/tui/screens/post_reader.ex
  - lib/foglet_bbs/tui/screens/register.ex
  - lib/foglet_bbs/tui/screens/register/state.ex
  - lib/foglet_bbs/tui/screens/thread_list.ex
  - lib/foglet_bbs/tui/screens/verify.ex
  - lib/foglet_bbs/tui/screens/verify/state.ex
  - lib/foglet_bbs/tui/size_gate.ex
  - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
  - lib/foglet_bbs/tui/widgets/display/progress.ex
  - lib/foglet_bbs/tui/widgets/display/tree.ex
  - lib/foglet_bbs/tui/widgets/input/button.ex
  - lib/foglet_bbs/tui/widgets/input/checkbox.ex
  - lib/foglet_bbs/tui/widgets/input/menu.ex
  - lib/foglet_bbs/tui/widgets/input/tabs.ex
  - lib/foglet_bbs/tui/widgets/post/post_card.ex
  - lib/foglet_bbs/tui/widgets/progress/spinner.ex
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "rtk mix dialyzer exits 0"
    - ".dialyzer_ignore.exs no longer contains {\"lib/foglet_bbs/boards/server.ex\", :call_without_opaque}"
    - "wc -l .dialyzer_ignore.exs is strictly smaller than the pre-phase 54-line baseline"
    - "Every remaining ignore-list entry has either an inline comment OR shares a comment block above it explaining why it is kept"
    - "rtk mix precommit exits 0; rtk mix test exits 0 with ≥ baseline (1 property + 2161 tests, 0 failures)"
  artifacts:
    - path: ".dialyzer_ignore.exs"
      provides: "Reduced, annotated Dialyzer baseline"
      must_not_contain: '{"lib/foglet_bbs/boards/server.ex", :call_without_opaque}'
      grep_must_match: "Raxol element() return — opaque from caller perspective"
  key_links:
    - from: ".dialyzer_ignore.exs file header"
      to: "post-cleanup invariant: every kept entry has stated reason"
      via: "rewritten header comment block (D-07)"
      pattern: "every kept entry has a stated reason"
---

<objective>
Aggressively reduce the `.dialyzer_ignore.exs` baseline (currently 54 lines, 28 entries) by:
(a) FIXING the `boards/server.ex :call_without_opaque` real hint (D-04) — narrow `@spec` or `Multi.run/3` callback signatures so the warning is no longer emitted, then remove the entry.
(b) NARROWING every `:contract_supertype` spec where success-typing allows (D-03 bucket 1) — screen `init/1` returning `map()` → concrete state struct `t()`; state-module `default/0`/`put/2` returning typed struct; `update/3` event params from `term()` to a concrete event union where one exists.
(c) ANNOTATING the irreducible-noise residue (D-03 buckets 2 and 3, plus D-05 unknown_type and D-06 no_match) with shared comment blocks that explicitly classify why each kept entry stays.
(d) REWRITING the file-header comment to assert the post-cleanup invariant (D-07).

Purpose: Closes CONCERNS.md Tech Debt entry at line 103 ("Pre-existing Dialyzer warnings ignored, not fixed"). Establishes that every kept ignore entry has a reviewer-justified reason.

Output: `.dialyzer_ignore.exs` shrunk to a smaller line count, with the `:call_without_opaque` entry gone, every kept entry annotated, and the file header asserting the post-cleanup invariant. `rtk mix precommit && rtk mix test` green.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-SPEC.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-RESEARCH.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-PATTERNS.md
@AGENTS.md
@.dialyzer_ignore.exs
@mix.exs

<interfaces>
Pre-phase baseline (verified by RESEARCH.md and live grep):
  - .dialyzer_ignore.exs: 54 lines, 28 entries
  - 4 :unknown_type entries (Bucket A — KEEP per D-05)
  - 1 :call_without_opaque on boards/server.ex (Bucket B — FIX & REMOVE per D-04)
  - 20 :contract_supertype entries (Bucket C — split per D-03 into C1 narrow / C2 Raxol-opaque / C3 Ecto-opaque)
  - 2 :no_match string-pattern entries on account/prefs_form.ex and account/profile_form.ex (Bucket D — KEEP per D-06)

Bucket A (KEEP, D-05) — single shared comment block "# Ecto schema t/0 false positives — dialyzer cannot resolve t/0 from `use Ecto.Schema`":
  - {"lib/foglet_bbs/boards.ex", :unknown_type}
  - {"lib/foglet_bbs/boards/server.ex", :unknown_type}
  - {"lib/foglet_bbs/posts.ex", :unknown_type}
  - {"lib/foglet_bbs/threads.ex", :unknown_type}

Bucket B (FIX & REMOVE, D-04):
  - {"lib/foglet_bbs/boards/server.ex", :call_without_opaque} — caused by Multi.run/3 callbacks composing opaque Ecto types. Fix by tightening Multi.run/3 callback signatures inside run_post_insert_multi/5 and run_thread_create_multi/4, OR by narrowing the @spec on those private helpers. If the warning resists removal after a reasonable attempt, REVERT the spec change and keep the entry with rationale (last-resort per D-08); SPEC §Acceptance still demands removal, so escalate by trying both approaches.

Bucket C1 narrow candidates (FIX where possible, D-03 bucket 1) — 16 screen/state/size_gate modules:
  - lib/foglet_bbs/tui/screens/account.ex (init/1, update/3 event narrowing)
  - lib/foglet_bbs/tui/screens/board_list.ex (init/1, state struct t/0)
  - lib/foglet_bbs/tui/screens/login.ex (init/1 → Login.State.t())
  - lib/foglet_bbs/tui/screens/login/state.ex (default/0, put/2 → typed struct t/0)
  - lib/foglet_bbs/tui/screens/main_menu.ex (init/1, state struct)
  - lib/foglet_bbs/tui/screens/moderation.ex (init/1, state struct)
  - lib/foglet_bbs/tui/screens/new_thread.ex (init/1, state struct)
  - lib/foglet_bbs/tui/screens/sysop.ex (init/1, state struct)
  - lib/foglet_bbs/tui/screens/post_composer.ex (init/1, state struct)
  - lib/foglet_bbs/tui/screens/post_reader.ex (init/1, state struct)
  - lib/foglet_bbs/tui/screens/register.ex (init/1, state struct)
  - lib/foglet_bbs/tui/screens/register/state.ex (default/0, put/2 → typed struct t/0)
  - lib/foglet_bbs/tui/screens/thread_list.ex (init/1, state struct)
  - lib/foglet_bbs/tui/screens/verify.ex (init/1, state struct)
  - lib/foglet_bbs/tui/screens/verify/state.ex (default/0, put/2 → typed struct t/0)
  - lib/foglet_bbs/tui/size_gate.ex (reducer/state spec)

Bucket C2 (KEEP, D-03 bucket 2 — Raxol-opaque) — single shared comment block "# Raxol element() return — opaque from caller perspective":
  - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex (render-only)
  - lib/foglet_bbs/tui/widgets/display/progress.ex (render-only)
  - lib/foglet_bbs/tui/widgets/display/tree.ex (render-only)
  - lib/foglet_bbs/tui/widgets/input/button.ex (render is opaque; init/handle_event possibly narrowable)
  - lib/foglet_bbs/tui/widgets/input/checkbox.ex (same)
  - lib/foglet_bbs/tui/widgets/input/menu.ex (same)
  - lib/foglet_bbs/tui/widgets/input/tabs.ex (same)
  - lib/foglet_bbs/tui/widgets/post/post_card.ex (render + helpers)
  - lib/foglet_bbs/tui/widgets/progress/spinner.ex (render-only)

Bucket C3 (KEEP, D-03 bucket 3 — Ecto-opaque) — single shared comment block "# Ecto.Changeset.t/0 / Ecto.Multi.t/0 supertype — Ecto opaque types":
  - Populated EMPIRICALLY: any Bucket C1 candidate that resists narrowing because it returns `Ecto.Changeset.t/0` or `Ecto.Multi.t/0` falls into this group.

Bucket D (KEEP, D-06) — preserve existing inline rationale comment:
  - {"lib/foglet_bbs/tui/screens/account/prefs_form.ex", "The pattern can never match the type true."}
  - {"lib/foglet_bbs/tui/screens/account/profile_form.ex", "The pattern can never match the type true."}
  - Existing comment at lines 48-49: "# Phase 25 Account forms intentionally expose a defensive :no_match fallback / # even though current call sites only route supported form events." — KEEP VERBATIM per D-06.

Validation cadence (D-08): after each batch fix, run `rtk mix dialyzer` to verify the warning is gone. If a narrowing introduces a NEW warning (Pitfall 1 in RESEARCH), revert and reclassify the entry as Raxol-opaque (bucket C2) or Ecto-opaque (bucket C3).

TUI screen contract reminder (from `## State of the Art` table in RESEARCH):
  - Canonical contract is `init(Context.t()) :: map()`, `update(term(), map(), Context.t()) :: {map(), [Effect.t()]}`, `render(map(), Context.t()) :: any()`
  - Narrow `init/1` map() returns to a concrete state struct `t()`. Add a typed `@type t :: %__MODULE__{…}` if absent (cheap and enables removing the entry).
  - DO NOT narrow `render/1,2` returning `any()` (Pitfall 1 — Raxol element opacity).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix Bucket B — narrow boards/server.ex :call_without_opaque</name>
  <read_first>
    - lib/foglet_bbs/boards/server.ex (full file, ~210 lines — pay particular attention to run_post_insert_multi/5 around lines 120-155 and run_thread_create_multi/4 around lines 170-200; also @spec lines on these private helpers if any exist)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md (D-04 — fix strategy)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-RESEARCH.md (Pitfall 2 — "Removing :call_without_opaque on boards/server.ex re-emits when Multi result tuple is consumed"; "Bucket B" inventory)
    - .dialyzer_ignore.exs (locate the current Bucket B entry — single-line tuple `{"lib/foglet_bbs/boards/server.ex", :call_without_opaque}`)
  </read_first>
  <action>
    Goal: silence the `:call_without_opaque` warning on `lib/foglet_bbs/boards/server.ex` so the entry can be removed from `.dialyzer_ignore.exs`.

    Strategy (per D-04, ordered preference):
    1. **First attempt — tighten `Multi.run/3` callback signatures.** The callbacks inside `run_post_insert_multi/5` and `run_thread_create_multi/4` (e.g., `Multi.run(:post, fn repo, %{thread: t} -> … end)`) currently let dialyzer infer broader-than-actual return types. Add explicit return-shape annotations or refactor to use named module functions with `@spec`s that declare the exact `{:ok, term()}` / `{:error, term()}` shape.
    2. **Second attempt — narrow the helper @spec.** Add or tighten `@spec` on `run_post_insert_multi/5` and `run_thread_create_multi/4` so dialyzer can see a non-opaque success-result map shape (e.g., `@spec run_post_insert_multi(...) :: {:ok, %{post: Foglet.Posts.Post.t(), …}} | {:error, atom() | Ecto.Changeset.t()}`).
    3. **Third attempt (if 1+2 fail) — extract Multi composition into a private helper that returns a non-opaque result map**, decoupling the opaque `Ecto.Multi.t/0` from the caller.
    4. **Last resort (if all three fail and the warning resists)** — REVERT all changes, keep the ignore entry, and add a per-entry comment naming the resisting reason (this contradicts SPEC §Requirement 3's explicit acceptance "boards/server.ex :call_without_opaque is no longer present", so escalate by retrying approach 3 first).

    DO NOT:
    - Rename Multi step labels `:post` or `:thread_update` — load-bearing for handle_call pattern matches at server.ex:87 and :103.
    - Modify the public `handle_call` reply contract (success-side `{:ok, post}` / `{:ok, %{thread, post}}`; error-side `{:error, reason}`).
    - Convert `Repo.transaction/1` to `Repo.transact/1` — DOM-02 documents this divergence as locked.
    - Touch the `## Transaction strategy` moduledoc paragraph or the inline pointer comments added by Plan 02.

    After the fix:
    - Remove the line `{"lib/foglet_bbs/boards/server.ex", :call_without_opaque}` from `.dialyzer_ignore.exs`. (Note: the `:unknown_type` entry on the same file remains in Bucket A; do NOT remove that.)
    - Run `rtk mix dialyzer` and verify zero warnings on `boards/server.ex`.
  </action>
  <verify>
    <automated>! grep -n ':call_without_opaque' .dialyzer_ignore.exs &amp;&amp; rtk mix dialyzer</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -n ":call_without_opaque" .dialyzer_ignore.exs` returns no matches.
    - `rtk mix dialyzer` exits 0 with zero warnings.
    - `grep -nE "{:ok, %\\{post: post\\}}" lib/foglet_bbs/boards/server.ex` still matches the handle_call clause at line ~87 (success-side contract preserved).
    - `grep -nE "{:ok, %\\{thread_update:" lib/foglet_bbs/boards/server.ex` still matches at line ~103.
    - `grep -c "## Transaction strategy" lib/foglet_bbs/boards/server.ex` still equals 1 (Plan 02's moduledoc untouched).
    - `grep -c "Multi step labels :post / :thread_update are load-bearing" lib/foglet_bbs/boards/server.ex` still equals 2 (Plan 02's inline comments untouched).
    - `rtk mix test test/foglet_bbs/boards/` exits 0 (behavior parity).
  </acceptance_criteria>
  <done>
    `:call_without_opaque` warning eliminated on `boards/server.ex`; entry removed from `.dialyzer_ignore.exs`; behavior parity confirmed by boards test suite; `rtk mix dialyzer` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bucket C1 — narrow screen/state/size_gate :contract_supertype specs (16 modules)</name>
  <read_first>
    - .dialyzer_ignore.exs (locate every :contract_supertype entry on lib/foglet_bbs/tui/screens/* and lib/foglet_bbs/tui/size_gate.ex)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md (D-03 bucket 1 — narrow candidates)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-RESEARCH.md (Bucket C1 table; Pitfall 1; "## State of the Art" — canonical screen contract)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-PATTERNS.md (TUI screen contract shape)
    - lib/foglet_bbs/tui/screens/login.ex AND lib/foglet_bbs/tui/screens/login/state.ex (read together — login is the simplest narrow target and serves as a worked-example for the other screens; observe how the sibling state.ex file's `t/0` should be referenced from login.ex's init/1 @spec)
  </read_first>
  <action>
    For EACH of the 16 modules listed below, attempt to narrow the `:contract_supertype`-flagged specs per the D-03 bucket-1 strategy. Process one module at a time; after each narrow, run `rtk mix dialyzer` to confirm both that the original warning disappears AND no NEW warning emerges (Pitfall 1).

    Module-by-module narrow targets:
    1. `lib/foglet_bbs/tui/screens/account.ex` — narrow `init/1` return from `map()` to the concrete account-screen state struct (define `@type t/0` if missing); narrow `update/3` event param from `term()` to a concrete event union if defined.
    2. `lib/foglet_bbs/tui/screens/board_list.ex` — narrow `init/1` to state struct `t()`.
    3. `lib/foglet_bbs/tui/screens/login.ex` — narrow `init/1` return to `Foglet.TUI.Screens.Login.State.t()` (sibling state module).
    4. `lib/foglet_bbs/tui/screens/login/state.ex` — narrow `default/0` and `put/2` returns from `map()` to the module's own `%__MODULE__{}` struct; add `@type t :: %__MODULE__{…}` if absent.
    5. `lib/foglet_bbs/tui/screens/main_menu.ex` — narrow `init/1` to state struct.
    6. `lib/foglet_bbs/tui/screens/moderation.ex` — same.
    7. `lib/foglet_bbs/tui/screens/new_thread.ex` — same.
    8. `lib/foglet_bbs/tui/screens/sysop.ex` — same.
    9. `lib/foglet_bbs/tui/screens/post_composer.ex` — same.
    10. `lib/foglet_bbs/tui/screens/post_reader.ex` — same.
    11. `lib/foglet_bbs/tui/screens/register.ex` — narrow `init/1` to `Foglet.TUI.Screens.Register.State.t()`.
    12. `lib/foglet_bbs/tui/screens/register/state.ex` — narrow `default/0`, `put/2` to typed struct.
    13. `lib/foglet_bbs/tui/screens/thread_list.ex` — narrow `init/1` to state struct.
    14. `lib/foglet_bbs/tui/screens/verify.ex` — narrow `init/1` to `Foglet.TUI.Screens.Verify.State.t()`.
    15. `lib/foglet_bbs/tui/screens/verify/state.ex` — narrow `default/0`, `put/2` to typed struct.
    16. `lib/foglet_bbs/tui/size_gate.ex` — narrow reducer/state spec to concrete struct `t()`.

    Per-module workflow (apply to each):
    a. Read the module to identify the spec(s) currently flagged. Specs broader than success typing usually look like `@spec init(Context.t()) :: map()` or `@spec default() :: map()`.
    b. If the module already has a `defstruct […]` and an `@type t :: %__MODULE__{…}` (or sibling state module), reference that. If no `@type t/0` exists, define one inline based on the existing `defstruct` field list. (Cheap; explicitly endorsed by CONTEXT §specifics.)
    c. Update the `@spec` to the concrete `t()` (or specific union, e.g., `t() | nil` if appropriate).
    d. Run `rtk mix dialyzer`.
    e. If the original warning is gone and no new warning surfaced → REMOVE that module's `:contract_supertype` entry from `.dialyzer_ignore.exs`.
    f. If a NEW warning emerges (Pitfall 1, often Raxol-opaque on `render/1,2` or Ecto-opaque on changeset specs), REVERT the spec change for that path and reclassify the entry: it stays in `.dialyzer_ignore.exs` but moves to bucket C2 (Raxol-opaque) or C3 (Ecto-opaque) — to be grouped in Task 3.

    DO NOT narrow `render/1,2` returns from `any()` (Pitfall 1; render returns Raxol elements which are opaque from outside Raxol).

    DO NOT touch widget files in this task — they are Bucket C2 (Task 3).

    Track per-module outcome in the eventual SUMMARY.md (narrowed-and-removed vs. narrowed-failed-keep-as-bucket-C2/C3).
  </action>
  <verify>
    <automated>rtk mix dialyzer &amp;&amp; rtk mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rtk mix dialyzer` exits 0 throughout the task (after each per-module narrowing).
    - At least 5 entries removed from `.dialyzer_ignore.exs` between Tasks 1 and 2 combined (Assumption A3 in RESEARCH.md targets ≥ 5; Bucket B fix alone gives 1, so ≥ 4 narrowed C1 entries).
    - For modules where narrowing introduced a new warning, the original `:contract_supertype` entry remains in `.dialyzer_ignore.exs` and is queued for Task 3 reclassification (record per-module outcome in plan SUMMARY).
    - `rtk mix compile --warnings-as-errors` exits 0 (no spec syntax errors, no missing `@type t/0` references).
    - `rtk mix test --stale` exits 0 (no behavior change from typed-struct narrowing).
  </acceptance_criteria>
  <done>
    All 16 C1-candidate modules attempted; narrowable entries removed from `.dialyzer_ignore.exs`; resisting entries reclassified for Task 3; `rtk mix dialyzer` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Bucket C2/C3/A/D — annotate the irreducible-noise residue with shared comment blocks; rewrite file header (D-07)</name>
  <read_first>
    - .dialyzer_ignore.exs (current state after Tasks 1 and 2)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md (D-03 bucket 2/3, D-05, D-06, D-07)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-PATTERNS.md (".dialyzer_ignore.exs — QUAL-01 narrowing + annotation" — exact comment-block placement, indentation, trailing-comma rules)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-RESEARCH.md (Bucket A, C2, C3, D inventories)
  </read_first>
  <action>
    Re-organize the surviving entries in `.dialyzer_ignore.exs` into four bucket-grouped sections, each preceded by a single shared comment block, and rewrite the file header per D-07.

    **(1) Rewrite file header (D-07).** Replace the existing comment lines 1-15 (the "fail precommit on NEW warnings while letting the existing noise coexist" framing) with a stronger statement:

    Suggested header text (Claude's Discretion — match terse, rationale-first style; refine wording for fit):
    ```
    # Dialyzer ignore list — post v2.1 cleanup baseline (Phase 46).
    #
    # Invariant: every kept entry has a stated reason in the shared comment
    # block immediately above it. Entries without rationale are not allowed
    # back without reviewer justification.
    #
    # Buckets, in order:
    #   A — :unknown_type (Ecto schema t/0 false positives)
    #   C2 — :contract_supertype where the return type is Raxol element() (opaque)
    #   C3 — :contract_supertype where the return type is Ecto.Changeset.t/0 / Ecto.Multi.t/0 (opaque)
    #   D — :no_match defensive fallbacks (Phase 25 account forms)
    ```

    **(2) Bucket A — :unknown_type (D-05).** Group the 4 entries together with a single shared header comment INSIDE the list literal (indented):
    ```
      # Ecto schema t/0 false positives — dialyzer cannot resolve t/0 from `use Ecto.Schema`.
      # Not a real bug; entries kept until Ecto/Dialyxir resolves the upstream PLT issue.
      {"lib/foglet_bbs/boards.ex", :unknown_type},
      {"lib/foglet_bbs/boards/server.ex", :unknown_type},
      {"lib/foglet_bbs/posts.ex", :unknown_type},
      {"lib/foglet_bbs/threads.ex", :unknown_type},
    ```

    **(3) Bucket C2 — Raxol-opaque (D-03 bucket 2).** Group all surviving widget render entries (and any C1 entry that resisted narrowing because of a Raxol-element return) under a single shared comment block:
    ```
      # Raxol element() return — opaque from caller perspective.
      # Widget render/1,2 returns Raxol DSL elements composed from internal
      # types not exported in a Dialyzer-resolvable way. Narrowing here
      # would emit a fresh :contract_supertype on the same line.
      {"lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex", :contract_supertype},
      …
    ```
    Surviving entries (initial set; final list depends on Task 2 outcomes):
      - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
      - lib/foglet_bbs/tui/widgets/display/progress.ex
      - lib/foglet_bbs/tui/widgets/display/tree.ex
      - lib/foglet_bbs/tui/widgets/input/button.ex
      - lib/foglet_bbs/tui/widgets/input/checkbox.ex
      - lib/foglet_bbs/tui/widgets/input/menu.ex
      - lib/foglet_bbs/tui/widgets/input/tabs.ex
      - lib/foglet_bbs/tui/widgets/post/post_card.ex
      - lib/foglet_bbs/tui/widgets/progress/spinner.ex
    + any C1 entry that resisted narrowing because of a Raxol-element return (from Task 2's per-module outcome log).

    Optional widget narrowing pass: for widgets with non-render specs (e.g., button/checkbox/menu/tabs `init/1` and `handle_event/2`), attempt to narrow those specs first. If narrowing succeeds and the warning disappears, remove the entry. Only fall back to the C2 shared comment for genuinely-opaque render returns.

    **(4) Bucket C3 — Ecto-opaque (D-03 bucket 3).** Group any C1-candidate entries that resisted narrowing because the spec returns `Ecto.Changeset.t/0` or `Ecto.Multi.t/0` under a single shared comment block:
    ```
      # Ecto.Changeset.t/0 / Ecto.Multi.t/0 supertype — Ecto opaque types.
      # Spec is broader than success typing because Ecto does not expose
      # internal struct shape; narrowing requires non-public Ecto internals.
      {"lib/foglet_bbs/tui/screens/<module>.ex", :contract_supertype},
      …
    ```
    Surviving entries: empirically determined by Task 2's outcomes. May be empty if all C1 candidates either narrowed cleanly or fell into C2.

    **(5) Bucket D — :no_match (D-06).** PRESERVE the existing comment at lines 48-49 (or wherever it now sits) VERBATIM. Per D-06, "carry over the existing inline rationale; do not delete the comment that is already there." Augment the existing comment with the post-cleanup invariant note if needed:
    ```
      # Phase 25 Account forms intentionally expose a defensive :no_match fallback
      # even though current call sites only route supported form events.
      {"lib/foglet_bbs/tui/screens/account/prefs_form.ex",
       "The pattern can never match the type true."},
      {"lib/foglet_bbs/tui/screens/account/profile_form.ex",
       "The pattern can never match the type true."}
    ```

    **(6) Final ordering.** Per Claude's Discretion (CONTEXT §Discretion), arrange buckets in this order for readability: A → C2 → C3 → D. Within each bucket, alphabetical-by-path is fine.

    **(7) Trailing-comma rules** (PATTERNS.md): every entry except the last in the list literal has a trailing comma; the final entry has no trailing comma. After re-ordering, double-check the final entry's comma.

    **(8) Run `rtk mix dialyzer`** to confirm zero warnings. The shared-comment-block restructure cannot affect dialyzer behavior (only the surrounding `#` comments change), but verify the file is still a valid Elixir term.
  </action>
  <verify>
    <automated>rtk mix dialyzer &amp;&amp; test "$(wc -l &lt; .dialyzer_ignore.exs)" -lt 54 &amp;&amp; ! grep -c ':call_without_opaque' .dialyzer_ignore.exs</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l .dialyzer_ignore.exs` returns a value strictly less than 54 (the pre-phase line count).
    - `rtk mix dialyzer` exits 0.
    - `! grep -n ":call_without_opaque" .dialyzer_ignore.exs` returns no matches.
    - `grep -c "Raxol element() return — opaque from caller perspective" .dialyzer_ignore.exs` returns at least 1 (Bucket C2 shared header present).
    - `grep -c "Ecto schema t/0 false positives" .dialyzer_ignore.exs` returns at least 1 (Bucket A shared header present).
    - `grep -c "Phase 25 Account forms intentionally expose" .dialyzer_ignore.exs` returns at least 1 (Bucket D existing comment preserved per D-06).
    - `grep -c "every kept entry has a stated reason" .dialyzer_ignore.exs` returns at least 1 (D-07 file-header invariant text).
    - Every remaining `{path, …}` entry has either an inline comment within 5 lines above it OR is part of a shared comment block (visual/manual review; record in SUMMARY).
    - File parses as a valid Elixir term (verified by `rtk mix dialyzer` succeeding — it reads the file).
  </acceptance_criteria>
  <done>
    `.dialyzer_ignore.exs` re-organized into four documented buckets; line count strictly smaller than 54; every kept entry has stated rationale; `rtk mix dialyzer` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Run precommit + test floor (D-14 cadence gate)</name>
  <read_first>
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md (D-14)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-VALIDATION.md (per-task verification map row 46-03-*)
  </read_first>
  <action>
    Final cadence gate after all of Tasks 1, 2, 3 are committed:
    1. `rtk mix precommit` — full pipeline. Must include a clean Dialyzer run with the post-cleanup ignore file.
    2. `rtk mix test` — full suite. Must report ≥ 1 property + 2161 tests, 0 failures.

    Per D-14, both MUST exit 0. Any new dialyzer warning, Credo violation, Sobelow finding, or test regression is a hard stop.

    Particular attention:
    - The narrowed `@spec`s on screen/state modules MUST NOT cause new compile warnings. If `rtk mix compile --warnings-as-errors` (part of precommit) flags an undefined `t/0` reference, fix the missing `@type t/0` definition.
    - The narrowed `@spec`s MUST NOT introduce credo violations (e.g., overly-complex spec lines).
    - The Bucket B fix on `boards/server.ex` MUST NOT change observable behavior — full test suite proves this.

    Record observed test count and final `wc -l .dialyzer_ignore.exs` in the SUMMARY.
  </action>
  <verify>
    <automated>rtk mix precommit &amp;&amp; rtk mix test</automated>
  </verify>
  <acceptance_criteria>
    - `rtk mix precommit` exits 0.
    - `rtk mix test` exits 0.
    - Test summary line shows ≥ 1 property and ≥ 2161 tests with 0 failures.
    - Final `wc -l .dialyzer_ignore.exs` recorded in SUMMARY (must be < 54).
    - Final entry count of `.dialyzer_ignore.exs` recorded in SUMMARY (must be < 28).
  </acceptance_criteria>
  <done>
    Phase-46 plan-03 cadence gate green; QUAL-01 acceptance criteria all met; ready to hand off to plan 04 (QUAL-03).
  </done>
</task>

</tasks>

<verification>
- `! grep -n ":call_without_opaque" .dialyzer_ignore.exs`.
- `wc -l .dialyzer_ignore.exs` < 54.
- `rtk mix dialyzer` exits 0.
- Bucket headers present: Ecto schema t/0, Raxol element() return, Phase 25 Account forms, every kept entry has a stated reason.
- `rtk mix precommit` exits 0.
- `rtk mix test` exits 0 with ≥ baseline test count.
- `grep -c "## Transaction strategy" lib/foglet_bbs/boards/server.ex` still equals 1 (Plan 02 work intact).
</verification>

<success_criteria>
- QUAL-01 acceptance criteria from SPEC §Requirement 3 all met:
  - boards/server.ex :call_without_opaque entry removed.
  - Every remaining ignore entry has a comment within 5 lines above it (or shared block).
  - Total ignore-file line count strictly smaller than pre-phase (54).
- v2.0 test baseline preserved.
- Behavior parity for boards/server.ex success-side and error-side replies.
</success_criteria>

<output>
After completion, create `.planning/phases/46-domain-cleanup-and-final-quality-gate/46-03-SUMMARY.md` per template, recording:
- Pre/post line counts and entry counts of `.dialyzer_ignore.exs`.
- Per-module Bucket-C1 narrowing outcome table (narrowed-removed vs. resisted-reclassified-as-C2/C3).
- The chosen Bucket B fix approach (signature tightening / @spec narrow / helper extraction) and why.
- The final file header text (D-07 invariant).
- `rtk mix precommit` and `rtk mix test` outputs (test summary line).
- Pointer note: "Closes CONCERNS.md Tech Debt heading at line 103 — disposition will be applied in plan 04 (QUAL-03)."
</output>
