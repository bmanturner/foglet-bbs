---
phase: 46
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/foglet_bbs/boards/supervisor.ex
autonomous: true
requirements:
  - DOM-01

must_haves:
  truths:
    - "grep -n 'def boot_board_servers' lib/foglet_bbs/boards/supervisor.ex returns no matches"
    - "Application supervision tree boots successfully (rtk mix test green)"
    - "FogletBbs.Application.start/2 still calls Foglet.Boards.boot_board_servers/0 (the real impl in lib/foglet_bbs/boards.ex)"
  artifacts:
    - path: "lib/foglet_bbs/boards/supervisor.ex"
      provides: "DynamicSupervisor for board servers (no stub boot_board_servers/0)"
      contains: "use DynamicSupervisor"
      must_not_contain: "def boot_board_servers"
  key_links:
    - from: "lib/foglet_bbs/application.ex"
      to: "Foglet.Boards.boot_board_servers/0 (lib/foglet_bbs/boards.ex)"
      via: "direct call from start/2"
      pattern: "Foglet.Boards.boot_board_servers"
---

<objective>
Delete the misleading no-op `Foglet.Boards.Supervisor.boot_board_servers/0` stub at `lib/foglet_bbs/boards/supervisor.ex:35-46`. The real boot path lives in `Foglet.Boards.boot_board_servers/0` (in `lib/foglet_bbs/boards.ex`), and is what `FogletBbs.Application.start/2` actually calls.

Purpose: Remove dead, misleading code so future maintainers don't follow a phantom "Plan 03 implements the real query" trail. Closes CONCERNS.md Tech Debt entry at line 89.

Output: `lib/foglet_bbs/boards/supervisor.ex` with the `@doc` block + stub function deleted (lines 35-46), nothing else changed; `rtk mix precommit && rtk mix test` green.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-SPEC.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-RESEARCH.md
@.planning/phases/46-domain-cleanup-and-final-quality-gate/46-PATTERNS.md
@AGENTS.md

<interfaces>
Pre-deletion structure of lib/foglet_bbs/boards/supervisor.ex:
  - lines 1-33: module decl, @moduledoc, `use DynamicSupervisor`, `start_link/1`, `init/1`, `start_board/1` — KEEP UNCHANGED
  - lines 35-40: `@doc """ … """` — DELETE
  - lines 41-46: `def boot_board_servers do … :ok end` — DELETE
  - line 47 (`end`): KEEP

Real boot caller (do not modify):
  lib/foglet_bbs/application.ex line 32 — calls `Foglet.Boards.boot_board_servers()` (resolves to lib/foglet_bbs/boards.ex:39-46), NOT the stub.

Pitfall (Pitfall 4 in RESEARCH.md): `@doc` and the function MUST be deleted as a single unit. A dangling `@doc` not followed by a definition is a compile error.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the boot_board_servers/0 stub and its @doc block</name>
  <read_first>
    - lib/foglet_bbs/boards/supervisor.ex (full file, ~47 lines)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-RESEARCH.md (search for "DOM-01: deletion target" — exact text to delete)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-PATTERNS.md (search for "DOM-01 deletion" — post-deletion shape and pitfall warning)
    - lib/foglet_bbs/application.ex (lines 28-40 — confirm `Foglet.Boards.boot_board_servers()` at line 32, not via the stub)
    - lib/foglet_bbs/boards.ex (lines 39-46 — confirm real impl present and unchanged)
  </read_first>
  <action>
    Open `lib/foglet_bbs/boards/supervisor.ex`. Delete lines 35-46 inclusive (per D-01 target — `@doc` block AND the `def boot_board_servers do … :ok end` body, as a single unit per Pitfall 4).

    Exact lines to remove (verbatim from research):
    ```
      @doc """
      Boot all non-archived boards at application startup.
      Called from FogletBbs.Application.start/2 after the supervision tree is up.
      Full implementation is in Foglet.Boards context (Plan 03).
      This stub is replaced when Plan 03 creates Foglet.Boards.
      """
      def boot_board_servers do
        # Stub: no boards exist yet. Plan 03 implements the real query.
        # When Foglet.Boards context is implemented, Application.start/2
        # calls Foglet.Boards.boot_board_servers/0 (not this stub).
        :ok
      end
    ```

    Also remove the now-redundant blank line above the deleted block (between `start_board/1` and the deleted `@doc`) only if leaving it produces a double blank line at end-of-module. Otherwise preserve. The closing `end` of the module (line 47 pre-deletion) stays.

    Do NOT modify anything else in the file: `defmodule`, `@moduledoc`, `use DynamicSupervisor`, `start_link/1`, `init/1`, `start_board/1` — all unchanged.

    Do NOT modify `lib/foglet_bbs/boards.ex` (real `boot_board_servers/0` remains the single boot source of truth) or `lib/foglet_bbs/application.ex` (caller line 32 stays).
  </action>
  <verify>
    <automated>! grep -n "def boot_board_servers" lib/foglet_bbs/boards/supervisor.ex &amp;&amp; grep -c "Foglet.Boards.boot_board_servers" lib/foglet_bbs/application.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def boot_board_servers" lib/foglet_bbs/boards/supervisor.ex` returns no matches (exit non-zero).
    - `grep -n "def boot_board_servers" lib/foglet_bbs/boards.ex` still returns line ~39 (real impl untouched).
    - `grep -n "Foglet.Boards.boot_board_servers" lib/foglet_bbs/application.ex` still returns line ~32 (caller untouched).
    - `! grep -rn "Foglet.Boards.Supervisor.boot_board_servers" lib/ test/` returns no matches (no caller broken).
    - `rtk mix compile --warnings-as-errors` exits 0 (proves no dangling `@doc`, no caller depending on the stub).
  </acceptance_criteria>
  <done>
    Stub `@doc` and `boot_board_servers/0` removed from `lib/foglet_bbs/boards/supervisor.ex` (12 fewer lines); module compiles clean; no caller broken anywhere in `lib/` or `test/`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run precommit + test floor (D-14 cadence gate)</name>
  <read_first>
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-CONTEXT.md (D-14 — cadence rules and v2.0 baseline floor)
    - .planning/phases/46-domain-cleanup-and-final-quality-gate/46-VALIDATION.md (per-task verification map row 46-01-*)
    - mix.exs (lines 91-98 — precommit alias contents)
  </read_first>
  <action>
    Per D-14, run the two-command cadence gate after the Task 1 commit:
    1. `rtk mix precommit` (runs `compile --warnings-as-errors`, `deps.unlock --unused`, `format`, `credo --strict`, `sobelow --exit Low`, `dialyzer`).
    2. `rtk mix test` (runs the `test:` alias which migrates+seeds first, then full suite).

    Both MUST exit 0. The test summary line MUST report at least `1 property, 2161 tests, 0 failures` (count may grow but never shrink — D-14).

    If precommit fails (e.g., new dialyzer warning surfaced because the stub was suppressing one), STOP and surface the error. Do not paper over it; D-14 says "any new dialyzer warning, Credo violation, Sobelow finding, or test regression is a hard stop."

    If `rtk mix test` shows fewer than 2161 tests or any failures, STOP. The v2.0 baseline is the floor.

    Record observed test summary line in the plan SUMMARY (e.g., "1 property, 2174 tests, 0 failures").
  </action>
  <verify>
    <automated>rtk mix precommit &amp;&amp; rtk mix test</automated>
  </verify>
  <acceptance_criteria>
    - `rtk mix precommit` exits 0 (Credo, Sobelow, Dialyzer, format, compile-warnings-as-errors all clean).
    - `rtk mix test` exits 0.
    - Test summary line shows ≥ 1 property and ≥ 2161 tests with 0 failures (the v2.0 baseline floor per D-14).
    - SUMMARY.md records the observed test count delta vs. baseline.
  </acceptance_criteria>
  <done>
    Phase-46 plan-01 cadence gate green; ready to hand off to plan 02 (DOM-02).
  </done>
</task>

</tasks>

<verification>
- `! grep -n "def boot_board_servers" lib/foglet_bbs/boards/supervisor.ex` (no matches).
- `grep -n "def boot_board_servers" lib/foglet_bbs/boards.ex` (real impl still present).
- `! grep -rn "Foglet.Boards.Supervisor.boot_board_servers" lib/ test/` (no caller).
- `rtk mix precommit` exits 0.
- `rtk mix test` exits 0 with ≥ baseline test count.
</verification>

<success_criteria>
- DOM-01 acceptance criteria from SPEC §Requirement 1 all met.
- File line count of `lib/foglet_bbs/boards/supervisor.ex` is 12 lines smaller than pre-phase.
- v2.0 test baseline preserved.
- No new dialyzer warnings introduced (the deleted function had no `@spec`, so Bucket B/C of QUAL-01 is unaffected).
</success_criteria>

<output>
After completion, create `.planning/phases/46-domain-cleanup-and-final-quality-gate/46-01-SUMMARY.md` per template, recording:
- Lines deleted (exact pre/post line counts of supervisor.ex).
- Confirmed callers verified absent across `lib/` and `test/`.
- `rtk mix precommit` and `rtk mix test` outputs (test summary line).
- Pointer note: "Closes CONCERNS.md Tech Debt heading at line 89 — disposition will be applied in plan 04 (QUAL-03)."
</output>
