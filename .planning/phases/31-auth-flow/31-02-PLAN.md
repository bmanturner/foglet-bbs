---
phase: 31-auth-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - lib/foglet_bbs/tui/screens/login.ex
  - lib/foglet_bbs/tui/screens/login/state.ex
  - test/foglet_bbs/tui/screens/login_test.exs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "Login menu exposes Forgot Password in both email and no-email modes."
    - "Forgot Password validates email shape locally and malformed input never dispatches reset delivery."
    - "No-email reset confirmation is honest operator-assisted copy, not unavailable/error copy, and includes a token-consume entry path."
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/login.ex"
      provides: "Always-visible Forgot Password, email-only reset request, wrapped confirmation/no-email copy, and reset-consume entry command"
      contains: "Forgot password"
    - path: "lib/foglet_bbs/tui/screens/login/state.ex"
      provides: "Reset request state with email input and validation/message category"
      contains: "reset_request"
    - path: "test/foglet_bbs/tui/screens/login_test.exs"
      provides: "Login reset request key-routing and enumeration-safe outward-state tests"
      contains: "no_email"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/login.ex"
      to: "Foglet.Accounts.Verification.request_password_reset_delivery/1"
      via: "valid email-shaped reset request submission only"
      pattern: "request_password_reset_delivery"
    - from: "lib/foglet_bbs/tui/screens/login.ex"
      to: "Foglet.TUI.TextWidth.wrap/2"
      via: "reset confirmation/no-email copy rendering"
      pattern: "TextWidth\\.wrap"
---

<objective>
Wire the Forgot Password reset-request surface in Login.

Purpose: Deliver D-01, D-02, D-03, D-12, D-14, and D-15 for the reset request path while keeping no-email reset as a supported operator-assisted feature.
Output: Login reset request UI with local email validation, generic outward success, wrapped confirmation copy, and no-email token-entry discovery.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-auth-flow/31-SPEC.md
@.planning/phases/31-auth-flow/31-CONTEXT.md
@.planning/phases/31-auth-flow/31-RESEARCH.md
@.planning/phases/31-auth-flow/31-VALIDATION.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/screens/login.ex
@lib/foglet_bbs/tui/screens/login/state.ex
@lib/foglet_bbs/tui/text_width.ex
@test/foglet_bbs/tui/screens/login_test.exs

<interfaces>
Login currently routes by `LoginState.sub(state)` and uses `TextInput.render/2`.

```elixir
LoginState.reset_request()
LoginState.input_key(:identifier)
Verification.request_password_reset_delivery(email)
Verification.active_sysop_contact_emails()
Foglet.TUI.TextWidth.wrap(text, width)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Login tests for always-visible Forgot Password and local email validation</name>
  <files>test/foglet_bbs/tui/screens/login_test.exs</files>
  <read_first>
    .planning/phases/31-auth-flow/31-SPEC.md
    .planning/phases/31-auth-flow/31-CONTEXT.md
    .planning/phases/31-auth-flow/31-RESEARCH.md
    lib/foglet_bbs/tui/screens/login.ex
    lib/foglet_bbs/tui/screens/login/state.ex
    test/foglet_bbs/tui/screens/login_test.exs
  </read_first>
  <behavior>
    - In `"email"` and `"no_email"` delivery modes, rendered Login menu text contains `"Forgot password"`.
    - Pressing `F` in either mode sets `screen_state[:login].sub == :reset_request`.
    - Reset request field label text is `"Email:"` or equivalent and does not contain `"Handle or email"`.
    - Invalid inputs `""`, `"   "`, `"alice"`, `"alice@"`, `"alice@example"`, and `"a b@example.test"` leave `sub == :reset_request`, set an inline validation error, and create zero `reset_password` token rows.
    - Valid active and unknown email-shaped submissions produce the same `message_category` or equivalent state category.
  </behavior>
  <action>
    Rewrite the existing Login tests that currently expect Forgot Password only in email mode. Add cases per D-01, D-02, D-03, and D-17. Use token-row assertions against `Foglet.Accounts.UserToken` for invalid local email inputs so the test proves no Accounts reset delivery happened. Preserve existing forbidden browser-copy assertions for `/users/reset_password`, `http://`, and `https://`.
  </action>
  <acceptance_criteria>
    - `rg -n "Forgot password.*no_email|Handle or email|Email:|message_category|reset_password" test/foglet_bbs/tui/screens/login_test.exs` shows the new expectations.
    - `rg -n "alice@|alice@example|a b@example" test/foglet_bbs/tui/screens/login_test.exs` shows invalid-shape coverage.
    - `rtk mix test test/foglet_bbs/tui/screens/login_test.exs` fails before production changes because no-email `F` and local validation are not implemented yet.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/login_test.exs</automated>
  </verify>
  <done>Login tests lock the reset-request surface and enumeration-safe outward state before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reset request UI, wrapped copy, and no-email operator guidance</name>
  <files>lib/foglet_bbs/tui/screens/login.ex, lib/foglet_bbs/tui/screens/login/state.ex, test/foglet_bbs/tui/screens/login_test.exs</files>
  <read_first>
    .planning/phases/31-auth-flow/31-SPEC.md
    .planning/phases/31-auth-flow/31-CONTEXT.md
    .planning/phases/31-auth-flow/31-RESEARCH.md
    lib/foglet_bbs/tui/screens/login.ex
    lib/foglet_bbs/tui/screens/login/state.ex
    lib/foglet_bbs/tui/text_width.ex
    test/foglet_bbs/tui/screens/login_test.exs
  </read_first>
  <behavior>
    - `menu_keys/1` always inserts `{"F", "Forgot password"}` before Quit, independent of `Config.delivery_mode/0`.
    - `maybe_enter_reset_request/1` always enters `LoginState.reset_request()`.
    - Reset request renders an email-only field label and inline validation error beneath the field.
    - Valid email submissions in email mode call `Verification.request_password_reset_delivery(email)` and set generic success state/copy.
    - Valid email submissions in no-email mode set supported operator-assisted copy that says email delivery is disabled, says to contact a sysop/operator over SSH for a reset token, includes comma-separated `Verification.active_sysop_contact_emails/0` when present, uses fallback copy when absent, and advertises token entry.
    - Long reset/no-email messages are split into rows with `TextWidth.wrap/2` using a width derived from terminal content width.
  </behavior>
  <action>
    Modify `lib/foglet_bbs/tui/screens/login.ex` per D-01, D-02, D-03, D-12, D-14, and D-15. Add `alias Foglet.TUI.TextWidth`. Replace `@reset_unavailable_message` behavior with no-email operator-assisted copy; do not use the word `"unavailable"` as the user-facing no-email result. Add a simple local email validation helper matching `~r/^[^@\s]+@[^@\s]+\.[^@\s]+$/` or equivalent. Store reset request state fields such as `error`, `message`, and `message_category` in `Login.State.reset_request/0`. Render wrapped message rows by mapping every `TextWidth.wrap(message, width)` line to a separate `text/2` node. Keep the raw reset token out of this plan; only expose an entry hint such as `[T] Enter reset token`.
  </action>
  <acceptance_criteria>
    - `rg -n "TextWidth\\.wrap|active_sysop_contact_emails|message_category|Email:" lib/foglet_bbs/tui/screens/login.ex lib/foglet_bbs/tui/screens/login/state.ex` shows the implementation.
    - `! rg -n "Handle or email|Password reset by email is unavailable" lib/foglet_bbs/tui/screens/login.ex test/foglet_bbs/tui/screens/login_test.exs` succeeds.
    - `rg -n "\\{\"F\", \"Forgot password\"\\}|Enter reset token|operator|sysop" lib/foglet_bbs/tui/screens/login.ex test/foglet_bbs/tui/screens/login_test.exs` shows always-visible menu and no-email guidance.
    - `rtk mix test test/foglet_bbs/accounts/verification_test.exs test/foglet_bbs/tui/screens/login_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/accounts/verification_test.exs test/foglet_bbs/tui/screens/login_test.exs</automated>
  </verify>
  <done>Forgot Password is reachable in both delivery modes, validates email locally, and presents wrapped enumeration-safe/no-email reset request outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH user to Login reset request | Untrusted email input enters screen-local validation. |
| Login to Accounts | Only locally valid email-shaped input reaches Verification reset delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Information Disclosure | `submit_reset_request/1` | mitigate | Preserve generic outward state for valid email-shaped submissions and test active/unknown equality. |
| T-31-05 | Denial of Service | `render_reset_request/2` | mitigate | Wrap long copy with `TextWidth.wrap/2` and render one text row per wrapped line at compact widths. |
| T-31-06 | Spoofing | no-email operator guidance | accept | Copy names operator-assisted reset as a manual sysop path; no privilege is granted until Plan 03 consumes a valid token through Accounts. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/accounts/verification_test.exs test/foglet_bbs/tui/screens/login_test.exs`.
</verification>

<success_criteria>
AUTH-01, AUTH-02, and AUTH-03 reset-request behavior is implemented with no browser reset copy and no direct Repo calls from Login.
</success_criteria>

<output>
After completion, create `.planning/phases/31-auth-flow/31-02-SUMMARY.md`.
</output>
