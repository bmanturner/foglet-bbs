---
phase: 31-auth-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/foglet_bbs/accounts/verification.ex
  - lib/foglet_bbs/accounts/user_token.ex
  - test/foglet_bbs/accounts/verification_test.exs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "Active-user valid email reset requests create one reset_password token while valid unknown, inactive, and deleted emails create none."
    - "Accounts exposes active non-deleted sysop contact emails for no-email operator-assisted reset copy."
    - "A raw reset token can be consumed exactly once to update a password and remove outstanding reset_password tokens."
  artifacts:
    - path: "lib/foglet_bbs/accounts/verification.ex"
      provides: "Verification.request_password_reset_delivery/1 email-only side effects, sysop contact helper, and consume_reset_token/2 boundary"
      contains: "consume_reset_token"
    - path: "lib/foglet_bbs/accounts/user_token.ex"
      provides: "Raw reset-token verification support reusable inside an atomic transaction"
      contains: "reset_password"
    - path: "test/foglet_bbs/accounts/verification_test.exs"
      provides: "Domain tests for token side effects, sysop contacts, raw-token failure modes, and concurrent single-use consume"
      contains: "consume_reset_token"
  key_links:
    - from: "lib/foglet_bbs/accounts/verification.ex"
      to: "Foglet.Accounts.UserToken.verify_email_token_query/2"
      via: "raw reset token verification"
      pattern: "verify_email_token_query.*reset_password"
    - from: "lib/foglet_bbs/accounts/verification.ex"
      to: "FogletBbs.Repo.transact/1"
      via: "atomic password update and reset-token deletion"
      pattern: "Repo\\.transact"
---

<objective>
Create the Accounts/Verification foundation for Phase 31 auth reset behavior.

Purpose: Keep durable reset-token side effects, sysop contact selection, and raw-token consumption behind the Accounts boundary per D-08, D-09, D-13, and AGENTS.md.
Output: Tested domain functions that the Login screen can call without Repo access.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-auth-flow/31-SPEC.md
@.planning/phases/31-auth-flow/31-CONTEXT.md
@.planning/phases/31-auth-flow/31-RESEARCH.md
@.planning/phases/31-auth-flow/31-VALIDATION.md
@lib/foglet_bbs/accounts/verification.ex
@lib/foglet_bbs/accounts/user_token.ex
@lib/foglet_bbs/accounts/user.ex
@test/foglet_bbs/accounts/verification_test.exs

<interfaces>
Existing functions to preserve:

```elixir
Foglet.Accounts.Verification.request_password_reset_delivery(identifier)
Foglet.Accounts.Verification.reset_user_password(user, attrs)
Foglet.Accounts.Verification.generate_reset_token_for_operator(user)
Foglet.Accounts.UserToken.verify_email_token_query(raw_token, "reset_password")
Foglet.Accounts.User.password_changeset(user, attrs)
FogletBbs.Repo.transact(fn -> ... end)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add domain tests for email-only reset side effects and sysop contacts</name>
  <files>test/foglet_bbs/accounts/verification_test.exs</files>
  <read_first>
    .planning/phases/31-auth-flow/31-SPEC.md
    .planning/phases/31-auth-flow/31-CONTEXT.md
    .planning/phases/31-auth-flow/31-RESEARCH.md
    lib/foglet_bbs/accounts/verification.ex
    lib/foglet_bbs/accounts/user.ex
    lib/foglet_bbs/accounts.ex
    test/foglet_bbs/accounts/verification_test.exs
  </read_first>
  <behavior>
    - Valid active-user email in email delivery mode returns `{:ok, :generic_response}` and creates exactly one `reset_password` row.
    - Valid unknown, deleted, pending, suspended, and rejected emails return `{:ok, :generic_response}` and create zero `reset_password` rows.
    - Handle-shaped identifiers such as `"alice"` no longer create reset tokens through the reset request path.
    - `Verification.active_sysop_contact_emails/0` returns only active, non-deleted users with `role: :sysop`, sorted deterministically, and excludes deleted, pending, suspended, rejected, non-sysop, and nil-email users.
  </behavior>
  <action>
    Add ExUnit coverage in `test/foglet_bbs/accounts/verification_test.exs` per D-03, D-13, D-14, and D-16. Keep the existing `"email mode returns a generic response and delivers for an active email match"` coverage, but change or replace active-handle expectations so handle-only reset requests no longer create a token. Add a test named with `active_sysop_contact_emails/0` that creates active sysops, inactive sysops, deleted sysops, and active non-sysops, then asserts the exact returned email list contains only active non-deleted sysop emails.
  </action>
  <acceptance_criteria>
    - `rg -n "active_sysop_contact_emails|handle-only|reset_password" test/foglet_bbs/accounts/verification_test.exs` prints matching test code.
    - `rg -n "deleted|pending|suspended|rejected" test/foglet_bbs/accounts/verification_test.exs` shows valid-email generic-response cases.
    - `rtk mix test test/foglet_bbs/accounts/verification_test.exs` fails before production changes because `active_sysop_contact_emails/0` and email-only behavior are not implemented yet.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/accounts/verification_test.exs</automated>
  </verify>
  <done>Tests express D-03, D-13, D-14, and D-16 with durable token-row assertions rather than copy-only checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Verification helpers and atomic raw-token consume</name>
  <files>lib/foglet_bbs/accounts/verification.ex, lib/foglet_bbs/accounts/user_token.ex, test/foglet_bbs/accounts/verification_test.exs</files>
  <read_first>
    .planning/phases/31-auth-flow/31-SPEC.md
    .planning/phases/31-auth-flow/31-CONTEXT.md
    .planning/phases/31-auth-flow/31-RESEARCH.md
    lib/foglet_bbs/accounts/verification.ex
    lib/foglet_bbs/accounts/user_token.ex
    lib/foglet_bbs/accounts/user.ex
    test/foglet_bbs/accounts/verification_test.exs
  </read_first>
  <behavior>
    - `Verification.consume_reset_token(raw_token, %{password: password})` returns `{:ok, %User{}}` for one valid unexpired reset token.
    - Invalid, malformed, expired, and already-used tokens return `{:error, :invalid_or_expired}` without changing a password.
    - Concurrent consumption of the same raw token yields exactly one success and one generic failure.
    - Successful consumption uses `User.password_changeset/2` and removes outstanding `reset_password` tokens for the user.
  </behavior>
  <action>
    Implement `Verification.consume_reset_token/2` per D-08, D-09, D-10, D-11, and D-16. Use `Repo.transact/1`; decode/hash the raw token using `UserToken.verify_email_token_query(raw_token, "reset_password")` and add any narrow internal UserToken helper needed to lock or claim the matching `reset_password` row inside the same transaction. Use `User.password_changeset(user, attrs)` for password validation and hashing. Delete `UserToken.by_user_and_contexts_query(user, ["reset_password"])` after the password update succeeds. Return only `{:ok, user}` or `{:error, :invalid_or_expired}` / `{:error, %Ecto.Changeset{}}` at this boundary; do not return token-row details. Implement `Verification.active_sysop_contact_emails/0` as an Accounts/Verification helper that queries `User` rows where `role == :sysop`, `status == :active`, `deleted_at` is nil, and `email` is not nil, ordering by email or inserted_at deterministically. Narrow `request_password_reset_delivery/1` lookup to active non-deleted email matches for valid email strings; do not create tokens for handles.
  </action>
  <acceptance_criteria>
    - `rg -n "def consume_reset_token|Repo\\.transact|password_changeset|by_user_and_contexts_query" lib/foglet_bbs/accounts/verification.ex` shows the atomic boundary.
    - `rg -n "def active_sysop_contact_emails|role: :sysop|status: :active|deleted_at" lib/foglet_bbs/accounts/verification.ex test/foglet_bbs/accounts/verification_test.exs` shows the helper and tests.
    - `rg -n "verify_email_token_query\\(.*reset_password" lib/foglet_bbs/accounts/verification.ex lib/foglet_bbs/accounts/user_token.ex` shows reuse of the reset-token primitive.
    - `rtk mix test test/foglet_bbs/accounts/verification_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/accounts/verification_test.exs</automated>
  </verify>
  <done>Accounts/Verification owns email reset side effects, sysop contact selection, and atomic single-use raw-token consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH user to Accounts | Untrusted reset identifiers, raw tokens, and new passwords enter durable auth functions. |
| Accounts to database | Password hashes and hashed reset tokens are changed in Postgres. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | `Verification.consume_reset_token/2` | mitigate | Verify raw token, update password, and delete reset tokens inside `Repo.transact/1`; concurrent test must prove exactly one success. |
| T-31-02 | Information Disclosure | `Verification.request_password_reset_delivery/1` | mitigate | Return `{:ok, :generic_response}` for all valid email-shaped submissions in email mode and assert token side effects separately. |
| T-31-03 | Information Disclosure | `Verification.active_sysop_contact_emails/0` | mitigate | Return only active, non-deleted sysop emails; exclude inactive/deleted/non-sysop rows by query and test. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/accounts/verification_test.exs`.
</verification>

<success_criteria>
AUTH-01, AUTH-03, and AUTH-04 domain foundations pass focused tests. No browser reset route, Phoenix controller, or TUI Repo query is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/31-auth-flow/31-01-SUMMARY.md`.
</output>
