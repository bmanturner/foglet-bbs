---
phase: 31-auth-flow
plan: 03
type: execute
wave: 3
depends_on:
  - 31-01
  - 31-02
files_modified:
  - lib/foglet_bbs/tui/screens/login.ex
  - lib/foglet_bbs/tui/screens/login/state.ex
  - test/foglet_bbs/tui/screens/login_test.exs
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "The token-consume entry is reachable from the Login menu and the Forgot Password flow."
    - "The reset-consume form routes input deterministically among token, password, and confirmation fields."
    - "Successful raw-token consume returns to the logged-out Login menu and clears token/password fields."
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/login/state.ex"
      provides: "Inline reset_consume state with token, password, confirmation fields, masked password inputs, and focus helpers"
      contains: "reset_consume"
    - path: "lib/foglet_bbs/tui/screens/login.ex"
      provides: "Reset-consume rendering, key handling, validation, and Verification.consume_reset_token/2 call"
      contains: "consume_reset_token"
    - path: "test/foglet_bbs/tui/screens/login_test.exs"
      provides: "Entry path, focus, mismatch, escape-clearing, and success/failure tests"
      contains: "reset_consume"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/login.ex"
      to: "Foglet.Accounts.Verification.consume_reset_token/2"
      via: "submit reset consume form"
      pattern: "consume_reset_token"
    - from: "lib/foglet_bbs/tui/screens/login.ex"
      to: "Foglet.TUI.Widgets.Input.TextInput"
      via: "three inline fields"
      pattern: "TextInput\\.render"
---

<objective>
Build the inline Login `:reset_consume` sub-state.

Purpose: Let logged-out users consume an operator-provided raw reset token through the Accounts boundary without introducing Modal.Form or browser workflows.
Output: Token/new-password/confirmation form with deterministic focus, generic errors, and successful return to the Login menu.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-auth-flow/31-SPEC.md
@.planning/phases/31-auth-flow/31-CONTEXT.md
@.planning/phases/31-auth-flow/31-RESEARCH.md
@.planning/phases/31-auth-flow/31-VALIDATION.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/screens/login.ex
@lib/foglet_bbs/tui/screens/login/state.ex
@lib/foglet_bbs/tui/widgets/input/text_input.ex
@test/foglet_bbs/tui/screens/login_test.exs

<interfaces>
Use Login's existing inline focus pattern; do not introduce `Modal.Form`.

```elixir
LoginState.input_key(:token)
LoginState.input_key(:password)
LoginState.input_key(:password_confirmation)
TextInput.init(mask_char: "*")
Verification.consume_reset_token(raw_token, %{password: password})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reset-consume Login state and routing tests</name>
  <files>test/foglet_bbs/tui/screens/login_test.exs</files>
  <read_first>
    .planning/phases/31-auth-flow/31-SPEC.md
    .planning/phases/31-auth-flow/31-CONTEXT.md
    .planning/phases/31-auth-flow/31-RESEARCH.md
    lib/foglet_bbs/tui/screens/login.ex
    lib/foglet_bbs/tui/screens/login/state.ex
    test/foglet_bbs/tui/screens/login_test.exs
  </read_first>
  <behavior>
    - Pressing `T` or another concrete token-entry key from the Login menu enters `screen_state[:login].sub == :reset_consume`.
    - Pressing the same token-entry key from reset request confirmation enters `:reset_consume`.
    - Tab advances focus `:token -> :password -> :password_confirmation -> :token`; `:backtab` or Shift+Tab reverses the cycle.
    - Password and confirmation fields are initialized with `mask_char: "*"`; token field is not masked.
    - Mismatched password confirmation sets a generic inline error and does not call `Verification.consume_reset_token/2`.
    - Escape from `:reset_consume` returns to `%{sub: :menu}` and removes token/password input structs.
  </behavior>
  <action>
    Add tests per D-04, D-05, D-06, D-07, D-15, and D-17. Use concrete field atoms `:token`, `:password`, and `:password_confirmation`. Use `Access.key(:raxol_state)` to assert the exact TextInput values after key events. Include a test that types into each field after focus movement and proves characters land only in the focused input.
  </action>
  <acceptance_criteria>
    - `rg -n "reset_consume|password_confirmation|mask_char|:backtab|Enter reset token" test/foglet_bbs/tui/screens/login_test.exs` shows coverage.
    - `rg -n "mismatch|does not consume|escape.*clears" test/foglet_bbs/tui/screens/login_test.exs` shows blocked-submit and clearing tests.
    - `rtk mix test test/foglet_bbs/tui/screens/login_test.exs` fails before production changes because `:reset_consume` UI is not implemented yet.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/login_test.exs</automated>
  </verify>
  <done>Tests lock both reset-consume entry paths and form key routing before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reset-consume form and Accounts consume submission</name>
  <files>lib/foglet_bbs/tui/screens/login.ex, lib/foglet_bbs/tui/screens/login/state.ex, test/foglet_bbs/tui/screens/login_test.exs</files>
  <read_first>
    .planning/phases/31-auth-flow/31-SPEC.md
    .planning/phases/31-auth-flow/31-CONTEXT.md
    .planning/phases/31-auth-flow/31-RESEARCH.md
    lib/foglet_bbs/tui/screens/login.ex
    lib/foglet_bbs/tui/screens/login/state.ex
    lib/foglet_bbs/tui/widgets/input/text_input.ex
    test/foglet_bbs/tui/screens/login_test.exs
  </read_first>
  <behavior>
    - `LoginState.reset_consume/0` returns `%{sub: :reset_consume, focused_field: :token, token_input: %TextInput{}, password_input: masked, password_confirmation_input: masked, error: nil}`.
    - `LoginState.input_key/1` maps `:token`, `:password`, and `:password_confirmation` to the concrete input keys.
    - `Login.render/1` renders `:reset_consume` with three fields and no raw token in static text.
    - `Login.handle_key/2` routes `:tab`, `:backtab`, `:enter`, `:escape`, and character events for `:reset_consume`.
    - Matching password submission calls `Verification.consume_reset_token(token, %{password: password})`; success returns to Login menu; invalid token or password changeset failure stays on the form with generic error copy.
  </behavior>
  <action>
    Implement `:reset_consume` per D-04 through D-11 and D-15. Add a direct Login menu key `{"T", "Enter reset token"}` or equivalent concrete key and accept `T/t` in `handle_menu_key/2`. From reset request confirmation, accept the same `T/t` key. Add `render_reset_consume/2`, `handle_reset_consume_key/2`, `submit_reset_consume/1`, and any small focus helpers in `Login.State` needed for next/previous focus. Password mismatch must return an inline error such as `"Passwords do not match."` without calling Accounts. Generic consume failure copy must not reveal whether the token is invalid, expired, malformed, or already used. Do not place the raw token value in breadcrumb, status, command hints, modal text, log messages, or success/error copy.
  </action>
  <acceptance_criteria>
    - `rg -n "def reset_consume|:password_confirmation|mask_char: \"\\*\"|def input_key\\(:token\\)" lib/foglet_bbs/tui/screens/login/state.ex` shows state support.
    - `rg -n "render_reset_consume|handle_reset_consume_key|submit_reset_consume|consume_reset_token" lib/foglet_bbs/tui/screens/login.ex` shows UI wiring.
    - `rg -n "\\{\"T\", \"Enter reset token\"\\}|Passwords do not match|invalid or expired|reset_consume" lib/foglet_bbs/tui/screens/login.ex test/foglet_bbs/tui/screens/login_test.exs` shows entry and generic errors.
    - `rtk mix test test/foglet_bbs/accounts/verification_test.exs test/foglet_bbs/tui/screens/login_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/accounts/verification_test.exs test/foglet_bbs/tui/screens/login_test.exs</automated>
  </verify>
  <done>Logged-out token consumption is reachable from both required paths and uses the Accounts boundary for atomic consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH user to Login reset consume | Untrusted raw token and password fields enter screen-local state. |
| Login to Accounts | Raw token and password attrs cross into `Verification.consume_reset_token/2`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Information Disclosure | reset-consume errors | mitigate | Collapse invalid, malformed, expired, and already-used token outcomes to generic copy. |
| T-31-08 | Information Disclosure | chrome/status/command hints | mitigate | Render labels from sub-state only and add Plan 04 non-leak tests for raw token absence. |
| T-31-09 | Tampering | reset-consume submit | mitigate | Delegate password update and token deletion only to `Verification.consume_reset_token/2`; no Repo calls in Login. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/accounts/verification_test.exs test/foglet_bbs/tui/screens/login_test.exs`.
</verification>

<success_criteria>
AUTH-03 and AUTH-04 are wired into Login without Modal.Form, browser reset paths, or token-value leakage in static UI text.
</success_criteria>

<output>
After completion, create `.planning/phases/31-auth-flow/31-03-SUMMARY.md`.
</output>
