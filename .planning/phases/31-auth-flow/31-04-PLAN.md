---
phase: 31-auth-flow
plan: 04
type: execute
wave: 4
depends_on:
  - 31-02
  - 31-03
files_modified:
  - test/foglet_bbs/tui/layout_smoke_test.exs
  - test/foglet_bbs/tui/screens/login_test.exs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "Reset confirmation/no-email copy renders as multiple accessible rows at 64x22."
    - "The reset-consume breadcrumb/status/command surfaces never include raw token text."
    - "The full Phase 31 narrow suite passes across Accounts, Login, and layout smoke tests."
  artifacts:
    - path: "test/foglet_bbs/tui/layout_smoke_test.exs"
      provides: "Compact render and raw-token non-leak smoke tests"
      contains: "reset_consume"
    - path: "test/foglet_bbs/tui/screens/login_test.exs"
      provides: "Final integrated Login flow assertions if needed after smoke coverage"
      contains: "Forgot password"
  key_links:
    - from: "test/foglet_bbs/tui/layout_smoke_test.exs"
      to: "Foglet.TUI.Screens.Login.render/1"
      via: "64x22 render tree through Raxol layout engine"
      pattern: "Login\\.render"
    - from: "test/foglet_bbs/tui/layout_smoke_test.exs"
      to: "Foglet.TUI.Widgets.Chrome.BreadcrumbBar"
      via: "screen frame chrome generated during render"
      pattern: "reset_consume"
---

<objective>
Add compact rendering and non-leak verification for the completed auth flow.

Purpose: Prove the Phase 31 SSH/TUI behavior survives the 64x22 minimum and that raw reset tokens remain confined to direct input fields.
Output: Layout smoke tests plus final narrow/full verification commands.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-auth-flow/31-SPEC.md
@.planning/phases/31-auth-flow/31-CONTEXT.md
@.planning/phases/31-auth-flow/31-RESEARCH.md
@.planning/phases/31-auth-flow/31-VALIDATION.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/screens/login.ex
@lib/foglet_bbs/tui/screens/login/state.ex
@lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex
@test/foglet_bbs/tui/layout_smoke_test.exs
@test/foglet_bbs/tui/screens/login_test.exs

<interfaces>
Layout smoke helpers already available:

```elixir
Engine.apply_layout(tree, %{width: 64, height: 22})
text_elements(positioned)
text_rows(elements)
Login.render(state)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 64x22 reset copy wrap and no-email contact smoke tests</name>
  <files>test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    .planning/phases/31-auth-flow/31-SPEC.md
    .planning/phases/31-auth-flow/31-CONTEXT.md
    .planning/phases/31-auth-flow/31-RESEARCH.md
    lib/foglet_bbs/tui/screens/login.ex
    lib/foglet_bbs/tui/text_width.ex
    test/foglet_bbs/tui/layout_smoke_test.exs
  </read_first>
  <behavior>
    - A reset request confirmation rendered at `{64, 22}` produces at least two non-chrome message rows for long generic copy.
    - A no-email reset confirmation rendered at `{64, 22}` includes active sysop emails when present and excludes deleted/inactive/non-sysop emails.
    - Each rendered content row satisfies `TextWidth.display_width(row_text) <= 64`.
    - The no-sysop no-email fallback includes `"sysop"` or `"operator"` and does not include `"unavailable"`, `"http://"`, `"https://"`, or `"/users/reset_password"`.
  </behavior>
  <action>
    Add layout smoke tests per D-12, D-14, and D-18 using the existing `Engine.apply_layout/2`, `text_elements/1`, and `text_rows/1` helper style. Seed `Config.delivery_mode` or ETS cache as needed using existing test patterns. Build Login states directly with `screen_state[:login].sub == :reset_request`, a success/no-email message state from implemented code, and `terminal_size: {64, 22}`. Assert wrapped copy appears on multiple y positions and no content row exceeds 64 display columns.
  </action>
  <acceptance_criteria>
    - `rg -n "64, 22|TextWidth\\.display_width|operator|sysop|reset_request" test/foglet_bbs/tui/layout_smoke_test.exs` shows compact reset smoke coverage.
    - `rg -n "http://|https://|/users/reset_password|unavailable" test/foglet_bbs/tui/layout_smoke_test.exs` shows forbidden-copy assertions.
    - `rtk mix test test/foglet_bbs/tui/layout_smoke_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/layout_smoke_test.exs</automated>
  </verify>
  <done>AUTH-02 and AUTH-03 compact rendering are verified through the same layout engine used by the live TUI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add raw-token non-leak smoke tests and run final gates</name>
  <files>test/foglet_bbs/tui/layout_smoke_test.exs, test/foglet_bbs/tui/screens/login_test.exs</files>
  <read_first>
    .planning/phases/31-auth-flow/31-SPEC.md
    .planning/phases/31-auth-flow/31-CONTEXT.md
    .planning/phases/31-auth-flow/31-RESEARCH.md
    lib/foglet_bbs/tui/screens/login.ex
    lib/foglet_bbs/tui/screens/login/state.ex
    lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex
    test/foglet_bbs/tui/layout_smoke_test.exs
    test/foglet_bbs/tui/screens/login_test.exs
  </read_first>
  <behavior>
    - A `:reset_consume` state with token input value `"RAW-RESET-TOKEN-SHOULD-NOT-LEAK"` renders breadcrumb/status/command text without that raw token.
    - The token may appear only as the focused TextInput field value under test, not in chrome frame rows, command bar rows, modal text, status text, or breadcrumb text.
    - Breadcrumb for `:reset_consume` uses the Phase 27 auth shape and includes `"Enter Token"` without including the raw token value.
    - The narrow suite and `rtk mix precommit` are attempted after tests pass.
  </behavior>
  <action>
    Add smoke coverage per D-11 and D-18. Use a sentinel raw token string exactly `"RAW-RESET-TOKEN-SHOULD-NOT-LEAK"`. Render Login at `{64, 22}` and inspect positioned text elements. Separate direct input field text from chrome/status/command surfaces using existing chrome frame attributes where available; assert the sentinel is absent from breadcrumb/status/keybar/modal/static text. Add a final integrated Login test only if layout smoke cannot cleanly distinguish the direct input field from chrome surfaces. Run the focused command from `31-VALIDATION.md`, then run `rtk mix precommit`; if precommit fails because of known out-of-scope Dialyzer warnings, capture the failing command and exact blocker in the summary without weakening Phase 31 tests.
  </action>
  <acceptance_criteria>
    - `rg -n "RAW-RESET-TOKEN-SHOULD-NOT-LEAK|Enter Token|reset_consume" test/foglet_bbs/tui/layout_smoke_test.exs test/foglet_bbs/tui/screens/login_test.exs` shows non-leak coverage.
    - `rtk mix test test/foglet_bbs/accounts/verification_test.exs test/foglet_bbs/tui/screens/login_test.exs test/foglet_bbs/tui/layout_smoke_test.exs` passes.
    - `rtk mix precommit` is run and its result is recorded in `.planning/phases/31-auth-flow/31-04-SUMMARY.md`.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/accounts/verification_test.exs test/foglet_bbs/tui/screens/login_test.exs test/foglet_bbs/tui/layout_smoke_test.exs</automated>
    <automated>rtk mix precommit</automated>
  </verify>
  <done>AUTH-01 through AUTH-04 have compact render, no-leak, focused-suite, and precommit evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Login render state to chrome | Sensitive raw token value exists in screen-local input state while chrome/status/command text is generated. |
| Terminal layout engine | Long reset copy is positioned into compact SSH viewport rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-10 | Information Disclosure | `ScreenFrame`/breadcrumb/keybar around reset consume | mitigate | Render sentinel raw token state and assert sentinel absent from chrome/status/command surfaces. |
| T-31-11 | Denial of Service | compact reset/no-email copy | mitigate | Assert wrapped row count and row display width at 64x22. |
| T-31-12 | Repudiation | Phase verification evidence | mitigate | Run narrow suite and precommit; record exact result in plan summary. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/accounts/verification_test.exs test/foglet_bbs/tui/screens/login_test.exs test/foglet_bbs/tui/layout_smoke_test.exs`, then run `rtk mix precommit`.
</verification>

<success_criteria>
All Phase 31 requirements have automated domain, screen-state, and compact-render coverage. Raw reset tokens do not appear outside the direct input field.
</success_criteria>

<output>
After completion, create `.planning/phases/31-auth-flow/31-04-SUMMARY.md`.
</output>
