---
id: 24-04
phase: 24
plan: 04
type: execute
wave: 2
depends_on: [24-01, 24-02]
files_modified:
  - lib/foglet_bbs/tui/widgets/workspace/inspector.ex
  - test/foglet_bbs/tui/widgets/workspace/inspector_test.exs
autonomous: true
requirements: [CONSOLE-03]
tags: [tui, widgets, workspace, inspector, operator-console]
must_haves:
  truths:
    - "D-04: `Foglet.TUI.Widgets.Workspace.Inspector` exists under `lib/foglet_bbs/tui/widgets/workspace/`."
    - "D-15: Inspector is optional progressive enhancement for wide terminals; at 64x22 and 80x24 it may render collapsed/empty output."
    - "D-16: Inspector input is caller-provided selected item details and scoped action descriptors; the widget performs no domain mutations."
    - "D-17: Inspector tests cover selected board, user, invite, and empty/no-selection fixture shapes, including collapse below threshold."
    - "D-21/D-23: Inspector has primitive-level compact and wide size coverage under the mirrored widget test path."
  artifacts:
    - path: lib/foglet_bbs/tui/widgets/workspace/inspector.ex
      provides: "Wide-terminal selected-row detail/action renderer"
    - path: test/foglet_bbs/tui/widgets/workspace/inspector_test.exs
      provides: "Inspector collapse, fixture, action, and hygiene tests"
---

<objective>
Create `Foglet.TUI.Widgets.Workspace.Inspector`, the optional wide-terminal
detail panel for future operator-console selected-row workflows.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/24-operator-console-primitives/24-CONTEXT.md
@.planning/phases/24-operator-console-primitives/24-RESEARCH.md
@.planning/phases/24-operator-console-primitives/24-VALIDATION.md
@lib/foglet_bbs/tui/widgets/display/kv_grid.ex
@lib/foglet_bbs/tui/widgets/display/badge.ex
@lib/foglet_bbs/tui/theme.ex
@lib/foglet_bbs/tui/screens/sysop/boards_view.ex
@lib/foglet_bbs/tui/screens/sysop/users_view.ex
@lib/foglet_bbs/tui/screens/shared/invites_surface.ex
@test/support/foglet/tui/widget_helpers.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Inspector contract tests</name>
  <files>test/foglet_bbs/tui/widgets/workspace/inspector_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/widgets/display/kv_grid_test.exs
    - test/foglet_bbs/tui/widgets/display/badge_test.exs
    - test/support/foglet/tui/widget_helpers.ex
    - .planning/phases/24-operator-console-primitives/24-RESEARCH.md
  </read_first>
  <action>Create `Foglet.TUI.Widgets.Workspace.InspectorTest`. Use board fixture details (`Name`, `Slug`, `Category`, `Posting`), user fixture details (`Handle`, `Email`, `Status`, `Role`), invite fixture details (`Code`, `Status`, `Issuer`, `Inserted`), and empty/no-selection fixture. Assert `Inspector.render(selection, theme: Theme.default(), width: 132)` includes title, details, and action labels like `Edit`, `Archive`, `Suspend`, and `Revoke` only when those actions are supplied by the test. Assert `Inspector.render(selection, theme: theme, width: 64)` and `width: 80` flatten to `""` or a stable collapsed marker chosen by the implementation. Add no-selection test that renders `No selection` at wide width. Add theme hygiene.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/workspace/inspector_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains `defmodule Foglet.TUI.Widgets.Workspace.InspectorTest`.
    - Test file contains exact strings `Name`, `Slug`, `Handle`, `Email`, `Code`, `Issuer`, `Edit`, `Archive`, `Suspend`, and `Revoke`.
    - Test file calls render with `width: 64`, `width: 80`, and `width: 132`.
    - Test file calls `color_atom_leaked?` and `color_names()`.
    - Initial RED is acceptable only until Task 2 is complete.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement wide-terminal Workspace.Inspector</name>
  <files>lib/foglet_bbs/tui/widgets/workspace/inspector.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/widgets/display/kv_grid.ex
    - lib/foglet_bbs/tui/widgets/display/badge.ex
    - lib/foglet_bbs/tui/theme.ex
    - test/foglet_bbs/tui/widgets/workspace/inspector_test.exs
  </read_first>
  <action>Create `Foglet.TUI.Widgets.Workspace.Inspector` as a stateless render-only widget. Implement `render(selection, opts)` requiring `theme:` and accepting `width:`, `min_width:` defaulting to `100`, `title:`, `details:`, and `actions:`. If `width < min_width`, return an empty list or empty column with no visible text. If selection is `nil` at wide width, render `No selection` with `theme.dim.fg`. For selected data, render a heading, details using `Display.KvGrid.render(details, theme: theme, width: inspector_width)`, and caller-provided action descriptors as compact text labels. Action descriptors are display-only maps such as `%{key: "E", label: "Edit", role: :accent}`; style destructive roles through `theme.error`, inactive roles through `theme.dim`, and defaults through `theme.accent` or `theme.primary`. Do not infer actions from selection shape.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/workspace/inspector_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/widgets/workspace/inspector.ex` contains `defmodule Foglet.TUI.Widgets.Workspace.Inspector`.
    - `@moduledoc` mentions `D-04`, `D-15`, `D-16`, and `D-17`.
    - Source contains `@default_min_width 100` or an equivalent default min-width constant.
    - Source contains `KvGrid.render`.
    - Source does not contain `Foglet.Accounts`, `Foglet.Boards`, `Foglet.Config`, `Foglet.Posts`, or `Foglet.Threads`.
    - `rtk mix test test/foglet_bbs/tui/widgets/workspace/inspector_test.exs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-selected item -> inspector render tree | Details and actions are displayed only. |
| Inspector -> domain contexts | No crossing allowed; actions are descriptors, not commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Elevation of Privilege | Action descriptors | mitigate | Widget renders only caller-supplied actions and returns no mutation commands. |
| T-24-11 | Spoofing | Unavailable workflows | mitigate | Tests prove actions appear only when supplied by caller fixtures. |
| T-24-12 | Denial of Service | Compact terminals | mitigate | Collapse below the min-width threshold at 64 and 80 columns. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/workspace/inspector_test.exs`.
</verification>

<success_criteria>
- `Workspace.Inspector` exists and renders selected details/actions at wide width.
- Compact widths collapse intentionally.
- No domain calls, fake actions, or hardcoded terminal color atoms are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/24-operator-console-primitives/24-04-SUMMARY.md`.
</output>
