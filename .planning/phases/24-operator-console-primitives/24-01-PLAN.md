---
id: 24-01
phase: 24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/foglet_bbs/tui/widgets/display/badge.ex
  - test/foglet_bbs/tui/widgets/display/badge_test.exs
autonomous: true
requirements: [CONSOLE-01]
tags: [tui, widgets, display, badge, operator-console]
must_haves:
  truths:
    - "D-01: `Foglet.TUI.Widgets.Display.Badge` exists under `lib/foglet_bbs/tui/widgets/display/` as a stateless display primitive."
    - "D-06: Badge supports at least `:required`, `:subscribed`, `:locked`, `:sticky`, `:pending`, `:healthy`, `:error`, `:neutral`, and `:info` states."
    - "D-07: Badge output is compact and each required state is recognizable in flattened render output."
    - "D-08: Badge roles route through `Foglet.TUI.Presentation.theme_mappings().badges` and then `Foglet.TUI.Theme` slots; no hardcoded terminal color atoms."
    - "D-21/D-23: Badge has primitive-level 64-column, 80-column, and theme-hygiene tests under the mirrored widget test path."
  artifacts:
    - path: lib/foglet_bbs/tui/widgets/display/badge.ex
      provides: "Stateless compact state badge renderer"
    - path: test/foglet_bbs/tui/widgets/display/badge_test.exs
      provides: "Badge state, compactness, theme routing, and hygiene tests"
---

<objective>
Create `Foglet.TUI.Widgets.Display.Badge`, the compact state renderer used by
operator-console tables, grids, inspectors, and future screen conversions.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/24-operator-console-primitives/24-CONTEXT.md
@.planning/phases/24-operator-console-primitives/24-RESEARCH.md
@.planning/phases/24-operator-console-primitives/24-VALIDATION.md
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/presentation.ex
@lib/foglet_bbs/tui/theme.ex
@test/support/foglet/tui/widget_helpers.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Badge contract tests</name>
  <files>test/foglet_bbs/tui/widgets/display/badge_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/widgets/display/table_test.exs
    - test/foglet_bbs/tui/widgets/display/progress_test.exs
    - test/support/foglet/tui/widget_helpers.ex
    - lib/foglet_bbs/tui/presentation.ex
    - .planning/phases/24-operator-console-primitives/24-RESEARCH.md
  </read_first>
  <action>Create `Foglet.TUI.Widgets.Display.BadgeTest`. Import `Foglet.TUI.WidgetHelpers`. Write tests that call `Badge.render/2` with `theme: Theme.default()` for the states `:required`, `:subscribed`, `:locked`, `:sticky`, `:pending`, `:healthy`, `:error`, `:neutral`, and `:info`. Assert `flatten_text/1` includes recognizable state text for every state. Add a distinctive theme test proving rendered output changes when the relevant badge role maps to a different theme slot. Add a hygiene loop over `color_names/0`. Add a compactness test asserting each required state render has `TextWidth.display_width(flatten_text(tree)) <= 16` unless an explicit custom label is supplied.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/display/badge_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/foglet_bbs/tui/widgets/display/badge_test.exs` contains `defmodule Foglet.TUI.Widgets.Display.BadgeTest`.
    - Test file contains all exact atom strings `:required`, `:subscribed`, `:locked`, `:sticky`, `:pending`, `:healthy`, `:error`, `:neutral`, and `:info`.
    - Test file calls `Presentation.theme_mappings().badges`.
    - Test file calls `color_atom_leaked?` and `color_names()`.
    - Initial RED is acceptable only until Task 2 is complete.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stateless Display.Badge</name>
  <files>lib/foglet_bbs/tui/widgets/display/badge.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/widgets/README.md
    - lib/foglet_bbs/tui/presentation.ex
    - lib/foglet_bbs/tui/theme.ex
    - test/foglet_bbs/tui/widgets/display/badge_test.exs
  </read_first>
  <action>Create `Foglet.TUI.Widgets.Display.Badge`. Import `Raxol.Core.Renderer.View`. Require `theme: %Foglet.TUI.Theme{}` in `render/2`. Implement built-in state metadata for exactly: `:required`, `:subscribed`, `:locked`, `:sticky`, `:pending`, `:healthy`, `:error`, `:neutral`, and `:info`. Use role mapping `:required -> :warning`, `:subscribed -> :success`, `:locked -> :warning`, `:sticky -> :accent`, `:pending -> :info`, `:healthy -> :success`, `:error -> :error`, `:neutral -> :info`, `:info -> :info`. Resolve role through `Presentation.theme_mappings().badges`; because the Phase 17 map has only `:info`, `:success`, `:warning`, `:error`, and `:accent`, keep unsupported caller roles from crashing by falling back to `:info`. Render a compact text node such as `[required]` or caller `label:` with `fg` and `style` taken from the resolved theme slot. Do not use literal color atoms.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/display/badge_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/widgets/display/badge.ex` contains `defmodule Foglet.TUI.Widgets.Display.Badge`.
    - `@moduledoc` mentions `D-01`, `D-06`, `D-07`, and `D-08`.
    - Source contains `Presentation.theme_mappings().badges`.
    - Source contains all required state atoms.
    - Source does not contain `fg: :red`, `fg: :green`, `fg: :cyan`, `fg: :yellow`, `fg: :blue`, `fg: :magenta`, `fg: :white`, or `fg: :black`.
    - `rtk mix test test/foglet_bbs/tui/widgets/display/badge_test.exs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller state -> terminal render tree | Caller-provided labels are rendered as text only. |
| Widget -> domain contexts | No crossing allowed; Badge is presentation-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | Badge state labels | mitigate | Render labels only; no domain mutation or authorization logic. |
| T-24-02 | Spoofing | Badge state semantics | mitigate | Tests pin required state labels so future callers can recognize state in flattened output. |
| T-24-03 | Information Disclosure | Theme rendering | mitigate | Use semantic theme slots only; no environment or user data lookup. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/display/badge_test.exs`.
</verification>

<success_criteria>
- `Display.Badge` exists, is stateless, and renders every required state.
- Badge styling routes through Presentation badge mappings and Theme slots.
- Badge tests prove compact output and hardcoded color atom hygiene.
</success_criteria>

<output>
After completion, create `.planning/phases/24-operator-console-primitives/24-01-SUMMARY.md`.
</output>
