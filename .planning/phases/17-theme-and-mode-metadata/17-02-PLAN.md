---
phase: 17-theme-and-mode-metadata
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/foglet_bbs/tui/theme.ex
  - test/foglet_bbs/tui/theme_test.exs
autonomous: true
requirements:
  - THEME-01
requirements_addressed:
  - THEME-01
must_haves:
  truths:
    - "Every theme snapshot exposes non-empty success, info, and badge slots."
    - "Theme slots include success, informational, badge, selected, dim, warning, error, and accent states."
    - "Theme slot discovery is public enough for contract tests to validate mappings without duplicating private slot lists."
  artifacts:
    - path: "lib/foglet_bbs/tui/theme.ex"
      provides: "Semantic theme slot registry and flat snapshots"
      exports: ["slot_keys/0", "ids/0", "default/0", "resolve/1", "from_state/1"]
    - path: "test/foglet_bbs/tui/theme_test.exs"
      provides: "Palette-wide semantic slot tests"
  key_links:
    - from: "lib/foglet_bbs/tui/theme.ex"
      to: "test/foglet_bbs/tui/theme_test.exs"
      via: "Theme.ids/0 and Theme.slot_keys/0"
      pattern: "Theme\\.ids\\(\\).*Theme\\.resolve\\("
---

<objective>
Extend the existing `Foglet.TUI.Theme` registry with semantic slots needed by facelift widgets.

Purpose: Later tabs, rows, badges, command hints, and editor states need named semantic slots without hardcoded color atoms or palette retuning.
Output: `success`, `info`, and `badge` as first-class theme slots across every palette and focused ExUnit proof.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@SCREENS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-theme-and-mode-metadata/17-CONTEXT.md
@.planning/phases/17-theme-and-mode-metadata/17-RESEARCH.md
@.planning/phases/17-theme-and-mode-metadata/17-VALIDATION.md
@lib/foglet_bbs/tui/theme.ex
@test/foglet_bbs/tui/theme_test.exs

<interfaces>
Current public theme functions:
```elixir
@spec register_all() :: :ok
def register_all

@spec ids() :: [atom()]
def ids

@spec default() :: t()
def default

@spec from_state(map()) :: t()
def from_state(state)

@spec resolve(atom()) :: t()
def resolve(id) when is_atom(id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 17-02-01: Add first-class semantic theme slots</name>
  <files>lib/foglet_bbs/tui/theme.ex, test/foglet_bbs/tui/theme_test.exs</files>
  <read_first>
    - `lib/foglet_bbs/tui/theme.ex`.
    - `test/foglet_bbs/tui/theme_test.exs`.
    - `.planning/phases/17-theme-and-mode-metadata/17-CONTEXT.md` decisions D-08 through D-10.
    - `.planning/phases/17-theme-and-mode-metadata/17-VALIDATION.md` task 17-02-01.
  </read_first>
  <behavior>
    - `%Foglet.TUI.Theme{}` has `success`, `info`, and `badge` fields.
    - `Theme.slot_keys()` includes `:success`, `:info`, and `:badge` in addition to existing slots.
    - For every `Theme.ids()` id, `Theme.resolve(id).success`, `.info`, and `.badge` are non-empty maps.
    - `Theme.default()` and `Theme.from_state(%{})` return snapshots with non-empty `success`, `info`, and `badge`.
  </behavior>
  <action>
    Update `Foglet.TUI.Theme` per D-08: add `success`, `info`, and `badge` to `@type t`, `defstruct`, `@slot_keys`, and every existing palette map (`@gray_slots`, `@green_slots`, `@amber_slots`, `@cyan_slots`, `@paper_slots`, `@magenta_slots`, `@danger_slots`, `@ice_slots`, `@mono_slots`). Synthesize the new slots from existing palette colors per D-09: use each palette's existing positive/primary/accent family rather than retuning contrast. Concrete values: `success` should be a positive/primary styled map (`@green_slots.success` may use `#33ff66`; monochrome may use white bold), `info` should use the palette's existing accent/status color, and `badge` should use a compact label style based on `selected` or `accent` with non-empty `:fg` and, where the palette already has one, `:bg`. Add public `@doc "List of supported theme slot keys."` and `@spec slot_keys() :: [atom()]`, returning `@slot_keys`. Do not introduce a second palette registry.
  </action>
  <acceptance_criteria>
    - `rg -n "success:|info:|badge:" lib/foglet_bbs/tui/theme.ex` finds entries in the struct/type area and all palette maps.
    - `rg -n "def slot_keys|@spec slot_keys\\(\\) :: \\[atom\\(\\)\\]" lib/foglet_bbs/tui/theme.ex` finds the new public accessor.
    - `rg -n ":success|:info|:badge" test/foglet_bbs/tui/theme_test.exs` finds tests for the semantic slots.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/theme_test.exs</automated>
  </verify>
  <done>`Theme.resolve/1`, `Theme.default/0`, and `Theme.from_state/1` expose non-empty `success`, `info`, and `badge` maps for every existing theme id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 17-02-02: Prove baseline required slots remain present</name>
  <files>test/foglet_bbs/tui/theme_test.exs</files>
  <read_first>
    - `test/foglet_bbs/tui/theme_test.exs`.
    - `lib/foglet_bbs/tui/theme.ex`.
    - `.planning/REQUIREMENTS.md` requirement THEME-01.
  </read_first>
  <behavior>
    - `Theme.slot_keys()` includes `:success`, `:info`, `:badge`, `:selected`, `:dim`, `:warning`, `:error`, and `:accent`.
    - All required THEME-01 slots resolve to non-empty maps for every theme id.
  </behavior>
  <action>
    Add a `describe "slot_keys/0 (THEME-01)"` block asserting the required slots from THEME-01 are included in `Theme.slot_keys()`. Add a palette-wide test that iterates over `Theme.ids()` and asserts each required slot returns a non-empty map via `Map.fetch!(Theme.resolve(id), slot)`. Keep existing `from_state/1` tests unchanged except where struct construction must include new fields automatically through defaults.
  </action>
  <acceptance_criteria>
    - `rg -n "slot_keys/0 \\(THEME-01\\)|required_slots|Map.fetch!\\(Theme.resolve" test/foglet_bbs/tui/theme_test.exs` finds the coverage.
    - `rtk mix test test/foglet_bbs/tui/theme_test.exs` passes without watch-mode flags.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/theme_test.exs</automated>
  </verify>
  <done>THEME-01 is test-backed for both new semantic slots and pre-existing selected/dim/warning/error/accent states.</done>
</task>

</tasks>

<must_haves>
- `Foglet.TUI.Theme` has first-class `success`, `info`, and `badge` fields.
- Every palette in `@themes` includes non-empty maps for the new slots.
- `Theme.slot_keys/0` exposes the slot registry for later mapping validation.
- No palette retuning or visual redesign is performed.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme id to palette snapshot | User/session theme ids resolve to a flat theme snapshot consumed by render paths. |
| Theme slot registry to future widgets | Future widgets will trust `Theme.slot_keys/0` and semantic slots when routing styles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Denial of Service | `Foglet.TUI.Theme.resolve/1` | mitigate | Add tests proving all new slots are non-empty for every theme id, preventing nil/empty style crashes in consumers. |
| T-17-05 | Tampering | Theme slot registry | mitigate | Expose `slot_keys/0` from the existing registry and validate all required slots through ExUnit instead of duplicating private slot lists. |
| T-17-06 | Elevation of Privilege | Theme choice | mitigate | Keep theme data purely presentational; do not add any authorization, role, or mode logic to `Foglet.TUI.Theme`. |
</threat_model>

<verification>
Focused:
`rtk mix test test/foglet_bbs/tui/theme_test.exs`

Wave:
`rtk mix test test/foglet_bbs/tui/theme_test.exs test/foglet_bbs/tui/presentation_test.exs`
</verification>

<success_criteria>
- THEME-01 slots are present and non-empty across every theme id.
- `Theme.resolve/1`, `Theme.default/0`, and `Theme.from_state/1` return complete snapshots.
- Later contract tests can validate slot names through `Theme.slot_keys/0`.
- No Chrome V2, rich row, badge widget, command bar, editor frame, table preset, inspector, or screen layout conversion is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/17-theme-and-mode-metadata/17-02-SUMMARY.md`.
</output>
