---
phase: 17-theme-and-mode-metadata
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - lib/foglet_bbs/tui/presentation.ex
  - test/foglet_bbs/tui/presentation_test.exs
autonomous: true
requirements:
  - THEME-02
requirements_addressed:
  - THEME-02
must_haves:
  truths:
    - "Tabs, rows, badges, command hints, and editor states have one documented theme-slot mapping contract."
    - "Every mapping leaf references a real `Foglet.TUI.Theme` slot."
    - "The contract adds metadata only and does not implement visible facelift widgets or screen conversions."
  artifacts:
    - path: "lib/foglet_bbs/tui/presentation.ex"
      provides: "Theme mapping contract data"
      exports: ["theme_mappings/0"]
    - path: "test/foglet_bbs/tui/presentation_test.exs"
      provides: "THEME-02 mapping validity tests"
  key_links:
    - from: "lib/foglet_bbs/tui/presentation.ex"
      to: "lib/foglet_bbs/tui/theme.ex"
      via: "mapping leaves validated against Theme.slot_keys/0"
      pattern: "Theme\\.slot_keys\\(\\)"
---

<objective>
Freeze the project-local theme-slot mapping contract for current and future TUI primitives.

Purpose: Later widget phases need concrete state-to-slot names for tabs, rows, badges, command hints, and editor states without hardcoded color atoms or one-off styling systems.
Output: `Presentation.theme_mappings/0` plus ExUnit validation against `Foglet.TUI.Theme.slot_keys/0`.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@SCREENS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-theme-and-mode-metadata/17-CONTEXT.md
@.planning/phases/17-theme-and-mode-metadata/17-RESEARCH.md
@.planning/phases/17-theme-and-mode-metadata/17-VALIDATION.md
@lib/foglet_bbs/tui/presentation.ex
@lib/foglet_bbs/tui/theme.ex
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/widgets/input/tabs.ex
@lib/foglet_bbs/tui/widgets/list/list_row.ex
@lib/foglet_bbs/tui/widgets/chrome/key_bar.ex
@lib/foglet_bbs/tui/widgets/compose.ex

<interfaces>
Required from Plan 17-02:
```elixir
@spec Foglet.TUI.Theme.slot_keys() :: [atom()]
```

Existing widget evidence:
```elixir
# Tabs uses selected, unselected, border.
# ListRow uses selected, primary, unselected, dim.
# KeyBar uses accent and dim.
# Compose uses primary for current editor input rendering.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 17-03-01: Add mapping contract data</name>
  <files>lib/foglet_bbs/tui/presentation.ex, test/foglet_bbs/tui/presentation_test.exs</files>
  <read_first>
    - `lib/foglet_bbs/tui/presentation.ex`.
    - `test/foglet_bbs/tui/presentation_test.exs`.
    - `lib/foglet_bbs/tui/theme.ex`.
    - `.planning/phases/17-theme-and-mode-metadata/17-CONTEXT.md` decisions D-11 through D-13.
    - `.planning/phases/17-theme-and-mode-metadata/17-VALIDATION.md` task 17-03-01.
  </read_first>
  <behavior>
    - `Presentation.theme_mappings/0` returns categories `:tabs`, `:rows`, `:badges`, `:commands`, and `:editor`.
    - Mappings include selected/unselected tabs; row selected/unread/normal/metadata/disabled states; badge info/success/warning/error/accent states; command group/key/destructive/inactive states; and editor focus/counter states.
    - Every mapping leaf is an atom included in `Theme.slot_keys()`.
  </behavior>
  <action>
    Add `alias Foglet.TUI.Theme` to `Foglet.TUI.Presentation` if needed. Add public `@doc` and `@spec theme_mappings() :: %{required(atom()) => %{required(atom()) => atom()}}`. Return exactly this map: `tabs: %{selected: :selected, unselected: :unselected, indicator: :accent, border: :border}`, `rows: %{selected: :selected, unread: :primary, normal: :unselected, metadata: :dim, disabled: :dim}`, `badges: %{info: :info, success: :success, warning: :warning, error: :error, accent: :accent}`, `commands: %{group: :dim, key: :accent, destructive: :error, inactive: :dim}`, `editor: %{focused: :accent, unfocused: :border, input: :primary, counter: :dim, counter_warning: :warning, counter_error: :error}`. Add tests asserting exact category and key coverage and validating all leaves with `slot in Theme.slot_keys()`. Do not create `Display.Badge`, `List.RichRow`, `Chrome.CommandBar`, `Composer.EditorFrame`, table presets, inspectors, or screen layout changes.
  </action>
  <acceptance_criteria>
    - `rg -n "def theme_mappings|tabs:|rows:|badges:|commands:|editor:" lib/foglet_bbs/tui/presentation.ex` finds the mapping function and categories.
    - `rg -n "selected: :selected|unread: :primary|info: :info|destructive: :error|counter_warning: :warning|counter_error: :error" lib/foglet_bbs/tui/presentation.ex` finds the concrete mappings.
    - `rg -n "Theme.slot_keys|theme_mappings/0 \\(THEME-02\\)|:badges|:commands|:editor" test/foglet_bbs/tui/presentation_test.exs` finds the validation tests.
    - `test ! -e lib/foglet_bbs/tui/widgets/display/badge.ex` and `test ! -e lib/foglet_bbs/tui/widgets/chrome/command_bar.ex` remain true unless those files already existed before this plan.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/presentation_test.exs</automated>
  </verify>
  <done>THEME-02 mappings are concrete, documented as data, and validated against real theme slots.</done>
</task>

<task type="auto">
  <name>Task 17-03-02: Run phase validation and precommit</name>
  <files>lib/foglet_bbs/tui/presentation.ex, test/foglet_bbs/tui/presentation_test.exs</files>
  <read_first>
    - `.planning/phases/17-theme-and-mode-metadata/17-VALIDATION.md`.
    - `lib/foglet_bbs/tui/presentation.ex`.
    - `test/foglet_bbs/tui/presentation_test.exs`.
    - `test/foglet_bbs/tui/theme_test.exs`.
  </read_first>
  <action>
    Run the Phase 17 validation contract exactly: focused test command `rtk mix test test/foglet_bbs/tui/theme_test.exs test/foglet_bbs/tui/presentation_test.exs`, then full `rtk mix test`, then final `rtk mix precommit`. Fix only issues caused by Phase 17 changes. Do not use watch-mode flags.
  </action>
  <acceptance_criteria>
    - The command output for `rtk mix test test/foglet_bbs/tui/theme_test.exs test/foglet_bbs/tui/presentation_test.exs` exits 0.
    - The command output for `rtk mix test` exits 0.
    - The command output for `rtk mix precommit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/theme_test.exs test/foglet_bbs/tui/presentation_test.exs</automated>
    <automated>rtk mix test</automated>
    <automated>rtk mix precommit</automated>
  </verify>
  <done>Phase 17 validation passes at focused, full-suite, and precommit levels.</done>
</task>

</tasks>

<must_haves>
- `Presentation.theme_mappings/0` includes `:tabs`, `:rows`, `:badges`, `:commands`, and `:editor`.
- The mapping leaves reference only slots from `Theme.slot_keys/0`.
- Mapping coverage includes selected/unselected tabs, row selected/unread/normal/metadata/disabled, badge info/success/warning/error/accent, command group/key/destructive/inactive, and editor focus/counter states.
- No visible facelift primitives or screen conversions are implemented.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mapping contract to future widgets | Later widget implementations will consume these slot names as trusted style metadata. |
| Presentation metadata to authorization | Mode and mapping metadata may be visible to code near operator screens, but must not grant or deny permissions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering | `Presentation.theme_mappings/0` | mitigate | Validate every mapping leaf against `Theme.slot_keys/0`; tests fail if a future edit references a nonexistent slot. |
| T-17-08 | Elevation of Privilege | Presentation metadata near operator screens | mitigate | Keep mappings and modes display-only; no authorization calls, role checks, or screen access decisions in `Foglet.TUI.Presentation`. |
| T-17-09 | Denial of Service | Future widget style lookup | mitigate | Contract tests prevent invalid slot names that could crash future render paths. |
</threat_model>

<verification>
Focused:
`rtk mix test test/foglet_bbs/tui/presentation_test.exs`

Phase:
`rtk mix test test/foglet_bbs/tui/theme_test.exs test/foglet_bbs/tui/presentation_test.exs`
`rtk mix test`
`rtk mix precommit`
</verification>

<success_criteria>
- THEME-02 is implemented as a project-local, tested mapping contract.
- Mapping tests validate referenced slot names against `Foglet.TUI.Theme`.
- Presentation mode remains independent from theme id, theme preview state, and palette data.
- No Chrome V2, rich row, badge widget, command bar, editor frame, table preset, inspector, or screen layout conversion is included.
</success_criteria>

<output>
After completion, create `.planning/phases/17-theme-and-mode-metadata/17-03-SUMMARY.md`.
</output>
