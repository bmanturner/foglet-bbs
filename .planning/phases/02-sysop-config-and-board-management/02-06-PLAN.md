---
phase: 02-sysop-config-and-board-management
plan: 02-06
type: execute
wave: 5
depends_on: [02-05]
gap_closure: true
files_modified:
  - lib/foglet_bbs/boards.ex
  - lib/foglet_bbs/tui/screens/sysop/boards_view.ex
  - lib/foglet_bbs/tui/screens/sysop/limits_form.ex
  - test/foglet_bbs/tui/screens/sysop_test.exs
autonomous: true
requirements: [SYSO-02, SYSO-03]
must_haves:
  truths:
    - "BOARDS tab Modal.Form submit handles {:error, :board_server_unavailable} and any non-changeset, non-forbidden atom error without CaseClauseError."
    - "BOARDS tab submit atom errors reset the active modal and emit {:error_modal, message, :main_menu} for the parent Sysop screen to route safely."
    - "LIMITS tab ordinary character input such as %{key: :char, char: \"x\"} does not raise BadBooleanError."
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/sysop/boards_view.ex"
      provides: "catch-all atom error handling for Modal.Form submit results"
      contains: "{:error, reason} when is_atom(reason)"
    - path: "lib/foglet_bbs/boards.ex"
      provides: "accurate create_board/3 spec including {:error, :board_server_unavailable}"
      contains: ":board_server_unavailable"
    - path: "lib/foglet_bbs/tui/screens/sysop/limits_form.ex"
      provides: "nil-safe ctrl/meta char guard"
      contains: "Map.get(event, :ctrl) || Map.get(event, :meta)"
    - path: "test/foglet_bbs/tui/screens/sysop_test.exs"
      provides: "regression coverage for BOARDS atom errors and LIMITS plain char input"
      contains: "board_server_unavailable"
  key_links:
    - from: "BoardsView.handle_submit_payload/2"
      to: "Foglet.Boards.create_board/3"
      via: "dispatch_submit(:create_board, payload, state)"
      pattern: "case dispatch_submit\\(kind, payload, state\\)"
    - from: "LimitsForm.handle_key/2"
      to: "ordinary Raxol char events"
      via: "%{key: :char, char: c} without :ctrl/:meta keys"
      pattern: "Map\\.get\\(event, :ctrl\\) \\|\\| Map\\.get\\(event, :meta\\)"
---

<objective>
Close the remaining Phase 02 verification gap and the tightly related advisory
review warning without replanning completed Phase 2 feature work.

Purpose: Sysop BOARDS and LIMITS TUI handlers must degrade safely for expected
runtime edge cases instead of crashing the screen.
Output: One gap-closure patch plus focused regression coverage in the existing
Sysop TUI test file, followed by `rtk mix precommit` if feasible.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-sysop-config-and-board-management/02-CONTEXT.md
@.planning/phases/02-sysop-config-and-board-management/02-RESEARCH.md
@.planning/phases/02-sysop-config-and-board-management/02-VERIFICATION.md
@.planning/phases/02-sysop-config-and-board-management/02-REVIEW.md
@.planning/phases/02-sysop-config-and-board-management/02-04-SUMMARY.md
@.planning/phases/02-sysop-config-and-board-management/02-05-SUMMARY.md

<interfaces>
Existing BOARDS submit shape:
```elixir
defp handle_submit_payload(payload, %__MODULE__{modal_kind: kind} = state) do
  case dispatch_submit(kind, payload, state) do
    {:ok, _result} -> ...
    {:error, %Ecto.Changeset{} = cs} -> ...
    {:error, :forbidden} -> ...
  end
end
```

Known domain error that currently escapes the case:
```elixir
Foglet.Boards.create_board(actor, category_id, attrs)
# can return {:error, :board_server_unavailable}
```

Existing parent routing expectation:
```elixir
{:error_modal, message, :main_menu}
```

LIMITS bug source:
```elixir
if Map.get(event, :ctrl) or Map.get(event, :meta) do
```
For `%{key: :char, char: "x"}`, both `Map.get/2` calls return `nil`; strict
`or` raises `BadBooleanError`. Use `||`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route BOARDS Modal.Form atom errors to safe error modal</name>
  <files>lib/foglet_bbs/boards.ex, lib/foglet_bbs/tui/screens/sysop/boards_view.ex, test/foglet_bbs/tui/screens/sysop_test.exs</files>
  <read_first>
    - `.planning/phases/02-sysop-config-and-board-management/02-VERIFICATION.md` gap "BOARDS tab create/edit/submit errors route to the error modal instead of crashing or disappearing."
    - `.planning/phases/02-sysop-config-and-board-management/02-REVIEW.md` WR-02
    - `lib/foglet_bbs/tui/screens/sysop/boards_view.ex` functions `handle_form_event/2`, `handle_submit_payload/2`, `dispatch_submit/3`, and `reset_modal/1`
    - `lib/foglet_bbs/boards.ex` `@spec create_board/3` and `create_board/3`
    - `test/foglet_bbs/tui/screens/sysop_test.exs` existing describe blocks "BOARDS tab create flow (SYSO-03)" and "BOARDS tab forbidden routing (SYSO-03, D-24)"
  </read_first>
  <action>
    First add a failing regression test in `test/foglet_bbs/tui/screens/sysop_test.exs` near the existing BOARDS forbidden-routing tests. The test must exercise the real `Sysop.handle_key/2` -> `BoardsView.handle_form_event/2` -> `handle_submit_payload/2` path with a Modal.Form submit returning an atom error. Prefer the narrowest reliable seam already present in tests:
    - Build a BOARDS tab state with a `%ModalForm{}` whose `on_submit` callback stores a payload using `Process.put({BoardsView, :pending_submit}, payload)`.
    - Use the production `:create_board` modal path. Force the known domain tuple by stopping `Foglet.Boards.Supervisor` for this test with supervised-test synchronization, then submit a valid board payload. `Foglet.Boards.create_board/3` will insert, fail `BoardSupervisor.start_board/1`, roll back the row, and return `{:error, :board_server_unavailable}`. Do not use `Process.sleep/1`; if synchronization is needed, monitor the supervisor process before stopping it and assert the `:DOWN` message.
    - The expected result is no exception, `new_state.current_screen == :main_menu`, `new_state.modal.type == :error`, and the BOARDS view modal reset to `nil`.
    - The test name must include the exact tuple: `{:error, :board_server_unavailable}`.

    Then update `lib/foglet_bbs/tui/screens/sysop/boards_view.ex`:
    - Keep the existing `{:ok, _result}`, `{:error, %Ecto.Changeset{}}`, and `{:error, :forbidden}` branches unchanged in behavior.
    - Add a branch after `:forbidden`:
      ```elixir
      {:error, reason} when is_atom(reason) ->
        {reset_modal(state), [{:error_modal, db_error_message(reason), :main_menu}]}
      ```
    - Add or reuse a private `db_error_message/1` helper returning a sysop-readable string. It must include a specific message for `:board_server_unavailable`, for example `"Board server unavailable. Please retry."`, and a generic atom fallback that includes `inspect(reason)` so future atom errors are safe and diagnosable.
    - Do not add direct database writes, supervisor termination, or alternative board creation flows in TUI code.

    Also update `lib/foglet_bbs/boards.ex` `@spec create_board/3` to include `{:error, :board_server_unavailable}` because the function already returns that tuple. Do not change the function body unless compile/dialyzer requires a small type-only adjustment.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/sysop_test.exs --trace</automated>
    <automated>rtk mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "board_server_unavailable" test/foglet_bbs/tui/screens/sysop_test.exs lib/foglet_bbs/tui/screens/sysop/boards_view.ex lib/foglet_bbs/boards.ex` shows the regression test, UI message branch, and updated spec.
    - `rg -n "\\{:error, reason\\} when is_atom\\(reason\\)" lib/foglet_bbs/tui/screens/sysop/boards_view.ex` finds the generic safe atom branch.
    - Submitting a Modal.Form payload that leads to `{:error, :board_server_unavailable}` does not raise `CaseClauseError`.
    - The resulting app state has `%Foglet.TUI.Modal{type: :error}` and `current_screen: :main_menu`.
  </acceptance_criteria>
  <done>BOARDS Modal.Form submit atom errors route to the shared safe error modal instead of crashing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix LIMITS plain char BadBooleanError and run phase gate</name>
  <files>lib/foglet_bbs/tui/screens/sysop/limits_form.ex, test/foglet_bbs/tui/screens/sysop_test.exs</files>
  <read_first>
    - `.planning/phases/02-sysop-config-and-board-management/02-REVIEW.md` WR-01
    - `lib/foglet_bbs/tui/screens/sysop/limits_form.ex` `handle_key/2`
    - `lib/foglet_bbs/tui/screens/sysop/site_form.ex` nil-safe char guard precedent
    - `test/foglet_bbs/tui/screens/sysop_test.exs` existing LIMITS tab tests around ordinary char input
  </read_first>
  <action>
    First add a failing regression test in `test/foglet_bbs/tui/screens/sysop_test.exs` under the LIMITS tab tests:
    - Activate the LIMITS tab for a sysop user.
    - Send `Sysop.handle_key(%{key: :char, char: "x"}, state)`.
    - Assert the call does not raise. Accept either `:no_match` from the parent or `{:update, new_state, _cmds}` with unchanged LIMITS drafts, but the test must fail before the `or` -> `||` change because of `BadBooleanError`.

    Then update `lib/foglet_bbs/tui/screens/sysop/limits_form.ex` `handle_key/2`:
    ```elixir
    if Map.get(event, :ctrl) || Map.get(event, :meta) do
      {state, []}
    else
      {apply_char(c, state), []}
    end
    ```
    Match the `SiteForm.handle_key/2` pattern exactly. Do not change LIMITS validation semantics: ordinary non-digit characters should remain ignored by `apply_char/2` for integer fields.

    After both tasks are green, run the focused Phase 2 checks and then the repo gate if feasible:
    - `rtk mix test test/foglet_bbs/config_test.exs test/foglet_bbs/boards/boards_test.exs test/foglet_bbs/tui/screens/sysop_test.exs`
    - `rtk mix precommit`
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/sysop_test.exs --trace</automated>
    <automated>rtk mix test test/foglet_bbs/config_test.exs test/foglet_bbs/boards/boards_test.exs test/foglet_bbs/tui/screens/sysop_test.exs</automated>
    <automated>rtk mix precommit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Map.get\\(event, :ctrl\\) \\|\\| Map.get\\(event, :meta\\)" lib/foglet_bbs/tui/screens/sysop/limits_form.ex` finds the nil-safe guard.
    - `rg -n "BadBooleanError|plain character|ordinary character|%\\{key: :char, char: \\\"x\\\"\\}" test/foglet_bbs/tui/screens/sysop_test.exs` finds the regression test.
    - `%{key: :char, char: "x"}` on LIMITS tab does not raise `BadBooleanError`.
    - `rtk mix test test/foglet_bbs/tui/screens/sysop_test.exs --trace` passes.
    - If `rtk mix precommit` cannot be completed because of pre-existing unrelated work, record the exact failure in `02-06-SUMMARY.md`; otherwise it must pass.
  </acceptance_criteria>
  <done>LIMITS ordinary character input is crash-safe, and focused Phase 2 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH user input -> TUI screen handlers | Untrusted keyboard events enter `Sysop.handle_key/2` and tab submodule `handle_key/2` functions. |
| TUI BOARDS form payload -> Foglet.Boards context | Typed Modal.Form payload crosses from UI state into actor-aware domain mutation functions. |
| Domain mutation result -> TUI modal routing | Domain errors are translated into user-visible modal events and navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | D | `BoardsView.handle_submit_payload/2` | mitigate | Add `{:error, reason} when is_atom(reason)` branch so `:board_server_unavailable` and future atom errors cannot crash the TUI process with `CaseClauseError`. |
| T-02-06-02 | D | `LimitsForm.handle_key/2` | mitigate | Replace strict boolean `or` with nil-safe `||` so ordinary char events without modifier keys cannot raise `BadBooleanError`. |
| T-02-06-03 | E | BOARDS submit authorization | accept | Existing `{:error, :forbidden}` branch and actor-aware `Foglet.Boards.*` calls remain unchanged; this plan does not weaken authorization or add screen-only permission checks. |
| T-02-06-04 | I | BOARDS atom error messages | mitigate | Use specific non-secret user-facing message for `:board_server_unavailable`; generic fallback may include `inspect(reason)` only for atom reasons, not exception structs or database internals. |
</threat_model>

<verification>
Run, in order:

1. `rtk mix test test/foglet_bbs/tui/screens/sysop_test.exs --trace`
2. `rtk mix test test/foglet_bbs/config_test.exs test/foglet_bbs/boards/boards_test.exs test/foglet_bbs/tui/screens/sysop_test.exs`
3. `rtk mix compile --warnings-as-errors`
4. `rtk mix precommit`

If step 4 fails for unrelated pre-existing work, capture the exact failure and
why it is unrelated in `02-06-SUMMARY.md`; do not broaden this gap-closure plan
into unrelated phase work.
</verification>

<success_criteria>
- Phase 02 verification gap is closed: BOARDS Modal.Form submit atom errors route to a safe error modal instead of raising `CaseClauseError`.
- Advisory WR-01 is closed: LIMITS tab plain character input does not raise `BadBooleanError`.
- Regression coverage exists in `test/foglet_bbs/tui/screens/sysop_test.exs` for both paths.
- No completed Phase 2 feature behavior is replanned or rewritten.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sysop-config-and-board-management/02-06-SUMMARY.md`.
</output>
