---
phase: 01.1
plan: 03
type: execute
wave: 3
depends_on: [01.1-02]
files_modified:
  - lib/foglet_bbs/tui/widgets/README.md
autonomous: true
requirements: []
spec_requirements: [REQ-8]    # Byte-identity of modal.ex + precommit gate (SPEC Acceptance Criteria final two bullets)
tags: [tui, docs, precommit, catalog]

must_haves:
  truths:
    - "`lib/foglet_bbs/tui/widgets/README.md` lists Modal.Form under the Compose / Modal section (per SPEC Acceptance Criteria + D-30)"
    - "`mix precommit` passes end-to-end (format, credo --strict, sobelow, dialyzer, warnings-as-errors)"
    - "`git diff --exit-code lib/foglet_bbs/tui/widgets/modal.ex` returns 0 — existing Modal module unchanged (REQ-8)"
    - "Full test suite `mix test` is green"
  artifacts:
    - path: "lib/foglet_bbs/tui/widgets/README.md"
      provides: "Canonical widget catalog listing including Modal.Form"
      contains: "Modal.Form"
  key_links:
    - from: "lib/foglet_bbs/tui/widgets/README.md"
      to: "lib/foglet_bbs/tui/widgets/modal/form.ex"
      via: "relative-path markdown link under Compose / Modal section"
      pattern: "modal/form.ex"
---

<objective>
Finalize Phase 01.1 by (a) adding a `Modal.Form` entry to the widget catalog README and (b) running `mix precommit` to confirm format, credo --strict, sobelow, dialyzer, and warnings-as-errors all pass. Also verify REQ-8 byte-identity one last time before phase sign-off.

Purpose: Ship-readiness gate — SPEC Acceptance Criteria explicitly requires README update + `mix precommit` green.
Output: Updated `lib/foglet_bbs/tui/widgets/README.md` + precommit verification artifact.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-shared-modal-form-primitive/01.1-SPEC.md
@.planning/phases/01.1-shared-modal-form-primitive/01.1-02-SUMMARY.md
@CLAUDE.md
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/widgets/modal/form.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Modal.Form entry to widget catalog README</name>
  <read_first>
    - lib/foglet_bbs/tui/widgets/README.md (existing catalog structure; the "Compose / Modal" table is around line 27-32)
  </read_first>
  <action>
    Edit `lib/foglet_bbs/tui/widgets/README.md`. In the existing "Compose / Modal" section (the table that currently contains rows for `Compose` and `Modal`), add a third row for `Modal.Form` immediately below the `Modal` row. Keep the existing rows byte-identical. New row:

    ```markdown
    | `Modal.Form` | [`modal/form.ex`](modal/form.ex) | Stateful modal-overlay form container with typed fields, Tab/Shift-Tab focus, submit/cancel callbacks, and caller-set inline errors (D-14, Phase 01.1) |
    ```

    Do not reorganize other sections. Do not change wording of existing rows. The only diff to this file should be the one new line plus (optionally) the section-header renaming if you want to surface the new content — but simpler is to leave the heading as-is and just add the row.
  </action>
  <acceptance_criteria>
    - `grep -q "^| \`Modal.Form\` |" lib/foglet_bbs/tui/widgets/README.md` succeeds.
    - `grep -q "modal/form.ex" lib/foglet_bbs/tui/widgets/README.md` succeeds.
    - `grep -q "Phase 01.1" lib/foglet_bbs/tui/widgets/README.md` succeeds.
    - Existing rows for `Compose` and `Modal` remain present: `grep -cE "^\\| \`(Compose|Modal|Modal\\.Form)\` \\|" lib/foglet_bbs/tui/widgets/README.md` returns 3.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Modal.Form" lib/foglet_bbs/tui/widgets/README.md && grep -q "modal/form.ex" lib/foglet_bbs/tui/widgets/README.md</automated>
  </verify>
  <done>README lists Modal.Form under Compose / Modal section with a working relative-path link to the implementation file.</done>
</task>

<task type="auto">
  <name>Task 2: Run `mix precommit` and verify REQ-8 byte-identity</name>
  <read_first>
    - CLAUDE.md (project guidelines — precommit is the authoritative gate)
    - lib/foglet_bbs/tui/widgets/modal.ex (file that MUST be byte-identical to HEAD)
  </read_first>
  <action>
    Run the project's full precommit suite and the REQ-8 regression check in sequence:

    ```bash
    mix precommit
    git diff --exit-code lib/foglet_bbs/tui/widgets/modal.ex
    mix test test/foglet_bbs/tui/widgets/modal_test.exs
    ```

    If `mix precommit` reports any failures (format, credo --strict, sobelow, dialyzer, or warnings-as-errors), fix them in `lib/foglet_bbs/tui/widgets/modal/form.ex` only. Do NOT touch `modal.ex` (REQ-8 guard). Re-run until all three commands return 0.

    Common fixes likely to be surfaced:
    - `mix format` whitespace drift → `mix format lib/foglet_bbs/tui/widgets/modal/form.ex`
    - Credo `Credo.Check.Warning.UnusedEnumOperation` / `Predicate.*` naming → rename helpers with `?` suffix where appropriate
    - Dialyzer `@spec` mismatches → tighten or correct typespecs in `form.ex`
    - Sobelow is unlikely to flag a pure TUI widget, but if it does on (e.g.) a `String.to_atom` call, replace with `String.to_existing_atom` or a whitelist lookup

    If dialyzer flags a pattern in textarea/MultiLineInput wiring that is an acceptable cross-module inference issue, prefer fixing the spec over silencing — only use `@dialyzer` skips as a last resort, and document any skip with a `# Phase 01.1:` comment.
  </action>
  <acceptance_criteria>
    - `mix precommit` exits 0.
    - `git diff --exit-code lib/foglet_bbs/tui/widgets/modal.ex` exits 0 (REQ-8 byte-identity preserved).
    - `mix test test/foglet_bbs/tui/widgets/modal_test.exs` exits 0.
    - `mix test test/foglet_bbs/tui/widgets/modal/form_test.exs` exits 0.
    - `mix test` (full suite) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix precommit && git diff --exit-code lib/foglet_bbs/tui/widgets/modal.ex && mix test</automated>
  </verify>
  <done>Precommit green, REQ-8 guard upheld, full suite green. Phase 01.1 is ship-ready.</done>
</task>

</tasks>

<verification>
- `mix precommit` returns 0
- `git diff --exit-code lib/foglet_bbs/tui/widgets/modal.ex` returns 0
- `grep -q "Modal.Form" lib/foglet_bbs/tui/widgets/README.md` succeeds
- `mix test` returns 0 (full suite)
</verification>

<success_criteria>
- All SPEC Acceptance Criteria bullets are satisfied, including the final two: README entry present and `mix precommit` passing.
- REQ-8 byte-identity of `modal.ex` explicitly proven via `git diff --exit-code` (not just inferred from a passing test).
- Phase 01.1 is ready for `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-shared-modal-form-primitive/01.1-03-SUMMARY.md` per the summary template.
</output>
