---
phase: "01.1"
plan: "01"
subsystem: "tui/widgets"
tags: [tui, raxol, widget, testing, tdd]

dependency_graph:
  requires: []
  provides:
    - "test/foglet_bbs/tui/widgets/modal/form_test.exs (TDD RED baseline for Modal.Form)"
  affects:
    - "lib/foglet_bbs/tui/widgets/modal/form.ex (plan 02 must turn these tests green)"

tech_stack:
  added: []
  patterns:
    - "TDD RED: test file created before implementation module exists"
    - "Shift-Tab shape %{key: :tab, shift: true} verified against Raxol vendor source"

key_files:
  created:
    - "test/foglet_bbs/tui/widgets/modal/form_test.exs"
  modified: []

decisions:
  - "Shift-Tab event shape is %{key: :tab, shift: true} — matches foglet widget convention (simple flat map) and is consistent with the %{type: :key, data: %{key: 'Tab', shift: true}} raw Raxol shape seen in vendor/raxol/lib/raxol/ui/components/modal/events.ex:94"

metrics:
  duration: "~8 minutes"
  completed: "2026-04-23T22:58:37Z"
  tasks_completed: 2
  files_created: 1
  files_modified: 0
---

# Phase 01.1 Plan 01: Modal.Form Test Suite (TDD RED) Summary

**One-liner:** 15-test failing suite encoding REQ-1..REQ-7 + REQ-9 against the planned Modal.Form API, with Shift-Tab shape empirically verified from Raxol vendor source.

## What Was Built

A complete failing test suite at `test/foglet_bbs/tui/widgets/modal/form_test.exs` that:

- Covers all planned Modal.Form SPEC requirements (REQ-1 through REQ-7, REQ-9)
- Uses the same test structure and helper imports as existing widget tests (`text_input_test.exs`)
- Fails at compile time with `CompileError` because `Foglet.TUI.Widgets.Modal.Form` does not yet exist — the correct TDD RED state
- Locks the API contract (init/1, handle_event/2, render/2, set_errors/2) before implementation

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Discover Shift-Tab event shape | d97829d | (research only, no file) |
| 2 | Write failing test suite | d97829d | test/foglet_bbs/tui/widgets/modal/form_test.exs |

## TDD Gate Compliance

- RED gate: `test(01.1)` commit d97829d exists — tests fail with CompileError on Modal.Form
- GREEN gate: pending (plan 02)
- REFACTOR gate: pending (plan 02/03)

## Key Finding: Shift-Tab Event Shape

**Source:** `vendor/raxol/lib/raxol/ui/components/modal/events.ex:94`
```elixir
%{type: :key, data: %{key: "Tab", shift: true}}
```

**Foglet widget convention:** `%{key: :tab, shift: true}` — the simple flat-map variant consistent with how all other keys are sent in foglet TUI widget tests (`:tab`, `:enter`, `:escape`, `:char`).

The comment in the test file cites the vendor source so plan 02's implementation can write its handler guard against this verified shape.

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- `test/foglet_bbs/tui/widgets/modal/form_test.exs` exists: FOUND
- Commit d97829d exists: FOUND
- Test count >= 15: 15 tests
- `mix test modal_test.exs` still passes: 13 tests, 0 failures
- Tests fail with CompileError on Modal.Form: CONFIRMED
