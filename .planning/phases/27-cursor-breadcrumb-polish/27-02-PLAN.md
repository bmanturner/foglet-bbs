---
phase: 27-cursor-breadcrumb-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex
  - test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs
  - test/foglet_bbs/tui/screens/login_test.exs
autonomous: true
requirements:
  - BREAD-01
must_haves:
  truths:
    - "Register shows breadcrumb parts `Foglet`, `Login`, `Register`."
    - "Forgot Password shows breadcrumb parts `Foglet`, `Login`, `Forgot Password`."
    - "Verify shows breadcrumb parts `Foglet`, `Login`, `Verify`."
    - "`:reset_consume` shows breadcrumb parts `Foglet`, `Login`, `Forgot Password`, `Enter Token` without adding password reset token behavior."
    - "Returning to Login menu shows only `Foglet`, `Login`."
  artifacts:
    - path: "lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex"
      provides: "Central auth/Login breadcrumb mapping"
      contains: "login_parts"
    - path: "test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs"
      provides: "Direct breadcrumb part and formatting coverage"
      contains: "Enter Token"
    - path: "test/foglet_bbs/tui/screens/login_test.exs"
      provides: "Login sub-state back-to-menu breadcrumb behavior"
      contains: "reset_consume"
  key_links:
    - from: "lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex"
      to: "lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex"
      via: "ScreenFrame.normalize_chrome/2 default breadcrumb_parts"
      pattern: "BreadcrumbBar.parts_for"
    - from: "lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex"
      to: "Login screen_state"
      via: "current_screen plus screen_state[:login][:sub]"
      pattern: ":reset_consume"
---

<objective>
Implement BREAD-01 in the shared `Foglet.TUI.Widgets.Chrome.BreadcrumbBar.parts_for/1` mapping.

Purpose: Auth flow location must be honest in shared chrome so later Login/Register/Forgot/Verify work can be visually verified.
Output: Auth-specific breadcrumb paths and focused tests.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-cursor-breadcrumb-polish/27-RESEARCH.md
@.planning/phases/27-cursor-breadcrumb-polish/27-VALIDATION.md
@lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex
@lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
@lib/foglet_bbs/tui/screens/login.ex
@lib/foglet_bbs/tui/screens/register.ex
@lib/foglet_bbs/tui/screens/verify.ex
@test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs
@test/foglet_bbs/tui/screens/login_test.exs

<interfaces>
`ScreenFrame.render/4` defaults `breadcrumb_parts` by calling `BreadcrumbBar.parts_for(state)`.
`BreadcrumbBar.parts_for/1` derives paths from `%{current_screen: atom, screen_state: map}`.
`Login` currently uses `screen_state[:login][:sub]` values `:menu`, `:login_form`, and `:reset_request`; Phase 27 should add recognition for `:reset_consume` in breadcrumb tests/mapping only, leaving reset token consumption to Phase 31 (`AUTH-04`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify auth breadcrumb paths centrally</name>
  <files>test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs, test/foglet_bbs/tui/screens/login_test.exs</files>
  <read_first>
    Read `test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs`, `test/foglet_bbs/tui/screens/login_test.exs`, `lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex`, `lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex`, and `lib/foglet_bbs/tui/screens/login.ex`.
  </read_first>
  <behavior>
    - Test 1: `BreadcrumbBar.parts_for(%{current_screen: :register}) == ["Foglet", "Login", "Register"]`.
    - Test 2: `BreadcrumbBar.parts_for(%{current_screen: :verify}) == ["Foglet", "Login", "Verify"]`.
    - Test 3: `BreadcrumbBar.parts_for(%{current_screen: :login, screen_state: %{login: %{sub: :reset_request}}}) == ["Foglet", "Login", "Forgot Password"]`.
    - Test 4: `BreadcrumbBar.parts_for(%{current_screen: :login, screen_state: %{login: %{sub: :reset_consume}}}) == ["Foglet", "Login", "Forgot Password", "Enter Token"]`.
    - Test 5: `BreadcrumbBar.parts_for(%{current_screen: :login, screen_state: %{login: %{sub: :menu}}}) == ["Foglet", "Login"]`.
    - Test 6: Login screen Escape from `:reset_request` and from a test state shaped as `:reset_consume` returns state to `%{sub: :menu}` or is directly represented as menu in breadcrumb fixtures; do not implement token submission.
  </behavior>
  <action>
    Add auth breadcrumb expectations to `test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs` in the `parts_for/1` describe block. Add focused Login screen tests that render or call `BreadcrumbBar.parts_for/1` against Login `screen_state` for `:reset_request`, `:reset_consume`, and `:menu`, and assert the exact strings `Foglet`, `Login`, `Forgot Password`, and `Enter Token`. The `:reset_consume` coverage must be a breadcrumb-recognizable state shape only; do not add reset token fields, Accounts calls, password validation, or delivery-mode behavior in this phase.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs test/foglet_bbs/tui/screens/login_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "Register|Forgot Password|Verify|Enter Token|reset_consume" test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs test/foglet_bbs/tui/screens/login_test.exs` shows exact auth breadcrumb coverage. `rtk rg -n "consume|reset_password|UserToken|new_password" test/foglet_bbs/tui/screens/login_test.exs` must not show new Phase 27 reset-token behavior beyond existing tests.
  </acceptance_criteria>
  <done>BREAD-01 tests fail against the old `:login`-only breadcrumb mapping and do not pull in Phase 31 reset-token behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map Login sub-states and auth screens in BreadcrumbBar</name>
  <files>lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex, test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs, test/foglet_bbs/tui/screens/login_test.exs</files>
  <read_first>
    Read `lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex`, `lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex`, `lib/foglet_bbs/tui/screens/login.ex`, and the tests added in Task 1.
  </read_first>
  <action>
    In `Foglet.TUI.Widgets.Chrome.BreadcrumbBar`, replace `defp parts_for_screen(_state, :login), do: [@root, "Login"]` with `defp parts_for_screen(state, :login), do: login_parts(state)`. Add `defp parts_for_screen(_state, :register), do: [@root, "Login", "Register"]` and `defp parts_for_screen(_state, :verify), do: [@root, "Login", "Verify"]`. Add private `login_parts/1` that reads `screen_state_for(state, :login) |> Map.get(:sub, :menu)` and returns: `[:menu]`, `:login_form`, or unknown values as `[@root, "Login"]`; `:reset_request` as `[@root, "Login", "Forgot Password"]`; `:reset_consume` as `[@root, "Login", "Forgot Password", "Enter Token"]`. Preserve `normalize_parts/1`, separators, truncation, and all existing non-auth mappings.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs test/foglet_bbs/tui/screens/login_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "defp parts_for_screen\\(state, :login\\)|defp parts_for_screen\\(_state, :register\\)|defp parts_for_screen\\(_state, :verify\\)|defp login_parts|:reset_consume|Enter Token" lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex` shows the central implementation. `rtk rg -n "breadcrumb_parts.*Register|breadcrumb_parts.*Verify" lib/foglet_bbs/tui/screens lib/foglet_bbs/tui/widgets -g '*.ex'` does not show new per-screen breadcrumb overrides.
  </acceptance_criteria>
  <done>Shared chrome breadcrumbs render the exact auth paths for Register, Forgot Password, Verify, reset-consume, and Login menu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Screen state to shared chrome | Screen-local auth state is summarized into always-visible chrome. |
| Auth sub-flow to user navigation | Breadcrumb text influences where a user thinks they are in the auth flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Spoofing | `BreadcrumbBar.parts_for/1` auth paths | mitigate | Centralize Register/Forgot/Verify/reset-consume mapping with exact string tests, preventing auth screens from appearing as root-only or unrelated locations. |
| T-27-05 | Information Disclosure | Reset consume breadcrumb | mitigate | Breadcrumb displays only `"Enter Token"` and never token values, identifiers, or password fields; tests must not introduce reset token data into chrome. |
| T-27-06 | Repudiation | Login back navigation | mitigate | Tests assert returning to `%{sub: :menu}` maps back to `["Foglet", "Login"]`, making stale breadcrumb segments detectable. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs test/foglet_bbs/tui/screens/login_test.exs`.
</verification>

<success_criteria>
BREAD-01 is satisfied in shared chrome: auth screens and Login sub-states produce the exact required breadcrumb paths, while reset token behavior remains outside Phase 27.
</success_criteria>

<output>
After completion, create `.planning/phases/27-cursor-breadcrumb-polish/27-02-SUMMARY.md`.
</output>
