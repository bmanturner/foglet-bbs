---
phase: 27-cursor-breadcrumb-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01
  - 27-02
files_modified:
  - test/foglet_bbs/tui/layout_smoke_test.exs
autonomous: true
requirements:
  - CURSOR-01
  - BREAD-01
must_haves:
  truths:
    - "Focused TextInput cursor behavior is covered in rendered Login, Register, Forgot Password, Account Profile, Account Preferences, and Sysop Site surfaces at 64x22 and 80x24."
    - "Verify remains covered as its existing slot-buffer input surface without forcing a shared TextInput conversion in Phase 27."
    - "Shared chrome breadcrumb text appears correctly in positioned render output for Register, Forgot Password, Verify, reset-consume, and Login menu states."
  artifacts:
    - path: "test/foglet_bbs/tui/layout_smoke_test.exs"
      provides: "64x22 and 80x24 render-tree verification for cursor and breadcrumb polish"
      contains: "CURSOR-01"
  key_links:
    - from: "test/foglet_bbs/tui/layout_smoke_test.exs"
      to: "Foglet.TUI.Widgets.Input.TextInput"
      via: "screen fixtures using shared TextInput.render/2"
      pattern: "TextInput.init"
    - from: "test/foglet_bbs/tui/layout_smoke_test.exs"
      to: "Foglet.TUI.Widgets.Chrome.BreadcrumbBar"
      via: "ScreenFrame render output"
      pattern: "Enter Token"
---

<objective>
Add cross-surface render smoke coverage for Phase 27 after the shared widget and chrome changes land.

Purpose: The phase goal is visual reliability across screens, not only isolated unit behavior.
Output: Layout smoke tests for 64x22 and 80x24 cursor/breadcrumb surfaces.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-cursor-breadcrumb-polish/27-RESEARCH.md
@.planning/phases/27-cursor-breadcrumb-polish/27-VALIDATION.md
@test/foglet_bbs/tui/layout_smoke_test.exs
@lib/foglet_bbs/tui/widgets/input/text_input.ex
@lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex
@lib/foglet_bbs/tui/screens/login.ex
@lib/foglet_bbs/tui/screens/register.ex
@lib/foglet_bbs/tui/screens/verify.ex

<interfaces>
`test/foglet_bbs/tui/layout_smoke_test.exs` already renders screens through `Raxol.UI.Layout.Engine.apply_layout/2` and collects positioned text elements.
Use existing fixture style in that file for `%Foglet.TUI.App{}` states.
Verify uses a six-character slot-buffer UI, not `TextInput`; Phase 27 should explicitly keep Verify covered as an auth breadcrumb and existing code-entry surface rather than replacing it with TextInput.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 64x22 and 80x24 render smoke coverage for cursor surfaces</name>
  <files>test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    Read `test/foglet_bbs/tui/layout_smoke_test.exs`, `lib/foglet_bbs/tui/screens/login.ex`, `lib/foglet_bbs/tui/screens/register.ex`, `lib/foglet_bbs/tui/widgets/modal/form.ex`, and `lib/foglet_bbs/tui/screens/verify.ex`.
  </read_first>
  <behavior>
    - Test 1: For Login form, Register combined form, Forgot Password (`screen_state[:login][:sub] == :reset_request`), Account Profile, Account Preferences, and Sysop Site fixture states, render at `{64, 22}` and `{80, 24}` and assert exactly one focused cursor marker `"▌"` appears for the focused TextInput.
    - Test 2: For a focused field with value typed as `"abcde"` then two backspaces, assert the positioned text sequence places `"▌"` after `"abc"` using a helper based on `TextWidth.display_width/1`.
    - Test 3: For Verify, render the existing buffer value, assert the code-entry surface still appears, and document in the test name that Verify is a custom slot-buffer surface covered without TextInput conversion.
  </behavior>
  <action>
    Extend `test/foglet_bbs/tui/layout_smoke_test.exs` with a `describe "Phase 27 cursor surfaces (CURSOR-01)"` block. Reuse existing `apply/1`, `text_elements/1`, and `%App{}` fixture patterns. Build TextInput values through `TextInput.handle_event/2` for the five-character/two-backspace case rather than seeding cursor position manually. Include fixture states for Login, Register, Forgot Password, Account Profile, Account Preferences, and Sysop Site using the existing screen state shapes already present in tests. For Verify, assert the custom buffer text such as `"XK7"` remains visible and that no task converts it to `TextInput`.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/layout_smoke_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "CURSOR-01|64, 22|80, 24|abcde|backspace|Verify.*slot|▌" test/foglet_bbs/tui/layout_smoke_test.exs` shows render-smoke coverage. `rtk rg -n "TextInput\\.init|TextInput\\.render" lib/foglet_bbs/tui/screens/verify.ex` returns no matches.
  </acceptance_criteria>
  <done>Layout smoke tests prove shared cursor rendering is visible on the required TextInput surfaces at both target terminal sizes, and Verify remains deliberately covered as its existing slot-buffer input surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add render smoke coverage for auth breadcrumbs</name>
  <files>test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    Read `test/foglet_bbs/tui/layout_smoke_test.exs`, `lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex`, `lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex`, `lib/foglet_bbs/tui/screens/login.ex`, `lib/foglet_bbs/tui/screens/register.ex`, and `lib/foglet_bbs/tui/screens/verify.ex`.
  </read_first>
  <behavior>
    - Test 1: Register render output at `{64, 22}` and `{80, 24}` contains `Foglet`, `Login`, and `Register` in breadcrumb text.
    - Test 2: Forgot Password render output at both sizes contains `Foglet`, `Login`, and `Forgot Password`.
    - Test 3: Verify render output at both sizes contains `Foglet`, `Login`, and `Verify`.
    - Test 4: A Login state shaped with `screen_state[:login][:sub] == :reset_consume` produces breadcrumb text containing `Foglet`, `Login`, `Forgot Password`, and `Enter Token`.
    - Test 5: A Login menu state at both sizes contains `Foglet` and `Login` but not `Forgot Password`, `Register`, `Verify`, or `Enter Token`.
  </behavior>
  <action>
    Add a `describe "Phase 27 auth breadcrumbs (BREAD-01)"` block to `test/foglet_bbs/tui/layout_smoke_test.exs`. Render through the real screen modules and `ScreenFrame` where possible, then collect positioned text values and assert exact breadcrumb segment presence. For the `:reset_consume` case, use a Login state fixture sufficient for `BreadcrumbBar.parts_for/1` and `ScreenFrame` output; do not add token inputs, password inputs, Accounts calls, or success/error behavior.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/layout_smoke_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "BREAD-01|Forgot Password|Enter Token|reset_consume|Register|Verify" test/foglet_bbs/tui/layout_smoke_test.exs` shows render-smoke breadcrumb coverage. `rtk rg -n "Accounts\\.|UserToken|new_password|reset_password" test/foglet_bbs/tui/layout_smoke_test.exs` must not show new reset-token behavior.
  </acceptance_criteria>
  <done>Layout smoke tests prove the shared chrome breadcrumb renders correct auth paths at 64x22 and 80x24 and pops back to Login menu text when sub-state returns to menu.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Phase 27 validation gate</name>
  <files>test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    Read `.planning/phases/27-cursor-breadcrumb-polish/27-VALIDATION.md` and the summary files from Plans 27-01 and 27-02 if present.
  </read_first>
  <action>
    Run the phase quick command exactly: `rtk mix test test/foglet_bbs/tui/widgets/input/text_input_test.exs test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs test/foglet_bbs/tui/screens/login_test.exs test/foglet_bbs/tui/layout_smoke_test.exs`. Then run `rtk mix precommit`. If `rtk mix precommit` fails on unrelated pre-existing failures, capture the failing command and keep the focused Phase 27 command green.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/input/text_input_test.exs test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs test/foglet_bbs/tui/screens/login_test.exs test/foglet_bbs/tui/layout_smoke_test.exs</automated>
    <automated>rtk mix precommit</automated>
  </verify>
  <acceptance_criteria>
    Focused Phase 27 test command exits 0. `rtk mix precommit` exits 0 or the summary records unrelated pre-existing failures with exact failing output.
  </acceptance_criteria>
  <done>Phase 27 has focused automated validation and a precommit result recorded for downstream verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render tree to terminal cells | Layout engine output is the last automated proxy before SSH visual verification. |
| Auth state to visible breadcrumb | Breadcrumb render output is visible during sensitive auth flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | Spoofing | Auth breadcrumb render smoke | mitigate | Assert exact auth breadcrumb segments in positioned render output at 64x22 and 80x24, including pop-back to Login menu. |
| T-27-08 | Information Disclosure | Reset consume smoke fixture | mitigate | Fixture uses only sub-state atoms and expected labels; no reset token or password values are introduced into layout tests. |
| T-27-09 | Denial of Service | Cursor rendering across compact terminals | mitigate | Layout smoke tests at 64x22 and 80x24 ensure cursor/breadcrumb additions do not crash render/layout for required auth and form surfaces. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/input/text_input_test.exs test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs test/foglet_bbs/tui/screens/login_test.exs test/foglet_bbs/tui/layout_smoke_test.exs`, then `rtk mix precommit`.
</verification>

<success_criteria>
CURSOR-01 and BREAD-01 are verified together at the rendered TUI surface for 64x22 and 80x24, including Verify's custom slot-buffer coverage and reset-consume breadcrumb recognition without Phase 31 token behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/27-cursor-breadcrumb-polish/27-03-SUMMARY.md`.
</output>
