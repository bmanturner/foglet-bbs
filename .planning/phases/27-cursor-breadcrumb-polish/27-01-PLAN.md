---
phase: 27-cursor-breadcrumb-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/foglet_bbs/tui/widgets/input/text_input.ex
  - test/foglet_bbs/tui/widgets/input/text_input_test.exs
autonomous: true
requirements:
  - CURSOR-01
must_haves:
  truths:
    - "A focused single-line TextInput renders the cursor at the active insertion point, not before the field."
    - "Typing five characters then backspacing twice leaves the visible cursor after the third displayed cell."
    - "Blurred and disabled TextInput renders contain no cursor marker."
    - "Masked TextInput rendering never exposes the raw password value while placing the cursor."
  artifacts:
    - path: "lib/foglet_bbs/tui/widgets/input/text_input.ex"
      provides: "Shared single-line TextInput cursor rendering"
      contains: "render_with_cursor_marker"
    - path: "test/foglet_bbs/tui/widgets/input/text_input_test.exs"
      provides: "Cursor position, mask, blur, and cell-width tests"
      contains: "TextWidth.display_width"
  key_links:
    - from: "lib/foglet_bbs/tui/widgets/input/text_input.ex"
      to: "Foglet.TUI.TextWidth"
      via: "cell-width-aware display splitting"
      pattern: "TextWidth"
    - from: "lib/foglet_bbs/tui/widgets/input/text_input.ex"
      to: "Raxol.UI.Components.Input.TextInput"
      via: "existing raxol_state.cursor_pos"
      pattern: "cursor_pos"
---

<objective>
Implement CURSOR-01 in the shared `Foglet.TUI.Widgets.Input.TextInput` so every screen that already uses the widget gets the same insertion-point cursor behavior.

Purpose: Later form/auth visual verification depends on seeing where typed input will land.
Output: Updated TextInput rendering and focused widget tests.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-cursor-breadcrumb-polish/27-RESEARCH.md
@.planning/phases/27-cursor-breadcrumb-polish/27-VALIDATION.md
@lib/foglet_bbs/tui/widgets/input/text_input.ex
@lib/foglet_bbs/tui/text_width.ex
@vendor/raxol/lib/raxol/ui/components/input/text_input.ex
@test/foglet_bbs/tui/widgets/input/text_input_test.exs

<interfaces>
`Foglet.TUI.Widgets.Input.TextInput.render/2` accepts `focused: true | false`, `bordered: true | false`, and required `theme: %Foglet.TUI.Theme{}`.
`TextInput.handle_event/2` delegates mutation to Raxol and stores the insertion index at `state.raxol_state.cursor_pos`.
`Foglet.TUI.TextWidth.display_width/1` is the cell-width source of truth. Use existing helpers from this module; do not introduce `String.length/1` or byte-size cursor math for visible cursor placement.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify insertion-point cursor behavior</name>
  <files>test/foglet_bbs/tui/widgets/input/text_input_test.exs</files>
  <read_first>
    Read `test/foglet_bbs/tui/widgets/input/text_input_test.exs`, `lib/foglet_bbs/tui/widgets/input/text_input.ex`, `lib/foglet_bbs/tui/text_width.ex`, and `vendor/raxol/lib/raxol/ui/components/input/text_input.ex`.
  </read_first>
  <behavior>
    - Test 1: Starting from `TextInput.init([])`, send `%{key: :char, char: "a"}` through `"e"`, then two `%{key: :backspace}` events; assert `state.raxol_state.cursor_pos == 3`, render with `focused: true`, and assert the text before the cursor has `TextWidth.display_width("abc")`.
    - Test 2: A focused input with value `"aあe"` and cursor after `"aあ"` renders the cursor after cell width `3`, proving the test uses cell width and not character count.
    - Test 3: `focused: false` renders no `"▌"` marker.
    - Test 4: A disabled render path, if represented by existing opts or Raxol state, renders no `"▌"` marker; if the widget has no disabled option, add `disabled: true` support to `render/2` and test it.
    - Test 5: A masked input with value `"secret"`, `mask_char: "*"`, and `focused: true` renders `"******"` plus cursor placement but `inspect(rendered)` does not contain `"secret"`.
  </behavior>
  <action>
    Replace the existing test named `"focused: true shows active cursor marker before input text"` with insertion-point expectations for CURSOR-01. Add a helper in the test file that walks the rendered tree text and computes the display width before the first `"▌"` marker using `Foglet.TUI.TextWidth.display_width/1`. Keep all tests under `describe "render/2 — smoke (D-18)"` or a new `describe "render/2 — insertion cursor (CURSOR-01)"`. Do not assert `String.length/1` for cursor columns. Do not add per-screen cursor tests in this plan.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/input/text_input_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "TextWidth\\.display_width|cursor_pos|focused: false|disabled: true|secret" test/foglet_bbs/tui/widgets/input/text_input_test.exs` shows the new cursor, blur/disabled, and mask coverage. `rtk rg -n "focused: true shows active cursor marker before input text|▌ mytext" test/foglet_bbs/tui/widgets/input/text_input_test.exs` returns no matches.
  </acceptance_criteria>
  <done>TextInput cursor tests fail against the old leading-marker implementation and describe the CURSOR-01 behavior exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the cursor inside TextInput using Raxol cursor_pos</name>
  <files>lib/foglet_bbs/tui/widgets/input/text_input.ex, test/foglet_bbs/tui/widgets/input/text_input_test.exs</files>
  <read_first>
    Read `lib/foglet_bbs/tui/widgets/input/text_input.ex`, `lib/foglet_bbs/tui/text_width.ex`, `vendor/raxol/lib/raxol/ui/components/input/text_input.ex`, and the tests added in Task 1.
  </read_first>
  <action>
    In `Foglet.TUI.Widgets.Input.TextInput`, remove the focused leading prefix behavior that renders `text("▌ ", ...)` before `RaxolTextInput.render/2`. Implement cursor rendering by deriving `display_text` from `rs.value`, `rs.mask_char`, and `rs.placeholder`: when `value == ""`, render the cursor at cell column 0 and retain placeholder text after it using the existing placeholder theme; when `mask_char` is present, use `String.duplicate(mask_char, length(String.graphemes(value)))`; otherwise use `value`. Split display text at `rs.cursor_pos` using grapheme-aware logic and verify visible prefix width with `TextWidth.display_width/1`. Render a `row style: %{gap: 0}` containing left text, `text("▌", fg: theme.accent.fg, style: [:bold])`, and right text. Pass through to `RaxolTextInput.render/2` only for unfocused or disabled render paths. Add `disabled = Keyword.get(opts, :disabled, Map.get(rs, :disabled, false))`; disabled inputs must behave as unfocused for cursor rendering. Keep color values routed through `Theme` and `build_input_theme/1`.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/input/text_input_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "text\\(\"▌ \"|RaxolTextInput\\.render\\(rs_with_theme, %\\{}\\)" lib/foglet_bbs/tui/widgets/input/text_input.ex` does not show the old leading-marker branch for focused inputs. `rtk rg -n "cursor_pos|TextWidth|disabled" lib/foglet_bbs/tui/widgets/input/text_input.ex` shows the shared cursor-position implementation.
  </acceptance_criteria>
  <done>Focused TextInput renders one `"▌"` at `raxol_state.cursor_pos`; unfocused and disabled inputs render no cursor; masked inputs do not leak raw values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH input event to TextInput state | Untrusted keystrokes mutate local widget state before rendering. |
| Sensitive value to render tree | Password values cross from state into UI output and must remain masked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | `TextInput.render/2` masked inputs | mitigate | Build display text from `mask_char` before cursor splitting and keep the existing `inspect(rendered) refute "secret"` test with focused cursor coverage. |
| T-27-02 | Spoofing | Focused cursor display | mitigate | Render cursor only when `focused: true` and not disabled; add tests for focused, blurred, and disabled output. |
| T-27-03 | Tampering | Cursor cell math | mitigate | Use `Foglet.TUI.TextWidth.display_width/1` in tests and implementation-adjacent helpers; reject byte/character-length cursor assertions for visible columns. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/input/text_input_test.exs`.
</verification>

<success_criteria>
CURSOR-01 is satisfied at the shared widget layer: the insertion cursor follows `raxol_state.cursor_pos`, cell-width tests cover backspace and wide graphemes, and masked focused rendering does not leak raw values.
</success_criteria>

<output>
After completion, create `.planning/phases/27-cursor-breadcrumb-polish/27-01-SUMMARY.md`.
</output>
