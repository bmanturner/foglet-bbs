---
phase: 40-verification-documentation
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - lib/foglet_bbs/tui/app.ex
  - lib/foglet_bbs/tui/screen.ex
  - lib/foglet_bbs/tui/render_fixtures.ex
  - lib/foglet_bbs/tui/screens/post_reader.ex
  - lib/foglet_bbs/tui/screens/post_composer.ex
  - lib/foglet_bbs/tui/screens/new_thread.ex
  - test/foglet_bbs/tui/app_runtime_contract_test.exs
  - test/foglet_bbs/tui/screens/post_reader_test.exs
  - test/foglet_bbs/tui/screens/post_composer_test.exs
  - test/foglet_bbs/tui/screens/new_thread_test.exs
autonomous: true
requirements: [VERIFY-02, VERIFY-03]
must_haves:
  truths:
    - "D-03: production dependence on transitional screen callbacks render/1, handle_key/2, and init_screen_state/1 is removed or explicitly bounded after fixtures/tests move to init/1, update/3, and render/2."
    - "D-04: nested helper modules with local handle_key/2 functions are not swept into this cleanup unless they are part of the production Foglet.TUI.Screen/App fallback boundary."
  artifacts:
    - path: "lib/foglet_bbs/tui/app.ex"
      provides: "App dispatch through update/3/render/2 without production legacy callback fallback"
      contains: "route_screen_update"
---

<objective>
Remove or explicitly bound the production legacy callback surface. App should route screen behavior through `init/1`, `update/3`, and `render/2`; render fixtures and migrated tests should stop depending on legacy `init_screen_state/1`, `handle_key/2`, and `render/1` where the new reducer/render boundary exists.
</objective>

<context>
@.planning/phases/40-verification-documentation/40-CONTEXT.md
@.planning/phases/40-verification-documentation/40-RESEARCH.md
@.planning/phases/40-verification-documentation/40-PATTERNS.md
@AGENTS.md
</context>

<tasks>
<task type="execute">
  <name>Task 1: Move fixtures and tests to new contract seams</name>
  <files>
    lib/foglet_bbs/tui/render_fixtures.ex
    test/foglet_bbs/tui/screens/post_reader_test.exs
    test/foglet_bbs/tui/screens/post_composer_test.exs
    test/foglet_bbs/tui/screens/new_thread_test.exs
  </files>
  <read_first>
    lib/foglet_bbs/tui/render_fixtures.ex
    lib/foglet_bbs/tui/screens/post_reader.ex
    lib/foglet_bbs/tui/screens/post_composer.ex
    lib/foglet_bbs/tui/screens/new_thread.ex
    test/foglet_bbs/tui/screens/post_reader_test.exs
    test/foglet_bbs/tui/screens/post_composer_test.exs
    test/foglet_bbs/tui/screens/new_thread_test.exs
  </read_first>
  <action>
    Replace direct migrated-screen legacy calls with `Screen.init(context)`, `Screen.update(message, local_state, context)`, and `Screen.render(local_state, context)`. For render fixtures, use `App.build_context/1` and `App.put_screen_state/3` with `init/1` or first-class `State.new/1` constructors instead of `init_screen_state/1` for Login, Register, Verify, Account, Moderation, and Sysop where practical.
  </action>
  <acceptance_criteria>
    - `rtk mix test test/foglet_bbs/tui/screens/post_reader_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs test/foglet_bbs/tui/screens/new_thread_test.exs` exits 0.
    - `rtk mix foglet.tui.render main_menu --width 80 --height 24` exits 0.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Remove App production fallback dispatch</name>
  <files>
    lib/foglet_bbs/tui/app.ex
    test/foglet_bbs/tui/app_runtime_contract_test.exs
    test/foglet_bbs/tui/app_test.exs
  </files>
  <read_first>
    lib/foglet_bbs/tui/app.ex (`do_update({:key, ...})`, `render_screen/1`, `route_screen_update/3`)
    test/foglet_bbs/tui/app_runtime_contract_test.exs
    test/foglet_bbs/tui/app_test.exs
  </read_first>
  <action>
    Delete the `handle_key/2` fallback branch from App key dispatch and the `render/1` fallback branch from `render_screen/1` once all production known screens export the new callbacks. Missing `update/3` should no-op only for non-production override screens without crashing; production known screens should be covered by tests. Add or update App runtime tests proving keys and renders for a sample new-contract screen do not require legacy callbacks.
  </action>
  <acceptance_criteria>
    - `rtk rg -n ":handle_key|, :render, \\[state\\]|process_screen_commands\\(" lib/foglet_bbs/tui/app.ex` does not show production fallback dispatch to screen `handle_key/2` or `render/1`.
    - `rtk mix test test/foglet_bbs/tui/app_runtime_contract_test.exs test/foglet_bbs/tui/app_test.exs` exits 0.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Update Screen behaviour docs and optional callbacks</name>
  <files>lib/foglet_bbs/tui/screen.ex</files>
  <read_first>
    lib/foglet_bbs/tui/screen.ex
    .planning/phases/40-verification-documentation/40-CONTEXT.md
  </read_first>
  <action>
    Rewrite `Foglet.TUI.Screen` module docs so the canonical contract is `init/1`, `update/3`, `render/2`, and optional `subscriptions/2`. Remove transitional callback declarations from the behaviour if no production/test path still needs them; otherwise move them into an explicitly bounded non-production compatibility note with the exact remaining callers.
  </action>
  <acceptance_criteria>
    - `rtk mix test test/foglet_bbs/tui/screen_test.exs test/foglet_bbs/tui/app_runtime_contract_test.exs` exits 0.
    - `rtk rg -n "transitional|legacy render callback|legacy key handler|init_screen_state" lib/foglet_bbs/tui/screen.ex` either exits 1 or returns text that explicitly says the remaining surface is test-only/bounded.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run `rtk mix test test/foglet_bbs/tui/app_runtime_contract_test.exs test/foglet_bbs/tui/app_test.exs` and affected screen tests.
</verification>

<success_criteria>
- App production dispatch no longer depends on screen legacy callbacks.
- Tests/fixtures use the new screen contract or document any bounded compatibility exceptions.
</success_criteria>
