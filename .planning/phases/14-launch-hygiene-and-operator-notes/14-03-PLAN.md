---
phase: 14-launch-hygiene-and-operator-notes
plan: 3
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - README.md
  - test/readme_operator_notes_test.exs
  - .planning/phases/14-launch-hygiene-and-operator-notes/14-BLOCKERS.md
autonomous: true
requirements:
  - HYGN-03
  - HYGN-02
requirements_addressed:
  - HYGN-03
  - HYGN-02
must_haves:
  truths:
    - "A sysop can use root README.md to understand SSH-first operation, Email mode, no-email mode, relevant break-glass Mix tasks, launch caveats, and nested-doc status."
    - "README.md does not present browser admin, webhook notifications, email digests, delivery retry queues, outbound delivery logs, or full case-management moderation as v1.2 capabilities."
    - "README.md tells operators to keep SMTP secrets in environment/runtime config, not DB-backed runtime config."
  artifacts:
    - path: "README.md"
      provides: "Canonical pre-alpha operator guide"
      contains: "## Operator Notes"
    - path: "test/readme_operator_notes_test.exs"
      provides: "Executable README operator-note checks"
      contains: "SMTP mode"
  key_links:
    - from: "README.md"
      to: "Foglet.Config.delivery_mode/0"
      via: "operator delivery-mode notes"
      pattern: "delivery_mode"
    - from: "README.md"
      to: "lib/mix/tasks"
      via: "break-glass task list"
      pattern: "mix foglet\\."
    - from: "test/readme_operator_notes_test.exs"
      to: "README.md"
      via: "file-content assertions"
      pattern: "File\\.read!"
---

<objective>
Replace root README target-state copy with canonical pre-alpha operator notes.

Purpose: HYGN-03 and D-08 through D-10 require root `README.md` to be the practical operator guide for SMTP and no-email operation. It must remain honest about launch caveats and unsupported workflows.

Output: Updated root README, executable README assertions, and final blocker-log status.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-launch-hygiene-and-operator-notes/14-SPEC.md
@.planning/phases/14-launch-hygiene-and-operator-notes/14-CONTEXT.md
@.planning/phases/14-launch-hygiene-and-operator-notes/14-RESEARCH.md
@.planning/phases/14-launch-hygiene-and-operator-notes/14-BLOCKERS.md
@.planning/phases/14-launch-hygiene-and-operator-notes/14-01-SUMMARY.md
@.planning/phases/14-launch-hygiene-and-operator-notes/14-02-SUMMARY.md
@lib/mix/tasks/foglet.user.reset_password.ex
@lib/mix/tasks/foglet.user.verification_code.ex
@lib/mix/tasks/foglet.user.status.ex
@lib/mix/tasks/foglet.board_subscriptions.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 14-03-01: Add executable README operator-note contract</name>
  <files>test/readme_operator_notes_test.exs, README.md</files>
  <read_first>
    AGENTS.md
    README.md
    .planning/phases/14-launch-hygiene-and-operator-notes/14-SPEC.md
    .planning/phases/14-launch-hygiene-and-operator-notes/14-CONTEXT.md
    .planning/phases/14-launch-hygiene-and-operator-notes/14-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: README contains `## Operator Notes`.
    - Test 2: README contains `SSH-first`, `SMTP mode`, `no-email mode`, `delivery_mode`, `mix foglet.user.reset_password`, `mix foglet.user.verification_code`, `mix foglet.user.status`, and `mix foglet.board_subscriptions`.
    - Test 3: README contains `nested docs` and explains those docs may be future-oriented or internal design material.
    - Test 4: README does not contain unsupported target-state claims: `Opt-in email digests`, `webhook notifications`, `delivery retry queue`, `outbound delivery logs`, `full case-management`, or `end-user web UI`.
  </behavior>
  <action>
    Create `test/readme_operator_notes_test.exs` with `use ExUnit.Case, async: true`. The test should read `README.md` using `File.read!/1`, assert required operator-note phrases, and refute the unsupported phrases listed above. Write the tests first so the current target-state README fails. Keep assertions phrase-level and operator-focused; do not snapshot the whole README.
  </action>
  <acceptance_criteria>
    `rg -n "Operator Notes|SMTP mode|no-email mode|delivery_mode|mix foglet\\.user\\.reset_password|mix foglet\\.board_subscriptions|nested docs" test/readme_operator_notes_test.exs` shows the required README contract.
    `rg -n "Opt-in email digests|webhook notifications|delivery retry queue|outbound delivery logs|full case-management|end-user web UI" test/readme_operator_notes_test.exs` shows unsupported claims are refuted.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/readme_operator_notes_test.exs</automated>
  </verify>
  <done>README has an executable operator-note contract that initially captures HYGN-03 and launch-honesty expectations.</done>
</task>

<task type="auto">
  <name>Task 14-03-02: Rewrite README as pre-alpha operator guide</name>
  <files>README.md, test/readme_operator_notes_test.exs, .planning/phases/14-launch-hygiene-and-operator-notes/14-BLOCKERS.md</files>
  <read_first>
    AGENTS.md
    README.md
    test/readme_operator_notes_test.exs
    .planning/phases/14-launch-hygiene-and-operator-notes/14-BLOCKERS.md
    .planning/phases/14-launch-hygiene-and-operator-notes/14-01-SUMMARY.md
    .planning/phases/14-launch-hygiene-and-operator-notes/14-02-SUMMARY.md
    lib/mix/tasks/foglet.user.reset_password.ex
    lib/mix/tasks/foglet.user.verification_code.ex
    lib/mix/tasks/foglet.user.status.ex
    lib/mix/tasks/foglet.board_subscriptions.ex
  </read_first>
  <action>
    Rewrite root `README.md` as the canonical pre-alpha operator guide per D-08, D-09, and D-10. Keep the project identity concise, then include a section headed exactly `## Operator Notes`. The README must state these concrete points:
    `Foglet is SSH-first; Phoenix is operational infrastructure and not an end-user browser workflow.`
    `SMTP mode uses delivery_mode=email for transactional delivery, while SMTP host, port, username, password, and adapter settings belong in environment/runtime config, not DB-backed runtime config.`
    `No-email mode uses delivery_mode=no_email and requires explicit operator retrieval workflows; no email is sent in this mode.`
    Include break-glass task examples for `mix foglet.user.reset_password HANDLE`, `mix foglet.user.verification_code HANDLE`, `mix foglet.user.status --actor SYSOP --user HANDLE --status active`, and `mix foglet.board_subscriptions list --user HANDLE`.
    Include launch caveats stating browser admin, webhook notifications, email digests, delivery retry queues, outbound delivery logs, and full case-management moderation are not v1.2 pre-alpha capabilities.
    Include nested-doc status using the phrase `nested docs may contain future-oriented or internal design material`.
    Do not create `docs/SYSOP.md` or move the operator guide into nested docs.
    If `14-BLOCKERS.md` contains real blockers, add a short `Known Launch Blockers` section in README that points to `.planning/phases/14-launch-hygiene-and-operator-notes/14-BLOCKERS.md` without claiming the blocked behavior works.
  </action>
  <acceptance_criteria>
    `rg -n "## Operator Notes|SSH-first|SMTP mode|no-email mode|delivery_mode=email|delivery_mode=no_email|environment/runtime config|mix foglet\\.user\\.reset_password|mix foglet\\.user\\.verification_code|mix foglet\\.user\\.status|mix foglet\\.board_subscriptions|nested docs may contain future-oriented or internal design material" README.md` returns matches.
    `rg -n "Opt-in email digests|webhook notifications|delivery retry queue|outbound delivery logs|full case-management|end-user web UI" README.md` returns no matches except inside a caveat sentence that explicitly says the feature is not a v1.2 pre-alpha capability.
    `test -f docs/SYSOP.md` must fail unless the file already existed before this task; do not create it.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/readme_operator_notes_test.exs test/foglet_bbs/tui/screens/launch_copy_audit_test.exs</automated>
  </verify>
  <done>Root README is the canonical pre-alpha operator guide and passes executable launch-honesty checks.</done>
</task>

<task type="auto">
  <name>Task 14-03-03: Run phase hygiene gate</name>
  <files>README.md, test/readme_operator_notes_test.exs, .planning/phases/14-launch-hygiene-and-operator-notes/14-BLOCKERS.md</files>
  <read_first>
    AGENTS.md
    .planning/phases/14-launch-hygiene-and-operator-notes/14-VALIDATION.md
    README.md
    test/readme_operator_notes_test.exs
    test/foglet_bbs/tui/screens/launch_copy_audit_test.exs
    .planning/phases/14-launch-hygiene-and-operator-notes/14-BLOCKERS.md
  </read_first>
  <action>
    Run the targeted README and launch-copy checks, then run `rtk mix precommit`. If precommit fails because of unrelated dirty worktree files from before Phase 14, do not revert user changes; record the exact failing command and file names in the plan summary. If failures are caused by Phase 14 files, fix them. Ensure the final summary lists any real blocker rows from `14-BLOCKERS.md`.
  </action>
  <acceptance_criteria>
    `rtk mix test test/readme_operator_notes_test.exs test/foglet_bbs/tui/screens/launch_copy_audit_test.exs` passes.
    `rtk mix precommit` passes, or the summary explicitly states the unrelated pre-existing files that blocked it.
    `rg -n "Known Launch Blockers|No upstream Phase 9-13 blockers" README.md .planning/phases/14-launch-hygiene-and-operator-notes/14-BLOCKERS.md` shows the blocker state is visible.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/readme_operator_notes_test.exs test/foglet_bbs/tui/screens/launch_copy_audit_test.exs && rtk mix precommit</automated>
  </verify>
  <done>Phase 14 operator notes and launch-copy checks are verified, with precommit status or unrelated-worktree blockers clearly reported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> operator actions | Operators may configure delivery mode, secrets, and break-glass tasks based on README instructions. |
| Documentation -> supported-scope expectations | README can incorrectly imply unsupported workflows or hide launch blockers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Information disclosure | README SMTP notes | mitigate | README must state SMTP secrets belong in environment/runtime config, not DB-backed runtime config. |
| T-14-08 | Spoofing | README supported-feature claims | mitigate | README tests and copy audit refute unsupported browser, webhook, digest, retry/log, and case-management claims. |
| T-14-09 | Repudiation | Known blocker communication | mitigate | README must point to blocker log when blockers exist, and the summary must report final blocker state. |
</threat_model>

<verification>
Run `rtk mix test test/readme_operator_notes_test.exs test/foglet_bbs/tui/screens/launch_copy_audit_test.exs` and `rtk mix precommit`.
</verification>

<success_criteria>
HYGN-03 is complete: root README is practical for SMTP and no-email operation, honors SSH-first scope, and does not imply unsupported v1.2 features.
</success_criteria>

<output>
After completion, create `.planning/phases/14-launch-hygiene-and-operator-notes/14-03-SUMMARY.md`
</output>
