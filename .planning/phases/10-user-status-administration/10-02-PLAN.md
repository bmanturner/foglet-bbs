---
phase: 10-user-status-administration
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - lib/foglet_bbs/tui/screens/sysop.ex
  - lib/foglet_bbs/tui/screens/sysop/state.ex
  - lib/foglet_bbs/tui/screens/sysop/users_view.ex
  - test/foglet_bbs/tui/screens/sysop_test.exs
autonomous: true
requirements:
  - USER-01
  - USER-02
  - USER-03
must_haves:
  truths:
    - "Sysop can open the USERS tab and see pending users."
    - "Sysop can approve/reject pending users and suspend/reactivate active/suspended users from terminal UI."
    - "USERS tab failures surface terminal copy instead of silently no-oping."
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/sysop/users_view.ex"
      provides: "screen-local USERS tab state/render/key handling"
      exports: ["init/1", "handle_key/2", "render/2"]
    - path: "lib/foglet_bbs/tui/screens/sysop/state.ex"
      provides: "users_view field in Sysop screen state"
      contains: "users_view"
    - path: "lib/foglet_bbs/tui/screens/sysop.ex"
      provides: "USERS tab delegation"
      contains: "UsersView"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/sysop/users_view.ex"
      to: "lib/foglet_bbs/accounts.ex"
      via: "list and transition API calls"
      pattern: "Accounts\\.(list_user_status_admin_targets|transition_user_status)"
    - from: "lib/foglet_bbs/tui/screens/sysop.ex"
      to: "lib/foglet_bbs/tui/screens/sysop/users_view.ex"
      via: "delegate_to_submodule"
      pattern: "\"USERS\" -> delegate_to_submodule"
---

<objective>
Replace the Sysop USERS placeholder with a delegated terminal-native user administration tab.

Purpose: USER-01 through USER-03 require a real SSH/TUI sysop path that consumes the Accounts transition boundary without adding browser administration.
Output: `UsersView` submodule, Sysop state/delegation wiring, and focused screen tests.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-user-status-administration/10-CONTEXT.md
@.planning/phases/10-user-status-administration/10-RESEARCH.md
@.planning/phases/10-user-status-administration/10-VALIDATION.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/screens/sysop.ex
@lib/foglet_bbs/tui/screens/sysop/state.ex
@lib/foglet_bbs/tui/screens/sysop/boards_view.ex
@test/foglet_bbs/tui/screens/sysop_test.exs

<interfaces>
Use the Plan 01 Accounts contracts:
```elixir
Foglet.Accounts.list_user_status_admin_targets(actor)
Foglet.Accounts.transition_user_status(actor, target_user_or_handle, target_status)
```

Create this submodule contract:
```elixir
Foglet.TUI.Screens.Sysop.UsersView.init(current_user: user)
Foglet.TUI.Screens.Sysop.UsersView.handle_key(event, users_view) :: {users_view, events}
Foglet.TUI.Screens.Sysop.UsersView.render(users_view, theme)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add UsersView state, render, and list loading</name>
  <read_first>
    .planning/phases/10-user-status-administration/10-CONTEXT.md
    .planning/phases/10-user-status-administration/10-VALIDATION.md
    docs/raxol/getting-started/WIDGET_GALLERY.md
    lib/foglet_bbs/tui/widgets/README.md
    lib/foglet_bbs/tui/screens/sysop/users_view.ex
    lib/foglet_bbs/tui/screens/sysop/state.ex
    lib/foglet_bbs/tui/screens/sysop/boards_view.ex
    test/foglet_bbs/tui/screens/sysop_test.exs
  </read_first>
  <files>lib/foglet_bbs/tui/screens/sysop/users_view.ex, lib/foglet_bbs/tui/screens/sysop/state.ex, test/foglet_bbs/tui/screens/sysop_test.exs</files>
  <behavior>
    - Test 1: USERS tab render includes pending, active, suspended, and rejected non-deleted handles returned by Accounts.
    - Test 2: deleted users are absent from USERS tab render.
    - Test 3: no users render `"No administrable users."` and key hints still render.
  </behavior>
  <action>Create `Foglet.TUI.Screens.Sysop.UsersView` with `%UsersView{current_user, groups, rows, selection_index, message}`. `init/1` calls `Accounts.list_user_status_admin_targets(current_user)` and stores grouped rows as `{:pending, user}`, `{:active, user}`, `{:suspended, user}`, and `{:rejected, user}` in that group order. `render/2` displays title `"User status administration"`, rows like `"pending  @handle  email@example.com"`, selected row via `Foglet.TUI.Widgets.List.ListRow.render/3`, and footer `"[A] Approve  [R] Reject  [S] Suspend  [U] Reactivate  [j/k] Move"`. Add `users_view: nil` to `Sysop.State` type and defstruct. Do not query Repo directly in UsersView; call only `Foglet.Accounts.list_user_status_admin_targets/1`.</action>
  <acceptance_criteria>
    - `rg -n 'defmodule Foglet\\.TUI\\.Screens\\.Sysop\\.UsersView' lib/foglet_bbs/tui/screens/sysop/users_view.ex` returns one match.
    - `rg -n 'list_user_status_admin_targets' lib/foglet_bbs/tui/screens/sysop/users_view.ex test/foglet_bbs/tui/screens/sysop_test.exs` returns matches.
    - `rg -n 'users_view' lib/foglet_bbs/tui/screens/sysop/state.ex` returns matches in type and defstruct sections.
    - `rg -n 'No administrable users\\.|User status administration|\\[A\\] Approve' lib/foglet_bbs/tui/screens/sysop/users_view.ex test/foglet_bbs/tui/screens/sysop_test.exs` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/sysop_test.exs</automated>
  </verify>
  <done>USERS tab has a delegated state/render module that lists Accounts-provided non-deleted status groups.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire USERS tab actions through Accounts transitions</name>
  <read_first>
    .planning/phases/10-user-status-administration/10-CONTEXT.md
    .planning/phases/10-user-status-administration/10-VALIDATION.md
    lib/foglet_bbs/tui/screens/sysop.ex
    lib/foglet_bbs/tui/screens/sysop/users_view.ex
    lib/foglet_bbs/tui/screens/sysop/boards_view.ex
    test/foglet_bbs/tui/screens/sysop_test.exs
  </read_first>
  <files>lib/foglet_bbs/tui/screens/sysop.ex, lib/foglet_bbs/tui/screens/sysop/users_view.ex, test/foglet_bbs/tui/screens/sysop_test.exs</files>
  <behavior>
    - Test 1: pressing `A` on a pending row calls Accounts and refreshes the row as active.
    - Test 2: pressing `R` on a pending row refreshes the row as rejected.
    - Test 3: pressing `S` on an active row refreshes the row as suspended.
    - Test 4: pressing `U` on a suspended row refreshes the row as active.
    - Test 5: invalid action on the selected row sets visible message text containing `"Invalid status transition."`.
  </behavior>
  <action>Alias `Foglet.TUI.Screens.Sysop.UsersView` in `Sysop`, render `"USERS"` with `UsersView.render/2`, and delegate `"USERS"` keys via `delegate_to_submodule(event, state, ss, :users_view, UsersView)`. In `UsersView.handle_key/2`, support `j`/Down and `k`/Up movement plus `A`/`a` => `:active`, `R`/`r` => `:rejected`, `S`/`s` => `:suspended`, `U`/`u` => `:active`. Call `Accounts.transition_user_status(current_user, selected_user, target_status)`, refresh lists after success, and set message strings exactly: `"Status changed: @#{handle} #{from} -> #{to}."`, `"Forbidden."`, `"User not found."`, `"Deleted users cannot be changed."`, `"Invalid status transition."`, `"Invalid target status."`, and `"Status changed: @#{handle} #{from} -> #{to}. Notification failed."` when delivery is `{:failed, _}` in later plans. Do not add Repo calls, browser routes, role editing, search, or pagination.</action>
  <acceptance_criteria>
    - `rg -n 'UsersView' lib/foglet_bbs/tui/screens/sysop.ex` returns alias, render, and delegate matches.
    - `rg -n 'transition_user_status' lib/foglet_bbs/tui/screens/sysop/users_view.ex` returns one or more matches.
    - `rg -n 'Forbidden\\.|Deleted users cannot be changed\\.|Invalid status transition\\.|Notification failed' lib/foglet_bbs/tui/screens/sysop/users_view.ex test/foglet_bbs/tui/screens/sysop_test.exs` returns matches.
    - `rg -n 'FogletBbs\\.Repo|Repo\\.' lib/foglet_bbs/tui/screens/sysop/users_view.ex` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/sysop_test.exs</automated>
  </verify>
  <done>USERS tab administers statuses through Accounts and shows concrete result copy for success and failure states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard input -> UsersView | Sysop key presses request status changes. |
| UsersView -> Accounts | TUI crosses into authoritative domain APIs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Elevation of Privilege | USERS tab actions | mitigate | UsersView calls `Accounts.transition_user_status/3`; no TUI-side authorization shortcut is trusted. |
| T-10-07 | Tampering | USERS selected row | mitigate | Selected target is the loaded `%User{}` from Accounts; mutation still validates transition in Accounts. |
| T-10-08 | Repudiation | terminal result copy | mitigate | Render exact success/failure message strings from tagged Accounts results. |
| T-10-09 | Information Disclosure | user list | mitigate | `list_user_status_admin_targets/1` is sysop-authorized and excludes deleted users. |
| T-10-10 | Spoofing | browser/admin surface confusion | accept | This plan adds only SSH/TUI modules under `Foglet.TUI`; no Phoenix route is introduced. |
</threat_model>

<verification>
Run:
```bash
rtk mix test test/foglet_bbs/tui/screens/sysop_test.exs
rtk mix compile --warnings-as-errors
```
</verification>

<success_criteria>
USER-01, USER-02, and USER-03 are reachable from the Sysop USERS tab through delegated TUI state and Accounts APIs, with no Repo mutation or browser workflow in the TUI.
</success_criteria>

<output>
After completion, create `.planning/phases/10-user-status-administration/10-02-SUMMARY.md`
</output>
