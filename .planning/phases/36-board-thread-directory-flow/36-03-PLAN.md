---
phase: 36-board-thread-directory-flow
plan: "03"
type: execute
wave: 2
depends_on:
  - "36-01"
  - "36-02"
files_modified:
  - lib/foglet_bbs/tui/app.ex
  - lib/foglet_bbs/tui/render_fixtures.ex
  - lib/foglet_bbs/tui/screens/board_list.ex
  - lib/foglet_bbs/tui/screens/board_list/state.ex
  - lib/foglet_bbs/tui/screens/thread_list.ex
  - lib/foglet_bbs/tui/screens/thread_list/state.ex
  - test/foglet_bbs/tui/app_runtime_contract_test.exs
  - test/foglet_bbs/tui/app_test.exs
  - test/foglet_bbs/tui/layout_smoke_test.exs
  - test/foglet_bbs/tui/screens/board_list_test.exs
  - test/foglet_bbs/tui/screens/thread_list_test.exs
autonomous: true
requirements:
  - SCREEN-03
user_setup: []
must_haves:
  truths:
    - "D-09 - Preserve legacy top-level current_board/current_thread only as a temporary compatibility bridge where later unmigrated Phase 37 screens still require them."
    - "D-12 - Remove or reduce App clauses for load_boards, boards_loaded, subscribe_to_board, unsubscribe_from_board, board_subscription_changed, load_threads, and threads_loaded so App no longer owns BoardList/ThreadList local data or feedback."
    - "D-13 - App remains responsible only for generic runtime concerns including modal precedence, SizeGate, Raxol callbacks, session lifecycle, generic effect interpretation, command dispatch, and PubSub forwarding."
    - "D-15 - App tests prove generic task-result routing and absence of BoardList/ThreadList local-flow mutation, not old board/thread App clauses."
    - "D-16 - Preserve category expand/collapse, board leaf navigation, subscribe/unsubscribe feedback, required-subscription protection, unread refresh behavior, thread sorting/metadata/glyphs, compose origin, Q-back behavior, SizeGate, modal precedence, and canonical board/thread render smoke."
    - "D-17 - Avoid tests that only assert text presence; use state, effects, task results, route params, and render smoke/layout contracts."
  artifacts:
    - "App no longer has board/thread directory local-flow ownership clauses after BoardList and ThreadList migrate."
    - "ThreadList board PubSub topic derivation uses route params or screen state instead of App current_board."
    - "RenderFixtures and layout smoke tests seed BoardList/ThreadList through screen-local state."
    - "Module docs describe BoardList/ThreadList as screen-owned reducers."
  key_links:
    - "App.update({:command_result, {:screen_task_result, :board_list, op, result}}, state) routes to BoardList.update/3."
    - "App.update({:command_result, {:screen_task_result, :thread_list, :load_threads, result}}, state) routes to ThreadList.update/3."
    - "App.subscribe/1 includes board:<id> for ThreadList from current route params or ThreadList.State.board_id."
---

<objective>
Remove App ownership of BoardList and ThreadList local data after both screens
own their reducers, then update PubSub topic derivation, render fixtures, smoke
tests, and docs so the board/thread directory flow uses route params and
screen-local state as its source of truth.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-board-thread-directory-flow/36-SPEC.md
@.planning/phases/36-board-thread-directory-flow/36-CONTEXT.md
@.planning/phases/36-board-thread-directory-flow/36-RESEARCH.md
@.planning/phases/36-board-thread-directory-flow/36-VALIDATION.md
@.planning/phases/34-runtime-contract-effects/34-CONTEXT.md
@.planning/phases/35-auth-home-screens/35-CONTEXT.md
@lib/foglet_bbs/tui/screen.ex
@lib/foglet_bbs/tui/context.ex
@lib/foglet_bbs/tui/effect.ex
@lib/foglet_bbs/tui/app.ex
@lib/foglet_bbs/tui/render_fixtures.ex
@lib/foglet_bbs/tui/screens/board_list.ex
@lib/foglet_bbs/tui/screens/board_list/state.ex
@lib/foglet_bbs/tui/screens/thread_list.ex
@lib/foglet_bbs/tui/screens/thread_list/state.ex
@test/foglet_bbs/tui/app_runtime_contract_test.exs
@test/foglet_bbs/tui/app_test.exs
@test/foglet_bbs/tui/layout_smoke_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove App board/thread directory local-flow clauses</name>
  <files>lib/foglet_bbs/tui/app.ex, test/foglet_bbs/tui/app_runtime_contract_test.exs, test/foglet_bbs/tui/app_test.exs</files>
  <read_first>lib/foglet_bbs/tui/app.ex, lib/foglet_bbs/tui/screens/board_list.ex, lib/foglet_bbs/tui/screens/thread_list.ex, test/foglet_bbs/tui/app_runtime_contract_test.exs, test/foglet_bbs/tui/app_test.exs, .planning/phases/36-board-thread-directory-flow/36-SPEC.md</read_first>
  <action>
After Plans 36-01 and 36-02 have migrated BoardList and ThreadList, remove or
reduce these App clauses so they no longer mutate BoardList/ThreadList local
data:
- `defp do_update({:load_boards}, state)`
- `defp do_update({:boards_loaded, boards}, state)`
- `defp do_update({:subscribe_to_board, board_id}, state)`
- `defp do_update({:unsubscribe_from_board, board_id}, state)`
- `defp do_update({:board_subscription_changed, action, result}, state)` variants
- `defp do_update({:load_threads, board_id}, state)`
- `defp do_update({:threads_loaded, threads}, state)`
- `defp put_board_list_feedback(state, feedback)`

Do not remove unrelated App clauses such as `{:load_boards_for_new_thread}`,
`{:boards_for_new_thread_loaded, ...}`, `{:load_posts, ...}`, or
`{:posts_loaded, ...}`; those belong to later phases or existing composer/post
flows.

If a small compatibility branch remains necessary for Phase 37 PostReader or
NewThread, name it explicitly with a comment containing `Phase 37 compatibility`
and prove with tests that BoardList and ThreadList do not use it as their source
of truth.

Add or update App tests so they prove:
- `{:command_result, {:screen_task_result, :board_list, :load_boards, {:ok, directory}}}` updates only `screen_state[:board_list]` through `BoardList.update/3`;
- `{:command_result, {:screen_task_result, :thread_list, :load_threads, {:ok, threads}}}` updates only `screen_state[:thread_list]` through `ThreadList.update/3`;
- App no longer assigns `state.board_list` from `{:boards_loaded, boards}`;
- App no longer assigns `state.current_thread_list` from `{:threads_loaded, threads}`;
- SizeGate and modal precedence still block screen key routing exactly as before.
  </action>
  <verify>rtk mix test test/foglet_bbs/tui/app_runtime_contract_test.exs test/foglet_bbs/tui/app_test.exs</verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/app.ex` does not contain `defp do_update({:boards_loaded, boards}`.
    - `lib/foglet_bbs/tui/app.ex` does not contain `defp do_update({:threads_loaded, threads}`.
    - `lib/foglet_bbs/tui/app.ex` does not contain `defp put_board_list_feedback`.
    - `test/foglet_bbs/tui/app_test.exs` contains `:screen_task_result, :board_list`.
    - `test/foglet_bbs/tui/app_test.exs` contains `:screen_task_result, :thread_list`.
    - `test/foglet_bbs/tui/app_test.exs` does not contain `assigns current_thread_list`.
    - `rtk mix test test/foglet_bbs/tui/app_runtime_contract_test.exs test/foglet_bbs/tui/app_test.exs` exits 0.
  </acceptance_criteria>
  <done>App routes board/thread results generically and no longer owns BoardList/ThreadList local data.</done>
</task>

<task type="auto">
  <name>Task 2: Derive ThreadList PubSub topics from route params or screen state</name>
  <files>lib/foglet_bbs/tui/app.ex, test/foglet_bbs/tui/app_test.exs</files>
  <read_first>lib/foglet_bbs/tui/app.ex, lib/foglet_bbs/tui/screens/thread_list/state.ex, test/foglet_bbs/tui/app_test.exs, .planning/phases/36-board-thread-directory-flow/36-CONTEXT.md</read_first>
  <action>
Change App PubSub topic derivation for ThreadList so the board topic source is:
1. `state.route_params[:board_id]` when `state.current_screen == :thread_list`;
2. `state.route_params["board_id"]` when present;
3. `state.screen_state[:thread_list].board_id` when it is a `%Foglet.TUI.Screens.ThreadList.State{}`;
4. no ThreadList board topic if none of those are present.

Do not use `state.current_board` to derive the ThreadList board topic.

Keep these existing topic behaviors unchanged:
- authenticated users subscribe to `"user:<id>"`;
- BoardList subscribes to the aggregate boards topic;
- PostReader/PostComposer thread topics may keep using `current_thread` until Phase 37.

Update App PubSub tests:
- replace the test named like `"thread_list screen adds board:<id> topic when current_board is set"` with a route-param/screen-state test;
- assert `state = %{state | current_screen: :thread_list, route_params: %{board_id: "b-99"}, current_board: nil}` includes `"board:b-99"`;
- assert `%ThreadList.State{board_id: "b-77"}` in `screen_state[:thread_list]` includes `"board:b-77"` when route params are empty;
- assert `current_board` alone no longer adds a ThreadList board topic.
  </action>
  <verify>rtk mix test test/foglet_bbs/tui/app_test.exs</verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/app.ex` contains `route_params`.
    - `lib/foglet_bbs/tui/app.ex` contains `ThreadList.State`.
    - `lib/foglet_bbs/tui/app.ex` does not contain `state.current_screen in [:thread_list] and state.current_board`.
    - `test/foglet_bbs/tui/app_test.exs` contains `route_params: %{board_id: "b-99"}`.
    - `test/foglet_bbs/tui/app_test.exs` contains `board_id: "b-77"`.
    - `rtk mix test test/foglet_bbs/tui/app_test.exs` exits 0.
  </acceptance_criteria>
  <done>ThreadList board PubSub topics come from route params or ThreadList local state.</done>
</task>

<task type="auto">
  <name>Task 3: Update render fixtures, smoke tests, and docs for local state</name>
  <files>lib/foglet_bbs/tui/render_fixtures.ex, lib/foglet_bbs/tui/screens/board_list.ex, lib/foglet_bbs/tui/screens/board_list/state.ex, lib/foglet_bbs/tui/screens/thread_list.ex, lib/foglet_bbs/tui/screens/thread_list/state.ex, test/foglet_bbs/tui/layout_smoke_test.exs, test/foglet_bbs/tui/screens/board_list_test.exs, test/foglet_bbs/tui/screens/thread_list_test.exs</files>
  <read_first>lib/foglet_bbs/tui/render_fixtures.ex, test/foglet_bbs/tui/layout_smoke_test.exs, lib/foglet_bbs/tui/screens/board_list.ex, lib/foglet_bbs/tui/screens/thread_list.ex, docs/raxol/getting-started/WIDGET_GALLERY.md, lib/foglet_bbs/tui/widgets/README.md</read_first>
  <action>
Update render fixtures and layout smoke setup so BoardList and ThreadList are
seeded through screen-local state:
- BoardList fixture state uses `%Foglet.TUI.Screens.BoardList.State{directory: directory, board_tree: BoardTree.init(directory: directory, id: "board-directory"), status: :loaded, feedback: nil}` under `screen_state[:board_list]`.
- ThreadList fixture state uses `%Foglet.TUI.Screens.ThreadList.State{board: board, board_id: board.id, threads: threads, selected_index: 0, status: :loaded}` under `screen_state[:thread_list]`.
- ThreadList fixture route params include `%{board: board, board_id: board.id}`.

Remove fixture reliance on App top-level `board_list` and
`current_thread_list` for BoardList/ThreadList rendering. Keep
`current_board` or `current_thread` only in fixtures for Phase 37 screens that
still require them, and add a `Phase 37 compatibility` comment where retained.

Update module docs:
- `BoardList` moduledoc states it is a screen-owned reducer over
  `BoardList.State` and `Foglet.TUI.Context`.
- `BoardList.State` moduledoc names directory rows, tree state, loading status,
  and feedback as local state.
- `ThreadList` moduledoc states it is a screen-owned reducer over
  `ThreadList.State` and route params.
- `ThreadList.State` moduledoc names selected board identity, loaded rows,
  loading status, and selected index as local state.

Update layout smoke tests so they construct local state structs rather than
App top-level board/thread fields for BoardList and ThreadList. Preserve
existing size/overlap assertions and thread-row cluster-width assertions.
  </action>
  <verify>rtk mix test test/foglet_bbs/tui/screens/board_list_test.exs test/foglet_bbs/tui/screens/thread_list_test.exs test/foglet_bbs/tui/layout_smoke_test.exs</verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/render_fixtures.ex` contains `BoardList.State`.
    - `lib/foglet_bbs/tui/render_fixtures.ex` contains `ThreadList.State`.
    - `lib/foglet_bbs/tui/render_fixtures.ex` does not contain `current_thread_list: synthetic_threads`.
    - `lib/foglet_bbs/tui/screens/board_list.ex` contains `screen-owned reducer`.
    - `lib/foglet_bbs/tui/screens/thread_list.ex` contains `screen-owned reducer`.
    - `test/foglet_bbs/tui/layout_smoke_test.exs` contains `BoardList.State`.
    - `test/foglet_bbs/tui/layout_smoke_test.exs` contains `ThreadList.State`.
    - `rtk mix test test/foglet_bbs/tui/screens/board_list_test.exs test/foglet_bbs/tui/screens/thread_list_test.exs test/foglet_bbs/tui/layout_smoke_test.exs` exits 0.
  </acceptance_criteria>
  <done>Fixtures, smoke tests, and docs reflect BoardList/ThreadList local ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App runtime -> migrated screen reducers | Generic task results and keys route into BoardList/ThreadList update loops. |
| Route/screen state -> PubSub topic derivation | ThreadList board subscriptions derive from route params or local state. |
| Render fixtures -> canonical smoke checks | Tests seed local state and must not mask App-owned field regressions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07 | Tampering | App cleanup | mitigate | Tests assert old board/thread App result messages no longer mutate top-level directory fields. |
| T-36-08 | Information Disclosure | PubSub topic derivation | mitigate | ThreadList subscribes only to the route/local board id for the visible ThreadList route. |
| T-36-09 | Denial of Service | Render fixtures and smoke tests | mitigate | Smoke tests keep layout bounds and overlap checks for migrated local-state renders. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `rtk mix test test/foglet_bbs/tui/screens/board_list_test.exs test/foglet_bbs/tui/screens/thread_list_test.exs test/foglet_bbs/tui/app_runtime_contract_test.exs test/foglet_bbs/tui/app_test.exs test/foglet_bbs/tui/layout_smoke_test.exs`
- [ ] `rtk mix compile --warnings-as-errors`
</verification>

<success_criteria>
- App no longer owns BoardList/ThreadList directory local data or task-result mutation.
- ThreadList board PubSub topics derive from route params or ThreadList local state.
- Render fixtures and layout smoke tests seed BoardList/ThreadList through local state.
- BoardList/ThreadList docs describe the new reducer ownership boundary.
- Targeted screen, App, runtime-contract, and layout smoke tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/36-board-thread-directory-flow/36-03-SUMMARY.md`.
</output>
