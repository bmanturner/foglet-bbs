---
phase: 07-oneliners-and-main-menu-social-strip
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - lib/foglet_bbs/tui/screens/main_menu.ex
  - test/foglet_bbs/tui/screens/main_menu_test.exs
  - test/foglet_bbs/tui/layout_smoke_test.exs
autonomous: true
requirements:
  - ONEL-01
must_haves:
  truths:
    - "User can view recent oneliners on the main menu."
    - "The main menu keeps navigation, role-gated entries, and key-bar affordances intact."
    - "Oneliner rows are one-line `@handle  body` rows with no timestamps."
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/main_menu.ex"
      provides: "Stateless split-pane main menu renderer and [O] key dispatch"
      contains: "Oneliners"
    - path: "test/foglet_bbs/tui/screens/main_menu_test.exs"
      provides: "TUI render tests for zero, one, many, clipping, no timestamps, and [O] command"
    - path: "test/foglet_bbs/tui/layout_smoke_test.exs"
      provides: "80x24 smoke coverage for split-pane main menu layout"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/main_menu.ex"
      to: "state.recent_oneliners"
      via: "render reads loaded app state"
      pattern: "recent_oneliners"
    - from: "lib/foglet_bbs/tui/screens/main_menu.ex"
      to: "Foglet.TUI.App"
      via: "[O] emits command handled by App"
      pattern: "\\{:open_oneliner_composer\\}"
---

<objective>
Render the Phase 7 social strip in `Foglet.TUI.Screens.MainMenu` without making the screen stateful.

Purpose: ONEL-01 requires recent persisted oneliners to be visible from the main menu while preserving the existing navigation surface.
Output: Split-pane main-menu render tests and stateless renderer changes.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-oneliners-and-main-menu-social-strip/07-CONTEXT.md
@.planning/phases/07-oneliners-and-main-menu-social-strip/07-RESEARCH.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/screens/main_menu.ex
@test/foglet_bbs/tui/screens/main_menu_test.exs
@test/foglet_bbs/tui/layout_smoke_test.exs
@test/support/foglet/tui/render_helpers.ex

<interfaces>
Expected state field supplied by Plan 03/App:
```elixir
state.recent_oneliners :: [%{body: String.t(), user: %{handle: String.t()}}] | nil
```
MainMenu must tolerate `nil` as "not loaded yet" and `[]` as an empty state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MainMenu tests for split-pane oneliners</name>
  <files>test/foglet_bbs/tui/screens/main_menu_test.exs, test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/screens/main_menu_test.exs
    - test/foglet_bbs/tui/layout_smoke_test.exs
    - lib/foglet_bbs/tui/screens/main_menu.ex
    - .planning/phases/07-oneliners-and-main-menu-social-strip/07-CONTEXT.md
    - .planning/phases/07-oneliners-and-main-menu-social-strip/07-RESEARCH.md
    - docs/raxol/getting-started/WIDGET_GALLERY.md
  </read_first>
  <behavior>
    - Test: nil/empty `recent_oneliners` renders panel title `Oneliners` and an empty-state string without crashing.
    - Test: one entry renders exactly as `@alice  hello`.
    - Test: many entries render no more than the chosen bounded display limit.
    - Test: a long handle is clipped to a presentation target around 12 characters and long body text does not create a second visual row.
    - Test: rendered text does not contain timestamp strings such as `2026-`, `AM`, or `PM` from oneliner rows.
    - Test: `function_exported?(MainMenu, :init_screen_state, 1)` remains false.
  </behavior>
  <action>Add failing/then-green tests for D-12 through D-15 and D-25. Use existing `collect_text_values/1` and layout smoke helpers. Construct entries as maps or `%Foglet.Oneliners.Entry{}` with preloaded `%Foglet.Accounts.User{handle: ...}`; do not hit the database in MainMenu tests. Update the 80x24 smoke test so it asserts the positioned text includes `"Welcome"`, `"[B]"`, `"[Q]"`, `"Oneliners"`, and at least one sample row like `"@alice  hello"` while maintaining at least three distinct y positions. Keep existing Account/Moderation/Sysop role visibility tests unchanged in behavior.</action>
  <acceptance_criteria>
    - `rg "Oneliners" test/foglet_bbs/tui/screens/main_menu_test.exs test/foglet_bbs/tui/layout_smoke_test.exs` returns at least two matches.
    - `rg "@alice  hello" test/foglet_bbs/tui/screens/main_menu_test.exs test/foglet_bbs/tui/layout_smoke_test.exs` returns at least one match.
    - `rg "init_screen_state" test/foglet_bbs/tui/screens/main_menu_test.exs` still returns one or more matches.
    - `rg "2026-|AM|PM" test/foglet_bbs/tui/screens/main_menu_test.exs` returns one or more negative timestamp assertions.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/foglet_bbs/tui/screens/main_menu_test.exs test/foglet_bbs/tui/layout_smoke_test.exs</automated>
  </verify>
  <done>MainMenu tests pin the split-pane strip, row format, clipping, no timestamp rendering, and preserved statelessness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stateless split-pane MainMenu rendering and O key command</name>
  <files>lib/foglet_bbs/tui/screens/main_menu.ex, test/foglet_bbs/tui/screens/main_menu_test.exs, test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/main_menu.ex
    - test/foglet_bbs/tui/screens/main_menu_test.exs
    - test/foglet_bbs/tui/layout_smoke_test.exs
    - docs/raxol/getting-started/WIDGET_GALLERY.md
    - .planning/phases/07-oneliners-and-main-menu-social-strip/07-CONTEXT.md
    - .planning/phases/07-oneliners-and-main-menu-social-strip/07-RESEARCH.md
  </read_first>
  <action>Modify `MainMenu.render/1` per D-12/D-13/D-14/D-15. Keep `MainMenu` pure and do not add `init_screen_state/1`. Build left navigation content exactly from the existing welcome line and visible menu items. Build right content with panel title `"Oneliners"` and rows from `Map.get(state, :recent_oneliners, []) || []`; nil means show copy `"No oneliners yet."`. Use a bounded display limit of `5` rows. Row format must be `"@#{handle}  #{body}"`; normalize missing handles to `"unknown"`, clip handles to 12 visible characters, omit timestamps, and truncate body text so rows are single-line within the right pane at 80 columns. Compose left/right with Raxol `split_pane(direction: :horizontal, ratio: {2, 3}, min_size: 24, children: [menu_panel, oneliners_panel])` or the equivalent local View DSL shape verified in `WIDGET_GALLERY.md`. Add key-bar item `{"O", "Oneliner"}` for authenticated main-menu users. Add `handle_key/2` for `"O"` and `"o"` returning `{:update, state, [{:open_oneliner_composer}]}` per D-20; this command is consumed by Plan 03. Do not add database calls, PubSub, hide controls, live typing, replies, reactions, or timestamps.</action>
  <acceptance_criteria>
    - `rg "split_pane" lib/foglet_bbs/tui/screens/main_menu.ex` returns one or more matches.
    - `rg "Oneliners" lib/foglet_bbs/tui/screens/main_menu.ex` returns one or more matches.
    - `rg "recent_oneliners" lib/foglet_bbs/tui/screens/main_menu.ex` returns one or more matches.
    - `rg "\\{\"O\", \"Oneliner\"\\}" lib/foglet_bbs/tui/screens/main_menu.ex` returns one match.
    - `rg "\\{:open_oneliner_composer\\}" lib/foglet_bbs/tui/screens/main_menu.ex` returns one match.
    - `rg "Repo|FogletBbs\\.Repo|Foglet\\.Oneliners" lib/foglet_bbs/tui/screens/main_menu.ex` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/foglet_bbs/tui/screens/main_menu_test.exs test/foglet_bbs/tui/layout_smoke_test.exs</automated>
  </verify>
  <done>Main menu renders recent loaded oneliners in a right-side `Oneliners` split-pane and emits `[O]` composer command without owning persistence or editor state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App state -> MainMenu render | Loaded domain data crosses into terminal rendering. |
| Keyboard input -> MainMenu command | User keypress crosses into app command routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | `MainMenu.render/1` | mitigate | Render only `state.recent_oneliners` supplied by domain listing; expose no hidden/moderation controls. |
| T-07-06 | Denial of Service | oneliner row rendering | mitigate | Limit rendered rows to 5, clip handle to 12 characters, and truncate body to one row. |
| T-07-07 | Tampering | `[O]` key handling | mitigate | Emit only `{:open_oneliner_composer}`; no direct DB writes from `MainMenu`. |
</threat_model>

<verification>
Run `mix test test/foglet_bbs/tui/screens/main_menu_test.exs test/foglet_bbs/tui/layout_smoke_test.exs`.
</verification>

<success_criteria>
MainMenu remains stateless, shows a bounded right-side `Oneliners` strip from loaded state, preserves existing menu/navigation behavior, and has automated 80x24 render coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/07-oneliners-and-main-menu-social-strip/07-02-SUMMARY.md`.
</output>
