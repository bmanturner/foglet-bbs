---
phase: 41
plan: 41-04
type: execute
wave: 2
depends_on: [41-03]
autonomous: true
requirements: [TUI-03, QUAL-02]
requirements_addressed: [TUI-03, QUAL-02]
files_modified:
  - lib/foglet_bbs/tui/screens/account/state.ex
  - lib/foglet_bbs/tui/screens/account/profile_form.ex
  - lib/foglet_bbs/tui/screens/account/prefs_form.ex
  - lib/foglet_bbs/tui/screens/sysop/site_form.ex
  - lib/foglet_bbs/tui/screens/sysop/site_form/state.ex
  - lib/foglet_bbs/tui/screens/sysop/boards_view.ex
  - lib/foglet_bbs/tui/widgets/modal/form/submit_stash.ex
  - test/foglet_bbs/tui/widgets/modal/form/submit_stash_test.exs
  - test/foglet_bbs/tui/screens/account_test.exs
  - test/foglet_bbs/tui/screens/sysop_test.exs
---

<objective>
Replace all remaining modal-submit process-dictionary consumers with explicit modal-submit effects and remove `Modal.Form.SubmitStash`.
</objective>

<must_haves>
<truths>
- D-05: Account, Sysop SiteForm, and BoardsView reducers must still receive `{:modal_submit, kind, payload}` where they already depend on that shape.
- D-06: Modal form consumers must preserve explicit submit payloads all the way back to App or the owning screen.
- D-07: App pending submit, `Modal.Form.SubmitStash`, Account profile/prefs submit stashes, Sysop site form submit stash, and `Sysop.BoardsView` `pending_submit` must be replaced.
- D-08: Direct App-shell coverage must prove success routing.
- D-09: Failure paths must remain visible.
- D-10: Unrelated test `Process.put/get` fakes may remain if they are not modal-submit payload handoffs.
</truths>
</must_haves>

<tasks>
<task id="41-04-01" type="execute">
<read_first>
- `lib/foglet_bbs/tui/screens/account/state.ex`
- `lib/foglet_bbs/tui/screens/account/profile_form.ex`
- `lib/foglet_bbs/tui/screens/account/prefs_form.ex`
- `lib/foglet_bbs/tui/effect.ex`
- `test/foglet_bbs/tui/screens/account_test.exs`
</read_first>
<action>
Remove Account form `SubmitStash` usage:

- In `Account.State.build_profile_form/1`, make `on_submit` return `Effect.modal_submit(:account, :profile, payload)`.
- In `Account.State.build_prefs_form/1`, make `on_submit` return `Effect.modal_submit(:account, :prefs, payload)`.
- In `ProfileForm.handle_key/3` and `PrefsForm.handle_key/3`, consume `{:submitted, %Effect{type: :modal_submit, payload: %{kind: :profile | :prefs, payload: payload}}}` or the equivalent explicit submit result, not `SubmitStash.pop/1`.
- Remove `alias Foglet.TUI.Widgets.Modal.Form.SubmitStash` and update moduledocs that recommend it.
- Preserve the existing emitted command tuples `{:account_save_profile, attrs}` and `{:account_save_prefs, attrs}` for inline Account tab behavior.
</action>
<acceptance_criteria>
- `rtk rg -n "SubmitStash|Process\\.(get|put|delete)" lib/foglet_bbs/tui/screens/account/state.ex lib/foglet_bbs/tui/screens/account/profile_form.ex lib/foglet_bbs/tui/screens/account/prefs_form.ex` exits with no matches.
- `rtk rg -n "Effect\\.modal_submit\\(:account, :profile|Effect\\.modal_submit\\(:account, :prefs|:account_save_profile|:account_save_prefs" lib/foglet_bbs/tui/screens/account` shows explicit effect construction and preserved command tuples.
- `rtk mix test test/foglet_bbs/tui/screens/account_test.exs` exits 0.
</acceptance_criteria>
</task>

<task id="41-04-02" type="execute">
<read_first>
- `lib/foglet_bbs/tui/screens/sysop/site_form.ex`
- `lib/foglet_bbs/tui/screens/sysop/site_form/state.ex`
- `lib/foglet_bbs/tui/effect.ex`
- `test/foglet_bbs/tui/screens/sysop_test.exs`
</read_first>
<action>
Remove Sysop SiteForm `SubmitStash` usage:

- Make the SiteForm form builder return `Effect.modal_submit(:sysop, :site_settings, payload)` or the existing Sysop reducer's expected site-settings kind if it differs.
- In `SiteForm.handle_key/2`, consume the explicit `{:submitted, %Effect{type: :modal_submit}}` result and build the same command/effect output it currently builds from `SubmitStash.pop/1`.
- Remove `SubmitStash` aliases/imports and docs from SiteForm modules.
</action>
<acceptance_criteria>
- `rtk rg -n "SubmitStash|Process\\.(get|put|delete)" lib/foglet_bbs/tui/screens/sysop/site_form.ex lib/foglet_bbs/tui/screens/sysop/site_form/state.ex` exits with no matches.
- `rtk rg -n "Effect\\.modal_submit\\(:sysop" lib/foglet_bbs/tui/screens/sysop/site_form.ex lib/foglet_bbs/tui/screens/sysop/site_form/state.ex` shows explicit effect construction.
- `rtk mix test test/foglet_bbs/tui/screens/sysop_test.exs` exits 0.
</acceptance_criteria>
</task>

<task id="41-04-03" type="execute">
<read_first>
- `lib/foglet_bbs/tui/screens/sysop/boards_view.ex`
- `lib/foglet_bbs/tui/effect.ex`
- `test/foglet_bbs/tui/screens/sysop_test.exs`
</read_first>
<action>
Remove `Sysop.BoardsView` pending-submit process dictionary:

- Replace `stash_submit/1` so board/category form submit callbacks return `Effect.modal_submit(:sysop, modal_kind, payload)` or a BoardsView-local explicit submit result that `handle_form_event/2` consumes directly.
- Remove `Process.put({__MODULE__, :pending_submit}, payload)`, `Process.get({__MODULE__, :pending_submit})`, and `Process.delete({__MODULE__, :pending_submit})`.
- Keep `handle_submit_payload/2` behavior for create/edit/delete commands, validation errors, modal clearing, and state transitions.
- Migrate `sysop_test.exs` helpers that currently `Process.put({BoardsView, :pending_submit}, payload)` to pass explicit submit results through the form/action path.
</action>
<acceptance_criteria>
- `rtk rg -n "pending_submit|Process\\.(get|put|delete)" lib/foglet_bbs/tui/screens/sysop/boards_view.ex test/foglet_bbs/tui/screens/sysop_test.exs` exits with no modal-submit handoff matches.
- `rtk rg -n "Effect\\.modal_submit|\\{:modal_submit" lib/foglet_bbs/tui/screens/sysop/boards_view.ex` shows the explicit submit path.
- `rtk mix test test/foglet_bbs/tui/screens/sysop_test.exs` exits 0.
</acceptance_criteria>
</task>

<task id="41-04-04" type="execute">
<read_first>
- `lib/foglet_bbs/tui/widgets/modal/form/submit_stash.ex`
- `test/foglet_bbs/tui/widgets/modal/form/submit_stash_test.exs`
- `lib/foglet_bbs/tui/widgets/modal/form.ex`
- `lib/foglet_bbs/tui/app.ex`
- `.planning/phases/41-tui-contract-and-modal-effects/41-SPEC.md`
</read_first>
<action>
Delete `Foglet.TUI.Widgets.Modal.Form.SubmitStash` and its dedicated tests after all production consumers are migrated. Then run a final grep audit and remove any stale documentation references that say modal submit payloads should use process dictionaries.
</action>
<acceptance_criteria>
- `test ! -f lib/foglet_bbs/tui/widgets/modal/form/submit_stash.ex`
- `test ! -f test/foglet_bbs/tui/widgets/modal/form/submit_stash_test.exs`
- `rtk rg -n "pending_screen_modal_submit|SubmitStash|pending_submit" lib/foglet_bbs/tui test/foglet_bbs/tui` exits with no matches.
- `rtk rg -n "Process\\.(get|put|delete)" lib/foglet_bbs/tui test/foglet_bbs/tui` shows no production modal-submit handoffs; unrelated test dependency fakes may remain only outside modal-submit routing.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `rtk rg -n "pending_screen_modal_submit|SubmitStash|pending_submit" lib/foglet_bbs/tui test/foglet_bbs/tui` exits with no matches.
- `rtk mix test test/foglet_bbs/tui/widgets/modal/form_test.exs test/foglet_bbs/tui/app_test.exs test/foglet_bbs/tui/screens/account_test.exs test/foglet_bbs/tui/screens/sysop_test.exs`
- `rtk mix precommit`
</verification>

<success_criteria>
- All known modal-submit process-dictionary handoffs are gone.
- The remaining modal submit path is explicit, effect-based, and covered through App-shell and consumer tests.
</success_criteria>
