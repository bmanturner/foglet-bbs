---
phase: 09-delivery-modes-and-onboarding-honesty
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - lib/foglet_bbs/accounts.ex
  - lib/foglet_bbs/tui/screens/register.ex
  - lib/foglet_bbs/tui/screens/verify.ex
  - test/foglet_bbs/accounts/accounts_test.exs
  - test/foglet_bbs/tui/screens/register_test.exs
  - test/foglet_bbs/tui/screens/verify_test.exs
autonomous: true
requirements:
  - MAIL-02
  - MAIL-03
  - MAIL-05
must_haves:
  truths:
    - "Registration verification delivery attempts email only in email mode."
    - "Verify resend keeps cooldown behavior and uses honest attempted-delivery copy."
    - "No TUI module treats raw verification-code generation as delivery."
  artifacts:
    - path: "lib/foglet_bbs/accounts.ex"
      provides: "verification delivery API"
      exports: ["deliver_verification_code/1"]
    - path: "lib/foglet_bbs/tui/screens/register.ex"
      provides: "registration verification delivery consumer"
      contains: "deliver_verification_code"
    - path: "lib/foglet_bbs/tui/screens/verify.ex"
      provides: "resend delivery consumer and cooldown feedback"
      contains: "deliver_verification_code"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/register.ex"
      to: "lib/foglet_bbs/accounts.ex"
      via: "registration success calls Accounts delivery API"
      pattern: "Accounts\\.deliver_verification_code"
    - from: "lib/foglet_bbs/tui/screens/verify.ex"
      to: "lib/foglet_bbs/accounts.ex"
      via: "resend calls Accounts delivery API"
      pattern: "Accounts\\.deliver_verification_code"
---

<objective>
Replace verification token generation as a fake delivery workflow with an Accounts-level Swoosh delivery API and honest Register/Verify copy.

Purpose: MAIL-02, MAIL-03, and MAIL-05 require verification delivery to be attempted before terminal copy says email was sent, while preserving existing Verify cooldown behavior per D-04 through D-07.
Output: `Accounts.deliver_verification_code/1`, updated Register and Verify screens, and focused tests.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-delivery-modes-and-onboarding-honesty/09-SPEC.md
@.planning/phases/09-delivery-modes-and-onboarding-honesty/09-CONTEXT.md
@.planning/phases/09-delivery-modes-and-onboarding-honesty/09-RESEARCH.md
@.planning/phases/09-delivery-modes-and-onboarding-honesty/09-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

<interfaces>
Existing verification contracts:

```elixir
Foglet.Accounts.build_verify_code(user) :: {:ok, String.t()} | {:error, Ecto.Changeset.t()}
Foglet.Accounts.verify_email_code(user, code) :: {:ok, User.t()} | {:error, :invalid_code | :expired}
Foglet.Config.require_email_verification?() :: boolean()
Foglet.Config.email_verify_resend_cooldown_seconds() :: integer()
```

Expected from Plan 01 if already available:

```elixir
Foglet.Config.delivery_mode() :: "email" | "no_email"
Foglet.Mailer.deliver(Swoosh.Email.t()) :: {:ok, term()} | {:error, term()}
Foglet.Accounts.Email.verification_code(user, code) :: Swoosh.Email.t()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Accounts verification delivery API</name>
  <files>lib/foglet_bbs/accounts.ex, test/foglet_bbs/accounts/accounts_test.exs</files>
  <read_first>
    .planning/phases/09-delivery-modes-and-onboarding-honesty/09-CONTEXT.md
    .planning/phases/09-delivery-modes-and-onboarding-honesty/09-SPEC.md
    lib/foglet_bbs/accounts.ex
    lib/foglet_bbs/accounts/email.ex
    lib/foglet_bbs/mailer.ex
    lib/foglet_bbs/config.ex
    test/foglet_bbs/accounts/accounts_test.exs
  </read_first>
  <behavior>
    - In `"email"` mode, `Accounts.deliver_verification_code/1` persists an `"email_verify"` token and calls `Foglet.Mailer.deliver/1`.
    - In `"no_email"` mode, `Accounts.deliver_verification_code/1` returns `{:error, :unavailable}` and creates no token.
    - Delivery failures return `{:error, :delivery_failed}` without exposing provider details.
  </behavior>
  <action>Add `@spec deliver_verification_code(User.t()) :: {:ok, :attempted} | {:error, :unavailable | :delivery_failed | Ecto.Changeset.t()}`. Implement it in `Foglet.Accounts`: branch on `Foglet.Config.delivery_mode()`, call existing `build_verify_code/1`, build email with `Foglet.Accounts.Email.verification_code(user, code)`, deliver via `Foglet.Mailer.deliver/1`, and return only `{:ok, :attempted}`, `{:error, :unavailable}`, `{:error, :delivery_failed}`, or changeset error. Do not remove `build_verify_code/1`; keep it as token primitive for tests/internal use. Add `Swoosh.TestAssertions` tests for delivered verification email in email mode and no token/no email in no-email mode.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/accounts/accounts_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'def deliver_verification_code' lib/foglet_bbs/accounts.ex` returns one match.
    - `rg -n 'Foglet\\.Mailer\\.deliver|Mailer\\.deliver' lib/foglet_bbs/accounts.ex` returns a match inside verification delivery.
    - `rg -n 'delivery_failed|unavailable|:attempted' lib/foglet_bbs/accounts.ex test/foglet_bbs/accounts/accounts_test.exs` returns matches.
    - `rtk mix test test/foglet_bbs/accounts/accounts_test.exs` passes.
  </acceptance_criteria>
  <done>Accounts owns verification delivery and tests prove email-mode attempted delivery and no-email unavailability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Register and Verify to delivery API with honest copy</name>
  <files>lib/foglet_bbs/tui/screens/register.ex, lib/foglet_bbs/tui/screens/verify.ex, test/foglet_bbs/tui/screens/register_test.exs, test/foglet_bbs/tui/screens/verify_test.exs</files>
  <read_first>
    .planning/phases/09-delivery-modes-and-onboarding-honesty/09-CONTEXT.md
    .planning/phases/09-delivery-modes-and-onboarding-honesty/09-SPEC.md
    lib/foglet_bbs/tui/screens/register.ex
    lib/foglet_bbs/tui/screens/verify.ex
    lib/foglet_bbs/accounts.ex
    test/foglet_bbs/tui/screens/register_test.exs
    test/foglet_bbs/tui/screens/verify_test.exs
  </read_first>
  <behavior>
    - Register no longer calls `Accounts.build_verify_code/1` directly.
    - Verify resend no longer calls `Accounts.build_verify_code/1` directly.
    - Verify render text no longer says "code emailed" unless delivery was attempted.
    - Resend cooldown and invalid-attempt cooldown remain independent.
  </behavior>
  <action>Replace Register's `maybe_build_verify_code/2` with `maybe_deliver_verification_code/2` that calls `Accounts.deliver_verification_code(user)` only when `screen == :verify`; on `{:ok, :attempted}`, route to Verify; on `{:error, :unavailable}`, show modal `"Email verification is unavailable because email delivery is disabled."`; on delivery failure show generic `"Verification instructions could not be sent. Please try again later."`. Remove dev-only verify-code logging from Register/Login-facing delivery if it implies user delivery. In Verify, change render prompt to `"Enter the 6-character verification code:"`, call `Accounts.deliver_verification_code/1` on resend, set modal `"If email delivery is available, new verification instructions have been sent."` only after `{:ok, :attempted}`, and keep existing resend cooldown state update. Preserve invalid-attempt cooldown state exactly: `cooldown_until` still gates code entry; `resend_cooldown_until` still gates resend only; resend still resets `attempts` and `cooldown_until`.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/register_test.exs test/foglet_bbs/tui/screens/verify_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Accounts\\.build_verify_code' lib/foglet_bbs/tui/screens/register.ex lib/foglet_bbs/tui/screens/verify.ex` returns no matches.
    - `rg -n 'deliver_verification_code' lib/foglet_bbs/tui/screens/register.ex lib/foglet_bbs/tui/screens/verify.ex` returns matches in both files.
    - `rg -n 'emailed to you|new code has been sent|notified by email' lib/foglet_bbs/tui/screens/register.ex lib/foglet_bbs/tui/screens/verify.ex` returns no false-delivery copy.
    - `rtk mix test test/foglet_bbs/tui/screens/register_test.exs test/foglet_bbs/tui/screens/verify_test.exs` passes.
  </acceptance_criteria>
  <done>Register and Verify consume Accounts delivery results, preserve cooldown semantics, and avoid false email claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI -> Accounts | User actions request verification delivery through context API. |
| Accounts -> Swoosh | Verification code exits Foglet over configured email provider only in email mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | `Register`/`Verify` TUI | mitigate | Remove direct `Accounts.build_verify_code/1` TUI calls so screens cannot bypass delivery-mode policy. |
| T-09-05 | Information Disclosure | Verify resend copy | mitigate | Use generic attempted-delivery copy and do not expose provider failures or raw codes. |
| T-09-06 | Denial of Service | Verify resend | mitigate | Preserve `email_verify_resend_cooldown_seconds` and resend cooldown state while changing delivery behavior. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/accounts/accounts_test.exs test/foglet_bbs/tui/screens/register_test.exs test/foglet_bbs/tui/screens/verify_test.exs`.
</verification>

<success_criteria>
MAIL-02 and MAIL-03 are operational through Swoosh email mode, and MAIL-05 false verification delivery copy is removed from Register and Verify.
</success_criteria>

<output>
After completion, create `.planning/phases/09-delivery-modes-and-onboarding-honesty/09-02-SUMMARY.md`.
</output>
