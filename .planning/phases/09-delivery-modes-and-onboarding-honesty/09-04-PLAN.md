---
phase: 09-delivery-modes-and-onboarding-honesty
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - lib/foglet_bbs/tui/screens/sysop/site_form.ex
  - test/foglet_bbs/tui/screens/sysop/site_form_test.exs
autonomous: true
requirements:
  - MAIL-01
  - MAIL-06
must_haves:
  truths:
    - "Sysop Site config exposes delivery_mode in the existing config surface."
    - "Sysop Site config blocks or clearly flags no_email plus require_email_verification=true."
    - "No separate browser or relay admin surface is introduced."
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/sysop/site_form.ex"
      provides: "delivery mode visibility/editability"
      contains: "delivery_mode"
    - path: "test/foglet_bbs/tui/screens/sysop/site_form_test.exs"
      provides: "operator config validation coverage"
      contains: "no_email"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/sysop/site_form.ex"
      to: "Foglet.Config.put/3"
      via: "actor-aware config writes"
      pattern: "Config\\.put\\("
---

<objective>
Expose delivery mode in the existing Sysop Site config form and prevent impossible no-email verification configuration.

Purpose: MAIL-01 and MAIL-06 require operator-visible mode behavior through the SSH/TUI Sysop surface, while D-12 through D-14 forbid separate admin surfaces and invalid no-email verification settings.
Output: Updated SiteForm key placement, cross-field guard, copy, and tests.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-delivery-modes-and-onboarding-honesty/09-SPEC.md
@.planning/phases/09-delivery-modes-and-onboarding-honesty/09-CONTEXT.md
@.planning/milestones/v1.1-phases/02-sysop-config-and-board-management/02-CONTEXT.md
@docs/DATA_MODEL.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

<interfaces>
Existing SiteForm contract:

```elixir
Foglet.TUI.Screens.Sysop.SiteForm.site_keys() :: [String.t()]
Foglet.TUI.Screens.Sysop.SiteForm.init(keyword()) :: %SiteForm{}
Foglet.TUI.Screens.Sysop.SiteForm.handle_key(map(), SiteForm.t()) :: {SiteForm.t(), [{atom(), any()}]}
Foglet.TUI.Screens.Sysop.SiteForm.render(SiteForm.t(), map()) :: any()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add delivery_mode to SiteForm placement</name>
  <files>lib/foglet_bbs/tui/screens/sysop/site_form.ex, test/foglet_bbs/tui/screens/sysop/site_form_test.exs</files>
  <read_first>
    .planning/phases/09-delivery-modes-and-onboarding-honesty/09-CONTEXT.md
    .planning/milestones/v1.1-phases/02-sysop-config-and-board-management/02-CONTEXT.md
    lib/foglet_bbs/tui/screens/sysop/site_form.ex
    lib/foglet_bbs/config/schema.ex
    test/foglet_bbs/tui/screens/sysop/site_form_test.exs
  </read_first>
  <behavior>
    - `SiteForm.site_keys/0` includes `"delivery_mode"` intentionally.
    - Rendered SiteForm shows the delivery-mode description and current value.
    - Space/leading-character enum selection works for `"email"` and `"no_email"`.
  </behavior>
  <action>Add `"delivery_mode"` to `@site_keys` immediately before `"require_email_verification"` per D-12 and D-13. Ensure enum cycling and first-character selection can choose `"email"` with `e` and `"no_email"` with `n`; because the enum values have distinct first letters no special dispatch is needed. Add tests asserting `SiteForm.site_keys()` exact order includes delivery mode, `init/1` loads the draft from Config, render contains `"delivery_mode"` and `"Outbound transactional delivery mode"`, and char input changes the draft between `"email"` and `"no_email"`.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/sysop/site_form_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '"delivery_mode"' lib/foglet_bbs/tui/screens/sysop/site_form.ex test/foglet_bbs/tui/screens/sysop/site_form_test.exs` returns matches.
    - `rg -n '@site_keys' -A8 lib/foglet_bbs/tui/screens/sysop/site_form.ex` shows `"delivery_mode"` before `"require_email_verification"`.
    - `rtk mix test test/foglet_bbs/tui/screens/sysop/site_form_test.exs` passes.
  </acceptance_criteria>
  <done>Delivery mode is intentionally placed and editable in the existing Sysop Site form.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Block no_email plus required verification in SiteForm submit</name>
  <files>lib/foglet_bbs/tui/screens/sysop/site_form.ex, test/foglet_bbs/tui/screens/sysop/site_form_test.exs</files>
  <read_first>
    .planning/phases/09-delivery-modes-and-onboarding-honesty/09-SPEC.md
    .planning/phases/09-delivery-modes-and-onboarding-honesty/09-CONTEXT.md
    lib/foglet_bbs/tui/screens/sysop/site_form.ex
    lib/foglet_bbs/config.ex
    test/foglet_bbs/tui/screens/sysop/site_form_test.exs
  </read_first>
  <behavior>
    - Submit with `delivery_mode == "no_email"` and `require_email_verification == true` does not call `Config.put/3`.
    - The form records errors on both keys or a clear error on the focused invalid key.
    - Submit with `"email" + true`, `"email" + false`, and `"no_email" + false` remains allowed.
  </behavior>
  <action>Add a pre-submit cross-field guard before the `Enum.reduce_while/3` write loop. If `Map.get(drafts, "delivery_mode") == "no_email"` and `Map.get(drafts, "require_email_verification") == true`, return state with errors: `"Email verification requires delivery_mode=email"` on `"require_email_verification"` and `"No-email mode cannot require email verification"` on `"delivery_mode"`, and no events. Do not put this cross-field rule in `Foglet.Config.Schema.validate/2` because that function validates one key at a time. Keep actor-aware writes through `Config.put/3`; do not use `Config.put!/3` in SiteForm.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/sysop/site_form_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Email verification requires delivery_mode=email|No-email mode cannot require email verification' lib/foglet_bbs/tui/screens/sysop/site_form.ex test/foglet_bbs/tui/screens/sysop/site_form_test.exs` returns matches.
    - `rg -n 'Config\\.put!' lib/foglet_bbs/tui/screens/sysop/site_form.ex` returns no matches.
    - `rtk mix test test/foglet_bbs/tui/screens/sysop/site_form_test.exs` passes.
  </acceptance_criteria>
  <done>Sysop TUI blocks invalid no-email verification configuration without bypassing context-level config authorization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sysop TUI -> Config context | Operator-entered config drafts cross into actor-aware runtime config writes. |
| config drafts -> user onboarding behavior | Invalid delivery/verification combinations can route users to dead-end flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Elevation of Privilege | `SiteForm.submit/1` | mitigate | Preserve `Config.put(actor, key, value)` authorization and do not write with `put!/3` from TUI. |
| T-09-11 | Tampering | delivery/verification config | mitigate | Reject `delivery_mode=no_email` with `require_email_verification=true` before any config write. |
| T-09-12 | Information Disclosure | Sysop copy | mitigate | Show mode implications without revealing provider credentials or env values. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/screens/sysop/site_form_test.exs`.
</verification>

<success_criteria>
MAIL-01 and MAIL-06 are represented in the existing Sysop config surface with invalid no-email verification blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/09-delivery-modes-and-onboarding-honesty/09-04-SUMMARY.md`.
</output>
