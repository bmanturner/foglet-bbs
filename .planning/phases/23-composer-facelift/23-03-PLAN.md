---
phase: 23-composer-facelift
plan: 3
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - lib/foglet_bbs/tui/screens/new_thread.ex
  - test/foglet_bbs/tui/screens/new_thread_test.exs
autonomous: true
requirements:
  - COMPOSER-01
  - COMPOSER-02
  - COMPOSER-03
  - COMPOSER-04
  - COMPOSER-05
must_haves:
  truths:
    - "New-thread compose mode renders through EditorFrame with visible Edit/Preview labels."
    - "D-03: Title input still uses Foglet.TUI.Widgets.Input.TextInput."
    - "D-11: Board and title context are visible where space allows, while title editing remains clear."
    - "D-12: Body preview uses MarkdownBody inside the same shell."
    - "D-13/D-14: Existing title/body focus, Tab mode rules, Ctrl+S, Ctrl+C/Esc, and Threads.create_thread behavior stay unchanged."
    - "D-16/D-18: NewThread behavior tests are extended and existing composer behavior tests keep passing."
  artifacts:
    - path: lib/foglet_bbs/tui/screens/new_thread.ex
      provides: "New-thread compose render migration to EditorFrame"
    - path: test/foglet_bbs/tui/screens/new_thread_test.exs
      provides: "New-thread render and behavior preservation tests"
  key_links:
    - from: lib/foglet_bbs/tui/screens/new_thread.ex
      to: lib/foglet_bbs/tui/widgets/composer/editor_frame.ex
      via: "EditorFrame.render"
    - from: lib/foglet_bbs/tui/screens/new_thread.ex
      to: lib/foglet_bbs/tui/widgets/input/text_input.ex
      via: "TextInput.render and TextInput.handle_event"
    - from: lib/foglet_bbs/tui/screens/new_thread.ex
      to: Foglet.Threads
      via: "threads_mod.create_thread(board.id, user_id, %{title: title, body: body})"
---

<objective>
Migrate the new-thread compose step into the shared editor shell.

Purpose: New-thread composition needs the same focused editor surface as replies while preserving the existing board step, title input engine, body input engine, preview path, and create-thread domain behavior.
Output: `NewThread.render_compose_step/2` delegates body shell presentation to `Composer.EditorFrame`, with focused tests.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/23-composer-facelift/23-CONTEXT.md
@.planning/phases/23-composer-facelift/23-VALIDATION.md
@.planning/phases/23-composer-facelift/23-SPEC.md
@.planning/phases/23-composer-facelift/23-01-SUMMARY.md
@lib/foglet_bbs/tui/screens/new_thread.ex
@lib/foglet_bbs/tui/screens/new_thread/state.ex
@lib/foglet_bbs/tui/widgets/composer/editor_frame.ex
@lib/foglet_bbs/tui/widgets/input/text_input.ex
@lib/foglet_bbs/tui/widgets/compose.ex
@lib/foglet_bbs/tui/widgets/post/markdown_body.ex
@test/foglet_bbs/tui/screens/new_thread_test.exs

<interfaces>
`NewThread.State` owns `title_input_state`, `body_input_state`, `focused`, and `mode`.

`handle_compose_key/3` keeps Ctrl+C/Esc, Tab, and Ctrl+S above title/body fallback clauses. Do not reorder those clauses.

`do_create_thread/5` must keep calling `threads_mod.create_thread(board.id, user_id, %{title: title, body: body})`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend NewThread compose render tests</name>
  <files>test/foglet_bbs/tui/screens/new_thread_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/screens/new_thread_test.exs
    - .planning/phases/23-composer-facelift/23-CONTEXT.md
    - .planning/phases/23-composer-facelift/23-VALIDATION.md
    - lib/foglet_bbs/tui/screens/new_thread.ex
    - lib/foglet_bbs/tui/widgets/composer/editor_frame.ex
  </read_first>
  <behavior>
    - Compose render contains `Composer`, `Edit`, `Preview`, board name `General`, `Title`, title value when present, and title counter `0 / 60 chars`.
    - Body-focused edit render contains body text and a body counter when an existing body limit is available; do not invent a new domain limit if none exists in code.
    - Preview render contains `Composer`, active Preview mode, and markdown-rendered body content.
    - Source inspection proves `TextInput.render` and `TextInput.handle_event` remain in `NewThread`.
    - Existing title max-length and create-thread behavior tests continue to pass.
  </behavior>
  <action>Extend the existing compose render and preview sections in `new_thread_test.exs` per D-16. Use `compose_state/1`, `put_title/3`, and `fresh_input/1` helpers. Add source assertions for `EditorFrame.render`, `TextInput.render`, `TextInput.handle_event`, and absence of a new `PostCard.render` preview path.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/new_thread_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains assertions for `Composer`, `Edit`, `Preview`, `General`, and `0 / 60 chars`.
    - Test file contains a source assertion for `EditorFrame.render`.
    - Test file contains source assertions for `TextInput.render` and `TextInput.handle_event`.
    - Existing title cap tests still exist under `describe "title length cap (D-13, D-14)"`.
  </acceptance_criteria>
  <done>NewThread tests describe the shell, title, board context, counters, preview, and preservation contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render NewThread compose step through EditorFrame</name>
  <files>lib/foglet_bbs/tui/screens/new_thread.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/new_thread.ex
    - lib/foglet_bbs/tui/screens/new_thread/state.ex
    - lib/foglet_bbs/tui/widgets/composer/editor_frame.ex
    - lib/foglet_bbs/tui/widgets/input/text_input.ex
    - lib/foglet_bbs/tui/widgets/compose.ex
    - lib/foglet_bbs/tui/widgets/post/markdown_body.ex
    - test/foglet_bbs/tui/screens/new_thread_test.exs
  </read_first>
  <action>Alias `Foglet.TUI.Widgets.Composer.EditorFrame`. Refactor only `render_compose_step/2` and helper render functions: keep title rendering through `TextInput.render(ss.title_input_state, bordered: false, theme: theme)`; compute title counter with `String.length(title_value)` and existing `cap = max_thread_title_length()`; build board/title context rows from `ss.board.name` and title value using `Foglet.TUI.TextWidth.truncate/2`; select edit child with existing `Compose.render_input(ss.body_input_state, body_focused, theme, empty_line_placeholder: " ")`; select preview child with existing `MarkdownBody.render(ss.body_input_state.value, body_width, theme)`; pass the title child, body/preview child, budgets, `ss.error`, width, height, `focused?`, and `theme` into `EditorFrame.render`. Do not modify board-step rendering, `handle_compose_key/3`, `do_submit/2`, `do_create_thread/5`, `threads_module/1`, or `max_thread_title_length/0` except for compile-required helper extraction.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/widgets/composer/editor_frame_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/screens/new_thread.ex` contains `alias Foglet.TUI.Widgets.Composer.EditorFrame`.
    - `lib/foglet_bbs/tui/screens/new_thread.ex` contains `EditorFrame.render`.
    - `lib/foglet_bbs/tui/screens/new_thread.ex` still contains `TextInput.render(ss.title_input_state`.
    - `lib/foglet_bbs/tui/screens/new_thread.ex` still contains `TextInput.handle_event(key_event, ss.title_input_state)`.
    - `lib/foglet_bbs/tui/screens/new_thread.ex` still contains `Compose.translate_key(key_event)` and `MultiLineInput.update(msg, ss.body_input_state)`.
    - `lib/foglet_bbs/tui/screens/new_thread.ex` still contains `threads_mod.create_thread(board.id, user_id, %{title: title, body: body})`.
    - Focused NewThread tests and EditorFrame tests pass.
  </acceptance_criteria>
  <done>`NewThread.render_compose_step/2` uses the shared shell while existing title/body input and create-thread behavior remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH key event -> NewThread screen | Untrusted terminal input reaches screen-owned title/body routing. |
| NewThread screen -> `Foglet.Threads` context | Thread creation crosses into domain mutation and authorization/policy checks. |
| Draft markdown -> preview render | Draft text is rendered for preview only and must not mutate data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | Title/body counters | mitigate | Counters remain visual; `TextInput` max length and `Threads.create_thread/3` changeset/context validation remain authoritative. |
| T-23-02 | Elevation of Privilege | Preview/editor mode | mitigate | No new mutation API; only existing Ctrl+S path reaches `do_create_thread/5`. |
| T-23-03 | Tampering | Tab/Ctrl+S/Ctrl+C routing | mitigate | Do not reorder `handle_compose_key/3`; existing clause-order tests remain. |
| T-23-04 | Denial of Service | Board/title context at narrow size | mitigate | Context is truncated/capped before mode control, title input, body surface, or counters disappear. |
</threat_model>

<verification>
Run:

```bash
rtk mix test test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/widgets/composer/editor_frame_test.exs
```
</verification>

<success_criteria>
- New-thread compose render has a shared shell, visible Edit/Preview row, board/title context, title counter, and body editor/preview surface.
- Title remains `TextInput`; body remains `MultiLineInput` through `Compose.render_input/4`.
- Create-thread submit/cancel/domain behavior tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/23-composer-facelift/23-03-SUMMARY.md`.
</output>
