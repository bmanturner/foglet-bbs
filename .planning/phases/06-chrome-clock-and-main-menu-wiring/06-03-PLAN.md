---
phase: 06-chrome-clock-and-main-menu-wiring
plan: 03
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - lib/foglet_bbs/tui/app.ex
  - test/foglet_bbs/tui/app_test.exs
autonomous: true
requirements:
  - MENU-02
must_haves:
  truths:
    - "The main menu receives a dedicated clock tick at least once per minute."
    - "Off-main-menu screens do not subscribe to the main-menu clock tick."
    - "Clock tick handling triggers rerender without changing navigation, modal, screen, or domain-loaded state."
  artifacts:
    - path: "lib/foglet_bbs/tui/app.ex"
      provides: "Main-menu-only clock subscription and tick update handler"
      contains: ":main_menu_clock_tick"
    - path: "test/foglet_bbs/tui/app_test.exs"
      provides: "Subscription and no-op update coverage"
      contains: "60_000"
  key_links:
    - from: "lib/foglet_bbs/tui/app.ex"
      to: "Raxol.Core.Runtime.Application.subscribe_interval/2"
      via: "App.subscribe/1"
      pattern: "subscribe_interval\\(60_000, :main_menu_clock_tick\\)"
    - from: "lib/foglet_bbs/tui/app.ex"
      to: "App.update/2"
      via: "do_update(:main_menu_clock_tick, state)"
      pattern: "do_update\\(:main_menu_clock_tick"
---

<objective>
Add the dedicated main-menu clock refresh path.

Purpose: satisfy MENU-02 using Raxol's subscription mechanism without overloading heartbeat behavior or creating off-screen timers.
Output: `:main_menu_clock_tick` interval subscription while on `:main_menu` plus no-op update handling.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-chrome-clock-and-main-menu-wiring/06-CONTEXT.md
@.planning/phases/06-chrome-clock-and-main-menu-wiring/06-SPEC.md
@.planning/phases/06-chrome-clock-and-main-menu-wiring/06-RESEARCH.md
@.planning/phases/06-chrome-clock-and-main-menu-wiring/06-VALIDATION.md
@docs/ARCHITECTURE.md
@docs/raxol/README.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/app.ex
@test/foglet_bbs/tui/app_test.exs

<interfaces>
Existing contracts to modify:

```elixir
Foglet.TUI.App.subscribe(state)
Foglet.TUI.App.update(message, state)
```

Concrete tick atom:

```elixir
:main_menu_clock_tick
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add main-menu-only interval subscription</name>
  <files>lib/foglet_bbs/tui/app.ex, test/foglet_bbs/tui/app_test.exs</files>
  <read_first>
    - lib/foglet_bbs/tui/app.ex
    - test/foglet_bbs/tui/app_test.exs
    - .planning/phases/06-chrome-clock-and-main-menu-wiring/06-CONTEXT.md
    - .planning/phases/06-chrome-clock-and-main-menu-wiring/06-RESEARCH.md
    - docs/raxol/README.md
    - docs/raxol/getting-started/WIDGET_GALLERY.md
  </read_first>
  <behavior>
    - `subscribe/1` returns `subscribe_interval(60_000, :main_menu_clock_tick)` when `state.current_screen == :main_menu`.
    - `subscribe/1` does not return `:main_menu_clock_tick` when `state.current_screen != :main_menu`.
    - Existing heartbeat and PubSub subscriptions continue to be returned as before.
  </behavior>
  <action>Modify `Foglet.TUI.App.subscribe/1`. Add a `clock` list after the existing `heartbeat` list and before the final return. Use exactly `if state.current_screen == :main_menu do [subscribe_interval(60_000, :main_menu_clock_tick)] else [] end`. Return `heartbeat ++ clock ++ pubsub_subs`. Keep `:heartbeat_tick` unchanged at `10_000` and do not merge clock refresh into heartbeat. Do not add a GenServer or `Process.send_after/3`; use Raxol `subscribe_interval/2` only.</action>
  <verify>
    <automated>mix test test/foglet_bbs/tui/app_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "clock =|subscribe_interval\\(60_000, :main_menu_clock_tick\\)|heartbeat \\+\\+ clock \\+\\+ pubsub_subs" lib/foglet_bbs/tui/app.ex` finds matches.
    - `rg -n "Process\\.send_after|GenServer\\.start_link|:heartbeat_tick.*main_menu_clock_tick|main_menu_clock_tick.*heartbeat_tick" lib/foglet_bbs/tui/app.ex` returns no matches.
    - `mix test test/foglet_bbs/tui/app_test.exs` passes the subscription tests.
  </acceptance_criteria>
  <done>Main-menu clock interval is present only on `:main_menu` and no slower than 60 seconds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add no-op clock tick update handler</name>
  <files>lib/foglet_bbs/tui/app.ex, test/foglet_bbs/tui/app_test.exs</files>
  <read_first>
    - lib/foglet_bbs/tui/app.ex
    - test/foglet_bbs/tui/app_test.exs
    - .planning/phases/06-chrome-clock-and-main-menu-wiring/06-CONTEXT.md
    - .planning/phases/06-chrome-clock-and-main-menu-wiring/06-VALIDATION.md
  </read_first>
  <behavior>
    - `App.update(:main_menu_clock_tick, state)` returns `{state, []}`.
    - The handler does not clear modal state, screen state, loaded board/thread/post data, or read positions.
    - The handler does not call `Foglet.Sessions.Session.heartbeat/1`.
  </behavior>
  <action>Add `defp do_update(:main_menu_clock_tick, state), do: {state, []}` near the existing `:heartbeat_tick` handler in `lib/foglet_bbs/tui/app.ex`. Keep it as a distinct clause from `:heartbeat_tick` per D-08 and D-10. The handler must not mutate `state` and must not emit commands.</action>
  <verify>
    <automated>mix test test/foglet_bbs/tui/app_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "defp do_update\\(:main_menu_clock_tick, state\\).*\\{state, \\[\\]\\}" lib/foglet_bbs/tui/app.ex` finds a match, or `rg -n "defp do_update\\(:main_menu_clock_tick" lib/foglet_bbs/tui/app.ex` plus file read shows the exact `{state, []}` body.
    - `rg -n "main_menu_clock_tick.*heartbeat|heartbeat.*main_menu_clock_tick|Session\\.heartbeat\\(state\\.session_pid\\)" lib/foglet_bbs/tui/app.ex` does not show the clock handler calling heartbeat.
    - `mix test test/foglet_bbs/tui/app_test.exs` passes.
  </acceptance_criteria>
  <done>Clock ticks are accepted as no-op state updates that allow Raxol to rerender without navigation or data mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raxol timer -> App.update/2 | Timer messages can reach the application without user input. |
| current_screen -> subscription set | Screen transitions determine which runtime timers are active. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Denial of service | `App.subscribe/1` | mitigate | Subscribe to `:main_menu_clock_tick` only when `state.current_screen == :main_menu`; no duplicate custom timers. |
| T-06-05 | Tampering | `App.update/2` | mitigate | `:main_menu_clock_tick` handler returns `{state, []}` and tests assert critical state fields remain unchanged. |
</threat_model>

<verification>
```bash
mix test test/foglet_bbs/tui/app_test.exs
```
</verification>

<success_criteria>
- `App.subscribe/1` emits `:main_menu_clock_tick` only on the main menu.
- `App.update/2` accepts the tick without mutating app state or issuing commands.
- Heartbeat behavior remains separate and unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/06-chrome-clock-and-main-menu-wiring/06-03-SUMMARY.md`.
</output>
