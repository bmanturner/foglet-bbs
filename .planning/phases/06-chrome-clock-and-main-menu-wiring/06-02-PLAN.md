---
phase: 06-chrome-clock-and-main-menu-wiring
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - lib/foglet_bbs/tui/widgets/chrome/clock_formatter.ex
  - lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
  - test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs
  - test/foglet_bbs/tui/layout_smoke_test.exs
autonomous: true
requirements:
  - MENU-01
must_haves:
  truths:
    - "Authenticated main-menu chrome shows current date and time in the active user's timezone and selected time format."
    - "Missing or invalid timezone/time-format data falls back without crashing."
    - "Non-main-menu chrome still renders the existing right-side identity text without a clock."
  artifacts:
    - path: "lib/foglet_bbs/tui/widgets/chrome/clock_formatter.ex"
      provides: "Pure deterministic clock formatter"
      exports: ["format/2"]
    - path: "lib/foglet_bbs/tui/widgets/chrome/status_bar.ex"
      provides: "Main-menu chrome clock integration"
      contains: ":main_menu"
    - path: "test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs"
      provides: "Passing deterministic clock tests"
  key_links:
    - from: "lib/foglet_bbs/tui/widgets/chrome/status_bar.ex"
      to: "lib/foglet_bbs/tui/widgets/chrome/clock_formatter.ex"
      via: "ClockFormatter.format/2"
      pattern: "ClockFormatter\\.format"
    - from: "lib/foglet_bbs/tui/widgets/chrome/status_bar.ex"
      to: "state.current_user"
      via: "Phase 5 refreshed session user snapshot"
      pattern: "current_user"
---

<objective>
Implement preference-aware top-right main-menu clock rendering in shared chrome.

Purpose: satisfy MENU-01 while preserving the locked chrome ownership decisions: `StatusBar` owns the clock, `ScreenFrame.render/4` remains the path, and non-main-menu screens keep existing identity behavior.
Output: `ClockFormatter` plus `StatusBar.render/2` main-menu clock composition.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-chrome-clock-and-main-menu-wiring/06-CONTEXT.md
@.planning/phases/06-chrome-clock-and-main-menu-wiring/06-SPEC.md
@.planning/phases/06-chrome-clock-and-main-menu-wiring/06-RESEARCH.md
@.planning/phases/06-chrome-clock-and-main-menu-wiring/06-VALIDATION.md
@.planning/phases/05-account-preferences-and-live-session-refresh/05-CONTEXT.md
@.planning/phases/05-account-preferences-and-live-session-refresh/05-SPEC.md
@docs/ARCHITECTURE.md
@docs/raxol/README.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
@lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
@test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs
@test/foglet_bbs/tui/layout_smoke_test.exs

<interfaces>
New contract this plan creates:

```elixir
defmodule Foglet.TUI.Widgets.Chrome.ClockFormatter do
  @spec format(DateTime.t(), map() | nil) :: String.t()
end
```

Existing contract this plan preserves:

```elixir
Foglet.TUI.Widgets.Chrome.StatusBar.render(state, title)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pure clock formatter</name>
  <files>lib/foglet_bbs/tui/widgets/chrome/clock_formatter.ex, test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs</files>
  <read_first>
    - lib/foglet_bbs/tui/widgets/chrome/clock_formatter.ex
    - test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs
    - lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
    - .planning/phases/06-chrome-clock-and-main-menu-wiring/06-CONTEXT.md
    - .planning/phases/06-chrome-clock-and-main-menu-wiring/06-RESEARCH.md
    - .planning/phases/05-account-preferences-and-live-session-refresh/05-CONTEXT.md
  </read_first>
  <behavior>
    - `format(~U[2026-04-24 18:05:00Z], %{timezone: "America/Chicago", preferences: %{"time_format" => "24h"}})` contains `2026-04-24 13:05`.
    - `format(~U[2026-04-24 00:05:00Z], %{timezone: "Etc/UTC", preferences: %{"time_format" => "12h"}})` contains `2026-04-24 12:05 AM`.
    - Invalid timezone falls back to `"Etc/UTC"`.
    - Invalid or missing time format falls back to `"12h"`.
  </behavior>
  <action>Create `lib/foglet_bbs/tui/widgets/chrome/clock_formatter.ex` with module `Foglet.TUI.Widgets.Chrome.ClockFormatter`. Implement `@spec format(DateTime.t(), map() | nil) :: String.t()`. Read timezone from `user.timezone` using direct struct/map field access where available, defaulting to `"Etc/UTC"` when nil, blank, invalid, or not a binary. Validate with `Timex.Timezone.exists?/1` per Phase 5. Convert with `Timex.Timezone.convert(now_utc, timezone)`, falling back to `"Etc/UTC"` if conversion fails. Read time format from `user.preferences["time_format"]`, accepting only `"12h"` and `"24h"`; default all other values to `"12h"`. Use compact string formats exactly `YYYY-MM-DD HH:MM` for `"24h"` and `YYYY-MM-DD hh:MM AM/PM` for `"12h"`; do not include seconds or full timezone names. Do not add a new date/time dependency.</action>
  <verify>
    <automated>mix test test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/foglet_bbs/tui/widgets/chrome/clock_formatter.ex` succeeds.
    - `rg -n "defmodule Foglet\\.TUI\\.Widgets\\.Chrome\\.ClockFormatter|@spec format\\(DateTime\\.t\\(\\), map\\(\\) \\| nil\\) :: String\\.t\\(\\)|Timex\\.Timezone\\.exists\\?|Timex\\.Timezone\\.convert|Etc/UTC|12h|24h" lib/foglet_bbs/tui/widgets/chrome/clock_formatter.ex` finds matches.
    - `rg -n "HTTPoison|Tesla|:httpc|DateTime\\.utc_now" lib/foglet_bbs/tui/widgets/chrome/clock_formatter.ex` returns no matches.
  </acceptance_criteria>
  <done>`ClockFormatter.format/2` passes deterministic 12h, 24h, invalid timezone, and invalid format tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire StatusBar main-menu clock branch</name>
  <files>lib/foglet_bbs/tui/widgets/chrome/status_bar.ex, test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs, test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    - lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
    - lib/foglet_bbs/tui/widgets/chrome/clock_formatter.ex
    - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
    - test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs
    - test/foglet_bbs/tui/layout_smoke_test.exs
    - .planning/phases/06-chrome-clock-and-main-menu-wiring/06-CONTEXT.md
  </read_first>
  <behavior>
    - Main-menu status bar right side includes a compact clock plus `@handle`.
    - Non-main-menu status bar right side remains exactly `@handle ` for authenticated users and `guest ` for guests.
    - Left-side text remains `" Foglet BBS — #{title}"`.
    - 80x24 layout smoke test still passes.
  </behavior>
  <action>Modify `Foglet.TUI.Widgets.Chrome.StatusBar.render/2`. Alias `Foglet.TUI.Widgets.Chrome.ClockFormatter`. Preserve the existing left text expression exactly as `text(" Foglet BBS — #{title}", fg: fg, bg: bg)`. Compute the right-side text with a private helper: when `state.current_screen == :main_menu` and `state.current_user` is present, use `clock_instant(state)` plus `ClockFormatter.format(instant, state.current_user)` and compose `"#{clock}  @#{handle}"`; otherwise keep existing behavior `@#{handle}` or `guest`. Implement `clock_instant/1` so tests can set `state.session_context[:clock_now]` to a `%DateTime{}`; when absent use `DateTime.utc_now()`. Do not call `Foglet.Repo`, `Foglet.Accounts`, or any persistence function from `StatusBar` or `ClockFormatter`.</action>
  <verify>
    <automated>mix test test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs test/foglet_bbs/tui/layout_smoke_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ClockFormatter|current_screen == :main_menu|clock_now|DateTime\\.utc_now|Foglet BBS —" lib/foglet_bbs/tui/widgets/chrome/status_bar.ex` finds matches.
    - `rg -n "Foglet\\.Repo|Foglet\\.Accounts|Repo\\." lib/foglet_bbs/tui/widgets/chrome/status_bar.ex lib/foglet_bbs/tui/widgets/chrome/clock_formatter.ex` returns no matches.
    - `mix test test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs test/foglet_bbs/tui/layout_smoke_test.exs` passes.
  </acceptance_criteria>
  <done>Main-menu chrome clock renders through StatusBar/ScreenFrame, preserves non-main-menu identity behavior, and keeps layout smoke green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 5 user snapshot -> ClockFormatter | `timezone` and `preferences["time_format"]` may be missing, stale, or invalid. |
| App state -> StatusBar render | Render code must not cross into persistence or mutate state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information disclosure / Denial of service | `StatusBar.render/2` | mitigate | Use only `state.current_user` and `state.session_context`; grep must show no `Foglet.Repo`, `Foglet.Accounts`, or `Repo.` calls. |
| T-06-03 | Denial of service | `ClockFormatter.format/2` | mitigate | Validate timezone with `Timex.Timezone.exists?/1`; fallback invalid/missing timezone to `"Etc/UTC"` and invalid/missing format to `"12h"`. |
</threat_model>

<verification>
```bash
mix test test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs test/foglet_bbs/tui/layout_smoke_test.exs
```
</verification>

<success_criteria>
- `ClockFormatter.format/2` is deterministic and pure for fixed instants.
- `StatusBar.render/2` uses the formatter only for `:main_menu` and authenticated users.
- Existing non-main-menu chrome and 80x24 layout smoke behavior remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/06-chrome-clock-and-main-menu-wiring/06-02-SUMMARY.md`.
</output>
