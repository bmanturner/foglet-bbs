# Phase 01.1: shared-modal-form-primitive - Discussion Log (Assumptions Mode)

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the analysis.

**Date:** 2026-04-23
**Phase:** 01.1-shared-modal-form-primitive
**Mode:** assumptions
**Areas analyzed:** Module Structure, Field Type Adapters, Focus Navigation, Coercion, Errors API, Render, Testing

## Assumptions Presented

### Module Structure & State Shape
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| D-14 stateful struct, no process; `init/1+handle_event/2+render/2` | Confident | `lib/foglet_bbs/tui/widgets/input/text_input.ex`; RESEARCH §Pattern 1 |
| `focus_index :: non_neg_integer()` (not atom) | Confident | RESEARCH §"NewThread Anti-Pattern"; SPEC REQ-4 |

### Field Type Adapters
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| `:text`/`:integer` reuse `TextInput` (stateful) | Confident | text_input.ex; RESEARCH §Standard Stack |
| `:boolean` uses `Checkbox.render/2` (stateless); FieldState holds boolean | Confident | checkbox.ex; RESEARCH §Pattern 3 |
| `:enum` uses `RadioGroup.render/3` (stateless); FieldState holds index | Confident | radio_group.ex; RESEARCH §Pattern 3 |
| `:textarea` uses `MultiLineInput` via `Compose.translate_key/1` | Confident | compose.ex; RESEARCH §Pattern 4 |

### Focus Navigation
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| Tab/Shift-Tab math: `rem(±1 + n, n)` for wrap | Confident | RESEARCH §Field Dispatch Pattern |
| Event interception order Esc→Tab→ShiftTab→Enter→child | Confident | RESEARCH §Pitfall 1 |
| Shift-Tab arrives as `%{key: :tab, shift: true}` | Likely | Analogy with `%{key: :char, ctrl: true}` in new_thread.ex:237; RESEARCH Open Question 1 |

### Coercion
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| Coerce at submit, not per-keystroke | Confident | RESEARCH §Code Examples |
| `:integer` uses `Integer.parse/1` returning nil on failure | Confident | RESEARCH §Pitfall 3; SPEC REQ-3 permits `integer()\|nil` |
| `:enum` returns choice value (not index) | Confident | Reduces caller boilerplate; consistent with RadioGroup contract |

### Errors API
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| `set_errors/2` is pure setter; caller orchestrates | Confident | SPEC REQ-7; RESEARCH §Pattern 5 |
| Form does not auto-dismiss on submit | Confident | SPEC REQ-7 ("caller-orchestrated") |

### Render
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| Bare `column`, no outer box (chrome from `render_modal_overlay`) | Confident | app.ex:165-178; RESEARCH §Pitfall 4 |
| Focused label = `theme.accent.fg` + `[:bold]`; else `theme.primary.fg` | Confident | new_thread.ex:101-103 |
| All colors via theme slots; no hardcoded atoms | Confident | D-07/D-09; enforced by `color_atom_leaked?/2` |

### Testing
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| Single test file at `test/foglet_bbs/tui/widgets/modal/form_test.exs` | Confident | RESEARCH §Test Patterns; SPEC REQ-9 |
| Mirror `text_input_test.exs` structure; import `WidgetHelpers` | Confident | text_input_test.exs; RESEARCH §Test Patterns |

## Corrections Made

No corrections — all assumptions confirmed by user.

## External Research

Inline verification only:
- Checked `vendor/raxol/lib/raxol/core/runtime/events/keyboard.ex` for Shift-Tab event shape — found tab/escape/enter key-name mapping but the upstream-to-downstream modifier flattening was not conclusively shown. The Likely confidence on D-14 (Shift-Tab shape) stands; the planner is instructed to drive the test off a real keystroke fixture.
