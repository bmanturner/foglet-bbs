---
phase: 04-shared-invite-surface-activation
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/foglet_bbs/tui/screens/moderation.ex
  - lib/foglet_bbs/tui/screens/moderation/state.ex
  - test/foglet_bbs/tui/screens/moderation_test.exs
autonomous: true
requirements: [INVT-01, MODR-04]
must_haves:
  truths:
    - "A moderator can open Moderation INVITES only when invite_code_generators is mods."
    - "Moderation INVITES generation is unlimited for permitted moderators through Foglet.Accounts."
    - "Moderation contains only thin INVITES delegation."
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/moderation/state.ex"
      provides: "Conditional INVITES tab labels and shared invite state"
    - path: "lib/foglet_bbs/tui/screens/moderation.ex"
      provides: "Moderation active-tab render and key delegation for INVITES"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/moderation.ex"
      to: "Foglet.TUI.Screens.Shared.InvitesActions"
      via: "active INVITES tab key delegation"
---

<objective>
Add the shared live `INVITES` tab to Moderation for moderator invite policy.

Purpose: Implement D-01, D-03, D-05, D-07, D-08, D-17, D-19, and MODR-04 without copying invite action logic into the moderation workspace.
Output: Moderation policy visibility and shared invite generation delegation under `"mods"`.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-shared-invite-surface-activation/04-SPEC.md
@.planning/phases/04-shared-invite-surface-activation/04-CONTEXT.md
@.planning/phases/04-shared-invite-surface-activation/04-RESEARCH.md
@.planning/phases/04-shared-invite-surface-activation/04-01-SUMMARY.md
@lib/foglet_bbs/tui/screens/shared/invites_actions.ex
@lib/foglet_bbs/tui/screens/shared/invites_state.ex
@lib/foglet_bbs/tui/screens/shared/invites_surface.ex
@AGENTS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add conditional Moderation INVITES tab</name>
  <read_first>
    - lib/foglet_bbs/tui/screens/moderation.ex
    - lib/foglet_bbs/tui/screens/moderation/state.ex
    - lib/foglet_bbs/tui/screens/shell_visibility.ex
    - test/foglet_bbs/tui/screens/moderation_test.exs
    - .planning/phases/04-shared-invite-surface-activation/04-CONTEXT.md
  </read_first>
  <files>lib/foglet_bbs/tui/screens/moderation.ex, lib/foglet_bbs/tui/screens/moderation/state.ex, test/foglet_bbs/tui/screens/moderation_test.exs</files>
  <action>Extend `Foglet.TUI.Screens.Moderation.State` with an `invites: InvitesState.t()` field and `tab_labels/1`, preserving the base order `["QUEUE", "LOG", "USERS", "SANCTIONS", "BOARDS"]` and appending `"INVITES"` only when `invites_visible?` is true. In `Moderation.render/1` and `handle_key/2`, compute visibility with `ShellVisibility.invites_visible?(state.current_user, state.session_context)` per D-01 and D-03, rebuild tabs when labels differ, and clamp `active_tab` if `INVITES` disappears. Tests must prove `{role: :mod, invite_code_generators: "mods"}` shows `INVITES`; `{role: :mod, "any_user"}`, `{role: :mod, "sysop_only"}`, regular users, and nil users do not get an accessible Moderation invite tab. Keep `ShellVisibility.moderation_visible?/1` as the entry guard.</action>
  <verify>
    <automated>mix test test/foglet_bbs/tui/screens/moderation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "invites:|InvitesState|tab_labels\\(.*invites" lib/foglet_bbs/tui/screens/moderation/state.ex` finds shared state and conditional labels.
    - `rg -n "ShellVisibility\\.invites_visible\\?" lib/foglet_bbs/tui/screens/moderation.ex` finds runtime policy use.
    - `rg -n "mods|sysop_only|any_user|nil user|INVITES" test/foglet_bbs/tui/screens/moderation_test.exs` finds policy matrix coverage.
  </acceptance_criteria>
  <done>Moderation owns shared invite state, appends INVITES only under the mods policy for moderators, and rebuilds stale tab state safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delegate Moderation INVITES actions to shared path</name>
  <read_first>
    - lib/foglet_bbs/tui/screens/moderation.ex
    - lib/foglet_bbs/tui/screens/moderation/state.ex
    - lib/foglet_bbs/tui/screens/shared/invites_actions.ex
    - test/foglet_bbs/tui/screens/moderation_test.exs
    - test/foglet_bbs/accounts/invite_test.exs
  </read_first>
  <files>lib/foglet_bbs/tui/screens/moderation.ex, test/foglet_bbs/tui/screens/moderation_test.exs</files>
  <action>Render the `"INVITES"` tab body with `InvitesSurface.render(ss.invites, theme)`. When active tab label is `"INVITES"` and the Tabs widget does not handle the key, call `InvitesActions.handle_key(event, ss.invites, state.current_user)` and write returned invite state back to `state.screen_state[:moderation].invites`. If tab navigation changes into `"INVITES"`, load list once through `InvitesActions.load/2` unless `items` is already a list. Tests must persist a moderator user, set policy `"mods"`, press `g` from active Moderation INVITES, assert `Accounts.list_invites(mod)` grows by exactly one, assert `last_generated_code` is set, and assert no per-screen `Accounts.create_invite/1` or `Accounts.revoke_invite/2` calls exist in `moderation.ex`.</action>
  <verify>
    <automated>mix test test/foglet_bbs/tui/screens/moderation_test.exs test/foglet_bbs/tui/screens/shared/invites_actions_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "InvitesSurface\\.render|InvitesActions\\.handle_key|InvitesActions\\.load" lib/foglet_bbs/tui/screens/moderation.ex` finds shared rendering and delegation.
    - `rg -n "Accounts\\.create_invite|Accounts\\.revoke_invite|Accounts\\.list_invites|FogletBbs\\.Repo" lib/foglet_bbs/tui/screens/moderation.ex` returns no matches.
    - `rg -n "persists exactly one invite|last_generated_code|unlimited|mods" test/foglet_bbs/tui/screens/moderation_test.exs` finds MODR-04 action coverage.
  </acceptance_criteria>
  <done>Moderation renders shared INVITES, delegates keys to shared actions, proves moderator generation under mods, and contains no direct invite lifecycle calls.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moderation visibility -> operator actions | Moderator workspace visibility is advisory and can be stale if not recomputed. |
| Moderation screen -> shared actions | Moderator key events request domain mutations through shared TUI code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | E | Moderation.handle_key/2 | mitigate | Delegate invite actions only to `InvitesActions`; `Foglet.Accounts` remains authoritative for moderator authorization and policy. |
| T-04-09 | T | Moderation.State tabs | mitigate | Recompute `ShellVisibility.invites_visible?/2` from runtime policy and clamp active tab if INVITES disappears. |
| T-04-10 | I | Moderation invite listing | mitigate | No direct Repo or list filtering in Moderation; shared action list comes from `Accounts.list_invites/1`. |
</threat_model>

<verification>
Run:
```bash
mix test test/foglet_bbs/tui/screens/moderation_test.exs test/foglet_bbs/tui/screens/shared/invites_actions_test.exs test/foglet_bbs/tui/screens/shared/invites_surface_test.exs
```
</verification>

<success_criteria>
- Moderators see Moderation `INVITES` only under `"mods"`.
- Moderator invite generation is unlimited through `Accounts.create_invite/1` policy semantics and is not capped by TUI code.
- Moderation contains only visibility, rendering dispatch, and shared action delegation for invites.
</success_criteria>

<must_haves>
- Truth: A moderator can use the shared Moderation INVITES tab under policy `"mods"`.
- Artifact: `lib/foglet_bbs/tui/screens/moderation/state.ex` contains conditional INVITES state.
- Key link: Moderation actions route through `InvitesActions`, not per-screen invite lifecycle code.
</must_haves>

<output>
After completion, create `.planning/phases/04-shared-invite-surface-activation/04-03-SUMMARY.md`.
</output>
