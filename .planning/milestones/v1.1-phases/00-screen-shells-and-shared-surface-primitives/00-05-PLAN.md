---
phase: 00
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - lib/foglet_bbs/tui/screens/moderation.ex
  - lib/foglet_bbs/tui/screens/moderation/state.ex
autonomous: true
requirements: [MODR-01]
tags: [tui, screens, moderation, phase-00]
must_haves:
  truths:
    - "Moderator or sysop can navigate into Moderation from main menu (post Plan 07) and see QUEUE, LOG, USERS, SANCTIONS, BOARDS tabs in that order"
    - "Moderation shell uses Foglet.TUI.Widgets.Input.Tabs for all tab navigation (D-05)"
    - "Moderation tab/focus state lives in state.screen_state[:moderation] as a dedicated state struct (D-04)"
    - "Moderation render path is read-only — no fake ban/approve/remove/sanction commands (D-13, T-00-02)"
    - "Menu visibility is NOT authorization — the shell also verifies role via ShellVisibility.moderation_visible?/1 to prevent drift"
    - "Pressing Q or q returns to :main_menu"
  artifacts:
    - path: "lib/foglet_bbs/tui/screens/moderation.ex"
      provides: "MODR-01 Moderation shell with five tabs, read-only placeholders"
      contains: "defmodule Foglet.TUI.Screens.Moderation"
      exports: ["render/1", "handle_key/2", "init_screen_state/1"]
    - path: "lib/foglet_bbs/tui/screens/moderation/state.ex"
      provides: "%Foglet.TUI.Screens.Moderation.State{} with tabs, active_tab"
      contains: "defmodule Foglet.TUI.Screens.Moderation.State"
  key_links:
    - from: "lib/foglet_bbs/tui/screens/moderation.ex"
      to: "Foglet.TUI.Widgets.Input.Tabs.handle_event/2"
      via: "tab key delegation"
      pattern: "Tabs\\.handle_event"
    - from: "lib/foglet_bbs/tui/screens/moderation.ex"
      to: "Foglet.TUI.Screens.Shared.InvitesSurface"
      via: "conditional INVITES tab rendering for Phase 4 readiness (not in the Phase 0 tab set but the shell must be structurally ready per D-07)"
      pattern: "InvitesSurface"
---

<objective>
Build the Moderation shell so MODR-01 is satisfied: a moderator or sysop can open a read-only Moderation workspace with `QUEUE`, `LOG`, `USERS`, `SANCTIONS`, and `BOARDS` tabs (D-10). All Plan 01 moderation_test.exs assertions and the Moderation portion of layout_smoke_test.exs flip GREEN after this plan.

Purpose: MODR-01, Phase 0 Success Criteria 1 and 2 for the Moderation surface. Enforces D-03, D-04, D-05, D-10, D-12, D-13. Security mitigation T-00-02 is pinned by the shell's own visibility check and by the no-fake-actions guards.

Output: Two new modules — `Foglet.TUI.Screens.Moderation` and `Foglet.TUI.Screens.Moderation.State`.

Note on INVITES tab: CONTEXT.md D-10 locks the Phase 0 Moderation tab set to exactly `["QUEUE", "LOG", "USERS", "SANCTIONS", "BOARDS"]`. The shared INVITES surface is NOT part of the Phase 0 Moderation tab set; it lights up in Phase 4 (Shared Invite Surface Activation). This plan does NOT add INVITES to the Moderation tab list. The plumbing for future inclusion is provided by the shared primitive from Plan 02 and does not require any current Moderation-side wiring beyond respecting the D-10 tab set exactly.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-screen-shells-and-shared-surface-primitives/00-CONTEXT.md
@.planning/phases/00-screen-shells-and-shared-surface-primitives/00-RESEARCH.md
@.planning/phases/00-screen-shells-and-shared-surface-primitives/00-01-SUMMARY.md
@.planning/phases/00-screen-shells-and-shared-surface-primitives/00-02-SUMMARY.md
@.planning/phases/00-screen-shells-and-shared-surface-primitives/00-03-SUMMARY.md
@CLAUDE.md

@lib/foglet_bbs/tui/screen.ex
@lib/foglet_bbs/tui/screens/board_list.ex
@lib/foglet_bbs/tui/screens/post_reader.ex
@lib/foglet_bbs/tui/screens/post_reader/state.ex
@lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
@lib/foglet_bbs/tui/widgets/input/tabs.ex
@lib/foglet_bbs/tui/screens/shell_visibility.ex
@lib/foglet_bbs/tui/theme.ex

@test/foglet_bbs/tui/screens/moderation_test.exs
@test/foglet_bbs/tui/layout_smoke_test.exs

<interfaces>
Screen behavior callbacks (same as Account — see Plan 04).

New state struct:
```elixir
# Foglet.TUI.Screens.Moderation.State
@tabs ["QUEUE", "LOG", "USERS", "SANCTIONS", "BOARDS"]

@type t :: %__MODULE__{
  tabs: Foglet.TUI.Widgets.Input.Tabs.t(),
  active_tab: non_neg_integer()
}
defstruct [:tabs, active_tab: 0]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Foglet.TUI.Screens.Moderation.State struct</name>
  <files>lib/foglet_bbs/tui/screens/moderation/state.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/post_reader/state.ex (direct analog — dedicated state module, new/1 constructor)
    - lib/foglet_bbs/tui/screens/account/state.ex (just created in Plan 04 — same screen-state pattern; do not copy the INVITES condition since Moderation's Phase 0 tab set does NOT include INVITES per D-10)
    - lib/foglet_bbs/tui/widgets/input/tabs.ex (Tabs.init/1 signature)
    - test/foglet_bbs/tui/screens/moderation_test.exs (test item: `init_screen_state/1 returns struct with active_tab: 0 and Tabs wrapper`)
    - .planning/phases/00-screen-shells-and-shared-surface-primitives/00-CONTEXT.md (D-10: `QUEUE`, `LOG`, `USERS`, `SANCTIONS`, `BOARDS` — exactly this order)
    - CLAUDE.md (never nest multiple modules in one file)
  </read_first>
  <behavior>
    - `%Foglet.TUI.Screens.Moderation.State{tabs: Tabs.t(), active_tab: 0}`.
    - `@tabs` module attribute holds the locked list `["QUEUE", "LOG", "USERS", "SANCTIONS", "BOARDS"]` — this list order is enforced by test item 3 in moderation_test.exs.
    - `new(opts)` calls `Tabs.init(tabs: @tabs, active: Keyword.get(opts, :active, 0))`.
    - Expose `tab_labels/0 → @tabs` (so tests and Plan 08+ can assert without repeating the literal).
    - No fake fields — no `queue_items`, `log_entries`, etc. Those arrive in Phase 8.
    - Moduledoc cites D-04, D-10, MODR-01 and notes that Phase 0 is shell-only.
  </behavior>
  <action>
    Create `lib/foglet_bbs/tui/screens/moderation/state.ex`:

    ```elixir
    defmodule Foglet.TUI.Screens.Moderation.State do
      @moduledoc """
      Screen-local state for `Foglet.TUI.Screens.Moderation` (D-04, D-10, MODR-01).

      The app stores this struct at `state.screen_state[:moderation]`.

      Tabs (D-10, locked order):
          ["QUEUE", "LOG", "USERS", "SANCTIONS", "BOARDS"]

      Phase 0 scope: UI focus only. Real queue/log/user/sanction/board data arrives
      in Phase 8 (Moderation Workspace Population). The shared INVITES tab is NOT
      part of the Phase 0 Moderation tab set; Phase 4 may introduce it when
      `invite_code_generators == "mods"`, but Phase 0's locked D-10 list excludes it.
      """

      alias Foglet.TUI.Widgets.Input.Tabs

      @tabs ["QUEUE", "LOG", "USERS", "SANCTIONS", "BOARDS"]

      @type t :: %__MODULE__{
              tabs: Tabs.t(),
              active_tab: non_neg_integer()
            }

      defstruct [:tabs, active_tab: 0]

      @spec new(keyword()) :: t()
      def new(opts \\ []) do
        active = Keyword.get(opts, :active, 0)
        %__MODULE__{tabs: Tabs.init(tabs: @tabs, active: active), active_tab: active}
      end

      @spec tab_labels() :: [String.t()]
      def tab_labels, do: @tabs
    end
    ```

    `mix format lib/foglet_bbs/tui/screens/moderation/state.ex`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors --force 2>&1 | tail -3 && mix format --check-formatted lib/foglet_bbs/tui/screens/moderation/state.ex</automated>
  </verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/screens/moderation/state.ex` exists
    - Contains `defmodule Foglet.TUI.Screens.Moderation.State`
    - Contains the exact locked list: `grep -q '\["QUEUE", "LOG", "USERS", "SANCTIONS", "BOARDS"\]' lib/foglet_bbs/tui/screens/moderation/state.ex`
    - Contains `defstruct [:tabs, active_tab: 0]`
    - Contains `def new` and `def tab_labels`
    - No fake data fields: `! grep -qE "(queue_items|log_entries|sanction_list|banned_users)" lib/foglet_bbs/tui/screens/moderation/state.ex`
    - `mix compile --warnings-as-errors --force` exits 0
    - `mix format --check-formatted lib/foglet_bbs/tui/screens/moderation/state.ex` exits 0
  </acceptance_criteria>
  <done>
    Moderation.State struct exists with the exact D-10 tab list and no fake data fields.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Foglet.TUI.Screens.Moderation screen module (render, handle_key, init_screen_state)</name>
  <files>lib/foglet_bbs/tui/screens/moderation.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/moderation/state.ex (just created)
    - lib/foglet_bbs/tui/screens/account.ex (direct sibling analog — copy the tab handling pattern exactly, adapting tabs and placeholder copy per D-10)
    - lib/foglet_bbs/tui/screens/board_list.ex (Q → :main_menu pattern)
    - lib/foglet_bbs/tui/screens/post_reader.ex (get_screen_state/1 helper pattern returning struct)
    - lib/foglet_bbs/tui/screen.ex (behavior contract)
    - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex (`ScreenFrame.render/4` signature)
    - lib/foglet_bbs/tui/widgets/input/tabs.ex (`Tabs.handle_event/2` and `Tabs.render/2`)
    - lib/foglet_bbs/tui/screens/shell_visibility.ex (`moderation_visible?/1`)
    - test/foglet_bbs/tui/screens/moderation_test.exs (10 tests — complete contract)
    - .planning/phases/00-screen-shells-and-shared-surface-primitives/00-CONTEXT.md (D-03, D-04, D-05, D-10, D-12, D-13)
    - .planning/phases/00-screen-shells-and-shared-surface-primitives/00-RESEARCH.md (Pattern 1, 2, 3; Pitfalls 1, 3)
    - CLAUDE.md (Access on structs; block rebinding)
  </read_first>
  <behavior>
    Implement `Foglet.TUI.Screens.Moderation` following Account's pattern from Plan 04 exactly, with these differences:

    - Alias `Foglet.TUI.Screens.Moderation.State` instead of Account.State.
    - No `InvitesSurface` alias — Moderation's Phase 0 tab set does not include INVITES (D-10).
    - Title passed to `ScreenFrame.render/4`: `"Moderation"`.
    - Tab bodies — one placeholder per tab per D-12 (empty/`[]` → obvious scaffold copy):
      - `"QUEUE"` → `text("Report queue will arrive in Phase 8.", fg: theme.warning.fg)` in a column.
      - `"LOG"` → `text("Audit log will arrive in Phase 8.", fg: theme.warning.fg)` in a column.
      - `"USERS"` → `text("User administration will arrive in Phase 8.", fg: theme.warning.fg)` in a column.
      - `"SANCTIONS"` → `text("Sanctions tooling will arrive in Phase 8.", fg: theme.warning.fg)` in a column.
      - `"BOARDS"` → `text("Board-scoped moderation will arrive in Phase 8.", fg: theme.warning.fg)` in a column.
    - Key bar: `[{"←/→", "Tab"}, {"1-5", "Jump"}, {"Q", "Back"}]`.

    - `handle_key/2`:
      - Q/q → `:main_menu`.
      - Defer to `Tabs.handle_event/2` for all other keys. Return `:no_match` when Tabs returns `nil` action AND state unchanged.
      - COMMANDS list is always `[]`. No fake ban, approve, remove, sanction operations.

    - Defensive role check (Pitfall 3 / Security Domain mitigation): Add a `render/1` guard that consults `ShellVisibility.moderation_visible?/1`. If the current user is not moderation-visible (somehow routed here despite menu hiding them — e.g., direct App.update({:navigate, :moderation}) from a test harness or future typed action), render a minimal "Not available" column through `ScreenFrame`. Do NOT crash. The Plan 01 tests exercise role `:mod` and `:sysop` only, so this branch is defensive and won't be hit by Plan 01 tests, but it prevents drift between menu visibility and shell behavior.

      Concrete shape:
      ```elixir
      def render(state) do
        if ShellVisibility.moderation_visible?(state.current_user) do
          render_authorized(state)
        else
          theme = Theme.from_state(state)
          empty = column(style: %{gap: 0}, do: [text("Moderation is not available.", fg: theme.warning.fg)])
          ScreenFrame.render(state, "Moderation", empty, [{"Q", "Back"}])
        end
      end
      ```

    - No fake-action functions: no `def ban_user`, `def approve_queue_item`, `def remove_post`, `def issue_sanction`.
    - No domain imports: no `alias Foglet.Accounts`, `Foglet.Sanctions`, `FogletBbs.Repo`.

    Moduledoc cites D-03, D-04, D-05, D-10, D-12, D-13, MODR-01. References Pitfall 3 explicitly and explains the defensive role check.
  </behavior>
  <action>
    Create `lib/foglet_bbs/tui/screens/moderation.ex` implementing the <behavior> spec. Use the pattern from `lib/foglet_bbs/tui/screens/account.ex` as the direct template and adapt it.

    After writing:
    1. `mix format lib/foglet_bbs/tui/screens/moderation.ex`.
    2. `mix compile --warnings-as-errors --force` — must exit 0.
    3. `mix test test/foglet_bbs/tui/screens/moderation_test.exs` — all 10 tests from Plan 01 must pass.
    4. `mix test test/foglet_bbs/tui/layout_smoke_test.exs` — the moderation shell smoke test passes.

    Expected fixes for any failing tests (debug guide):
    - Test asserts tabs `QUEUE`, `LOG`, `USERS`, `SANCTIONS`, `BOARDS` rendered in ORDER — verify `Tabs.render/2` output flattened text contains them in ascending position order; if Tabs renders them in declared order, this works because `@tabs` list order matches the test expectation.
    - Test asserts `handle_key` for digit '3' → tab 2 (USERS) — that relies on Tabs widget digit shortcut, which is already tested in tabs_test.exs item 5.
    - Test asserts Home → tab 0 and End → tab 4 — these delegate to Tabs.handle_event/2 which already implements them per tabs.ex moduledoc contract.
    - Test asserts `:no_match` for unknown key — correct handling of Tabs returning `nil` action AND unchanged state. If Tabs doesn't expose "state unchanged" cleanly, compare `Map.get(before.raxol_state, :active_index)` to `Map.get(after.raxol_state, :active_index)` as a proxy.
  </action>
  <verify>
    <automated>mix test test/foglet_bbs/tui/screens/moderation_test.exs test/foglet_bbs/tui/layout_smoke_test.exs --max-failures 3 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/screens/moderation.ex` exists
    - Contains `defmodule Foglet.TUI.Screens.Moderation`
    - Contains `@behaviour Foglet.TUI.Screen`
    - Contains `alias Foglet.TUI.Screens.Moderation.State`
    - Contains `alias Foglet.TUI.Screens.ShellVisibility`
    - Contains `alias Foglet.TUI.Widgets.Chrome.ScreenFrame`
    - Contains `alias Foglet.TUI.Widgets.Input.Tabs`
    - `grep -q "ScreenFrame.render(state, \"Moderation\"" lib/foglet_bbs/tui/screens/moderation.ex`
    - `grep -q "ShellVisibility.moderation_visible?" lib/foglet_bbs/tui/screens/moderation.ex` (defensive check)
    - All five Phase 8 placeholder messages present: `grep -q "Phase 8" lib/foglet_bbs/tui/screens/moderation.ex`
    - No fake-action function defs: `! grep -qE "def (ban_user|approve_queue_item|remove_post|issue_sanction|sanction_user)" lib/foglet_bbs/tui/screens/moderation.ex`
    - No domain imports: `! grep -qE "(FogletBbs\\.Repo|Foglet\\.Accounts\\.|Foglet\\.Sanctions\\.|Foglet\\.Moderation\\.)" lib/foglet_bbs/tui/screens/moderation.ex`
    - `mix test test/foglet_bbs/tui/screens/moderation_test.exs` exits 0 (all 10 Plan 01 tests pass)
    - Moderation smoke test in layout_smoke_test.exs passes
    - `mix compile --warnings-as-errors --force` exits 0
    - `mix format --check-formatted lib/foglet_bbs/tui/screens/moderation.ex` exits 0
  </acceptance_criteria>
  <done>
    Moderation screen renders the locked D-10 tab set through ScreenFrame, delegates tabs to `Tabs.handle_event/2`, returns to `:main_menu` on Q, consults `ShellVisibility.moderation_visible?/1` defensively, and never dispatches fake operator actions. All 10 Plan 01 moderation_test.exs tests green; moderation smoke test green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User key events → Moderation.handle_key/2 | Untrusted input; routed through Tabs widget or Q-to-main_menu. Unknown keys return `:no_match`. |
| Session role → render/1 authorization guard | Defensive check mirrors menu visibility in case an attacker-controlled navigation bypasses the menu (Pitfall 3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02 | Elevation of Privilege | Foglet.TUI.Screens.Moderation | mitigate | (a) Menu visibility gates entry (Plan 07); (b) Shell's own `render/1` consults `ShellVisibility.moderation_visible?/1` and renders a "Not available" column for unauthorized actors. Plan 01 tests exercise both `:mod` and `:sysop` succeed; `:user` would fall into the defensive branch if ever routed here. ASVS V4. |
| T-00-02-b | Tampering | Moderation placeholder copy | mitigate | Acceptance criterion greps for absence of `ban_user`, `approve_queue_item`, `remove_post`, `issue_sanction`, `sanction_user`. Plan 01 tests assert commands list contains no such tuples. ASVS V4, V5. |
| T-00-INPUT | Input Validation | handle_key/2 | mitigate | Unknown keys return `:no_match`; numeric input 1-5 routes to tab-jump via Tabs widget (whitelisted to ≤ 5 tabs, no index drift). ASVS V5. |
</threat_model>

<verification>
1. `mix test test/foglet_bbs/tui/screens/moderation_test.exs` — 10 tests pass.
2. Moderation smoke test in `test/foglet_bbs/tui/layout_smoke_test.exs` passes.
3. `mix compile --warnings-as-errors --force` exits 0.
4. `mix format --check-formatted` exits 0 on both new files.
5. Forbidden-function greps all confirm absence of fake-action definitions.
</verification>

<success_criteria>
- Moderation shell + state struct compile clean and pass all Plan 01 tests.
- MODR-01 satisfied: moderator/sysop can navigate into the shell (pending Plan 07 menu wiring).
- D-10 tab set rendered in exact locked order.
- D-13 respected: no fake moderation actions.
- T-00-02 mitigated via defensive role check + forbidden-function guards.
</success_criteria>

<output>
After completion, create `.planning/phases/00-screen-shells-and-shared-surface-primitives/00-05-SUMMARY.md` recording:
- Module paths created.
- Exact tab labels and order.
- Tests that flipped GREEN from moderation_test.exs.
- Confirmation that forbidden-function guards pass.
</output>
