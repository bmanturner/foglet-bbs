---
phase: 00
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/foglet_bbs/tui/screens/account_test.exs
  - test/foglet_bbs/tui/screens/moderation_test.exs
  - test/foglet_bbs/tui/screens/sysop_test.exs
  - test/foglet_bbs/tui/screens/shared/invites_surface_test.exs
  - test/foglet_bbs/tui/screens/main_menu_test.exs
  - test/foglet_bbs/tui/app_test.exs
  - test/foglet_bbs/tui/layout_smoke_test.exs
autonomous: true
requirements: [ACCT-01, MODR-01, SYSO-01]
tags: [tui, tests, tdd, screens, phase-00]
must_haves:
  truths:
    - "Failing tests exist for Account, Moderation, Sysop shells and shared InvitesSurface before any production code is written"
    - "main_menu_test.exs asserts new Account/Moderation/Sysop menu entries and role-gated visibility"
    - "app_test.exs asserts :account, :moderation, :sysop routing and screen_state seeding"
    - "layout_smoke_test.exs asserts each new shell renders tabs at distinct y positions within height=24"
    - "Every failing test describes the exact contract (tab labels, atoms, key semantics) implementation must satisfy"
  artifacts:
    - path: "test/foglet_bbs/tui/screens/account_test.exs"
      provides: "ACCT-01 behavior contract (PROFILE/PREFS tabs, conditional INVITES, read-only placeholders)"
      contains: "Foglet.TUI.Screens.AccountTest"
    - path: "test/foglet_bbs/tui/screens/moderation_test.exs"
      provides: "MODR-01 behavior contract (QUEUE/LOG/USERS/SANCTIONS/BOARDS, role-gate, placeholders)"
      contains: "Foglet.TUI.Screens.ModerationTest"
    - path: "test/foglet_bbs/tui/screens/sysop_test.exs"
      provides: "SYSO-01 behavior contract (SITE/BOARDS/LIMITS/SYSTEM/USERS, role-gate, placeholders)"
      contains: "Foglet.TUI.Screens.SysopTest"
    - path: "test/foglet_bbs/tui/screens/shared/invites_surface_test.exs"
      provides: "Shared INVITES placeholder/loading behavior and visibility helper logic"
      contains: "Foglet.TUI.Screens.Shared.InvitesSurfaceTest"
  key_links:
    - from: "test/foglet_bbs/tui/screens/account_test.exs"
      to: "Foglet.TUI.Screens.Account (not yet implemented)"
      via: "module reference in test setup and assertions"
      pattern: "Foglet.TUI.Screens.Account"
    - from: "test/foglet_bbs/tui/app_test.exs"
      to: "screen_module_for/1 (:account, :moderation, :sysop) (not yet implemented)"
      via: "route assertions on new screen atoms"
      pattern: ":account|:moderation|:sysop"
---

<objective>
Create the failing test bed (Wave 0) for Phase 0 before any production code is written.

Purpose: Every downstream plan (02-07) must have an `<automated>` verify command that points at these test files. Writing the tests first locks the behavior contract: tab labels, screen atoms, role-gate semantics, placeholder/loading/modal branches, and the shared INVITES surface visibility predicate. This is a pure Nyquist compliance step — after this plan, every other plan can run `mix test <file>` to prove progress.

Output: Four new test files and three extended test files. All tests MUST fail (or raise UndefinedFunctionError) at the end of this plan because production modules do not yet exist. That is the definition of success for Wave 0.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-screen-shells-and-shared-surface-primitives/00-CONTEXT.md
@.planning/phases/00-screen-shells-and-shared-surface-primitives/00-RESEARCH.md
@.planning/phases/00-screen-shells-and-shared-surface-primitives/00-VALIDATION.md
@CLAUDE.md

# Existing test exemplars — match their style
@test/foglet_bbs/tui/screens/main_menu_test.exs
@test/foglet_bbs/tui/screens/board_list_test.exs
@test/foglet_bbs/tui/app_test.exs
@test/foglet_bbs/tui/layout_smoke_test.exs
@test/foglet_bbs/tui/widgets/input/tabs_test.exs

# Production exemplars the tests will pattern-match against
@lib/foglet_bbs/tui/app.ex
@lib/foglet_bbs/tui/screens/main_menu.ex
@lib/foglet_bbs/tui/screens/board_list.ex
@lib/foglet_bbs/tui/widgets/input/tabs.ex

<interfaces>
Key contracts the tests will lock. Downstream plans MUST implement these exactly.

From lib/foglet_bbs/tui/screen.ex (existing behavior):
```elixir
@callback render(state :: app_state()) :: any()
@callback handle_key(key :: key_event(), state :: app_state()) :: handle_key_result()
@callback init_screen_state(opts :: keyword()) :: map()
```

From lib/foglet_bbs/tui/widgets/input/tabs.ex:
```elixir
@spec init(keyword()) :: t()           # opts: [:tabs, :active]
@spec handle_event(map(), t()) :: {t(), {:tab_changed, non_neg_integer()} | nil}
@spec render(t(), keyword()) :: any()  # opts: [:theme]
```

From lib/foglet_bbs/accounts/user.ex:
```elixir
@valid_roles [:user, :mod, :sysop]
```

New contracts these tests DEFINE (implementation follows in later plans):
- `Foglet.TUI.Screens.Account` — tabs `["PROFILE", "PREFS"]` base, `["PROFILE", "PREFS", "INVITES"]` when visible
- `Foglet.TUI.Screens.Moderation` — tabs `["QUEUE", "LOG", "USERS", "SANCTIONS", "BOARDS"]`
- `Foglet.TUI.Screens.Sysop` — tabs `["SITE", "BOARDS", "LIMITS", "SYSTEM", "USERS"]`
- `Foglet.TUI.Screens.Shared.InvitesSurface.visible?(user, invite_code_generators)` — boolean
- `Foglet.TUI.Screens.Shared.InvitesSurface.default_state/0` — placeholder-only state
- `Foglet.TUI.Screens.Shared.InvitesSurface.title/0 → "INVITES"`
- App routing: `screen_module_for(:account | :moderation | :sysop)`
- `state.screen_state[:account | :moderation | :sysop]` — struct from respective state module
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test files for Account, Moderation, Sysop shells and shared InvitesSurface</name>
  <files>
    - test/foglet_bbs/tui/screens/account_test.exs (new)
    - test/foglet_bbs/tui/screens/moderation_test.exs (new)
    - test/foglet_bbs/tui/screens/sysop_test.exs (new)
    - test/foglet_bbs/tui/screens/shared/invites_surface_test.exs (new)
  </files>
  <read_first>
    - .planning/phases/00-screen-shells-and-shared-surface-primitives/00-CONTEXT.md (D-03 through D-13, especially tab sets D-08/D-10/D-11)
    - .planning/phases/00-screen-shells-and-shared-surface-primitives/00-RESEARCH.md (Patterns 1-4, Anti-Patterns)
    - test/foglet_bbs/tui/screens/main_menu_test.exs (style: use %Foglet.TUI.App{} |> Map.from_struct and collect_text_values helper)
    - test/foglet_bbs/tui/screens/board_list_test.exs (style: render with nil state renders loading; render with [] renders placeholder)
    - lib/foglet_bbs/tui/widgets/input/tabs.ex (Tabs contract: init/1 returns struct with raxol_state, handle_event/2 returns {state, {:tab_changed, idx} | nil})
    - lib/foglet_bbs/tui/screens/main_menu.ex (MainMenu uses ScreenFrame.render(state, title, content, key_list))
    - lib/foglet_bbs/accounts/user.ex (role atoms :user, :mod, :sysop)
  </read_first>
  <behavior>
    Per D-03, D-04, D-05, D-08, D-09, D-10, D-11, D-12, D-13:

    ### account_test.exs (covers ACCT-01, T-00-01)
    Test module: `Foglet.TUI.Screens.AccountTest`. Setup: build a `%Foglet.TUI.App{current_screen: :account, current_user: %Foglet.Accounts.User{id: "u1", handle: "alice", role: :user}, session_context: %{}, terminal_size: {80, 24}, screen_state: %{}}` and feed it to the screen through the same pattern as `main_menu_test.exs`. Use `Map.from_struct/1` like existing tests.

    Write these tests (all must fail — `UndefinedFunctionError` or assertion failures):

    1. `test "init_screen_state/1 returns a struct with active_tab: 0 and a Tabs wrapper state"` — asserts the state module exists, has `active_tab == 0`, and `tabs` field is a `%Foglet.TUI.Widgets.Input.Tabs{}` struct.
    2. `test "render/1 does not crash with default screen state"` — seed `screen_state: %{account: Account.init_screen_state()}` and call `Account.render(state)`.
    3. `test "render/1 shows PROFILE and PREFS tab labels by default"` — flatten text via `collect_text_values/1` helper copied from `main_menu_test.exs`, assert `"PROFILE" in flat` and `"PREFS" in flat`.
    4. `test "render/1 omits INVITES when InvitesSurface.visible?/2 returns false"` — use a user with `role: :user` and stub/override invite policy to a value that makes `visible?` return false; assert `"INVITES"` is NOT in flat text.
    5. `test "render/1 includes INVITES when InvitesSurface.visible?/2 returns true"` — user with `role: :sysop` (always visible per D-07); assert `"INVITES" in flat`.
    6. `test "render/1 renders scaffold-only placeholder copy (no fake save buttons)"` — flat text MUST NOT contain any of `["Save", "Generate", "Revoke", "Approve"]`. Include this as a parameterized check.
    7. `test "handle_key/2 with Right arrow advances active_tab via Tabs.handle_event/2"` — invoke with `%{key: :right}`, assert returned state has `screen_state.account.active_tab == 1`.
    8. `test "handle_key/2 with digit '2' jumps to second tab"` — `%{key: :char, char: "2"}` → `active_tab == 1`.
    9. `test "handle_key/2 with 'Q' or 'q' returns to :main_menu"` — assert `current_screen == :main_menu`.
    10. `test "handle_key/2 with unknown key returns :no_match"` — `%{key: :char, char: "z"}`.
    11. `test "Account screen does NOT dispatch any fake operator commands (Save/Generate/Revoke)"` — for every key simulated, the returned commands list contains no tuple with first element in `[:save_profile, :generate_invite, :revoke_invite, :approve_user]`.

    ### moderation_test.exs (covers MODR-01, T-00-02)
    Test module: `Foglet.TUI.Screens.ModerationTest`. Same setup pattern but `current_screen: :moderation` and `role: :mod`.

    1. `test "init_screen_state/1 returns struct with active_tab: 0 and Tabs wrapper"`.
    2. `test "render/1 does not crash with default screen state"`.
    3. `test "render/1 shows all five tab labels: QUEUE, LOG, USERS, SANCTIONS, BOARDS (in that order)"` — flat text contains each; also assert order by finding index positions and checking they ascend.
    4. `test "render/1 renders scaffold-only placeholder copy (no fake moderation actions)"` — forbidden substrings: `["Ban", "Unban", "Sanction", "Approve", "Remove", "Delete"]` appearing as action affordances (key-bar entries or buttons).
    5. `test "handle_key/2 with Right arrow advances active_tab"`.
    6. `test "handle_key/2 with digit '3' jumps to index 2 (USERS)"`.
    7. `test "handle_key/2 with Home returns to tab 0"`, `test "End jumps to last tab"`.
    8. `test "handle_key/2 with 'Q'/'q' returns to :main_menu"`.
    9. `test "handle_key/2 unknown key returns :no_match"`.
    10. `test "Moderation screen does NOT dispatch fake moderation commands"` — commands list must not contain tuples with first element in `[:ban_user, :approve_queue_item, :remove_post, :issue_sanction]`.

    ### sysop_test.exs (covers SYSO-01, T-00-03)
    Test module: `Foglet.TUI.Screens.SysopTest`. Setup uses `role: :sysop`, `current_screen: :sysop`.

    1. `test "init_screen_state/1 returns struct with active_tab: 0 and Tabs wrapper"`.
    2. `test "render/1 does not crash with default screen state"`.
    3. `test "render/1 shows all five tab labels in order: SITE, BOARDS, LIMITS, SYSTEM, USERS"`.
    4. `test "render/1 renders scaffold-only placeholder copy (no fake config writes)"` — forbidden substrings as action affordances: `["Save", "Apply", "Revert", "Set"]` appearing in key-bar or button contexts.
    5. `test "handle_key/2 advances through all five tabs with Right arrow"` — loop and assert `active_tab` walks 0→1→2→3→4 and stays at 4.
    6. `test "handle_key/2 with digit '5' jumps to USERS tab"`.
    7. `test "handle_key/2 with 'Q'/'q' returns to :main_menu"`.
    8. `test "handle_key/2 unknown key returns :no_match"`.
    9. `test "Sysop screen does NOT dispatch fake config-write commands"` — commands list must not contain tuples with first element in `[:save_config, :apply_config, :set_config]`.

    ### shared/invites_surface_test.exs (covers Phase 0 Success Criterion 3, T-00-04)
    Test module: `Foglet.TUI.Screens.Shared.InvitesSurfaceTest`. Subject under test: `Foglet.TUI.Screens.Shared.InvitesSurface`.

    1. `test "title/0 returns \"INVITES\""`.
    2. `test "default_state/0 returns a struct with items: [] (placeholder, not loading)"` — asserts the shape needed by later phases; `items` is `[]` (empty, placeholder branch) NOT `nil` (loading branch). Note: D-12 says `nil` = loading and `[]` = placeholder; Phase 0 default must be `[]` so the shell renders the non-operational placeholder message by default.
    3. `test "visible?/2 returns true for role :sysop regardless of policy"` — per research Pattern 4 example, sysop always sees INVITES scaffold in Phase 0.
    4. `test "visible?/2 returns true for role :mod when policy is \"mods\""`.
    5. `test "visible?/2 returns true for role :user when policy is \"any_user\""`.
    6. `test "visible?/2 returns false for role :user when policy is \"sysop_only\""`.
    7. `test "visible?/2 returns false for role :mod when policy is \"sysop_only\""`.
    8. `test "visible?/2 returns false when user is nil"`.
    9. `test "render/2 with %{items: nil} state renders loading branch"` — assert output tree contains a Spinner-adjacent `"Loading"` substring (flattened text).
    10. `test "render/2 with %{items: []} state renders placeholder copy that is obviously scaffold-only"` — assert flat text contains `"scaffold"` OR `"not yet"` OR `"later phase"` — some phrasing that makes it clear the tab is non-operational.
    11. `test "render/2 with %{items: [_|_]} state does NOT crash (future-facing)"` — pass `%{items: [%{code: "XYZ"}]}`, assert function returns a map/tree without raising. Phase 0 may choose any safe rendering; this test locks "does not crash".
    12. `test "InvitesSurface defines no fake generate/revoke functions"` — `refute function_exported?(InvitesSurface, :generate_invite, 1)` and same for `:revoke_invite, :save_invite`.

    ### Test-file hygiene (all four files)
    - `use ExUnit.Case, async: true` — all Phase 0 tests must be async-safe.
    - Import theme only where render/2 needs it: `alias Foglet.TUI.Theme` and use `Theme.default()`.
    - DO NOT instantiate a real SSH session, DB, or start processes. All tests are pure state-in/state-out.
    - Copy the `collect_text_values/1` helper from `test/foglet_bbs/tui/screens/main_menu_test.exs` into a private function in each file (or extract to `test/support/` if the reviewer prefers — defer to executor judgment).
  </behavior>
  <action>
    Create each file with the tests described in <behavior>.

    File locations (exact):
    - `test/foglet_bbs/tui/screens/account_test.exs`
    - `test/foglet_bbs/tui/screens/moderation_test.exs`
    - `test/foglet_bbs/tui/screens/sysop_test.exs`
    - `test/foglet_bbs/tui/screens/shared/invites_surface_test.exs` (create the `shared/` subdirectory)

    Follow the exact style of `test/foglet_bbs/tui/screens/main_menu_test.exs`:
    - Use `use ExUnit.Case, async: true`.
    - Setup block returns `%{state: state}` where `state` is a `%Foglet.TUI.App{...} |> Map.from_struct()`.
    - Define a private `collect_text_values/1` helper (copy from main_menu_test.exs).
    - Scope each test group with `describe "init_screen_state/1"`, `describe "render/1"`, `describe "handle_key/2"`.

    After writing all four files, run `mix test test/foglet_bbs/tui/screens/account_test.exs` and verify it fails with `UndefinedFunctionError` or compilation error referencing `Foglet.TUI.Screens.Account`. This is the expected RED state.

    Do NOT implement any production code to make tests pass — that is the job of later plans. The ONLY acceptable outcome of this task is "tests compile OR fail with UndefinedFunctionError referring to not-yet-existent modules".

    Per D-13: placeholder-copy checks (forbidden substrings) MUST be present in all three shell tests to prevent later plans from accidentally introducing fake operator actions.
  </action>
  <verify>
    <automated>mix test test/foglet_bbs/tui/screens/account_test.exs test/foglet_bbs/tui/screens/moderation_test.exs test/foglet_bbs/tui/screens/sysop_test.exs test/foglet_bbs/tui/screens/shared/invites_surface_test.exs 2>&1 | grep -E "(UndefinedFunctionError|CompileError|does not exist|Foglet\.TUI\.Screens\.Account|Foglet\.TUI\.Screens\.Moderation|Foglet\.TUI\.Screens\.Sysop|Foglet\.TUI\.Screens\.Shared\.InvitesSurface)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `test/foglet_bbs/tui/screens/account_test.exs` exists and contains `defmodule Foglet.TUI.Screens.AccountTest`
    - `test/foglet_bbs/tui/screens/moderation_test.exs` exists and contains `defmodule Foglet.TUI.Screens.ModerationTest`
    - `test/foglet_bbs/tui/screens/sysop_test.exs` exists and contains `defmodule Foglet.TUI.Screens.SysopTest`
    - `test/foglet_bbs/tui/screens/shared/invites_surface_test.exs` exists and contains `defmodule Foglet.TUI.Screens.Shared.InvitesSurfaceTest`
    - `grep -q "QUEUE" test/foglet_bbs/tui/screens/moderation_test.exs && grep -q "SANCTIONS" test/foglet_bbs/tui/screens/moderation_test.exs && grep -q "LOG" test/foglet_bbs/tui/screens/moderation_test.exs && grep -q "USERS" test/foglet_bbs/tui/screens/moderation_test.exs && grep -q "BOARDS" test/foglet_bbs/tui/screens/moderation_test.exs`
    - `grep -q "SITE" test/foglet_bbs/tui/screens/sysop_test.exs && grep -q "LIMITS" test/foglet_bbs/tui/screens/sysop_test.exs && grep -q "SYSTEM" test/foglet_bbs/tui/screens/sysop_test.exs`
    - `grep -q "PROFILE" test/foglet_bbs/tui/screens/account_test.exs && grep -q "PREFS" test/foglet_bbs/tui/screens/account_test.exs && grep -q "INVITES" test/foglet_bbs/tui/screens/account_test.exs`
    - `grep -q "visible?" test/foglet_bbs/tui/screens/shared/invites_surface_test.exs`
    - `grep -q "use ExUnit.Case, async: true" test/foglet_bbs/tui/screens/account_test.exs`
    - `mix compile --warnings-as-errors` exits 0 (tests compile even if subjects are undefined — tests reference modules that may not exist yet, which is fine; compilation failures only occur on unresolved imports/aliases, not on unresolved function calls)
    - `mix format --check-formatted test/foglet_bbs/tui/screens/account_test.exs test/foglet_bbs/tui/screens/moderation_test.exs test/foglet_bbs/tui/screens/sysop_test.exs test/foglet_bbs/tui/screens/shared/invites_surface_test.exs` exits 0
  </acceptance_criteria>
  <done>
    Four test files exist under `test/foglet_bbs/tui/screens/`, they reference the future modules `Foglet.TUI.Screens.Account`, `Foglet.TUI.Screens.Moderation`, `Foglet.TUI.Screens.Sysop`, and `Foglet.TUI.Screens.Shared.InvitesSurface`, and `mix test` on each file fails with `UndefinedFunctionError` or compile-error referring to those not-yet-created modules. Placeholder-forbidden-substring checks, tab-label assertions, visibility matrix, and `Q` → `:main_menu` semantics are all encoded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend main_menu_test.exs, app_test.exs, and layout_smoke_test.exs with failing assertions for the new shells</name>
  <files>
    - test/foglet_bbs/tui/screens/main_menu_test.exs (extended)
    - test/foglet_bbs/tui/app_test.exs (extended)
    - test/foglet_bbs/tui/layout_smoke_test.exs (extended)
  </files>
  <read_first>
    - test/foglet_bbs/tui/screens/main_menu_test.exs (CURRENT CONTENTS — all 98 lines; new tests go alongside existing ones)
    - test/foglet_bbs/tui/app_test.exs (CURRENT CONTENTS lines 1-120 — new describe blocks append to existing)
    - test/foglet_bbs/tui/layout_smoke_test.exs (CURRENT CONTENTS — see the `main_menu renders welcome and all menu items at distinct y positions` test as the analog; new Account/Moderation/Sysop smoke tests follow its pattern)
    - .planning/phases/00-screen-shells-and-shared-surface-primitives/00-CONTEXT.md (D-01: Account is standard menu destination; D-02: Moderation for :mod/:sysop, Sysop for :sysop only)
    - lib/foglet_bbs/tui/screens/main_menu.ex (@menu_items and @menu_keys pattern)
    - lib/foglet_bbs/tui/app.ex (lines 30-72: screen type union and struct; lines 805-813: screen_module_for/1 clauses)
    - lib/foglet_bbs/accounts/user.ex (role values)
  </read_first>
  <behavior>
    ### main_menu_test.exs additions (per D-01, D-02)

    Add a new describe block `describe "Phase 0 shell entry points"`:

    1. `test "authenticated user with role :user sees Account menu entry"` — flat text includes `"Account"` and some key label (e.g., `"[A]"`).
    2. `test "role :user does NOT see Moderation menu entry"` — flat text MUST NOT include `"Moderation"`.
    3. `test "role :user does NOT see Sysop menu entry"` — flat text MUST NOT include `"Sysop"`.
    4. `test "role :mod sees Account AND Moderation entries"` — flat text includes both; MUST NOT include `"Sysop"`.
    5. `test "role :sysop sees Account AND Moderation AND Sysop entries"` — all three visible.
    6. `test "'A'/'a' navigates to :account and seeds screen_state"` — `{:update, s, cmds}` → `s.current_screen == :account`, `s.screen_state.account` is a struct (not nil).
    7. `test "'M'/'m' navigates to :moderation for role :mod"` — similar shape.
    8. `test "'M'/'m' returns :no_match for role :user (menu entry not rendered, key bound guarded)"` — `MainMenu.handle_key(%{key: :char, char: "M"}, state_with_user_role) == :no_match`. This locks D-02 / Pitfall 3 — key handler respects visibility.
    9. `test "'S'/'s' navigates to :sysop for role :sysop"`.
    10. `test "'S'/'s' returns :no_match for role :mod and :user"`.

    ### app_test.exs additions (per D-03, D-04)

    Add a new describe block `describe "Phase 0 screen routing"`:

    1. `test "screen_module_for/1 maps :account to Foglet.TUI.Screens.Account"` — since `screen_module_for/1` is private, assert via behavior: navigating to `:account` and calling `App.view(state)` does not crash and calls the right render path. Use `App.update({:navigate, :account}, state)` then verify `state.current_screen == :account` and `App.view(new_state)` returns a non-nil map.
    2. `test "screen_module_for/1 maps :moderation similarly"`.
    3. `test "screen_module_for/1 maps :sysop similarly"`.
    4. `test "App @type screen includes :account, :moderation, :sysop (compile-time check)"` — this is a structural check: use `Code.fetch_docs/1` or typespec introspection. If too brittle, substitute: `refute App.update({:navigate, :account}, state) |> elem(0) |> Map.get(:current_screen) == :login` — i.e. navigation actually succeeds.
    5. `test "init/1 does NOT route authenticated users into :account/:moderation/:sysop by default (stays :main_menu)"` — defends against regression.

    ### layout_smoke_test.exs additions (per Phase 0 Success Criterion 2)

    Add three new tests, one per shell, patterned after the existing `main_menu renders welcome and all menu items at distinct y positions`:

    1. `test "account shell renders PROFILE/PREFS tab labels at distinct x positions within height=24"` — build `%App{current_screen: :account, current_user: %{id: "u1", handle: "alice", role: :user, status: :active}, screen_state: %{account: Account.init_screen_state()}, terminal_size: {80, 24}}`, run `Account.render(state) |> apply_layout/1`, assert `"PROFILE"` and `"PREFS"` appear as text elements, assert max_y <= 24.
    2. `test "moderation shell renders all five tab labels within height=24"` — tab labels `["QUEUE", "LOG", "USERS", "SANCTIONS", "BOARDS"]` all present, max_y <= 24. User role `:mod`.
    3. `test "sysop shell renders all five tab labels within height=24"` — tab labels `["SITE", "BOARDS", "LIMITS", "SYSTEM", "USERS"]` all present, max_y <= 24. User role `:sysop`.

    Each test alias-imports `Foglet.TUI.Screens.{Account, Moderation, Sysop}` (these modules don't yet exist — that is the RED state).

    DO NOT introduce DB or SSH setup. Follow the existing `layout_smoke_test.exs` `setup` that seeds config ETS cache.
  </behavior>
  <action>
    Edit each of the three test files to append the tests described in <behavior>.

    For `main_menu_test.exs`: The existing setup uses `role: :user`. Add a second setup helper (e.g., a private `build_state/1` taking `role`) so the new tests can exercise each role path without duplication. Do NOT remove existing tests — append.

    For `app_test.exs`: Add a new `describe "Phase 0 screen routing"` block at the end of the file, before the closing `end` of the outer module. Keep imports/aliases scoped where sensible.

    For `layout_smoke_test.exs`: Add an alias line `alias Foglet.TUI.Screens.{Account, Moderation, Sysop}` near the existing `alias Foglet.TUI.Screens.{...}` block. Append the three new tests after the existing `new_thread compose step...` test and before the closing `end`.

    Verify:
    - `mix test test/foglet_bbs/tui/screens/main_menu_test.exs` fails on new tests but NOT on pre-existing ones (regression guard).
    - `mix test test/foglet_bbs/tui/app_test.exs` similarly.
    - `mix test test/foglet_bbs/tui/layout_smoke_test.exs` similarly.

    All three files must still compile (`mix compile --warnings-as-errors` passes).
  </action>
  <verify>
    <automated>mix test test/foglet_bbs/tui/screens/main_menu_test.exs test/foglet_bbs/tui/app_test.exs test/foglet_bbs/tui/layout_smoke_test.exs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Phase 0 shell entry points" test/foglet_bbs/tui/screens/main_menu_test.exs`
    - `grep -q "Phase 0 screen routing" test/foglet_bbs/tui/app_test.exs`
    - `grep -q "account shell renders" test/foglet_bbs/tui/layout_smoke_test.exs`
    - `grep -q "moderation shell renders" test/foglet_bbs/tui/layout_smoke_test.exs`
    - `grep -q "sysop shell renders" test/foglet_bbs/tui/layout_smoke_test.exs`
    - `grep -q "role :mod" test/foglet_bbs/tui/screens/main_menu_test.exs`
    - `grep -q "role :sysop" test/foglet_bbs/tui/screens/main_menu_test.exs`
    - `mix compile --warnings-as-errors --force` exits 0
    - `mix format --check-formatted test/foglet_bbs/tui/screens/main_menu_test.exs test/foglet_bbs/tui/app_test.exs test/foglet_bbs/tui/layout_smoke_test.exs` exits 0
    - Existing tests in all three files still pass (new tests fail — expected RED)
  </acceptance_criteria>
  <done>
    Three existing test files extended with Phase 0 assertions: menu role-gating, screen_module_for routing, layout smoke for all three new shells. Existing tests unaffected. New tests fail with `UndefinedFunctionError` or assertion failures — confirmed RED state for Wave 0 completion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → production code | Tests reference modules that do not yet exist; planner must not let this weaken later production code with "tests expected less" shortcuts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | account_test.exs / ACCT-01 | mitigate | Tests assert read-only shell, no fake save commands, scaffold-only placeholder copy (ASVS V3, V5). Forbidden-substring guard prevents implementation drift. |
| T-00-02 | Elevation of Privilege | moderation_test.exs / MODR-01 | mitigate | Tests assert role-gated menu entry (`:mod`/`:sysop` only) AND that unauthorized key ('M'/'m' for role `:user`) returns `:no_match`. Forbidden-substring guard prevents fake moderation actions (ASVS V4, V5). |
| T-00-03 | Elevation of Privilege | sysop_test.exs / SYSO-01 | mitigate | Tests assert role-gated menu entry (`:sysop` only) AND that 'S'/'s' for `:mod` and `:user` returns `:no_match`. Forbidden config-write command list prevents fake operations (ASVS V4, V5). |
| T-00-04 | Tampering | shared/invites_surface_test.exs | mitigate | Tests assert `refute function_exported?(InvitesSurface, :generate_invite, 1)` etc., locking the primitive as placeholder-only. Visibility matrix covers all role/policy combinations (ASVS V4, V5). |
</threat_model>

<verification>
After this plan completes:
1. `mix test test/foglet_bbs/tui/screens/account_test.exs` fails with UndefinedFunctionError referring to Foglet.TUI.Screens.Account.
2. `mix test test/foglet_bbs/tui/screens/moderation_test.exs` fails similarly.
3. `mix test test/foglet_bbs/tui/screens/sysop_test.exs` fails similarly.
4. `mix test test/foglet_bbs/tui/screens/shared/invites_surface_test.exs` fails similarly.
5. `mix test test/foglet_bbs/tui/screens/main_menu_test.exs` passes pre-existing tests, fails new ones.
6. `mix test test/foglet_bbs/tui/app_test.exs` same.
7. `mix test test/foglet_bbs/tui/layout_smoke_test.exs` same.
8. `mix compile --warnings-as-errors --force` exits 0 (tests compile cleanly).
9. `mix format --check-formatted` exits 0 for all seven test files.
</verification>

<success_criteria>
- Wave 0 test bed is complete — every downstream plan (02-07) has an `<automated>` command that points into these files.
- No production code has been written — that is the job of Plans 02-07.
- All Phase 0 locked decisions (D-01 through D-13) are encoded as test assertions.
- Security threats T-00-01 through T-00-04 have explicit test guards (role-gating, forbidden-substring, no-fake-actions).
- The failing tests are the contract Plans 02-07 must satisfy.
</success_criteria>

<output>
After completion, create `.planning/phases/00-screen-shells-and-shared-surface-primitives/00-01-SUMMARY.md` recording:
- Test file paths created.
- Which decisions (D-01..D-13) map to which test assertions.
- Exact RED-state failures (module names + test descriptions) that Plans 02-07 must flip to GREEN.
</output>
