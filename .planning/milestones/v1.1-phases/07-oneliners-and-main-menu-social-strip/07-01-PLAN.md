---
phase: 07-oneliners-and-main-menu-social-strip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/foglet_bbs/oneliners.ex
  - lib/foglet_bbs/oneliners/entry.ex
  - priv/repo/migrations/*_create_oneliners.exs
  - test/foglet_bbs/oneliners/oneliners_test.exs
autonomous: true
requirements:
  - ONEL-01
  - ONEL-03
must_haves:
  truths:
    - "User oneliners are persisted in Postgres and survive application restart."
    - "Recent visible oneliners can be loaded newest first for the main menu."
    - "Blank and 121-character bodies are rejected before insert; 120-character bodies are accepted."
    - "The same authenticated user cannot create two visible oneliners in a row."
  artifacts:
    - path: "lib/foglet_bbs/oneliners.ex"
      provides: "Foglet.Oneliners context with create and recent visible list APIs"
      exports: ["create_entry/2", "list_recent_visible/1"]
    - path: "lib/foglet_bbs/oneliners/entry.ex"
      provides: "Foglet.Oneliners.Entry schema and create_changeset/2"
      contains: "timestamps(updated_at: false)"
    - path: "priv/repo/migrations/*_create_oneliners.exs"
      provides: "oneliners table with hidden default and visible-recents partial index"
      contains: "create table(:oneliners"
    - path: "test/foglet_bbs/oneliners/oneliners_test.exs"
      provides: "database-backed domain coverage for Phase 7 persistence rules"
  key_links:
    - from: "lib/foglet_bbs/oneliners.ex"
      to: "FogletBbs.Repo"
      via: "Repo.insert and Ecto query"
      pattern: "Repo\\.(insert|all)"
    - from: "lib/foglet_bbs/oneliners.ex"
      to: "Foglet.Oneliners.Entry"
      via: "schema changeset and query"
      pattern: "Entry\\.create_changeset"
---

<objective>
Create the Postgres-backed oneliner domain foundation required by D-01 through D-11.

Purpose: Phase 7 needs durable, validated oneliners before any TUI can render or submit them.
Output: Migration, schema, context API, and database-backed tests for recent visible listing and creation rules.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/DATA_MODEL.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-oneliners-and-main-menu-social-strip/07-CONTEXT.md
@.planning/phases/07-oneliners-and-main-menu-social-strip/07-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@lib/foglet_bbs/schema.ex
@lib/foglet_bbs/accounts/user.ex
@test/support/accounts_fixtures.ex

<interfaces>
New public API contract:
```elixir
Foglet.Oneliners.create_entry(%Foglet.Accounts.User{} = user, map()) ::
  {:ok, Foglet.Oneliners.Entry.t()} | {:error, Ecto.Changeset.t()} | {:error, :same_user_latest_visible}

Foglet.Oneliners.list_recent_visible(pos_integer()) :: [Foglet.Oneliners.Entry.t()]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add oneliner migration and schema</name>
  <files>priv/repo/migrations/*_create_oneliners.exs, lib/foglet_bbs/oneliners/entry.ex, test/foglet_bbs/oneliners/oneliners_test.exs</files>
  <read_first>
    - docs/DATA_MODEL.md
    - .planning/phases/07-oneliners-and-main-menu-social-strip/07-CONTEXT.md
    - .planning/phases/07-oneliners-and-main-menu-social-strip/07-RESEARCH.md
    - lib/foglet_bbs/schema.ex
    - lib/foglet_bbs/accounts/user.ex
    - priv/repo/migrations/20260424001147_create_invite_codes.exs
    - test/foglet_bbs/accounts/invite_test.exs
    - test/support/accounts_fixtures.ex
  </read_first>
  <behavior>
    - Test: `Entry.create_changeset(%Entry{user_id: user.id}, %{body: "hello"})` is valid and stores trimmed body.
    - Test: `%Entry{}` defines fields `:body`, `:hidden`, `:hidden_reason`, `:user_id`, `:hidden_by_id`, `:inserted_at` and does not define `:updated_at`.
    - Test: blank body and a 121-character body produce changeset errors; a 120-character body is valid.
  </behavior>
  <action>Create `Foglet.Oneliners.Entry` per D-01/D-04/D-06/D-07 using `use Foglet.Schema`, table `"oneliners"`, fields `body: :string`, `hidden: :boolean` default `false`, `hidden_reason: :string`, `belongs_to :user, Foglet.Accounts.User`, `belongs_to :hidden_by, Foglet.Accounts.User`, and `timestamps(updated_at: false)`. Add `@type t :: %__MODULE__{}` and `@doc/@spec create_changeset(t(), map()) :: Ecto.Changeset.t()`. `create_changeset/2` must cast only `[:body]`, trim with `update_change(:body, &String.trim/1)`, validate required `[:body, :user_id]`, validate length `min: 1, max: 120`, and must not cast `user_id`, `hidden`, `hidden_reason`, or `hidden_by_id` per D-05/D-11. Create a migration named with `mix ecto.gen.migration create_oneliners`; its table must be `create table(:oneliners, primary_key: false)`, include `add :id, :uuid, primary_key: true`, `add :body, :string, null: false`, `add :hidden, :boolean, null: false, default: false`, `add :hidden_reason, :string`, `add :user_id, references(:users, type: :uuid, on_delete: :delete_all), null: false`, `add :hidden_by_id, references(:users, type: :uuid, on_delete: :nilify_all)`, and `timestamps(type: :utc_datetime_usec, updated_at: false)`. Add `create index(:oneliners, [:inserted_at], where: "hidden = false")`.</action>
  <acceptance_criteria>
    - `rg "defmodule Foglet\\.Oneliners\\.Entry" lib/foglet_bbs/oneliners/entry.ex` returns one match.
    - `rg "cast\\(attrs, \\[:body\\]\\)" lib/foglet_bbs/oneliners/entry.ex` returns one match.
    - `rg "validate_length\\(:body, min: 1, max: 120\\)" lib/foglet_bbs/oneliners/entry.ex` returns one match.
    - `rg "timestamps\\(updated_at: false\\)" lib/foglet_bbs/oneliners/entry.ex` returns one match.
    - `rg "create table\\(:oneliners, primary_key: false\\)" priv/repo/migrations` returns one match.
    - `rg "where: \"hidden = false\"" priv/repo/migrations` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/foglet_bbs/oneliners/oneliners_test.exs</automated>
  </verify>
  <done>Migration and schema implement the locked field shape, hidden default, no updated_at timestamp, and 120-character domain validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Oneliners context APIs and domain tests</name>
  <files>lib/foglet_bbs/oneliners.ex, test/foglet_bbs/oneliners/oneliners_test.exs</files>
  <read_first>
    - lib/foglet_bbs/oneliners.ex
    - lib/foglet_bbs/oneliners/entry.ex
    - test/foglet_bbs/oneliners/oneliners_test.exs
    - lib/foglet_bbs/accounts.ex
    - test/support/accounts_fixtures.ex
    - .planning/phases/07-oneliners-and-main-menu-social-strip/07-CONTEXT.md
    - .planning/phases/07-oneliners-and-main-menu-social-strip/07-RESEARCH.md
  </read_first>
  <behavior>
    - Test: `create_entry(user, %{body: "hello"})` inserts one row with `user_id == user.id` and `hidden == false`.
    - Test: caller-supplied `%{user_id: other.id}` is ignored; persisted `user_id` is the authenticated actor.
    - Test: `list_recent_visible(3)` returns only `hidden == false`, newest first, at most 3 rows, with `entry.user` preloaded.
    - Test: if the latest visible entry belongs to the same actor, `create_entry/2` returns `{:error, :same_user_latest_visible}` and row count is unchanged.
  </behavior>
  <action>Create `Foglet.Oneliners` per D-01/D-03/D-05/D-08/D-09/D-10. Add `@moduledoc`, `@entry_limit_default 5`, public `create_entry/2`, public `list_recent_visible/1`, and accurate specs. `create_entry(%User{} = user, attrs)` must set ownership by building `%Entry{user_id: user.id}` before `Entry.create_changeset(attrs)`. Before inserting, query the latest visible entry ordered by `desc: :inserted_at` and reject with `{:error, :same_user_latest_visible}` when `latest.user_id == user.id`; otherwise insert. Use `Repo.transact/1` if a clean local pattern fits; unwrap successful transaction results so callers receive `{:ok, entry}` and `{:error, reason}`. `list_recent_visible(limit)` must clamp non-positive limits to `0`, cap normal limits to a bounded maximum of `20`, filter `where: e.hidden == false`, order `[desc: e.inserted_at]`, `limit: ^bounded_limit`, and `preload: [:user]`. Do not add a GenServer, PubSub, cache, retention policy, hide API, or sysop-editable max length in this phase per D-02/D-11/D-19.</action>
  <acceptance_criteria>
    - `rg "defmodule Foglet\\.Oneliners" lib/foglet_bbs/oneliners.ex` returns one match.
    - `rg "def create_entry\\(%User\\{\\} = user, attrs\\)" lib/foglet_bbs/oneliners.ex` returns one match.
    - `rg "%Entry\\{user_id: user\\.id\\}" lib/foglet_bbs/oneliners.ex` returns one match.
    - `rg "same_user_latest_visible" lib/foglet_bbs/oneliners.ex test/foglet_bbs/oneliners/oneliners_test.exs` returns at least two matches.
    - `rg "preload: \\[:user\\]" lib/foglet_bbs/oneliners.ex` returns one match.
    - `rg "hidden == false" lib/foglet_bbs/oneliners.ex` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/foglet_bbs/oneliners/oneliners_test.exs</automated>
  </verify>
  <done>Domain context satisfies ONEL-01 and ONEL-03 with persistence, bounded recent visible listing, author preload, actor-owned creates, and same-user back-to-back rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI/App -> Foglet.Oneliners | Untrusted form attrs cross into domain persistence. |
| Foglet.Oneliners -> Postgres | Validated domain records cross into durable storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing/Elevation of Privilege | `Foglet.Oneliners.create_entry/2` | mitigate | Set `user_id` from `%Foglet.Accounts.User{id: ...}` and never cast caller `user_id`. |
| T-07-02 | Tampering | `Foglet.Oneliners.Entry.create_changeset/2` | mitigate | Cast only `[:body]`, trim body, require body/user_id, validate length `min: 1, max: 120`. |
| T-07-03 | Information Disclosure | `Foglet.Oneliners.list_recent_visible/1` | mitigate | Query `hidden == false` only and preload only `:user` for handle rendering. |
| T-07-04 | Denial of Service | `Foglet.Oneliners.create_entry/2` | mitigate | Reject latest-visible same-user posts and cap recent listing to maximum 20 rows. |
</threat_model>

<verification>
Run `mix test test/foglet_bbs/oneliners/oneliners_test.exs` after each task in this plan.
</verification>

<success_criteria>
`Foglet.Oneliners` exposes the locked create/list APIs; oneliners persist to Postgres; recent visible listing is bounded, newest-first, and author-preloaded; all domain tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-oneliners-and-main-menu-social-strip/07-01-SUMMARY.md`.
</output>
