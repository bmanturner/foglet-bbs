---
phase: 13-board-subscription-management
plan: 3
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - lib/mix/tasks/foglet.board_subscriptions.ex
  - test/mix/tasks/foglet.board_subscriptions_test.exs
autonomous: true
requirements:
  - SUBS-04
must_haves:
  truths:
    - "Operator can list a user's board subscriptions from a break-glass Mix task."
    - "Operator can subscribe a user to an active board through the task."
    - "Operator cannot use the task to unsubscribe a required board or mutate archived boards."
  artifacts:
    - path: "lib/mix/tasks/foglet.board_subscriptions.ex"
      provides: "Break-glass operator subscription task"
      exports: ["run/1"]
    - path: "test/mix/tasks/foglet.board_subscriptions_test.exs"
      provides: "Task behavior and safety tests"
      contains: "required_subscription"
  key_links:
    - from: "lib/mix/tasks/foglet.board_subscriptions.ex"
      to: "lib/foglet_bbs/boards.ex"
      via: "context API calls only"
      pattern: "Boards\\.(board_directory_for|subscribe_user_to_board|unsubscribe_user_from_board)"
---

<objective>
Add the Phase 13 operator break-glass subscription management path.

Purpose: SUBS-04 is satisfied by a Mix task per D-15, while D-16 requires it to use the same `Foglet.Boards` rules as the terminal path.
Output: `mix foglet.board_subscriptions` task and task tests.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-board-subscription-management/13-SPEC.md
@.planning/phases/13-board-subscription-management/13-CONTEXT.md
@.planning/phases/13-board-subscription-management/13-RESEARCH.md
@lib/mix/tasks/foglet.user.create.ex
@lib/mix/tasks/foglet.user.promote.ex
@lib/mix/tasks/foglet.user.reset_password.ex
@lib/foglet_bbs/accounts.ex
@lib/foglet_bbs/boards.ex

<interfaces>
Use existing Mix task style:
```elixir
use Mix.Task
@shortdoc "..."
def run(args) do
  {:ok, _} = Application.ensure_all_started(:foglet_bbs)
  {opts, rest} = OptionParser.parse!(args, strict: [...])
end
```

Task command contract:
```bash
rtk mix foglet.board_subscriptions list --user HANDLE_OR_EMAIL
rtk mix foglet.board_subscriptions subscribe --user HANDLE_OR_EMAIL --board BOARD_SLUG
rtk mix foglet.board_subscriptions unsubscribe --user HANDLE_OR_EMAIL --board BOARD_SLUG
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13-03-01: Implement break-glass subscription Mix task</name>
  <read_first>AGENTS.md; .planning/phases/13-board-subscription-management/13-SPEC.md; .planning/phases/13-board-subscription-management/13-CONTEXT.md; lib/mix/tasks/foglet.user.create.ex; lib/foglet_bbs/accounts.ex; lib/foglet_bbs/boards.ex; test/mix/tasks</read_first>
  <files>lib/mix/tasks/foglet.board_subscriptions.ex; test/mix/tasks/foglet.board_subscriptions_test.exs</files>
  <behavior>
    - `list --user HANDLE_OR_EMAIL` prints subscribed and unsubscribed active boards with status.
    - `subscribe --user HANDLE_OR_EMAIL --board SLUG` calls `Boards.subscribe_user_to_board/2` and prints success.
    - `unsubscribe --user HANDLE_OR_EMAIL --board SLUG` calls `Boards.unsubscribe_user_from_board/2` and prints success only when allowed.
    - Unknown user, unknown board, archived board, and required-board unsubscribe exit non-zero with explicit output.
  </behavior>
  <action>Create `Mix.Tasks.Foglet.BoardSubscriptions`. Parse a positional action plus strict `--user` and `--board` switches with `OptionParser.parse!/2`. Resolve users by handle or email using existing `Foglet.Accounts` lookup functions; if no suitable public lookup exists, add a small context helper in `Foglet.Accounts` only if necessary and test it in the same file path owning Accounts, but keep this plan's task itself as a thin adapter. Resolve boards by slug through a context helper, not direct Repo from the task; if Plan 13-01 did not add `get_board_by_slug/1` non-bang, add it in `Foglet.Boards` with `nil` return and focused tests. For all mutations, call `Boards.subscribe_user_to_board/2` and `Boards.unsubscribe_user_from_board/2`; do not call `Repo.insert`, `Repo.delete`, or `Subscription.changeset` in the task. Use exact task name `foglet.board_subscriptions`. Print required-board unsubscribe output containing `required subscription`, archived-board output containing `archived`, unknown-user output containing `Unknown user`, and unknown-board output containing `Unknown board`, per D-17.</action>
  <verify>
    <automated>rtk mix test test/mix/tasks/foglet.board_subscriptions_test.exs</automated>
  </verify>
  <acceptance_criteria>`rg -n "defmodule Mix.Tasks.Foglet.BoardSubscriptions|foglet.board_subscriptions|subscribe_user_to_board|unsubscribe_user_from_board" lib/mix/tasks/foglet.board_subscriptions.ex test/mix/tasks/foglet.board_subscriptions_test.exs` shows the task and tests; `rg -n "Repo\\.|Subscription\\.changeset" lib/mix/tasks/foglet.board_subscriptions.ex` returns no matches.</acceptance_criteria>
  <done>Operator subscription inspection and adjustment is available through a safe break-glass Mix task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell args to Mix task | CLI input selects user, board, and action. |
| Mix task to domain context | Break-glass adapter must not bypass rules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Elevation of Privilege | `mix foglet.board_subscriptions` | mitigate | Task routes mutations through `Foglet.Boards` context APIs only. |
| T-13-09 | Tampering | task unsubscribe action | mitigate | Required-board unsubscribe receives `{:error, :required_subscription}` and exits non-zero without direct delete. |
| T-13-10 | Denial of Service | invalid task arguments | mitigate | Strict OptionParser switches and explicit usage errors exit before domain calls. |
| T-13-11 | Repudiation | break-glass task output | accept | Phase requires explicit output, not durable audit logging. |
</threat_model>

<verification>
Run `rtk mix test test/mix/tasks/foglet.board_subscriptions_test.exs`.
</verification>

<success_criteria>
SUBS-04 is satisfied without building deferred Sysop `USERS` subscription management.
</success_criteria>

<output>
After completion, create `.planning/phases/13-board-subscription-management/13-03-SUMMARY.md`.
</output>
