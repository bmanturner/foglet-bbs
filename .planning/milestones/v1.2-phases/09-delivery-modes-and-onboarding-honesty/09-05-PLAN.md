---
phase: 09-delivery-modes-and-onboarding-honesty
plan: 05
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-03
files_modified:
  - lib/mix/tasks/foglet.user.reset_password.ex
  - test/mix/tasks/foglet_user_reset_password_test.exs
autonomous: true
requirements:
  - MAIL-04
  - MAIL-05
  - MAIL-06
must_haves:
  truths:
    - "Operator break-glass reset task remains available."
    - "No-email mode does not present normal user reset delivery as available."
    - "Mix reset output is honest about break-glass generation and does not imply email delivery."
  artifacts:
    - path: "lib/mix/tasks/foglet.user.reset_password.ex"
      provides: "mode-aware operator reset task"
      contains: "delivery_mode"
    - path: "test/mix/tasks/foglet_user_reset_password_test.exs"
      provides: "Mix reset task mode tests"
      contains: "no_email"
  key_links:
    - from: "lib/mix/tasks/foglet.user.reset_password.ex"
      to: "Foglet.Config.delivery_mode/0"
      via: "task checks mode before reporting reset availability"
      pattern: "Config\\.delivery_mode"
---

<objective>
Align the break-glass reset Mix task with delivery modes and honest operator copy.

Purpose: MAIL-04 preserves break-glass reset intent, while MAIL-05 and MAIL-06 require no-email mode not to masquerade as normal user reset delivery.
Output: Updated Mix task behavior/copy and tests.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-delivery-modes-and-onboarding-honesty/09-SPEC.md
@.planning/phases/09-delivery-modes-and-onboarding-honesty/09-CONTEXT.md
@.planning/phases/09-delivery-modes-and-onboarding-honesty/09-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

<interfaces>
Existing task contract:

```elixir
mix foglet.user.reset_password HANDLE
Foglet.Accounts.deliver_user_reset_password_instructions(user, url_fn) :: {:ok, String.t()}
Foglet.Config.delivery_mode() :: "email" | "no_email"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make reset Mix task delivery-mode aware</name>
  <files>lib/mix/tasks/foglet.user.reset_password.ex, test/mix/tasks/foglet_user_reset_password_test.exs</files>
  <read_first>
    .planning/phases/09-delivery-modes-and-onboarding-honesty/09-SPEC.md
    .planning/phases/09-delivery-modes-and-onboarding-honesty/09-CONTEXT.md
    lib/mix/tasks/foglet.user.reset_password.ex
    lib/foglet_bbs/accounts.ex
    lib/foglet_bbs/config.ex
    test/mix/tasks/foglet_user_reset_password_test.exs
  </read_first>
  <behavior>
    - In email mode, the task can still generate a break-glass reset token for an existing active user.
    - In no-email mode, the task exits non-zero with copy that normal password reset delivery is unavailable.
    - Task output never says email was sent.
  </behavior>
  <action>Alias `Foglet.Config` in `Mix.Tasks.Foglet.User.ResetPassword`. Before token generation, branch on `Config.delivery_mode()`. In `"no_email"`, print `"Password reset delivery is unavailable because Foglet is in no-email mode."` and `"Use an operator break-glass password change procedure instead of relaying a reset link."`, then `exit({:shutdown, 1})`. In `"email"`, preserve token generation through `Accounts.deliver_user_reset_password_instructions/2` but change copy to `"Break-glass reset URL for #{user.handle}:"` and `"This URL was generated for operator use; no email was sent by this task."`. Keep unknown/deleted user errors as operator-only diagnostics. Do not call `Accounts.request_password_reset_delivery/1` from this Mix task because that API is enumeration-safe and intentionally hides operator diagnostics.</action>
  <verify>
    <automated>rtk mix test test/mix/tasks/foglet_user_reset_password_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Config\\.delivery_mode|no-email mode|Break-glass reset URL|no email was sent by this task' lib/mix/tasks/foglet.user.reset_password.ex test/mix/tasks/foglet_user_reset_password_test.exs` returns matches.
    - `rg -n 'has been emailed|email was sent|sent by email' lib/mix/tasks/foglet.user.reset_password.ex test/mix/tasks/foglet_user_reset_password_test.exs` returns no false-delivery copy.
    - `rtk mix test test/mix/tasks/foglet_user_reset_password_test.exs` passes.
  </acceptance_criteria>
  <done>Reset Mix task is mode-aware, preserves break-glass behavior, and never claims delivery it did not attempt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> Accounts | Mix task can generate reset tokens outside normal TUI flow. |
| Accounts token -> operator stdout | Raw reset URL/token leaves application through terminal output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Information Disclosure | reset Mix task stdout | mitigate | Label output as break-glass operator URL and explicitly say the task did not send email. |
| T-09-14 | Tampering | no-email reset path | mitigate | Block normal reset-delivery presentation in no-email mode with non-zero exit. |
| T-09-15 | Repudiation | operator task behavior | accept | Existing Mix task has operator shell visibility only; Phase 9 does not add audit logs. Copy makes operator action explicit. |
</threat_model>

<verification>
Run `rtk mix test test/mix/tasks/foglet_user_reset_password_test.exs`.
</verification>

<success_criteria>
MAIL-04 break-glass reset remains intact, MAIL-05 false reset-delivery claims are removed, and MAIL-06 no-email implications are explicit in Mix tooling.
</success_criteria>

<output>
After completion, create `.planning/phases/09-delivery-modes-and-onboarding-honesty/09-05-SUMMARY.md`.
</output>
