---
phase: 11-posting-policy-enforcement
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - lib/foglet_bbs/posting_policy.ex
  - lib/foglet_bbs/posts.ex
  - test/foglet_bbs/posts/posts_test.exs
autonomous: true
requirements:
  - POST-02
  - POST-03
must_haves:
  truths:
    - "Reply creation follows the same `postable_by` role matrix as thread creation."
    - "Normal users cannot reply to locked threads."
    - "Sysops can reply to locked threads when board posting policy permits them."
    - "Moderators with `:site` or `{:board, board_id}` scopes can reply to locked threads when board posting policy permits them."
    - "Rejected reply attempts do not persist replies, mutate thread counters, mutate user post_count, or advance board message numbers."
  artifacts:
    - path: "lib/foglet_bbs/posting_policy.ex"
      provides: "Shared locked-thread bypass predicate"
      contains: "def can_bypass_thread_lock?"
    - path: "lib/foglet_bbs/posts.ex"
      provides: "Reply policy and lock preflight before board-server delegation"
      contains: "def create_reply"
    - path: "test/foglet_bbs/posts/posts_test.exs"
      provides: "POST-02/POST-03 role-policy, lock-bypass, and no-side-effect tests"
      contains: "POST-03"
  key_links:
    - from: "lib/foglet_bbs/posts.ex"
      to: "Foglet.Authorization.scopes_for/2"
      via: "PostingPolicy.can_bypass_thread_lock?/2"
      pattern: "scopes_for"
    - from: "lib/foglet_bbs/posts.ex"
      to: "Foglet.Boards.Server.create_post/4"
      via: "only after policy and lock preflight returns :ok"
      pattern: "Boards\\.Server\\.create_post"
---

<objective>
Enforce reply posting policy and locked-thread rejection in `Foglet.Posts.create_reply/4` before any board-server write.

Purpose: POST-02 and POST-03 require replies to honor board policy and thread lock state while preserving scoped moderator/sysop locked-thread bypass.
Output: Reply context preflight, lock-bypass helper, and focused tests for policy, lock, and side-effect invariants.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-posting-policy-enforcement/11-CONTEXT.md
@.planning/phases/11-posting-policy-enforcement/11-SPEC.md
@.planning/phases/11-posting-policy-enforcement/11-RESEARCH.md
@.planning/phases/11-posting-policy-enforcement/11-VALIDATION.md
@docs/DATA_MODEL.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/11-posting-policy-enforcement/11-01-SUMMARY.md

<interfaces>
Created by Plan 11-01:

```elixir
Foglet.PostingPolicy.can_post?(user, board)
{:error, :posting_not_allowed}
```

Existing contracts:

```elixir
Foglet.Posts.create_reply(thread_id, board_id, user_id, attrs)
Foglet.Boards.Server.create_post(board_id, thread_id, user_id, attrs)
Foglet.Authorization.scopes_for(user, :lock_thread)
Foglet.Threads.Thread fields: :id, :board_id, :locked, :post_count
```

Locked-thread error:

```elixir
{:error, :thread_locked}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reply policy and lock regression tests</name>
  <files>test/foglet_bbs/posts/posts_test.exs</files>
  <read_first>
    - test/foglet_bbs/posts/posts_test.exs
    - test/foglet_bbs/threads/threads_test.exs
    - test/support/boards_fixtures.ex
    - lib/foglet_bbs/posts.ex
    - lib/foglet_bbs/threads.ex
    - lib/foglet_bbs/authorization.ex
    - lib/foglet_bbs/boards/server.ex
    - .planning/phases/11-posting-policy-enforcement/11-SPEC.md
    - .planning/phases/11-posting-policy-enforcement/11-VALIDATION.md
  </read_first>
  <behavior>
    - Reply matrix matches thread matrix: `:members` permits active `:user`, `:mod`, `:sysop`; `:mods_only` permits active `:mod`, `:sysop`; `:sysop_only` permits active `:sysop`.
    - Disallowed reply returns `{:error, :posting_not_allowed}`.
    - Locked thread with active regular user returns `{:error, :thread_locked}`.
    - Locked thread with active `:mod` or `:sysop` succeeds when `postable_by` permits the actor.
    - Locked-thread bypass does not bypass board policy: active `:mod` on `:sysop_only` locked thread returns `{:error, :posting_not_allowed}`.
    - Rejected replies leave `Foglet.Posts.Post` count, parent thread `post_count`, parent thread `last_post_at`, posting user `post_count`, persisted `boards.next_message_number`, and board-server `:sys.get_state(pid).next_number` unchanged.
  </behavior>
  <action>Add POST-02/POST-03 tests under `describe "create_reply/4 posting policy and locks (POST-02, POST-03)"`. Use existing `setup_board_with_server/0`, `setup_thread/2`, `Foglet.Threads.lock_thread/1`, and `allow_board_server!/1`. For no-side-effect assertions, capture counts and persisted records before calling `Foglet.Posts.create_reply/4`, then reload with `Repo.get!/2`; assert exact error atoms `:posting_not_allowed` and `:thread_locked`. Do not create board-moderator membership tables or browser flows.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/posts/posts_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg "create_reply/4 posting policy and locks \\(POST-02, POST-03\\)" test/foglet_bbs/posts/posts_test.exs` returns a match.
    - `rg ":thread_locked" test/foglet_bbs/posts/posts_test.exs` returns a match.
    - `rg ":posting_not_allowed" test/foglet_bbs/posts/posts_test.exs` returns at least 3 matches.
    - `rg "post_count" test/foglet_bbs/posts/posts_test.exs` returns a match in the POST-02/POST-03 describe block.
    - `rg "next_message_number" test/foglet_bbs/posts/posts_test.exs` returns a match.
    - `rtk mix test test/foglet_bbs/posts/posts_test.exs` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <done>POST-02 and POST-03 tests prove reply policy, locked-thread bypass, and no-side-effect rejection behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce reply policy and locked-thread preflight</name>
  <files>lib/foglet_bbs/posting_policy.ex, lib/foglet_bbs/posts.ex</files>
  <read_first>
    - lib/foglet_bbs/posting_policy.ex
    - lib/foglet_bbs/posts.ex
    - lib/foglet_bbs/authorization.ex
    - lib/foglet_bbs/accounts/user.ex
    - lib/foglet_bbs/boards/board.ex
    - lib/foglet_bbs/threads/thread.ex
    - lib/foglet_bbs/boards/server.ex
    - .planning/phases/11-posting-policy-enforcement/11-CONTEXT.md
    - .planning/phases/11-posting-policy-enforcement/11-RESEARCH.md
  </read_first>
  <action>Extend `Foglet.PostingPolicy` with public `@spec can_bypass_thread_lock?(Foglet.Accounts.User.t() | nil, Ecto.UUID.t()) :: boolean()`. Implement exact behavior per D-08/D-09: return true when `Foglet.Authorization.scopes_for(user, :lock_thread)` contains `:site` or `{:board, board_id}`; return false otherwise. Update `Foglet.Posts.create_reply/4` per D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, and D-10: load `Foglet.Accounts.User` by `user_id`, `Foglet.Boards.Board` by `board_id`, and `Foglet.Threads.Thread` by `thread_id`; reject missing user/board/thread or mismatched `thread.board_id != board_id` with `{:error, :posting_not_allowed}`; reject failed `PostingPolicy.can_post?/2` with `{:error, :posting_not_allowed}`; reject `thread.locked == true` when `PostingPolicy.can_bypass_thread_lock?(user, board_id)` is false with `{:error, :thread_locked}`; call `Foglet.Boards.Server.create_post(board_id, thread_id, user_id, attrs)` only after all gates pass. Do not let lock bypass override `postable_by`; the board policy gate must remain independent.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/posts/posts_test.exs test/foglet_bbs/threads/threads_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg "def can_bypass_thread_lock\\?" lib/foglet_bbs/posting_policy.ex` returns a match.
    - `rg "Authorization\\.scopes_for\\(user, :lock_thread\\)" lib/foglet_bbs/posting_policy.ex` returns a match.
    - `rg ":thread_locked" lib/foglet_bbs/posts.ex` returns a match.
    - `rg ":posting_not_allowed" lib/foglet_bbs/posts.ex` returns a match.
    - `rg "Repo\\.get\\(Thread, thread_id\\)" lib/foglet_bbs/posts.ex` returns a match.
    - `rg "Boards\\.Server\\.create_post\\(board_id, thread_id, user_id, attrs\\)" lib/foglet_bbs/posts.ex` returns a match.
    - `rtk mix test test/foglet_bbs/posts/posts_test.exs test/foglet_bbs/threads/threads_test.exs` passes.
  </acceptance_criteria>
  <done>`Foglet.Posts.create_reply/4` rejects policy and lock denials before board-server delegation and preserves successful board-server writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI/SSH caller -> `Foglet.Posts.create_reply/4` | Untrusted `thread_id`, `board_id`, `user_id`, and attrs enter the reply mutation boundary. |
| `Foglet.Posts` -> `Foglet.Authorization.scopes_for/2` | Locked-thread bypass depends on persisted actor status and stable scope shapes. |
| `Foglet.Posts` -> `Foglet.Boards.Server` | Crossing this boundary allocates message numbers and mutates post/thread/user counters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Elevation of Privilege | `Foglet.Posts.create_reply/4` | mitigate | Reload persisted user, board, and thread; enforce active poster plus `postable_by` matrix before reply creation. |
| T-11-06 | Tampering | Locked threads | mitigate | Return `{:error, :thread_locked}` for locked threads unless `scopes_for(user, :lock_thread)` contains `:site` or `{:board, board_id}`. |
| T-11-07 | Elevation of Privilege | Lock bypass | mitigate | Run board posting policy independently so moderator/sysop lock bypass never overrides `postable_by`. |
| T-11-08 | Tampering | Reply side effects | mitigate | Tests assert rejected replies do not change post rows, thread counters, user `post_count`, persisted `boards.next_message_number`, or board-server `next_number`. |
| T-11-09 | Spoofing | `thread_id`/`board_id` pair | mitigate | Load persisted thread and reject mismatched `thread.board_id != board_id` before board-server delegation. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/posts/posts_test.exs test/foglet_bbs/threads/threads_test.exs`.
</verification>

<success_criteria>
POST-02 and POST-03 are covered by tests and implementation. Disallowed replies return `{:error, :posting_not_allowed}` or `{:error, :thread_locked}` without allocating board message numbers or mutating post/thread/user state.
</success_criteria>

<output>
After completion, create `.planning/phases/11-posting-policy-enforcement/11-02-SUMMARY.md`
</output>
