---
phase: 18-chrome-v2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/foglet_bbs/tui/widgets/chrome/command_bar.ex
  - lib/foglet_bbs/tui/widgets/chrome/normalizer.ex
  - lib/foglet_bbs/tui/widgets/chrome/key_bar.ex
  - test/foglet_bbs/tui/widgets/chrome/command_bar_test.exs
  - test/foglet_bbs/tui/widgets/chrome/normalizer_test.exs
autonomous: true
requirements:
  - CHROME-03
  - CHROME-05
must_haves:
  truths:
    - "`Chrome.CommandBar` renders grouped command hints inside a width budget."
    - "Lower-priority hints are dropped before higher-priority hints under constrained width."
    - "Flat `{key, description}` lists normalize into grouped commands instead of rendering through a separate footer."
  artifacts:
    - path: "lib/foglet_bbs/tui/widgets/chrome/command_bar.ex"
      provides: "Grouped command rendering and priority truncation"
      exports: ["render/3", "normalize_groups/1"]
    - path: "lib/foglet_bbs/tui/widgets/chrome/normalizer.ex"
      provides: "Compatibility adapter from legacy key lists to Chrome V2 command groups"
      exports: ["commands/1", "command/3"]
  key_links:
    - from: "lib/foglet_bbs/tui/widgets/chrome/normalizer.ex"
      to: "lib/foglet_bbs/tui/widgets/chrome/command_bar.ex"
      via: "normalized command group data"
      pattern: "CommandBar"
    - from: "lib/foglet_bbs/tui/widgets/chrome/command_bar.ex"
      to: "lib/foglet_bbs/tui/text_width.ex"
      via: "display width budgeting"
      pattern: "TextWidth\\."
---

<objective>
Create the grouped command bar and compatibility normalizer.

Purpose: Replace the old flat footer rendering model with a single Chrome V2 command path per D-02 and D-03.
Output: A tested `Chrome.CommandBar` and `Chrome.Normalizer` that can serve both new grouped callers and existing simple key-list callers.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-chrome-v2/18-SPEC.md
@.planning/phases/18-chrome-v2/18-CONTEXT.md
@.planning/phases/18-chrome-v2/18-RESEARCH.md
@.planning/phases/18-chrome-v2/18-VALIDATION.md
@.planning/phases/17-theme-and-mode-metadata/17-03-SUMMARY.md
@SCREENS.md
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/text_width.ex
@lib/foglet_bbs/tui/theme.ex
@lib/foglet_bbs/tui/presentation.ex
@lib/foglet_bbs/tui/widgets/chrome/key_bar.ex
@test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 18-02-01: Add grouped CommandBar with priority truncation</name>
  <files>lib/foglet_bbs/tui/widgets/chrome/command_bar.ex, test/foglet_bbs/tui/widgets/chrome/command_bar_test.exs</files>
  <read_first>
    .planning/phases/18-chrome-v2/18-SPEC.md
    .planning/phases/18-chrome-v2/18-CONTEXT.md
    SCREENS.md
    lib/foglet_bbs/tui/widgets/README.md
    lib/foglet_bbs/tui/text_width.ex
    lib/foglet_bbs/tui/theme.ex
    lib/foglet_bbs/tui/presentation.ex
    lib/foglet_bbs/tui/widgets/chrome/key_bar.ex
  </read_first>
  <behavior>
    - A roomy render includes group labels such as `Navigate`, `Actions`, and `System`.
    - Hints render in stable priority order: priority `0` before `10`, `10` before `50`.
    - At constrained width, lower-priority hints are omitted before higher-priority hints.
    - Rendered output uses `TextWidth.display_width/1` and never exceeds `width:`.
    - Command theme slots use `Presentation.theme_mappings().commands` and `Theme` slots.
  </behavior>
  <action>
    Create `Foglet.TUI.Widgets.Chrome.CommandBar` with command group data accepted as maps or structs shaped like `%{label: "Navigate", commands: [%{key: "j/k", label: "Move", priority: 10}]}`. Implement `render(theme, groups, opts \\ [])` and `normalize_groups/1`. Render groups in ascending minimum command priority, then commands in ascending `priority`; default missing priority to `50`. Required stable group labels for tests: `Navigate`, `Actions`, `System`, `Tabs`, `Field`, `Save`, `Refresh`. Use `Presentation.theme_mappings().commands.group`, `.key`, `.destructive`, and `.inactive` to select theme slots. Use `TextWidth.display_width/1` and `TextWidth.truncate/2` for all width-sensitive text. Do not implement authorization or command execution in this module; it is passive display only.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/chrome/command_bar_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "defmodule Foglet.TUI.Widgets.Chrome.CommandBar|def render|def normalize_groups|TextWidth\\.display_width|TextWidth\\.truncate|Navigate|Actions|System|priority" lib/foglet_bbs/tui/widgets/chrome/command_bar.ex test/foglet_bbs/tui/widgets/chrome/command_bar_test.exs` returns matches.
    `rtk rg -n ":red|:green|:cyan|:yellow|:blue|:magenta|:white|:black" lib/foglet_bbs/tui/widgets/chrome/command_bar.ex` returns no hardcoded color matches.
  </acceptance_criteria>
  <done>`CommandBar` renders grouped commands, obeys width budgets, and drops lower-priority hints first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18-02-02: Add legacy key-list compatibility normalizer</name>
  <files>lib/foglet_bbs/tui/widgets/chrome/normalizer.ex, lib/foglet_bbs/tui/widgets/chrome/key_bar.ex, test/foglet_bbs/tui/widgets/chrome/normalizer_test.exs</files>
  <read_first>
    .planning/phases/18-chrome-v2/18-SPEC.md
    .planning/phases/18-chrome-v2/18-CONTEXT.md
    lib/foglet_bbs/tui/widgets/chrome/key_bar.ex
    lib/foglet_bbs/tui/widgets/chrome/command_bar.ex
    test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs
  </read_first>
  <behavior>
    - `Normalizer.commands([{"Q", "Back"}])` returns one `System` command group containing key `Q`, label `Back`, priority `0`.
    - Navigation keys like `j/k`, arrows, `Enter`, and `Tab` normalize into `Navigate`, `Tabs`, or `Field` groups.
    - Action keys like `s/u`, `S/Enter`, `F`, and `R` normalize into `Actions`, `Save`, or `Refresh` groups.
    - `KeyBar.render/3`, if retained, delegates to `CommandBar.render/3` and is no longer an independent footer implementation.
  </behavior>
  <action>
    Create `Foglet.TUI.Widgets.Chrome.Normalizer` with `commands/1` for legacy flat lists and `command/3` for explicit group/key/label construction. Map common existing key descriptions to stable groups: `Back`, `Quit`, `Cancel` -> `System`; `Tab`, `Field`, `←/→`, `1-5`, `1-6` -> `Tabs` or `Field`; `j/k`, `Enter`, `Open`, `Select`, `Collapse/Expand` -> `Navigate`; `Save`, `S/Enter` -> `Save`; `Refresh` -> `Refresh`; all others -> `Actions`. Give `System` and core navigation commands priority `0`, save/field/tab priority `10`, ordinary actions priority `30`, and verbose optional hints priority `50`. Update `Chrome.KeyBar.render/3` to call `Normalizer.commands(keys)` and `CommandBar.render(theme, groups, opts)` or reduce it to a documented compatibility wrapper. Do not leave the old centered flat row implementation as the production path.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/chrome/normalizer_test.exs test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "defmodule Foglet.TUI.Widgets.Chrome.Normalizer|def commands|def command|System|Navigate|Actions|Save|Refresh" lib/foglet_bbs/tui/widgets/chrome/normalizer.ex test/foglet_bbs/tui/widgets/chrome/normalizer_test.exs` returns matches.
    `rtk rg -n "CommandBar.render|Normalizer.commands" lib/foglet_bbs/tui/widgets/chrome/key_bar.ex` returns matches.
    `rtk rg -n "row style: .*justify_content: :center|Enum.flat_map\\(keys" lib/foglet_bbs/tui/widgets/chrome/key_bar.ex` returns no matches.
  </acceptance_criteria>
  <done>Legacy simple key lists feed the grouped command contract, and `KeyBar` is compatibility-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screen key metadata -> command chrome | Rendered command hints can influence user perception but cannot authorize mutations. |
| legacy key lists -> grouped commands | Compatibility input must not become a second footer implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | S | `CommandBar.render/3` | mitigate | Keep hints passive and make grouping explicit so UI does not imply unavailable commands. |
| T-18-05 | E | `Normalizer.commands/1` | mitigate | Do not route command display to action execution; mutations stay in screen handlers and contexts. |
| T-18-06 | D | `CommandBar.render/3` | mitigate | Enforce width budgets and priority truncation so narrow terminals keep essential navigation/system hints first. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/chrome/command_bar_test.exs test/foglet_bbs/tui/widgets/chrome/normalizer_test.exs test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs`.
</verification>

<success_criteria>
`Chrome.CommandBar` is the single grouped command renderer, and legacy simple key lists normalize into it without preserving a parallel footer path.
</success_criteria>

<output>
After completion, create `.planning/phases/18-chrome-v2/18-02-SUMMARY.md`.
</output>
