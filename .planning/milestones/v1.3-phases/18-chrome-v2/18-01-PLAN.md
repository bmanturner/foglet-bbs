---
phase: 18-chrome-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex
  - lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
  - test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs
  - test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs
autonomous: true
requirements:
  - CHROME-01
  - CHROME-02
must_haves:
  truths:
    - "Users see breadcrumb chrome rooted at Foglet for named BBS and operator screens."
    - "Users see BBS and operator status atoms selected by presentation mode."
    - "Optional unread/activity/scope/system status is omitted when source data is absent."
  artifacts:
    - path: "lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex"
      provides: "Shared breadcrumb formatter and width-safe truncation"
      exports: ["parts_for/1", "render/3", "format/2"]
    - path: "lib/foglet_bbs/tui/widgets/chrome/status_bar.ex"
      provides: "Mode-aware top chrome status rendering"
      exports: ["render/3", "status_atoms/1"]
  key_links:
    - from: "lib/foglet_bbs/tui/widgets/chrome/status_bar.ex"
      to: "lib/foglet_bbs/tui/presentation.ex"
      via: "Presentation.mode_for!/1"
      pattern: "Presentation\\.mode_for!"
    - from: "lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex"
      to: "lib/foglet_bbs/tui/text_width.ex"
      via: "TextWidth.truncate/2 and display_width/1"
      pattern: "TextWidth\\."
---

<objective>
Create the Chrome V2 breadcrumb and status primitives.

Purpose: Establish the shared top-chrome contracts required by D-04 through D-09 before any screen caller migration.
Output: A shared `Chrome.BreadcrumbBar` plus a mode-aware `Chrome.StatusBar` that uses Phase 16 width helpers and Phase 17 presentation metadata.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-chrome-v2/18-SPEC.md
@.planning/phases/18-chrome-v2/18-CONTEXT.md
@.planning/phases/18-chrome-v2/18-RESEARCH.md
@.planning/phases/18-chrome-v2/18-VALIDATION.md
@.planning/phases/17-theme-and-mode-metadata/17-01-SUMMARY.md
@.planning/phases/17-theme-and-mode-metadata/17-02-SUMMARY.md
@.planning/phases/17-theme-and-mode-metadata/17-03-SUMMARY.md
@SCREENS.md
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/text_width.ex
@lib/foglet_bbs/tui/theme.ex
@lib/foglet_bbs/tui/presentation.ex
@lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
@test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs
</context>

<interfaces>
From `Foglet.TUI.Presentation`:
```elixir
@spec mode_for!(screen() | atom()) :: :bbs | :operator
@spec theme_mappings() :: theme_mappings()
```

From `Foglet.TUI.TextWidth`:
```elixir
TextWidth.display_width(binary)
TextWidth.truncate(binary, width)
TextWidth.pad_trailing(binary, width)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 18-01-01: Add breadcrumb formatter contract and tests</name>
  <files>lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex, test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs</files>
  <read_first>
    .planning/phases/18-chrome-v2/18-SPEC.md
    .planning/phases/18-chrome-v2/18-CONTEXT.md
    SCREENS.md
    lib/foglet_bbs/tui/widgets/README.md
    lib/foglet_bbs/tui/text_width.ex
    lib/foglet_bbs/tui/presentation.ex
    lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
  </read_first>
  <behavior>
    - `BreadcrumbBar.parts_for(%{current_screen: :login})` returns `["Foglet", "Login"]`.
    - `BreadcrumbBar.parts_for(%{current_screen: :main_menu})` returns `["Foglet", "Home"]`.
    - `BreadcrumbBar.parts_for(%{current_screen: :board_list})` returns `["Foglet", "Boards"]`.
    - `BreadcrumbBar.parts_for(%{current_screen: :thread_list, current_board: %{name: "general"}})` returns `["Foglet", "Boards", "general"]`.
    - `BreadcrumbBar.parts_for(%{current_screen: :post_reader, current_board: %{name: "general"}, current_thread: %{title: "Unicode screenshots thread"}})` returns `["Foglet", "general", "Unicode screenshots thread"]`.
    - `BreadcrumbBar.parts_for(%{current_screen: :account, screen_state: %{account: %{active_tab: 1}}})` is rooted at `Foglet` and includes `Account`.
    - Formatting uses `" ▸ "` by default and `" > "` when `ascii?: true`.
    - Formatted text width never exceeds the supplied `width:` option.
  </behavior>
  <action>
    Create `Foglet.TUI.Widgets.Chrome.BreadcrumbBar` with pure functions `parts_for/1`, `format/2`, and `render/3` per D-04, D-05, D-06, and D-12. Keep the module under `Foglet.TUI.Widgets.Chrome`; do not add screen-built final breadcrumb strings. `parts_for/1` must derive central paths for `:login`, `:main_menu`, `:board_list`, `:thread_list`, `:post_reader`, `:new_thread`, `:post_composer`, `:account`, `:moderation`, and `:sysop` using existing state fields only (`current_board.name`, `current_thread.title`, and active tab labels when safely available). Unknown or incomplete state must still return a non-empty path rooted at `Foglet`. `format/2` must join with `" ▸ "` unless `ascii?: true`, then use `" > "`, and must call `Foglet.TUI.TextWidth.truncate/2` when `width:` is supplied. `render/3` must accept `theme` explicitly and route styles through `theme.title` or `theme.status_bar`, not hardcoded colors.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "defmodule Foglet.TUI.Widgets.Chrome.BreadcrumbBar|def parts_for|def format|def render|TextWidth\\.truncate| ▸ | > " lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex` returns matches.
    `rtk rg -n ":login|:main_menu|:board_list|:thread_list|:post_reader|:new_thread|:post_composer|:account|:moderation|:sysop|Foglet" test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs` returns matches.
  </acceptance_criteria>
  <done>All named Phase 18 screens have a central breadcrumb path rooted at `Foglet`, Unicode and ASCII formatting are tested, and width truncation is delegated to `TextWidth`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18-01-02: Make status atoms mode-aware and honest</name>
  <files>lib/foglet_bbs/tui/widgets/chrome/status_bar.ex, test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs</files>
  <read_first>
    .planning/phases/18-chrome-v2/18-SPEC.md
    .planning/phases/18-chrome-v2/18-CONTEXT.md
    .planning/phases/17-theme-and-mode-metadata/17-01-SUMMARY.md
    lib/foglet_bbs/tui/presentation.ex
    lib/foglet_bbs/tui/theme.ex
    lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
    test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs
  </read_first>
  <behavior>
    - `StatusBar.status_atoms/1` calls `Presentation.mode_for!/1` using `state.current_screen`.
    - BBS mode can render handle and time and may include only already-present `unread_count` or `activity_label` atoms.
    - Operator mode can render handle and time and may include only already-present `operator_scope` or `system_status` atoms.
    - Guest or absent user state renders `guest` and does not fabricate unread, scope, or system status.
    - Existing fixed-clock behavior and time-only status tests still pass.
  </behavior>
  <action>
    Extend `Foglet.TUI.Widgets.Chrome.StatusBar` for Chrome V2 per D-07, D-08, and D-09. Keep `ClockFormatter.format/2` unchanged. Add public `status_atoms/1` returning ordered strings for display. Use `Foglet.TUI.Presentation.mode_for!/1`; never infer mode from theme, role, or authorization. For BBS mode, include `@handle` or `guest`, optional `unread #{count}` when `state.unread_count` is a positive integer, optional `activity_label` when present, then clock for authenticated users. For operator mode, include `@handle` or `guest`, optional `scope #{value}` from `state.operator_scope`, optional `system_status`, then clock for authenticated users. Do not render placeholder strings like `unread 0`, `scope unknown`, or `system ok` unless those exact values exist in state. Update `render/3` to accept a breadcrumb/chrome model or breadcrumb parts from Plan 03 while preserving `render(state, title)` compatibility during Wave 1.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "Presentation\\.mode_for!|def status_atoms|unread|operator_scope|system_status|guest" lib/foglet_bbs/tui/widgets/chrome/status_bar.ex test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs` returns matches.
    `rtk rg -n "unread 0|scope unknown|system ok" lib/foglet_bbs/tui/widgets/chrome/status_bar.ex test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs` returns no matches.
  </acceptance_criteria>
  <done>Status atoms are selected by Phase 17 mode metadata, optional data is honest, and current handle/time behavior remains covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session state -> chrome render | User/session/operator data is read for display but must not become authorization logic. |
| screen state -> breadcrumb | Domain labels are displayed as location context only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | S | `BreadcrumbBar.parts_for/1` | accept | Breadcrumbs are display-only and do not unlock routes or domain actions. |
| T-18-02 | I | `StatusBar.status_atoms/1` | mitigate | Render only existing session/status fields and omit absent optional atoms per D-09. |
| T-18-03 | E | `StatusBar.status_atoms/1` | mitigate | Use `Presentation.mode_for!/1` only for presentation; do not use mode as authorization. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/chrome/breadcrumb_test.exs test/foglet_bbs/tui/widgets/chrome/status_bar_test.exs`.
</verification>

<success_criteria>
`BreadcrumbBar` and `StatusBar` provide tested top-chrome contracts for CHROME-01 and CHROME-02 without changing screen behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/18-chrome-v2/18-01-SUMMARY.md`.
</output>
