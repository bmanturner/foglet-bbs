---
phase: 18-chrome-v2
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
  - 18-02
files_modified:
  - lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
  - test/foglet_bbs/tui/widgets/chrome/screen_frame_test.exs
  - test/foglet_bbs/tui/layout_smoke_test.exs
autonomous: true
requirements:
  - CHROME-01
  - CHROME-02
  - CHROME-03
  - CHROME-04
must_haves:
  truths:
    - "`Chrome.ScreenFrame` composes breadcrumb/status, content, separator, command bar, and bottom border as one frame."
    - "The command bar is inside the frame above the bottom border."
    - "Chrome layout remains usable at 64x22, 80x24, and 132x50 without text overlap or content displacement."
  artifacts:
    - path: "lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex"
      provides: "Single screen-facing Chrome V2 composition boundary"
      exports: ["render/4"]
    - path: "test/foglet_bbs/tui/widgets/chrome/screen_frame_test.exs"
      provides: "Frame composition contract tests"
    - path: "test/foglet_bbs/tui/layout_smoke_test.exs"
      provides: "Positioned layout size-contract coverage"
  key_links:
    - from: "lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex"
      to: "lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex"
      via: "top chrome rendering"
      pattern: "BreadcrumbBar"
    - from: "lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex"
      to: "lib/foglet_bbs/tui/widgets/chrome/command_bar.ex"
      via: "bottom command chrome rendering"
      pattern: "CommandBar"
---

<objective>
Integrate Chrome V2 primitives into `Chrome.ScreenFrame`.

Purpose: Keep `ScreenFrame.render/4` as the single screen-facing boundary while replacing old title/keybar internals with shared Chrome V2 composition.
Output: ScreenFrame render and layout tests that prove top chrome, content, and command chrome occupy coherent positions at required terminal sizes.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-chrome-v2/18-SPEC.md
@.planning/phases/18-chrome-v2/18-CONTEXT.md
@.planning/phases/18-chrome-v2/18-RESEARCH.md
@.planning/phases/18-chrome-v2/18-VALIDATION.md
@.planning/phases/18-chrome-v2/18-01-SUMMARY.md
@.planning/phases/18-chrome-v2/18-02-SUMMARY.md
@lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
@lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex
@lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
@lib/foglet_bbs/tui/widgets/chrome/command_bar.ex
@lib/foglet_bbs/tui/widgets/chrome/normalizer.ex
@test/foglet_bbs/tui/layout_smoke_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 18-03-01: Compose Chrome V2 through ScreenFrame</name>
  <files>lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex, test/foglet_bbs/tui/widgets/chrome/screen_frame_test.exs</files>
  <read_first>
    .planning/phases/18-chrome-v2/18-SPEC.md
    .planning/phases/18-chrome-v2/18-CONTEXT.md
    lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
    lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex
    lib/foglet_bbs/tui/widgets/chrome/status_bar.ex
    lib/foglet_bbs/tui/widgets/chrome/command_bar.ex
    lib/foglet_bbs/tui/widgets/chrome/normalizer.ex
  </read_first>
  <behavior>
    - `ScreenFrame.render(state, "Boards", content, [{"Q", "Back"}])` renders a breadcrumb containing `Foglet`.
    - The same call renders command text through `CommandBar` group output, not `KeyBar.render/3`.
    - Caller-provided content appears between the top chrome/divider and the command chrome.
    - `render/4` remains callable by existing screens during migration.
  </behavior>
  <action>
    Update `Foglet.TUI.Widgets.Chrome.ScreenFrame.render/4` per D-01 and D-02. Preserve the public arity and current caller shape: `render(state, title_or_chrome, content_element, commands)`. Internally normalize to a Chrome V2 model with breadcrumb parts from `BreadcrumbBar.parts_for(state)` when only a legacy title is supplied, status atoms from `StatusBar.status_atoms(state)`, and command groups from `Normalizer.commands(commands)` unless `commands` are already grouped. Compose exactly: outer bordered box -> top `StatusBar.render(state, chrome, theme)` or equivalent breadcrumb/status row -> divider -> caller `content_element` -> divider/separator -> `CommandBar.render(theme, chrome.command_groups, width: inner_width)` -> bottom border owned by the outer box. Remove the `alias Chrome.KeyBar` production dependency from `screen_frame.ex`.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/chrome/screen_frame_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "BreadcrumbBar|StatusBar|CommandBar|Normalizer" lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex test/foglet_bbs/tui/widgets/chrome/screen_frame_test.exs` returns matches.
    `rtk rg -n "KeyBar" lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex` returns no matches.
  </acceptance_criteria>
  <done>`ScreenFrame.render/4` is Chrome V2 internally while preserving the existing screen-facing call path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18-03-02: Add positioned Chrome V2 size-contract coverage</name>
  <files>test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    .planning/phases/18-chrome-v2/18-VALIDATION.md
    .planning/phases/18-chrome-v2/18-CONTEXT.md
    test/foglet_bbs/tui/layout_smoke_test.exs
    lib/foglet_bbs/tui/widgets/chrome/screen_frame.ex
    lib/foglet_bbs/tui/widgets/chrome/command_bar.ex
    lib/foglet_bbs/tui/widgets/chrome/breadcrumb_bar.ex
  </read_first>
  <behavior>
    - Positioned layout is tested at `{64, 22}`, `{80, 24}`, and `{132, 50}`.
    - Text elements remain within the terminal width at each size.
    - Breadcrumb/status text, content text, and command text occupy distinct coherent y positions.
    - Tests inspect positioned output from `Raxol.UI.Layout.Engine.apply_layout/2`, not only render-tree text presence.
  </behavior>
  <action>
    Extend `test/foglet_bbs/tui/layout_smoke_test.exs` with a `Chrome V2 size contracts` describe block. Render a representative `ScreenFrame.render/4` with `current_screen: :thread_list`, `current_user` with fixed clock, `current_board: %{name: "general"}`, and content text `"BODY SENTINEL"`. For each size `{64, 22}`, `{80, 24}`, and `{132, 50}`, call `Raxol.UI.Layout.Engine.apply_layout/2`, collect positioned `%{type: :text}` elements, assert every text element has `x >= 0`, `y >= 0`, and `x + TextWidth.display_width(text) <= width`, assert one breadcrumb/status text contains `Foglet`, one content text contains `BODY SENTINEL`, one command text contains a grouped command label or key, and assert their y positions are ordered top < content < command.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/layout_smoke_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n "Chrome V2 size contracts|\\{64, 22\\}|\\{80, 24\\}|\\{132, 50\\}|apply_layout|BODY SENTINEL|TextWidth.display_width" test/foglet_bbs/tui/layout_smoke_test.exs` returns matches.
  </acceptance_criteria>
  <done>Chrome V2 has positioned no-overlap coverage for the required minimum, compact, and wide terminal sizes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screen render -> shared chrome | Screen content and key metadata cross into the shared frame renderer. |
| terminal size -> positioned layout | Narrow terminal constraints can hide or displace status/command text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | T | `ScreenFrame.render/4` | mitigate | Preserve caller content as supplied and keep domain mutations outside chrome. |
| T-18-08 | D | `ScreenFrame.render/4` | mitigate | Add positioned size tests at 64x22, 80x24, and 132x50. |
| T-18-09 | S | command chrome | mitigate | Render grouped hints inside the frame so command affordances are visually associated with the active screen. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/chrome/screen_frame_test.exs test/foglet_bbs/tui/layout_smoke_test.exs`.
</verification>

<success_criteria>
Shared `ScreenFrame` uses Chrome V2 primitives with layout evidence for CHROME-01 through CHROME-04.
</success_criteria>

<output>
After completion, create `.planning/phases/18-chrome-v2/18-03-SUMMARY.md`.
</output>
