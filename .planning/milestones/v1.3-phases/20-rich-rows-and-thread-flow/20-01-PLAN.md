---
phase: 20-rich-rows-and-thread-flow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/foglet_bbs/tui/widgets/list/rich_row_test.exs
autonomous: true
requirements:
  - RICHROW-01
  - THREADS-02
tags:
  - tui
  - widget
  - test-scaffold
  - rich-row
must_haves:
  truths:
    - "rich_row_test.exs exists with describe blocks covering the eight-cell selected×unselected × glyph × metadata matrix"
    - "Targeted test command fails because Foglet.TUI.Widgets.List.RichRow is missing or undefined (compile or UndefinedFunctionError)"
    - "Hardcoded color-atom hygiene assertion is present and active"
    - "Non-ThreadList state-atom acceptance test exists (Phase 21 reuse)"
    - "64-cell long-title truncation test exists"
    - "Cluster-width property test asserts TextWidth.display_width(cluster) == @cluster_width across every state combination including unknown atoms"
    - "Selection-vs-state-glyph precedence test asserts a selected sticky row's sticky glyph still renders through theme.info.fg"
    - "metadata: \"\" and metadata: nil tests assert no metadata text run is emitted"
  artifacts:
    - path: "test/foglet_bbs/tui/widgets/list/rich_row_test.exs"
      provides: "RichRow widget unit-test scaffold (RED)"
      contains: "defmodule Foglet.TUI.Widgets.List.RichRowTest"
  key_links:
    - from: "test/foglet_bbs/tui/widgets/list/rich_row_test.exs"
      to: "Foglet.TUI.Widgets.List.RichRow"
      via: "alias + render call"
      pattern: "alias Foglet.TUI.Widgets.List.RichRow"
    - from: "test/foglet_bbs/tui/widgets/list/rich_row_test.exs"
      to: "Foglet.TUI.WidgetHelpers"
      via: "import for flatten_text/1, assert_text_run/3, color_atom_leaked?/2"
      pattern: "import Foglet.TUI.WidgetHelpers"
---

<objective>
Create the RichRow widget unit-test scaffold (Wave 0 RED). The test file must
fail before any production code is written — RichRow does not yet exist. The
scaffold encodes the SPEC requirement 1, 4, 5, and 6 acceptance matrix from
CONTEXT.md D-12 and the RESEARCH.md test patterns, plus the additional
acceptance criteria from 20-REVIEWS.md (selection-vs-state-glyph precedence,
cluster-width property invariant, optional metadata).

Purpose: Lock the public API contract and acceptance matrix in code BEFORE the
RichRow module exists. This guarantees Wave 1 work has a deterministic GREEN
target rather than a moving spec.

Output: `test/foglet_bbs/tui/widgets/list/rich_row_test.exs` with describe
blocks covering: selection × glyph × metadata matrix, generic state atoms,
64-cell truncation, theme-routing hygiene, cluster fixed-width property
invariant, selection-vs-state-glyph precedence, and optional metadata.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-rich-rows-and-thread-flow/20-SPEC.md
@.planning/phases/20-rich-rows-and-thread-flow/20-CONTEXT.md
@.planning/phases/20-rich-rows-and-thread-flow/20-RESEARCH.md
@.planning/phases/20-rich-rows-and-thread-flow/20-REVIEWS.md
@.planning/phases/20-rich-rows-and-thread-flow/20-VALIDATION.md
@AGENTS.md

<interfaces>
<!-- Pre-extracted contracts the executor needs. No codebase exploration required. -->

The RichRow public API the tests will exercise (per D-01 / D-02, updated for
20-REVIEWS.md user decision #3):

```elixir
# Required keyword args
RichRow.render(
  title: String.t(),
  state_cluster: [atom()],   # e.g. [], [:unread], [:sticky, :locked]
  selected: boolean(),
  theme: %Foglet.TUI.Theme{}
)

# Optional keyword args
#   :metadata     (default "" — nil and "" both render as no metadata text run)
#   :width        (default 80)
#   :focus_marker (default "▌ ")
#   :emphasis     (e.g. :bold for unread)
```

Test helpers available via `import Foglet.TUI.WidgetHelpers`:

```elixir
flatten_text(tree) :: String.t()
assert_text_run(tree, content :: String.t(), expected_keys :: keyword()) :: map()
color_atom_leaked?(serialized :: String.t(), color :: String.t()) :: boolean()
color_names() :: ["red", "green", "cyan", "yellow", "blue", "magenta", "white", "black"]
text_runs(tree) :: [map()]
```

Theme construction: `theme = Foglet.TUI.Theme.default()` returns a `%Theme{}`
with `accent`, `info`, `badge`, `warning`, `dim`, `selected`, `unselected` slots.

Reference test pattern: `test/foglet_bbs/tui/widgets/list/list_row_test.exs`
(metadata-aware row tests) and `test/foglet_bbs/tui/widgets/list/smart_list_test.exs`
(`@focused_marker` + `▌` assertions).

TextWidth: `Foglet.TUI.TextWidth.display_width(string) :: non_neg_integer()` for
cluster-width invariance and 64-cell budget assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create rich_row_test.exs scaffold with full RED matrix</name>
  <files>test/foglet_bbs/tui/widgets/list/rich_row_test.exs</files>
  <read_first>
    - .planning/phases/20-rich-rows-and-thread-flow/20-CONTEXT.md (D-01, D-02, D-05, D-06, D-08, D-09, D-12)
    - .planning/phases/20-rich-rows-and-thread-flow/20-SPEC.md (Requirements 1, 4, 5, 6 acceptance criteria)
    - .planning/phases/20-rich-rows-and-thread-flow/20-RESEARCH.md (Pattern 4: Test Assertion with assert_text_run; Pitfall 4: assert_text_run is substring search)
    - .planning/phases/20-rich-rows-and-thread-flow/20-REVIEWS.md (HIGH #1, #2; MEDIUM #6; user decisions #1, #2, #3)
    - test/foglet_bbs/tui/widgets/list/list_row_test.exs (sibling test pattern reference for metadata-aware rows)
    - test/foglet_bbs/tui/widgets/list/smart_list_test.exs (sibling test pattern reference for `▌` focus marker assertions)
    - test/support/foglet/tui/widget_helpers.ex (helper signatures: flatten_text/1, assert_text_run/3, color_atom_leaked?/2, color_names/0, text_runs/1)
    - lib/foglet_bbs/tui/text_width.ex (display_width/1 signature)
    - lib/foglet_bbs/tui/theme.ex lines 1-50 (Theme.default/0 entry point)
  </read_first>
  <behavior>
    The scaffold MUST fail before RichRow exists. Each describe block targets a
    single SPEC acceptance criterion so that — once the module exists in
    Wave 1 — failures are pinpoint-localizable.

    Per 20-REVIEWS MEDIUM #6, the RED-first acceptance is reworded: a missing
    module produces a compile-time failure that short-circuits per-test
    enumeration. The acceptance is "the file is present and the targeted test
    command fails because `Foglet.TUI.Widgets.List.RichRow` is missing or
    undefined." We document the test inventory (the tests that will exist
    once Wave 1 lands), but we do NOT promise individual RED counts.

    Tests in this file (post-Wave-1, all GREEN):

    Matrix block (8 tests):
    - Tests 1-8 (selected × unselected × with-glyphs × with-metadata): exercise
      the eight-cell matrix from SPEC requirement 1 acceptance via RichRow.render/1

    Focused-row uniqueness (2 tests, SPEC req 4 — THREADS-02):
    - Test 9: focused unread row has at least one styling property (`bg` or
      `:bold`) that no non-focused row in the same render shares
    - Test 10: focused read row also passes the same uniqueness assertion

    Cluster-width property invariant (1 test, D-03 / 20-REVIEWS HIGH #1):
    - Test 11: cluster string display width is identical to @cluster_width
      (= 4) across every state combination — including [], single atoms,
      pairs, all three, and unknown atoms like [:subscribed, :required].
      This replaces a runtime guard in production (per user decision #1).

    Generic state-atom acceptance (1 test, SPEC req 6 / D-02):
    - Test 12: `state_cluster: [:subscribed, :required]` renders without
      referencing Foglet.Threads and pads to the same display width as
      `state_cluster: []`

    64-cell width contract (2 tests, SPEC req 5):
    - Test 13: long title at width: 64 — cluster fully present, full metadata
      string present, title contains `…`, total display width ≤ 64
    - Test 14: 20-cell minimum title attempt is preserved when budget allows

    Theme-routing hygiene (1 test, RICHROW-01):
    - Test 15: no hardcoded color atom leaks for any selection × state ×
      metadata combination

    Focus marker convention (1 test, D-08):
    - Test 16: selected row begins with `▌ `, unselected with two-space gutter

    Glyph identity (3 tests, D-05):
    - Test 17: [:unread] cluster contains ◆ (U+25C6)
    - Test 18: [:sticky] cluster contains ● (U+25CF)
    - Test 19: [:locked] cluster contains ⚿ (U+26BF)

    Selection-vs-state-glyph precedence (3 tests, 20-REVIEWS HIGH #2):
    - Test 20: a selected unread row produces a ◆ text run with
      `fg == theme.accent.fg` (NOT `theme.selected.fg`); the run also has
      `bg == theme.selected.bg` and `:bold` in `:style`
    - Test 21: a selected sticky row produces a ● text run with
      `fg == theme.info.fg`; the run also has `bg == theme.selected.bg`
    - Test 22: a selected locked row produces a ⚿ text run with
      `fg == theme.warning.fg`; the run also has `bg == theme.selected.bg`

    Optional metadata (3 tests, 20-REVIEWS user decision #3):
    - Test 23: omitting `:metadata` entirely renders without crashing and
      produces no text run whose content matches an `@`-handle pattern
    - Test 24: `metadata: nil` is equivalent to omitting `:metadata`
    - Test 25: `metadata: ""` is equivalent to omitting `:metadata`; no
      trailing space is emitted (assert the title body extends to fill the
      residual width when no metadata is supplied)
  </behavior>
  <action>
Create `test/foglet_bbs/tui/widgets/list/rich_row_test.exs` with this structure:

```elixir
defmodule Foglet.TUI.Widgets.List.RichRowTest do
  use ExUnit.Case, async: true

  import Foglet.TUI.WidgetHelpers,
    only: [
      flatten_text: 1,
      assert_text_run: 3,
      color_atom_leaked?: 2,
      color_names: 0,
      text_runs: 1
    ]

  alias Foglet.TUI.TextWidth
  alias Foglet.TUI.Theme
  alias Foglet.TUI.Widgets.List.RichRow

  @cluster_width 4

  setup do
    %{theme: Theme.default()}
  end

  describe "render/1 — selection × state × metadata matrix (RICHROW-01)" do
    test "unselected, no state, with metadata renders left-padded title and right-aligned metadata", %{theme: theme}
    test "unselected, [:unread], with metadata applies emphasis :bold and accent fg to title", %{theme: theme}
    test "unselected, [:sticky], with metadata routes sticky glyph through theme.info.fg", %{theme: theme}
    test "unselected, [:locked], with metadata routes locked glyph through theme.warning.fg", %{theme: theme}
    test "selected, no state, with metadata renders ▌ marker and theme.selected.{fg,bg} + :bold", %{theme: theme}
    test "selected, [:unread], with metadata stacks selection bg over unread accent fg", %{theme: theme}
    test "selected, [:sticky, :locked], with metadata renders both glyphs in cluster", %{theme: theme}
    test "unselected, no state, no metadata renders title without metadata text", %{theme: theme}
  end

  describe "render/1 — focused-row uniqueness (THREADS-02 / SPEC req 4)" do
    test "focused unread row has at least one styling property unselected rows do not share", %{theme: theme}
    test "focused read row has at least one styling property unselected rows do not share", %{theme: theme}
  end

  describe "render/1 — cluster fixed-width property invariant (D-03 / 20-REVIEWS HIGH #1)" do
    test "leading cluster display width equals @cluster_width across every state combination", %{theme: theme}
  end

  describe "render/1 — generic state-atom acceptance (RICHROW-01 / SPEC req 6)" do
    test "renders [:subscribed, :required] cluster as visual whitespace without referencing Foglet.Threads", %{theme: theme}
  end

  describe "render/1 — 64-cell width contract (SPEC req 5)" do
    test "long title at width: 64 truncates with … while cluster and metadata render fully", %{theme: theme}
    test "below 20-cell budget, title still ends with … and full metadata is preserved", %{theme: theme}
  end

  describe "render/1 — theme-routing hygiene (RICHROW-01)" do
    test "no hardcoded color atom leaks for any selection × state × metadata combination", %{theme: theme}
  end

  describe "render/1 — focus marker convention (D-08)" do
    test "selected row begins with ▌ marker, unselected row begins with two-space gutter", %{theme: theme}
  end

  describe "render/1 — glyph identity (D-05)" do
    test "[:unread] cluster contains ◆ (U+25C6)", %{theme: theme}
    test "[:sticky] cluster contains ● (U+25CF)", %{theme: theme}
    test "[:locked] cluster contains ⚿ (U+26BF)", %{theme: theme}
  end

  describe "render/1 — selection-vs-state-glyph precedence (20-REVIEWS HIGH #2)" do
    test "selected [:unread] row: ◆ run keeps fg = theme.accent.fg and gains bg = theme.selected.bg", %{theme: theme}
    test "selected [:sticky] row: ● run keeps fg = theme.info.fg and gains bg = theme.selected.bg", %{theme: theme}
    test "selected [:locked] row: ⚿ run keeps fg = theme.warning.fg and gains bg = theme.selected.bg", %{theme: theme}
  end

  describe "render/1 — optional metadata default (20-REVIEWS user decision #3)" do
    test "omitting :metadata renders without crash and emits no metadata text run", %{theme: theme}
    test "metadata: nil is equivalent to omitting :metadata", %{theme: theme}
    test "metadata: \"\" is equivalent to omitting :metadata and emits no trailing gap", %{theme: theme}
  end
end
```

For each test body:

1. **Matrix tests (1-8)**: Build a render call like

   ```elixir
   tree =
     RichRow.render(
       title: "Thread alpha",
       metadata: "@alice · 3 posts · 2h ago",
       state_cluster: [:unread],
       selected: false,
       theme: theme,
       width: 80,
       emphasis: :bold
     )

   flat = flatten_text(tree)
   assert flat =~ "Thread alpha"
   assert flat =~ "@alice · 3 posts · 2h ago"
   assert flat =~ "◆"
   assert_text_run(tree, "Thread alpha", fg: theme.accent.fg, style: [:bold])
   ```

   Use `assert_text_run/3` for style-property assertions. Use `flatten_text/1` + `=~`
   for content presence. NEVER assert via `inspect(tree) =~ "..."` for style.

2. **Focused-row uniqueness (Tests 9-10)**: Render two rows in one call sequence —
   one with `selected: true` and one with `selected: false`, both with the same
   `state_cluster` — then walk both `text_runs/1` results and assert there exists
   at least one style key (`:bg`, or `:style` containing `:bold`) where the focused
   row's value differs from every non-focused row's value.

3. **Cluster fixed-width property invariant (Test 11)** — replaces the previous
   "render and measure flat" pattern. Per 20-REVIEWS HIGH #1, this test now
   directly enforces what was previously a runtime guard:

   ```elixir
   for state_cluster <- [
         [],
         [:unread],
         [:sticky],
         [:locked],
         [:unread, :sticky],
         [:unread, :locked],
         [:sticky, :locked],
         [:unread, :sticky, :locked],
         [:subscribed, :required],
         [:category]
       ] do
     tree =
       RichRow.render(
         title: "T",
         metadata: "m",
         state_cluster: state_cluster,
         selected: false,
         theme: theme,
         width: 80
       )

     # Find the cluster substring: it sits between the focus marker (2 cells
     # of whitespace for unselected rows) and the title body. Pull the first
     # text runs and concatenate the cluster glyphs (positions 2..@cluster_width-1
     # of the marker+cluster prefix).
     runs = text_runs(tree)
     prefix =
       runs
       |> Enum.map(&Map.get(&1, :text, ""))
       |> Enum.join()
       |> String.slice(0, 2 + @cluster_width)

     cluster = String.slice(prefix, 2, @cluster_width)

     assert TextWidth.display_width(cluster) == @cluster_width,
            "cluster width drift for state_cluster=#{inspect(state_cluster)}: " <>
              "got #{TextWidth.display_width(cluster)}, expected #{@cluster_width}"
   end
   ```

4. **Generic state atoms (Test 12)**: Render with `state_cluster: [:subscribed, :required]`
   and assert no `KeyError`/`FunctionClauseError`. Assert the cluster pads to
   the same display width as `state_cluster: []`. Do NOT alias or reference
   `Foglet.Threads` anywhere in this test.

5. **64-cell budget (Test 13)**: Build a 100-character title (`String.duplicate("x", 100)`),
   render with `width: 64`, assert:
   - `flat =~ "◆"` (cluster fully rendered)
   - `flat =~ "@alice"` (metadata fully rendered)
   - `flat =~ "…"` (title truncated)
   - `TextWidth.display_width(flat) <= 64`

6. **Theme-routing hygiene (Test 15)**: After rendering each combination, run

   ```elixir
   serialized = inspect(tree, limit: :infinity)
   for color <- color_names() do
     refute color_atom_leaked?(serialized, color),
            "found hardcoded :#{color} atom for combo #{label}"
   end
   ```

7. **Focus marker (Test 16)**: Assert `flatten_text(selected_tree)` starts with
   `"▌ "` (U+258C + space) and `flatten_text(unselected_tree)` starts with
   `"  "` (two spaces).

8. **Glyph identity (Tests 17-19)**: Use direct `flatten_text(tree) =~ "◆"`
   for `[:unread]`, `=~ "●"` for `[:sticky]`, `=~ "⚿"` for `[:locked]`.

9. **Selection-vs-state-glyph precedence (Tests 20-22)** — per 20-REVIEWS HIGH #2:

   ```elixir
   tree =
     RichRow.render(
       title: "T",
       metadata: "m",
       state_cluster: [:sticky],
       selected: true,
       theme: theme,
       width: 40
     )

   runs = text_runs(tree)

   sticky_run =
     Enum.find(runs, fn run ->
       text = Map.get(run, :text, "")
       text =~ "●"
     end)

   assert sticky_run, "expected a text run containing ●, got runs=#{inspect(runs)}"

   assert Map.get(sticky_run, :fg) == theme.info.fg,
          "selected sticky glyph fg should remain theme.info.fg (#{inspect(theme.info.fg)}); " <>
            "got #{inspect(Map.get(sticky_run, :fg))}"

   assert Map.get(sticky_run, :bg) == theme.selected.bg,
          "selected sticky glyph should adopt theme.selected.bg (#{inspect(theme.selected.bg)}); " <>
            "got #{inspect(Map.get(sticky_run, :bg))}"
   ```

   Repeat for `[:unread]` (`fg == theme.accent.fg`, glyph `◆`) and `[:locked]`
   (`fg == theme.warning.fg`, glyph `⚿`).

10. **Optional metadata (Tests 23-25)** — per 20-REVIEWS user decision #3:

    ```elixir
    # Test 23 — omitted
    tree =
      RichRow.render(
        title: "T",
        state_cluster: [],
        selected: false,
        theme: theme,
        width: 40
      )

    runs = text_runs(tree)
    refute Enum.any?(runs, fn run ->
      text = Map.get(run, :text, "")
      text =~ ~r/@\w+/
    end), "expected no metadata text run when :metadata is omitted, got runs=#{inspect(runs)}"

    # Test 24 — nil
    tree_nil =
      RichRow.render(
        title: "T",
        metadata: nil,
        state_cluster: [],
        selected: false,
        theme: theme,
        width: 40
      )

    assert flatten_text(tree_nil) == flatten_text(tree)

    # Test 25 — empty string, also asserts no trailing gap
    tree_empty =
      RichRow.render(
        title: "T",
        metadata: "",
        state_cluster: [],
        selected: false,
        theme: theme,
        width: 40
      )

    assert flatten_text(tree_empty) == flatten_text(tree)

    # No trailing space: rstrip should not change the rendered length when
    # the row is built without metadata. (Padding may legitimately include
    # spaces; the assertion is that no metadata-shaped run appears.)
    flat_empty = flatten_text(tree_empty)
    refute flat_empty =~ ~r/·/,
           "expected no `·` separator when metadata is empty, got: #{inspect(flat_empty)}"
    ```

The file MUST compile under ExUnit and the targeted `mix test` MUST exit
non-zero. Do not stub a fake `RichRow` module to make tests pass. Wave 1
creates the real module.
  </action>
  <verify>
    <automated>rtk mix compile --no-validate-compile-env 2>&1 | tail -30 && rtk mix test test/foglet_bbs/tui/widgets/list/rich_row_test.exs 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `test/foglet_bbs/tui/widgets/list/rich_row_test.exs` exists.
    - `grep -c "defmodule Foglet.TUI.Widgets.List.RichRowTest do" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns 1.
    - `grep -c "alias Foglet.TUI.Widgets.List.RichRow$" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns 1.
    - `grep -c "import Foglet.TUI.WidgetHelpers" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns 1.
    - `grep -c "describe \"render/1" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least 9 (matrix, uniqueness, cluster invariant, generic, 64-cell, hygiene, marker, glyph identity, selection-precedence, optional metadata).
    - `grep -F "◆" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least one match (unread glyph assertion).
    - `grep -F "●" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least one match (sticky glyph assertion).
    - `grep -F "⚿" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least one match (locked glyph assertion).
    - `grep -F "▌" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least one match (focus marker assertion).
    - `grep -c "color_atom_leaked?" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least 1 (hygiene test active).
    - `grep -c "TextWidth.display_width" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least 2 (cluster invariant + 64-cell budget).
    - `grep -c ":subscribed" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least 1 (generic state-atom test present + cluster invariant unknown-atom row).
    - `grep -c "@cluster_width 4" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns 1 (test mirrors the production constant).
    - `grep -c "theme.info.fg" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least 2 (matrix sticky test + selection-precedence sticky test).
    - `grep -c "theme.accent.fg" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least 2 (matrix unread test + selection-precedence unread test).
    - `grep -c "theme.warning.fg" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least 2 (matrix locked test + selection-precedence locked test).
    - `grep -c "theme.selected.bg" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least 3 (one per selection-precedence test).
    - `grep -c "metadata: nil" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least 1.
    - `grep -c "metadata: \"\"" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns at least 1.
    - `rtk mix test test/foglet_bbs/tui/widgets/list/rich_row_test.exs` exits non-zero (RED) — the test command fails because `Foglet.TUI.Widgets.List.RichRow` is missing or undefined (compile error or `UndefinedFunctionError` on `RichRow.render/1`). Document the failure mode (compile vs runtime) in the SUMMARY.
    - `grep -c "alias Foglet.Threads" test/foglet_bbs/tui/widgets/list/rich_row_test.exs` returns 0 (generic-atom test does not couple to Foglet.Threads).
  </acceptance_criteria>
  <done>The scaffold compiles or fails to compile cleanly because `RichRow` is missing; the targeted test command fails RED for that reason; every SPEC requirement and 20-REVIEWS HIGH/user-decision item has at least one named test waiting for Wave 1 to GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A      | Phase 20 introduces no new trust boundaries. The plan creates a test-only file with no I/O, no user input, no network, no persistence, and no auth/session/authz code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Spoofing | rich_row_test.exs | N/A | Test-only file with no identity processing. ASVS V2 inapplicable per RESEARCH.md Security Domain table. |
| T-20-02 | Tampering | rich_row_test.exs | N/A | No persistence layer touched. ASVS V2/V5 inapplicable. |
| T-20-03 | Repudiation | rich_row_test.exs | N/A | No audit/logging surface. |
| T-20-04 | Information Disclosure | rich_row_test.exs | N/A | Inline test fixtures contain no secrets, PII, or credentials. |
| T-20-05 | Denial of Service | rich_row_test.exs | N/A | Test runtime is bounded by ExUnit; no external resources. |
| T-20-06 | Elevation of Privilege | rich_row_test.exs | N/A | No authorization scopes, no role checks. ASVS V4 inapplicable. |
</threat_model>

<verification>
- `rtk mix compile --warnings-as-errors` exits 0 (or fails because `RichRow` is missing — that is also acceptable).
- `rtk mix test test/foglet_bbs/tui/widgets/list/rich_row_test.exs` exits non-zero because `Foglet.TUI.Widgets.List.RichRow` is missing or undefined (compile error or `UndefinedFunctionError`). The SUMMARY documents which mode occurred.
- Every acceptance criterion grep returns the required count.
</verification>

<success_criteria>
- `test/foglet_bbs/tui/widgets/list/rich_row_test.exs` exists.
- The targeted test command fails RED specifically because `RichRow` is missing or undefined.
- All test inventory items (matrix, uniqueness, cluster invariant, generic atoms, 64-cell, hygiene, marker, glyph identity, selection-precedence, optional metadata) are present and grep-verifiable.
- Theme-routing hygiene assertion is active.
- Generic state-atom test does not import or alias `Foglet.Threads`.
- Selection-vs-state-glyph precedence tests exist for unread/sticky/locked and assert glyph fg keeps the state slot while bg adopts `theme.selected.bg`.
- Optional metadata tests cover omitted, `nil`, and `""`.
</success_criteria>

<output>
After completion, create `.planning/phases/20-rich-rows-and-thread-flow/20-01-SUMMARY.md` documenting:
- File created and total LOC.
- The exact RED failure mode (compile error vs UndefinedFunctionError) — per 20-REVIEWS MEDIUM #6 we do NOT promise per-test RED counts.
- Confirmation that no `Foglet.Threads` alias appears in the file.
- Note for Wave 1: this scaffold is the GREEN target.
</output>

## Changelog

- 2026-04-25 — Revised per 20-REVIEWS.md (codex). Addressed:
  - HIGH #1: Added cluster-width property test (Test 11) that asserts
    `TextWidth.display_width(cluster) == @cluster_width` across every state
    combination, replacing the runtime guard removed from production
    (per user decision #1).
  - HIGH #2: Added selection-vs-state-glyph precedence describe block
    (Tests 20-22) asserting selected unread/sticky/locked rows keep glyph
    `:fg` on their state slot while gaining `:bg = theme.selected.bg`.
  - MEDIUM #6: Reworded RED-first acceptance — no per-test RED enumeration;
    accept "compile error or UndefinedFunctionError because RichRow is missing".
  - User decision #3: Added optional-metadata describe block (Tests 23-25)
    covering omitted, `nil`, and `""` metadata.
