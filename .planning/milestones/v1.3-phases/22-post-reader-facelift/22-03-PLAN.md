---
id: 22-03
phase: 22
plan: 03
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - test/foglet_bbs/tui/layout_smoke_test.exs
autonomous: true
requirements: [READER-01, READER-02, READER-03, READER-04]
tags: [tui, layout-smoke, size-contract, verification, precommit]

must_haves:
  truths:
    - "D-15: `layout_smoke_test.exs` has a Phase 22 PostReader size-contract block for `{64,22}`, `{80,24}`, and `{132,50}`."
    - "D-04/D-11: Positioned render at all three sizes includes compact header text and compact `Posts X/N` progress."
    - "D-07/D-08/D-09: Positioned render at all three sizes includes a body gutter and selected body text without markdown semantics drift."
    - "D-10/D-13: Progress and header are non-scrolling surfaces while selected body remains visible."
    - "D-14: Focused screen tests remain green after layout-smoke coverage is added."
    - "No positioned text element overflows width or overlaps incoherently at the required size triple."
  artifacts:
    - path: "test/foglet_bbs/tui/layout_smoke_test.exs"
      provides: "Phase 22 PostReader layout size-contract coverage"
      contains: "Phase 22 PostReader"
  key_links:
    - from: "layout_smoke_test Phase 22 block"
      to: "PostReader.render/1"
      via: "Raxol layout engine at 64x22, 80x24, 132x50"
      pattern: "PostReader\\.render"
---

<objective>
Add the Phase 22 size-contract layout smoke tests and run the full finish-line verification. The layout block proves the reader facelift remains usable at 64x22, 80x24, and 132x50, with compact metadata, guttered body, compact progress, selected body visibility, and no positioned text overflow or incoherent overlap.

Purpose: Plans 22-01 and 22-02 implement the widget and screen behavior. This final plan proves the user-facing terminal composition survives the project’s required size triple and keeps the phase ready for execution verification.

Implements requirements: READER-01, READER-02, READER-03, READER-04.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-post-reader-facelift/22-CONTEXT.md
@.planning/phases/22-post-reader-facelift/22-SPEC.md
@.planning/phases/22-post-reader-facelift/22-RESEARCH.md
@.planning/phases/22-post-reader-facelift/22-VALIDATION.md
@.planning/phases/22-post-reader-facelift/22-01-PLAN.md
@.planning/phases/22-post-reader-facelift/22-02-PLAN.md
@test/foglet_bbs/tui/layout_smoke_test.exs
@test/foglet_bbs/tui/screens/post_reader_test.exs
@lib/foglet_bbs/tui/screens/post_reader.ex
@lib/foglet_bbs/tui/text_width.ex
@vendor/raxol/lib/raxol/ui/layout/engine.ex
</context>

<threat_model>
| Ref | Threat | Severity | Mitigation |
|-----|--------|----------|------------|
| T-22-01 | 64x22 render can overflow or obscure command chrome when fixed header/progress rows are added. | medium | Add positioned layout assertions at 64x22, 80x24, and 132x50 for every text element. |
| T-22-02 | Compact progress or gutter can appear in source tests but be clipped by real layout engine. | medium | Assert positioned text includes header, progress, gutter, and selected body after `Engine.apply_layout/2`. |
| T-22-03 | Full project gates can regress outside focused tests. | high | Run `rtk mix precommit` after focused tests pass. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 22 PostReader size-contract layout smoke tests</name>
  <files>test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/layout_smoke_test.exs
    - test/foglet_bbs/tui/screens/post_reader_test.exs
    - lib/foglet_bbs/tui/screens/post_reader.ex
    - lib/foglet_bbs/tui/text_width.ex
    - .planning/phases/22-post-reader-facelift/22-CONTEXT.md
    - .planning/phases/22-post-reader-facelift/22-RESEARCH.md
  </read_first>
  <action>
    Add a new `describe "Phase 22 PostReader size contracts"` block to `test/foglet_bbs/tui/layout_smoke_test.exs`.

    Build a PostReader state with:
    - `current_screen: :post_reader`
    - `current_user: %Foglet.Accounts.User{id: "u1", handle: "reader"}`
    - `current_board: %{id: "b1", name: "General"}`
    - `current_thread: %{id: "t1", title: "Reader Contract"}`
    - `posts`: 12 maps with message numbers 31 through 42, handles such as `mina`, and plain ASCII body text. The selected post at `selected_post_index: 2` and `message_number: 33` must have exactly `body: "Selected body sentinel\n\nMore content here."` so the sentinel survives viewport slicing at 64x22.
    - `screen_state.post_reader`: `PostReader.init_screen_state(selected_post_index: 2)` or an equivalent struct update.
    - `session_context: %{theme: Foglet.TUI.Theme.default()}`
    - `terminal_size` set inside the loop.

    For each `{width, height}` in `[{64, 22}, {80, 24}, {132, 50}]`, render with `PostReader.render(state)` and apply `apply_at_size({width, height})`.

    Assertions:
    - `text_elements(positioned)` is not empty.
    - Every text element has `x >= 0`, `y >= 0`, and `x + TextWidth.display_width(text) <= width`.
    - Flattened positioned text includes `Post 3 of 12`, `#33`, `@mina`, `Posts 3/12`, the gutter glyph `│` or `|`, and `Selected body sentinel`.
    - The header y-position is above the selected body y-position.
    - The progress y-position is strictly below the header y-position and strictly above the command bar y-position. Reuse the existing command detection pattern by finding text containing `Next`, `Prev`, or `Back`; do not assert breadcrumb text because Chrome owns breadcrumb truncation.
    - At least one command-bar text element containing `Next`, `Prev`, or `Back` appears on the bottom-most occupied command row, proving progress did not displace the command bar at 64x22.
    - For each row `y`, adjacent non-empty text elements sorted by x do not overlap: previous `x + TextWidth.display_width(previous.text) <= current.x`.

    Keep helper code local and minimal; reuse existing `text_elements/1`, `flatten_text/1`, `apply_at_size/2`, and `TextWidth.display_width/1` helpers where possible.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/layout_smoke_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/foglet_bbs/tui/layout_smoke_test.exs` contains `Phase 22 PostReader size contracts`.
    - `test/foglet_bbs/tui/layout_smoke_test.exs` contains `{64, 22}`, `{80, 24}`, and `{132, 50}` in the Phase 22 block.
    - `test/foglet_bbs/tui/layout_smoke_test.exs` contains assertions for `Post 3 of 12`, `#33`, `@mina`, `Posts 3/12`, and `Selected body sentinel`.
    - `test/foglet_bbs/tui/layout_smoke_test.exs` contains `Selected body sentinel\n\nMore content here.`
    - `test/foglet_bbs/tui/layout_smoke_test.exs` checks `TextWidth.display_width(text) <= width` or equivalent positioned overflow logic for each text element.
    - `rtk mix test test/foglet_bbs/tui/layout_smoke_test.exs` exits 0.
  </acceptance_criteria>
  <done>
    Layout smoke validates the Phase 22 reader at the required terminal size triple.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run focused and full finish-line verification</name>
  <files>test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    - .planning/phases/22-post-reader-facelift/22-VALIDATION.md
    - .planning/phases/22-post-reader-facelift/22-01-PLAN.md
    - .planning/phases/22-post-reader-facelift/22-02-PLAN.md
    - test/foglet_bbs/tui/widgets/post/post_card_test.exs
    - test/foglet_bbs/tui/screens/post_reader_test.exs
    - test/foglet_bbs/tui/layout_smoke_test.exs
  </read_first>
  <action>
    Run the Phase 22 focused suite and full project precommit after Tasks 1 through Plan 22-02 are complete:

    ```bash
    rtk mix test test/foglet_bbs/tui/widgets/post/post_card_test.exs test/foglet_bbs/tui/screens/post_reader_test.exs test/foglet_bbs/tui/layout_smoke_test.exs
    rtk mix precommit
    ```

    If either command fails, fix only Phase 22 implementation/test issues in the relevant files from plans 22-01 through 22-03. Do not refactor unrelated screens or domain contexts.
  </action>
  <verify>
    <automated>rtk mix precommit</automated>
  </verify>
  <acceptance_criteria>
    - `rtk mix test test/foglet_bbs/tui/widgets/post/post_card_test.exs test/foglet_bbs/tui/screens/post_reader_test.exs test/foglet_bbs/tui/layout_smoke_test.exs` exits 0.
    - `rtk mix precommit` exits 0.
  </acceptance_criteria>
  <done>
    Phase 22 focused tests and project precommit are green.
  </done>
</task>

</tasks>

<verification>
Run:

```bash
rtk mix test test/foglet_bbs/tui/widgets/post/post_card_test.exs test/foglet_bbs/tui/screens/post_reader_test.exs test/foglet_bbs/tui/layout_smoke_test.exs
rtk mix precommit
```
</verification>

<success_criteria>
- READER-01: Positioned render includes post position, stable message number, author, and age.
- READER-02: Positioned render includes body gutter and selected body text without overflow.
- READER-03: Positioned render includes compact progress at 64x22 and behavior tests remain green.
- READER-04: Full plan set proves shared PostCard-based reader composition through focused and layout tests.
</success_criteria>
