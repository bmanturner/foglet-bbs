---
id: 24-05
phase: 24
plan: 05
type: execute
wave: 3
depends_on: []
files_modified:
  - lib/foglet_bbs/tui/widgets/modal/form.ex
  - test/foglet_bbs/tui/widgets/modal/form_test.exs
autonomous: true
requirements: [CONSOLE-04]
tags: [tui, widgets, modal, form, operator-console]
must_haves:
  truths:
    - "D-05: `Foglet.TUI.Widgets.Modal.Form` is refreshed in place; no parallel form primitive is introduced."
    - "D-18: Modal.Form preserves `init/1`, `handle_event/2`, `render/2`, `set_errors/2`, typed coercion, Tab/Shift-Tab focus movement, Enter submit, Esc cancel, callbacks, textarea raw-value workaround, and body-only rendering."
    - "D-19: Modal.Form improves body hierarchy with stronger heading, clearer labels, visible required markers, inline field errors, base errors, and compact submit/cancel action footer."
    - "D-20: `Modal.Form.render/2` does not wrap output in a modal box, border, or centering container; app-level overlay remains the single owner of chrome."
    - "D-21/D-23: Modal.Form render and behavior tests cover required markers, errors, footer, no double chrome, size-relevant compact output, and theme hygiene."
  artifacts:
    - path: lib/foglet_bbs/tui/widgets/modal/form.ex
      provides: "Refreshed body-only Modal.Form renderer"
    - path: test/foglet_bbs/tui/widgets/modal/form_test.exs
      provides: "Preservation and visual hierarchy tests"
---

<objective>
Refresh `Modal.Form` body rendering while preserving its public behavior and
body-only overlay contract.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/24-operator-console-primitives/24-CONTEXT.md
@.planning/phases/24-operator-console-primitives/24-RESEARCH.md
@.planning/phases/24-operator-console-primitives/24-VALIDATION.md
@lib/foglet_bbs/tui/widgets/modal/form.ex
@test/foglet_bbs/tui/widgets/modal/form_test.exs
@lib/foglet_bbs/tui/widgets/modal.ex
@lib/foglet_bbs/tui/app.ex
@lib/foglet_bbs/tui/screens/sysop/boards_view.ex
@test/support/foglet/tui/widget_helpers.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Modal.Form tests for visual refresh</name>
  <files>test/foglet_bbs/tui/widgets/modal/form_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/widgets/modal/form_test.exs
    - lib/foglet_bbs/tui/widgets/modal/form.ex
    - .planning/phases/24-operator-console-primitives/24-RESEARCH.md
  </read_first>
  <action>Add tests without deleting existing behavior coverage. Add a render test with title `"Create board"` and fields `%{name: :slug, type: :text, label: "Slug", required: true}` and `%{name: :name, type: :text, label: "Name", required: true}`. Assert flattened output contains `Create board`, `Slug`, `Name`, a visible required marker such as `*` or `(required)`, `[Enter] Submit`, and `[Esc] Cancel`. Add a test that `Form.set_errors(state, %{slug: "can't be blank", base: "Board invalid"})` renders both messages. Add a no-double-chrome test that serializes `Form.render/2` and refutes new modal chrome markers: no `border:`, no `type: :box` introduced by Form, and no centering container text. Keep existing typed coercion, navigation, submit, cancel, textarea, and theme-hygiene tests intact.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/modal/form_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/foglet_bbs/tui/widgets/modal/form_test.exs` contains exact strings `Create board`, `Slug`, `Name`, `[Enter] Submit`, `[Esc] Cancel`, `can't be blank`, and `Board invalid`.
    - Test file still contains existing `typed coercion`, `Shift-Tab`, `Enter on last field`, and `Esc cancels` tests.
    - Test file contains a no-double-chrome assertion that refutes `border:` or `type: :box`.
    - Initial RED is acceptable only until Task 2 is complete.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh Modal.Form render body only</name>
  <files>lib/foglet_bbs/tui/widgets/modal/form.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/widgets/modal/form.ex
    - test/foglet_bbs/tui/widgets/modal/form_test.exs
    - lib/foglet_bbs/tui/widgets/input/text_input.ex
    - lib/foglet_bbs/tui/widgets/input/checkbox.ex
    - lib/foglet_bbs/tui/widgets/input/radio_group.ex
  </read_first>
  <action>Modify only render helpers in `Modal.Form`. Preserve public functions and event/coercion code. Replace the current title/divider/hint body with a stronger body-only column: title text using `theme.title.fg`, optional divider text using `theme.border.fg`, field label rows with focused labels using `theme.accent.fg` and unfocused labels using `theme.primary.fg`, visible required marker for specs where `Map.get(spec, :required, false)` is true, inline field errors using `theme.error.fg`, base errors using `theme.error.fg`, and a compact action footer text exactly containing `[Enter] Submit   [Esc] Cancel` styled with `theme.dim.fg`. Do not add `box`, border styles, modal centering, or overlay ownership to this module.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/modal/form_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/widgets/modal/form.ex` still contains `def init`, `def handle_event`, `def set_errors`, and `def render`.
    - Source contains `[Enter] Submit   [Esc] Cancel`.
    - Source contains `Map.get(spec, :required, false)` or equivalent required-field check.
    - Source does not add `box style:` inside `render/2` or `render_field`.
    - Source does not contain `border:` in new Modal.Form render code.
    - `rtk mix test test/foglet_bbs/tui/widgets/modal/form_test.exs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User draft input -> form field widgets | Existing field widgets and coercion remain unchanged. |
| Modal.Form -> app modal overlay | Form renders body only; app overlay owns border and centering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-13 | Tampering | Submit payload coercion | mitigate | Preserve existing typed coercion tests and code. |
| T-24-14 | Denial of Service | Modal nesting/double chrome | mitigate | Tests refute outer box/border in `Modal.Form.render/2`. |
| T-24-15 | Spoofing | Required/error state display | mitigate | Render tests pin required markers and inline/base errors. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/modal/form_test.exs`.
</verification>

<success_criteria>
- Existing Modal.Form behavior still passes.
- Refreshed body renders title, labels, required markers, inline/base errors, and action footer.
- Modal.Form remains body-only and does not introduce overlay chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/24-operator-console-primitives/24-05-SUMMARY.md`.
</output>
