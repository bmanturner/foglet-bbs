---
id: 24-03
phase: 24
plan: 03
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - lib/foglet_bbs/tui/widgets/display/console_table.ex
  - test/foglet_bbs/tui/widgets/display/console_table_test.exs
autonomous: true
requirements: [CONSOLE-03]
tags: [tui, widgets, display, table, operator-console]
must_haves:
  truths:
    - "D-03: A display-level operator table wrapper exists as an explicit operator-console public API."
    - "D-12: Console table wraps or delegates to `Foglet.TUI.Widgets.Display.Table`; it does not fork Raxol table behavior."
    - "D-13: Console table owns dense defaults for compact columns, row normalization, empty states, sortable/filterable/selectable flags, selected-row actions, and theme routing."
    - "D-14: Console table tests instantiate LOG, USERS, BOARDS, SSH-key, invite, and moderation-shaped fixture data and prove headers, rows, empty states, deterministic selection actions, and no domain logic."
    - "D-21/D-23: Console table has primitive-level size and theme-hygiene coverage under the mirrored widget test path."
  artifacts:
    - path: lib/foglet_bbs/tui/widgets/display/console_table.ex
      provides: "Operator-console table facade over Display.Table"
    - path: test/foglet_bbs/tui/widgets/display/console_table_test.exs
      provides: "Dense table fixture, empty-state, selection, and hygiene tests"
---

<objective>
Create the operator-console table facade that standardizes dense table defaults
while preserving the existing `Display.Table` foundation.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/24-operator-console-primitives/24-CONTEXT.md
@.planning/phases/24-operator-console-primitives/24-RESEARCH.md
@.planning/phases/24-operator-console-primitives/24-VALIDATION.md
@lib/foglet_bbs/tui/widgets/display/table.ex
@test/foglet_bbs/tui/widgets/display/table_test.exs
@lib/foglet_bbs/tui/screens/account/ssh_keys_surface.ex
@lib/foglet_bbs/tui/screens/moderation.ex
@lib/foglet_bbs/tui/screens/sysop/users_view.ex
@lib/foglet_bbs/tui/screens/sysop/boards_view.ex
@lib/foglet_bbs/tui/screens/shared/invites_surface.ex
@test/support/foglet/tui/widget_helpers.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ConsoleTable contract tests</name>
  <files>test/foglet_bbs/tui/widgets/display/console_table_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/widgets/display/table_test.exs
    - lib/foglet_bbs/tui/widgets/display/table.ex
    - lib/foglet_bbs/tui/screens/moderation.ex
    - lib/foglet_bbs/tui/screens/shared/invites_surface.ex
    - test/support/foglet/tui/widget_helpers.ex
  </read_first>
  <action>Create `Foglet.TUI.Widgets.Display.ConsoleTableTest`. Define fixture data maps for moderation log rows (`timestamp`, `moderator`, `target`, `reason`), moderation user rows (`handle`, `role`, `status`), moderation board rows (`name`, `slug`, `category`, `scope`), SSH key rows (`label`, `fingerprint`, `inserted_at`, `last_used_at`), invite rows (`code`, `status`, `issuer_id`, `inserted_at`), sysop user rows (`status`, `handle`, `email`), and sysop board rows (`category`, `slug`, `name`, `posting`). Tests should initialize `ConsoleTable.init(columns: ..., rows: ..., empty_state: "No rows.", sortable: true, filterable: true, selectable: true)`, assert render output includes headers and fixture cell values, assert empty rows render `No rows.`, assert `handle_event(%{key: :enter}, state)` returns `{:row_selected, row}` for the first row, and assert theme hygiene.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/display/console_table_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains `defmodule Foglet.TUI.Widgets.Display.ConsoleTableTest`.
    - Test file contains exact fixture strings `hide_oneliner`, `alice`, `general`, `SHA256:abc`, `INVITE1`, `pending`, and `mods_only`.
    - Test file asserts an empty state string `No rows.`.
    - Test file asserts `{:row_selected,`.
    - Test file calls `color_atom_leaked?` and `color_names()`.
    - Initial RED is acceptable only until Task 2 is complete.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ConsoleTable wrapper over Display.Table</name>
  <files>lib/foglet_bbs/tui/widgets/display/console_table.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/widgets/display/table.ex
    - lib/foglet_bbs/tui/theme.ex
    - test/foglet_bbs/tui/widgets/display/console_table_test.exs
  </read_first>
  <action>Create `Foglet.TUI.Widgets.Display.ConsoleTable` as a stateful facade. Define a struct with `:table`, `:columns`, `:rows`, `:empty_state`, `:selectable`, and `:last_action`. `init/1` must normalize columns by accepting `:id` or `:key`, defaulting widths to compact values such as 12 for unspecified columns, defaulting `empty_state` to `"No records."`, and passing `columns`, `rows`, `sortable`, `filterable`, and `page_size` to `Display.Table.init/1`. `handle_event/2` delegates to `Display.Table.handle_event/2` and stores the returned action. `render/2` requires `theme:`; when rows are empty, render only compact empty-state text with `theme.dim.fg`; otherwise delegate to `Display.Table.render/2`. Do not call screen modules or domain contexts.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/display/console_table_test.exs test/foglet_bbs/tui/widgets/display/table_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/widgets/display/console_table.ex` contains `defmodule Foglet.TUI.Widgets.Display.ConsoleTable`.
    - `@moduledoc` mentions `D-03`, `D-12`, `D-13`, and `D-14`.
    - Source contains `alias Foglet.TUI.Widgets.Display.Table`.
    - Source contains `Table.init`.
    - Source contains `Table.handle_event`.
    - Source contains `Table.render`.
    - Source does not contain `Foglet.Accounts`, `Foglet.Boards`, `Foglet.Config`, `Foglet.Posts`, or `Foglet.Threads`.
    - `rtk mix test test/foglet_bbs/tui/widgets/display/console_table_test.exs test/foglet_bbs/tui/widgets/display/table_test.exs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller rows -> table state | Rows are normalized and displayed only. |
| ConsoleTable -> Display.Table | Delegates rendering and keyboard state to existing table facade. |
| Widget -> domain contexts | No crossing allowed; table performs no mutations or authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Elevation of Privilege | Selected-row actions | mitigate | `handle_event/2` only returns row data; callers own authorization and mutations. |
| T-24-08 | Denial of Service | Empty table navigation | mitigate | Reuse `Display.Table` empty-row guard and add empty-state tests. |
| T-24-09 | Tampering | Table behavior fork | mitigate | Delegate init/event/render to `Display.Table` and keep wrapper defaults thin. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/display/console_table_test.exs test/foglet_bbs/tui/widgets/display/table_test.exs`.
</verification>

<success_criteria>
- `Display.ConsoleTable` exists and wraps `Display.Table`.
- Fixture tests cover operator row shapes, empty states, and selection actions.
- No domain calls or hardcoded terminal color atoms are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/24-operator-console-primitives/24-03-SUMMARY.md`.
</output>
