---
id: 24-02
phase: 24
plan: 02
type: execute
wave: 1
depends_on: [24-01]
files_modified:
  - lib/foglet_bbs/tui/widgets/display/kv_grid.ex
  - test/foglet_bbs/tui/widgets/display/kv_grid_test.exs
autonomous: true
requirements: [CONSOLE-02]
tags: [tui, widgets, display, kv-grid, text-width]
must_haves:
  truths:
    - "D-02: `Foglet.TUI.Widgets.Display.KvGrid` exists under `lib/foglet_bbs/tui/widgets/display/` as a stateless display primitive."
    - "D-09: KvGrid accepts caller-provided label/value entries plus optional state or badge metadata; it fetches no data and infers no domain behavior."
    - "D-10: KvGrid alignment, truncation, and padding use `Foglet.TUI.TextWidth`, not raw `String.length/1` layout math."
    - "D-11: KvGrid tests use Account profile/preference rows, Sysop system metric rows, site setting rows, runtime limit rows, and status summary rows at 64 and 80 columns."
    - "D-21/D-23: KvGrid has primitive-level size and theme-hygiene coverage under the mirrored widget test path."
  artifacts:
    - path: lib/foglet_bbs/tui/widgets/display/kv_grid.ex
      provides: "Width-safe label/value grid renderer"
    - path: test/foglet_bbs/tui/widgets/display/kv_grid_test.exs
      provides: "Fixture, width, badge, and theme tests for KvGrid"
---

<objective>
Create `Foglet.TUI.Widgets.Display.KvGrid`, a width-safe key/value grid for
Account, Sysop, site settings, runtime limits, and status summaries.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/24-operator-console-primitives/24-CONTEXT.md
@.planning/phases/24-operator-console-primitives/24-RESEARCH.md
@.planning/phases/24-operator-console-primitives/24-VALIDATION.md
@lib/foglet_bbs/tui/widgets/display/badge.ex
@lib/foglet_bbs/tui/text_width.ex
@lib/foglet_bbs/tui/theme.ex
@lib/foglet_bbs/tui/screens/account/profile_form.ex
@lib/foglet_bbs/tui/screens/account/prefs_form.ex
@lib/foglet_bbs/tui/screens/sysop/system_snapshot.ex
@test/support/foglet/tui/widget_helpers.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add KvGrid contract tests</name>
  <files>test/foglet_bbs/tui/widgets/display/kv_grid_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/widgets/display/badge_test.exs
    - test/foglet_bbs/tui/widgets/list/rich_row_test.exs
    - test/support/foglet/tui/widget_helpers.ex
    - lib/foglet_bbs/tui/text_width.ex
    - .planning/phases/24-operator-console-primitives/24-RESEARCH.md
  </read_first>
  <action>Create `Foglet.TUI.Widgets.Display.KvGridTest`. Define fixture lists named `account_profile_rows`, `account_prefs_rows`, `sysop_system_rows`, `site_setting_rows`, `runtime_limit_rows`, and `status_summary_rows`. Tests must render `KvGrid.render(rows, theme: Theme.default(), width: 64)` and `width: 80`, assert flattened output includes labels like `Location`, `Timezone`, `Version`, `Registration`, `Title limit`, and `Health`, and assert every collected text line or flattened row candidate has `TextWidth.display_width(line) <= width`. Add a status-row test with badge metadata that expects `healthy` and `pending` text from `Display.Badge`. Add theme-hygiene checks over `color_names/0`.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/display/kv_grid_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains `defmodule Foglet.TUI.Widgets.Display.KvGridTest`.
    - Test file contains exact labels `Location`, `Tagline`, `Real name`, `Timezone`, `Time format`, `Theme`, `Version`, `Uptime`, `Sessions`, `Registration`, `Title limit`, and `Health`.
    - Test file calls `TextWidth.display_width`.
    - Test file calls `color_atom_leaked?` and `color_names()`.
    - Initial RED is acceptable only until Task 2 is complete.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement width-safe KvGrid</name>
  <files>lib/foglet_bbs/tui/widgets/display/kv_grid.ex</files>
  <read_first>
    - lib/foglet_bbs/tui/text_width.ex
    - lib/foglet_bbs/tui/theme.ex
    - lib/foglet_bbs/tui/widgets/display/badge.ex
    - test/foglet_bbs/tui/widgets/display/kv_grid_test.exs
  </read_first>
  <action>Create `Foglet.TUI.Widgets.Display.KvGrid` as a stateless renderer. Implement `render(entries, opts)` requiring `theme: %Theme{}` and accepting `width:`, `label_width:`, and `gap:` options. Normalize entries with `:label`, `:value`, optional `:badge` or `:state`. For each row, truncate and pad the label with `TextWidth.truncate/2` and `TextWidth.pad_trailing/2`; calculate remaining value width from `width - label_width - gap - badge_width`; truncate value with `TextWidth.truncate/2`. If `:badge` or `:state` is present, append `Display.Badge.render(state, theme: theme)` after the value. Render rows with Raxol `text/2` or `row`/`column` while using only theme slot values. Do not call screen modules or domain contexts.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/display/kv_grid_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/widgets/display/kv_grid.ex` contains `defmodule Foglet.TUI.Widgets.Display.KvGrid`.
    - `@moduledoc` mentions `D-02`, `D-09`, `D-10`, and `D-11`.
    - Source contains `TextWidth.truncate`.
    - Source contains `TextWidth.pad_trailing`.
    - Source does not contain `String.length(`.
    - Source does not contain `Foglet.Accounts`, `Foglet.Boards`, `Foglet.Config`, `Foglet.Posts`, or `Foglet.Threads`.
    - `rtk mix test test/foglet_bbs/tui/widgets/display/kv_grid_test.exs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-provided row data -> render tree | Labels and values are displayed and truncated only. |
| Widget -> domain contexts | No crossing allowed; KvGrid never fetches data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Denial of Service | Wide labels/values | mitigate | Use `TextWidth.truncate/2` and width tests at 64 and 80 columns. |
| T-24-05 | Information Disclosure | KvGrid data loading | mitigate | Widget receives caller-provided entries and performs no domain queries. |
| T-24-06 | Tampering | Status badges | mitigate | Badge metadata is display-only and routes through `Display.Badge`. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/display/kv_grid_test.exs`.
</verification>

<success_criteria>
- `Display.KvGrid` exists and renders width-safe label/value rows.
- Fixture tests cover Account, Sysop, site setting, runtime limit, and status summary shapes.
- No domain calls or hardcoded terminal color atoms are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/24-operator-console-primitives/24-02-SUMMARY.md`.
</output>
