---
phase: 23-composer-facelift
plan: 4
type: execute
wave: 3
depends_on:
  - 23-02
  - 23-03
files_modified:
  - test/foglet_bbs/tui/layout_smoke_test.exs
autonomous: true
requirements:
  - COMPOSER-01
  - COMPOSER-02
  - COMPOSER-03
  - COMPOSER-04
  - COMPOSER-05
must_haves:
  truths:
    - "D-09: At 64x22, mode control, editor or preview content, and compact budget text stay visible for both composers."
    - "D-17: Layout smoke covers PostComposer and NewThread at 64x22, 80x24, and 132x50."
    - "D-18: Existing compose, app resize-gate, screen, and widget tests remain green."
    - "No positioned composer text overflows or overlaps at canonical v1.3 sizes."
  artifacts:
    - path: test/foglet_bbs/tui/layout_smoke_test.exs
      provides: "Phase 23 composer size-contract coverage"
  key_links:
    - from: test/foglet_bbs/tui/layout_smoke_test.exs
      to: lib/foglet_bbs/tui/screens/post_composer.ex
      via: "PostComposer.render positioned at canonical sizes"
    - from: test/foglet_bbs/tui/layout_smoke_test.exs
      to: lib/foglet_bbs/tui/screens/new_thread.ex
      via: "NewThread.render positioned at canonical sizes"
---

<objective>
Add final size-contract coverage and phase gate verification for Composer Facelift.

Purpose: The shared shell and both screen migrations must remain usable at Foglet's 64x22 minimum and pass the focused Phase 23 validation set before precommit.
Output: Layout smoke tests for both composer surfaces at `64x22`, `80x24`, and `132x50`.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/23-composer-facelift/23-CONTEXT.md
@.planning/phases/23-composer-facelift/23-VALIDATION.md
@.planning/phases/23-composer-facelift/23-SPEC.md
@.planning/phases/23-composer-facelift/23-02-SUMMARY.md
@.planning/phases/23-composer-facelift/23-03-SUMMARY.md
@test/foglet_bbs/tui/layout_smoke_test.exs
@lib/foglet_bbs/tui/screens/post_composer.ex
@lib/foglet_bbs/tui/screens/new_thread.ex
@test/foglet_bbs/tui/app_test.exs

<interfaces>
Use existing layout helpers in `layout_smoke_test.exs`: `apply_at_size/2`, `text_elements/1`, `flatten_text/1`, and `TextWidth.display_width/1`.

Composer fixture states should use `%Foglet.TUI.App{}` maps with `terminal_size: {width, height}` and screen-local state initialized through `PostComposer.init_screen_state/1` and `NewThread.init_screen_state/1`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add composer size-contract smoke tests</name>
  <files>test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/layout_smoke_test.exs
    - .planning/phases/23-composer-facelift/23-CONTEXT.md
    - .planning/phases/23-composer-facelift/23-VALIDATION.md
    - lib/foglet_bbs/tui/screens/post_composer.ex
    - lib/foglet_bbs/tui/screens/new_thread.ex
  </read_first>
  <behavior>
    - For each `{64, 22}`, `{80, 24}`, `{132, 50}`, `PostComposer.render/1` positioned output contains `Composer`, `Edit`, `Preview`, a body counter containing `/`, and draft body text or preview text.
    - For each canonical size, `NewThread.render/1` compose output contains `Composer`, `Edit`, `Preview`, `Title`, a title counter containing `60 chars`, and body text or preview text.
    - Every text element has `x >= 0`, `y >= 0`, `x + TextWidth.display_width(text) <= width`, and `y < height`.
    - Adjacent text elements on the same row do not overlap by display-width range.
    - At 64x22, required controls remain visible even if reply quote or board context is truncated.
  </behavior>
  <action>Add a `describe "composer — size contract"` block per D-17. Create local helper functions in the test module only if they are reused by both composer cases, such as `assert_positioned_text_fits!/3` and `assert_no_same_row_overlap!/3`. Build one edit-mode and one preview-mode fixture for `PostComposer`, and one compose-step edit-mode and preview-mode fixture for `NewThread`. Use real screen render functions; do not test `EditorFrame` directly here.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/layout_smoke_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/foglet_bbs/tui/layout_smoke_test.exs` contains `describe "composer — size contract"`.
    - The new describe block contains `PostComposer.render` and `NewThread.render`.
    - The new describe block iterates over exactly `[{64, 22}, {80, 24}, {132, 50}]`.
    - Assertions include `Composer`, `Edit`, `Preview`, and `60 chars`.
    - Assertions check text bounds with `TextWidth.display_width`.
  </acceptance_criteria>
  <done>Layout smoke tests prove both composer surfaces keep required controls visible and positioned safely at canonical sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Run Phase 23 focused validation and precommit</name>
  <files>test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    - .planning/phases/23-composer-facelift/23-VALIDATION.md
    - test/foglet_bbs/tui/widgets/composer/editor_frame_test.exs
    - test/foglet_bbs/tui/screens/post_composer_test.exs
    - test/foglet_bbs/tui/screens/new_thread_test.exs
    - test/foglet_bbs/tui/layout_smoke_test.exs
    - test/foglet_bbs/tui/widgets/compose_test.exs
    - test/foglet_bbs/tui/app_test.exs
  </read_first>
  <action>Run the Phase 23 quick validation command, then run the preservation tests for `compose_test.exs` and `app_test.exs`, then run `rtk mix precommit`. If any failure is directly caused by Phase 23 code, fix it in the touched implementation or tests without changing domain workflows or input engines. Do not relax assertions that enforce D-09, D-13, D-14, D-17, or D-18.</action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/composer/editor_frame_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/layout_smoke_test.exs</automated>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/compose_test.exs test/foglet_bbs/tui/app_test.exs</automated>
    <automated>rtk mix precommit</automated>
  </verify>
  <acceptance_criteria>
    - Phase 23 quick validation command exits 0.
    - `rtk mix test test/foglet_bbs/tui/widgets/compose_test.exs test/foglet_bbs/tui/app_test.exs` exits 0.
    - `rtk mix precommit` exits 0.
    - No implementation file contains new browser routes, autosave/draft persistence, attachments, polls, mentions, rich text editor, or `PostCard.render` preview plumbing for composer.
  </acceptance_criteria>
  <done>Focused composer validation, preservation tests, and full precommit pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render tree -> positioned terminal output | Composer text must stay within terminal bounds and avoid overlap at minimum size. |
| Test fixtures -> domain behavior | Tests use existing screen paths and must not invent alternate submit routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | Validation-state display | mitigate | Focused validation includes over-limit and existing submit tests from screen files. |
| T-23-02 | Elevation of Privilege | Composer submit paths | mitigate | Precommit and focused tests prove migrations did not add widget-level mutation paths. |
| T-23-03 | Tampering | Input and shortcut behavior | mitigate | Preservation tests include `compose_test.exs`, screen behavior tests, and app resize-gate tests. |
| T-23-04 | Denial of Service | 64x22 layout | mitigate | Layout smoke asserts required controls remain visible and positioned within bounds at 64x22. |
</threat_model>

<verification>
Run:

```bash
rtk mix test test/foglet_bbs/tui/layout_smoke_test.exs
rtk mix test test/foglet_bbs/tui/widgets/composer/editor_frame_test.exs test/foglet_bbs/tui/screens/post_composer_test.exs test/foglet_bbs/tui/screens/new_thread_test.exs test/foglet_bbs/tui/layout_smoke_test.exs
rtk mix test test/foglet_bbs/tui/widgets/compose_test.exs test/foglet_bbs/tui/app_test.exs
rtk mix precommit
```
</verification>

<success_criteria>
- Both composer screens satisfy the 64x22, 80x24, and 132x50 layout contract.
- Required mode controls, editor/preview content, and compact counters remain visible at minimum size.
- Full Phase 23 focused validation and precommit pass.
</success_criteria>

<output>
After completion, create `.planning/phases/23-composer-facelift/23-04-SUMMARY.md`.
</output>
