---
phase: 16-unicode-width-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - lib/foglet_bbs/tui/widgets/list/list_row.ex
  - test/foglet_bbs/tui/widgets/list/list_row_test.exs
autonomous: true
requirements:
  - WIDTH-02
  - WIDTH-03
  - WIDTH-04
must_haves:
  truths:
    - "Aligned metadata rows keep requested terminal width for ASCII and Unicode titles."
    - "Existing ASCII-heavy ListRow behavior remains compatible after display-width hardening."
  artifacts:
    - path: "lib/foglet_bbs/tui/widgets/list/list_row.ex"
      provides: "Width-aware `render_with_metadata/6` layout math"
      contains: "Foglet.TUI.TextWidth"
    - path: "test/foglet_bbs/tui/widgets/list/list_row_test.exs"
      provides: "ASCII regression and Unicode row-width assertions"
      contains: "display_width"
  key_links:
    - from: "lib/foglet_bbs/tui/widgets/list/list_row.ex"
      to: "lib/foglet_bbs/tui/text_width.ex"
      via: "display-width measurement, truncation, and padding"
      pattern: "TextWidth\\."
---

<objective>
Migrate `ListRow.render_with_metadata/6` from grapheme-count layout math to the shared display-width helper.

Purpose: Future rich rows depend on aligned metadata that does not drift when titles contain CJK, combining marks, or milestone glyphs.
Output: Width-aware row math and tests preserving current ASCII layout behavior.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-unicode-width-foundation/16-SPEC.md
@.planning/phases/16-unicode-width-foundation/16-CONTEXT.md
@.planning/phases/16-unicode-width-foundation/16-VALIDATION.md
@SCREENS.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/text_width.ex
@lib/foglet_bbs/tui/widgets/list/list_row.ex
@test/foglet_bbs/tui/widgets/list/list_row_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Unicode row-width assertions while preserving ASCII baselines</name>
  <files>test/foglet_bbs/tui/widgets/list/list_row_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/widgets/list/list_row_test.exs
    - lib/foglet_bbs/tui/widgets/list/list_row.ex
    - lib/foglet_bbs/tui/text_width.ex
    - .planning/phases/16-unicode-width-foundation/16-VALIDATION.md
    - SCREENS.md
  </read_first>
  <behavior>
    - Existing ASCII tests still assert exact strings, starts_with/ends_with behavior, ellipsis behavior, and selected/unread styling.
    - Layout width assertions for rows use `Foglet.TUI.TextWidth.display_width/1`, not `String.length/1`.
    - New cases cover accented Latin, combining marks, CJK, and at least `●`, `◆`, `▸`, `▾`, `✓`, `×` in titles or metadata.
  </behavior>
  <action>
    Update `test/foglet_bbs/tui/widgets/list/list_row_test.exs` to alias `Foglet.TUI.TextWidth`. Replace layout-sensitive `String.length(flat) == width` assertions with `TextWidth.display_width(flat) == width` while keeping existing exact ASCII assertions where they are compatibility checks. Add focused tests where `ListRow.render_with_metadata/6` receives titles such as `"café update"`, `"cafe\u0301 update"`, `"漢字 board report"`, and glyph-led titles/metadata containing the full milestone set. Assert flattened rendered output has display width equal to the requested width and still ends with metadata.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/list/list_row_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "TextWidth\\.display_width|cafe\\\\u0301|漢字|●|◆|▸|▾|✓|×" test/foglet_bbs/tui/widgets/list/list_row_test.exs` returns matches.
    - `rg -n "String\\.length\\(flat\\)" test/foglet_bbs/tui/widgets/list/list_row_test.exs` returns no matches.
    - Tests may fail before production migration only because row layout still uses grapheme-count math.
  </acceptance_criteria>
  <done>Row tests express WIDTH-02/WIDTH-03/WIDTH-04 behavior before changing production row math.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate ListRow metadata layout math to TextWidth</name>
  <files>lib/foglet_bbs/tui/widgets/list/list_row.ex, test/foglet_bbs/tui/widgets/list/list_row_test.exs</files>
  <read_first>
    - lib/foglet_bbs/tui/widgets/list/list_row.ex
    - test/foglet_bbs/tui/widgets/list/list_row_test.exs
    - lib/foglet_bbs/tui/text_width.ex
    - .planning/phases/16-unicode-width-foundation/16-CONTEXT.md
  </read_first>
  <action>
    In `Foglet.TUI.Widgets.List.ListRow`, alias `Foglet.TUI.TextWidth`. Update `compute_parts/4` so marker, metadata, title body, and padding widths are measured in terminal columns through `TextWidth.display_width/1`. Replace `truncate_title/2` internals with `TextWidth.truncate/2` or `TextWidth.slice_to_width/2` plus ellipsis handling while preserving the existing metadata-priority contract: metadata remains fully visible, title truncates first, and padding clamps to zero if metadata alone exceeds `width`. Keep `render/3` unchanged except for any harmless alias additions.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/list/list_row_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "TextWidth\\." lib/foglet_bbs/tui/widgets/list/list_row.ex` returns matches.
    - `rg -n "String\\.length\\(|String\\.slice\\(|String\\.pad_" lib/foglet_bbs/tui/widgets/list/list_row.ex` returns no matches in layout math.
    - `rtk mix test test/foglet_bbs/tui/widgets/list/list_row_test.exs` passes.
  </acceptance_criteria>
  <done>`ListRow.render_with_metadata/6` uses the shared helper for terminal layout width and keeps ASCII-heavy row behavior stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| thread/user text -> aligned row rendering | Untrusted titles, handles, and metadata are displayed in fixed-width row output. |
| row rendering -> user perception | Misalignment or overflow can visually spoof metadata association or selected-row boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | S/I | `ListRow.render_with_metadata/6` | mitigate | Preserve metadata-priority contract and assert rows end with expected metadata after Unicode title truncation. |
| T-16-05 | T/D | `ListRow.compute_parts/4` | mitigate | Use `TextWidth` for all terminal-column math and clamp padding to non-negative widths. |
| T-16-06 | E | `ListRow` tests | mitigate | Keep this migration display-only; do not alter authorization, filtering, or product validation paths. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/list/list_row_test.exs`.
</verification>

<success_criteria>
`ListRow.render_with_metadata/6` uses `Foglet.TUI.TextWidth`, Unicode row tests pass, existing ASCII row assertions still pass, and no layout-sensitive direct `String.length/1`, `String.slice/3`, or `String.pad_*` calls remain in `list_row.ex`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-unicode-width-foundation/16-02-SUMMARY.md`.
</output>
