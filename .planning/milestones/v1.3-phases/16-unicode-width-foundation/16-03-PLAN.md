---
phase: 16-unicode-width-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - lib/foglet_bbs/tui/widgets/chrome/key_bar.ex
  - lib/foglet_bbs/tui/widgets/modal.ex
  - lib/foglet_bbs/tui/screens/main_menu.ex
  - test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs
  - test/foglet_bbs/tui/widgets/modal_test.exs
autonomous: true
requirements:
  - WIDTH-02
  - WIDTH-03
  - WIDTH-04
  - WIDTH-05
must_haves:
  truths:
    - "Existing key-footer, modal wrapping, and main-menu clipping paths use the shared display-width helper."
    - "Unicode modal and keybar output stays inside intended terminal width."
  artifacts:
    - path: "lib/foglet_bbs/tui/widgets/chrome/key_bar.ex"
      provides: "Width-aware existing command-footer key hint rendering"
      contains: "TextWidth"
    - path: "lib/foglet_bbs/tui/widgets/modal.ex"
      provides: "Display-width-aware modal word wrapping"
      contains: "TextWidth.display_width"
    - path: "lib/foglet_bbs/tui/screens/main_menu.ex"
      provides: "Display-width-aware reusable oneliner clipping"
      contains: "TextWidth.slice_to_width"
    - path: "test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs"
      provides: "Chrome keybar Unicode width coverage"
    - path: "test/foglet_bbs/tui/widgets/modal_test.exs"
      provides: "Modal Unicode wrapping coverage"
  key_links:
    - from: "chrome/modal/main_menu TUI paths"
      to: "lib/foglet_bbs/tui/text_width.ex"
      via: "shared display-width truncation, wrapping, and clipping"
      pattern: "TextWidth\\."
---

<objective>
Migrate the existing command-footer, modal wrapping, and reusable main-menu clipping paths to `Foglet.TUI.TextWidth`.

Purpose: These are the layout-sensitive shared paths named by D-04 that would otherwise break glyph-heavy screens before Chrome V2 or rich rows arrive.
Output: Width-aware `Chrome.KeyBar`, `Modal.word_wrap/2`, main-menu clipping, and focused tests.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-unicode-width-foundation/16-SPEC.md
@.planning/phases/16-unicode-width-foundation/16-CONTEXT.md
@.planning/phases/16-unicode-width-foundation/16-VALIDATION.md
@SCREENS.md
@docs/raxol/getting-started/WIDGET_GALLERY.md
@lib/foglet_bbs/tui/widgets/README.md
@lib/foglet_bbs/tui/text_width.ex
@lib/foglet_bbs/tui/widgets/chrome/key_bar.ex
@lib/foglet_bbs/tui/widgets/modal.ex
@lib/foglet_bbs/tui/screens/main_menu.ex
@test/foglet_bbs/tui/widgets/modal_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add keybar and modal Unicode width tests</name>
  <files>test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs, test/foglet_bbs/tui/widgets/modal_test.exs</files>
  <read_first>
    - test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs
    - test/foglet_bbs/tui/widgets/modal_test.exs
    - lib/foglet_bbs/tui/widgets/chrome/key_bar.ex
    - lib/foglet_bbs/tui/widgets/modal.ex
    - lib/foglet_bbs/tui/text_width.ex
    - .planning/phases/16-unicode-width-foundation/16-VALIDATION.md
  </read_first>
  <behavior>
    - `Chrome.KeyBar.render/2` can be called with a `width:` option and does not produce flattened text wider than that display width.
    - Modal message wrapping measures display columns, not graphemes, for accented Latin, combining marks, CJK, and SCREENS.md glyphs.
    - Existing ASCII modal assertions still pass.
  </behavior>
  <action>
    Add `test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs` if missing. Use local flattening helpers matching the ListRow tests. Add keybar tests for ASCII hints and Unicode hints containing `"漢字"`, `"cafe\u0301"`, and `● ◆ ▸ ▾ ✓ ×`, asserting `TextWidth.display_width(flattened) <= width` when rendered with `width: 64` and `width: 80`. In `test/foglet_bbs/tui/widgets/modal_test.exs`, alias `Foglet.TUI.TextWidth`, replace layout-sensitive wrap length checks with `TextWidth.display_width(line) <= 50`, and add a Unicode wrapping test containing CJK plus the milestone glyphs.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs test/foglet_bbs/tui/widgets/modal_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs` exists.
    - `rg -n "TextWidth\\.display_width|width: 64|width: 80|cafe\\\\u0301|漢字|●|◆|▸|▾|✓|×" test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs test/foglet_bbs/tui/widgets/modal_test.exs` returns matches.
    - `rg -n "String\\.length\\(line\\)" test/foglet_bbs/tui/widgets/modal_test.exs` returns no matches.
  </acceptance_criteria>
  <done>Keybar and modal tests define the display-width behavior required by D-04, D-10, and D-11.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate Chrome.KeyBar and Modal.word_wrap to TextWidth</name>
  <files>lib/foglet_bbs/tui/widgets/chrome/key_bar.ex, lib/foglet_bbs/tui/widgets/modal.ex, test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs, test/foglet_bbs/tui/widgets/modal_test.exs</files>
  <read_first>
    - lib/foglet_bbs/tui/widgets/chrome/key_bar.ex
    - lib/foglet_bbs/tui/widgets/modal.ex
    - lib/foglet_bbs/tui/text_width.ex
    - test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs
    - test/foglet_bbs/tui/widgets/modal_test.exs
    - .planning/phases/16-unicode-width-foundation/16-CONTEXT.md
  </read_first>
  <action>
    In `Foglet.TUI.Widgets.Chrome.KeyBar`, add an optional `opts \\ []` argument to `render/3` while preserving `render(theme, keys)` compatibility. Use `Keyword.get(opts, :width, nil)`. Build hint segments in the existing `"[KEY] Description"` format and, when width is present, truncate lower-priority/rightmost hint text first with `TextWidth.truncate/2` so flattened output does not exceed the requested width. Keep styling split between accent key text and dim descriptions. In `Foglet.TUI.Widgets.Modal.word_wrap/2`, replace `String.length/1` checks with `TextWidth.display_width/1`; leave whitespace splitting behavior intact and do not split long words mid-word unless already necessary for existing behavior.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs test/foglet_bbs/tui/widgets/modal_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def render\\(theme, keys, opts \\\\ \\[\\]\\)|TextWidth\\." lib/foglet_bbs/tui/widgets/chrome/key_bar.ex lib/foglet_bbs/tui/widgets/modal.ex` returns matches.
    - `rg -n "String\\.length\\(" lib/foglet_bbs/tui/widgets/modal.ex` returns no matches.
    - `rtk mix test test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs test/foglet_bbs/tui/widgets/modal_test.exs` passes.
  </acceptance_criteria>
  <done>Existing keybar/footer and modal wrapping paths use `Foglet.TUI.TextWidth` for terminal layout width.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate main-menu clipping to TextWidth</name>
  <files>lib/foglet_bbs/tui/screens/main_menu.ex, test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>
    - lib/foglet_bbs/tui/screens/main_menu.ex
    - test/foglet_bbs/tui/layout_smoke_test.exs
    - lib/foglet_bbs/tui/text_width.ex
    - .planning/phases/16-unicode-width-foundation/16-CONTEXT.md
  </read_first>
  <action>
    In `Foglet.TUI.Screens.MainMenu`, alias `Foglet.TUI.TextWidth` and update private `clip/2` to use `TextWidth.slice_to_width/2` rather than `String.graphemes() |> Enum.take(limit)`. Keep `single_line/1`, selection, authorization visibility, and hide-oneliner behavior unchanged. Add or update a layout smoke assertion that a recent oneliner with CJK and milestone glyphs renders a row whose display width does not exceed the marker plus `@handle` and body limits already defined by `@oneliner_handle_limit` and `@oneliner_body_limit`.
  </action>
  <verify>
    <automated>rtk mix test test/foglet_bbs/tui/layout_smoke_test.exs test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs test/foglet_bbs/tui/widgets/modal_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "TextWidth\\.slice_to_width|alias Foglet\\.TUI\\.TextWidth" lib/foglet_bbs/tui/screens/main_menu.ex` returns matches.
    - `rg -n "String\\.graphemes\\(\\).*Enum\\.take|Enum\\.take\\(limit\\)" lib/foglet_bbs/tui/screens/main_menu.ex` returns no matches in `clip/2`.
    - `rg -n "漢字|●|◆|▸|▾|✓|×|TextWidth\\.display_width" test/foglet_bbs/tui/layout_smoke_test.exs` returns matches.
    - Focused tests pass.
  </acceptance_criteria>
  <done>Main-menu reusable clipping uses terminal display width without changing menu navigation, role visibility, or oneliner permissions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/admin text -> chrome/modal/menu display | Untrusted text enters shared display surfaces and may contain wide or combining Unicode. |
| display truncation -> command comprehension | Truncated key hints must not imply unavailable commands or hide required modal actions. |
| oneliner text -> main-menu panel | User-authored oneliners are clipped before display in a bounded panel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | S/I | `Chrome.KeyBar` | mitigate | Truncate display-only hint text by width while preserving key labels and existing command source; tests assert bounded output at 64/80 columns. |
| T-16-08 | D | `Modal.word_wrap/2` | mitigate | Measure modal lines by display width and keep line output within `@wrap_width` for representative Unicode inputs. |
| T-16-09 | I | `MainMenu.clip/2` | mitigate | Clip oneliner display by terminal width to prevent visual overflow into adjacent menu content. Authorization for hiding remains in existing Bodyguard call and is not changed. |
</threat_model>

<verification>
Run `rtk mix test test/foglet_bbs/tui/widgets/chrome/key_bar_test.exs test/foglet_bbs/tui/widgets/modal_test.exs test/foglet_bbs/tui/layout_smoke_test.exs`.
</verification>

<success_criteria>
Chrome keybar, modal wrapping, and main-menu clipping consume `Foglet.TUI.TextWidth`; Unicode tests pass; existing ASCII modal behavior stays green; no domain, auth, browser, or Chrome V2 behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/16-unicode-width-foundation/16-03-SUMMARY.md`.
</output>
