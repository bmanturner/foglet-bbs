---
phase: 39-app-shell-simplification
plan: 02
type: execute
wave: 1
depends_on: [39-01]
files_modified:
  - lib/foglet_bbs/tui/screen.ex
autonomous: true
requirements: [APP-03]
must_haves:
  truths:
    - "`Foglet.TUI.Screen` declares an optional `subscriptions/2` callback so screens may declare PubSub topic interest without App pattern-matching."
    - "Stateless screens compile and run unchanged — `subscriptions/2` is optional, not required."
  artifacts:
    - path: "lib/foglet_bbs/tui/screen.ex"
      provides: "@callback subscriptions/2 + entry in @optional_callbacks"
      contains: "subscriptions: 2"
  key_links:
    - from: "lib/foglet_bbs/tui/screen.ex"
      to: "Foglet.TUI.Screen.behaviour_info(:optional_callbacks)"
      via: "@optional_callbacks attribute"
      pattern: "subscriptions: 2"
---

<objective>
Add the `subscriptions/2` optional callback declaration to the `Foglet.TUI.Screen` behaviour so PostReader, ThreadList, and BoardList (Plans 39-03 and 39-04) can implement it. This is a contract-first task per the Interface-First Task Ordering rule: declare the callback before any implementer attempts `@impl true`.

Purpose: D-05 / SPEC R6. Without this declaration, implementing screens emit `unknown function` Dialyzer warnings and `mix precommit` fails.

Output: One file modified — `lib/foglet_bbs/tui/screen.ex` gains `@callback subscriptions(local_state(), Foglet.TUI.Context.t()) :: [String.t()]` plus a `subscriptions: 2` entry in `@optional_callbacks`.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-app-shell-simplification/39-CONTEXT.md
@.planning/phases/39-app-shell-simplification/39-SPEC.md
@.planning/phases/39-app-shell-simplification/39-PATTERNS.md
@AGENTS.md

<interfaces>
Existing pattern in `lib/foglet_bbs/tui/screen.ex` (full file, ~63 lines). The current `@optional_callbacks` block is the canonical extension point:

```elixir
@callback init(Foglet.TUI.Context.t()) :: local_state()
@callback update(message(), local_state(), Foglet.TUI.Context.t()) :: update_result()
@callback render(local_state(), Foglet.TUI.Context.t()) :: any()

# transitional callbacks:
@callback render(map()) :: any()
@callback handle_key(map(), term()) :: any()
@callback init_screen_state(map()) :: any()

@optional_callbacks init: 1,
                    update: 3,
                    render: 2,
                    render: 1,
                    handle_key: 2,
                    init_screen_state: 1
```

Per D-05 the new arity ordering MUST mirror existing `update/3` and `render/2` — `local_state` first, `Context.t()` second.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare subscriptions/2 optional callback on Foglet.TUI.Screen</name>
  <files>lib/foglet_bbs/tui/screen.ex</files>
  <behavior>
    After the change, all of the following hold:
    - `Foglet.TUI.Screen.behaviour_info(:optional_callbacks)` includes `{:subscriptions, 2}`.
    - The pin test added in Plan 39-01 / Task 2 / Step 3 (`@tag :phase39_target` in `screen_test.exs`) passes when included.
    - `mix compile --warnings-as-errors` stays green; no module currently implements `subscriptions/2` and the optional declaration does not require any implementation.
    - The transitional callbacks `render/1`, `handle_key/2`, `init_screen_state/1` are NOT removed (Phase 40 owns that).
  </behavior>
  <read_first>
    lib/foglet_bbs/tui/screen.ex (full file — only 63 lines)
    .planning/phases/39-app-shell-simplification/39-PATTERNS.md (lines 44-90 for the screen.ex section)
    .planning/phases/39-app-shell-simplification/39-CONTEXT.md (D-05, D-08)
  </read_first>
  <action>
    Read `lib/foglet_bbs/tui/screen.ex` end-to-end. After the existing `@callback render(local_state(), Foglet.TUI.Context.t()) :: any()` declaration (and after the transitional callback declarations if they appear in this region), add:

    ```elixir
    @callback subscriptions(local_state(), Foglet.TUI.Context.t()) :: [String.t()]
    ```

    Then locate the existing `@optional_callbacks` attribute. Append `subscriptions: 2` as the last entry, preserving the existing comma-separated keyword-list shape:

    ```elixir
    @optional_callbacks init: 1,
                        update: 3,
                        render: 2,
                        render: 1,
                        handle_key: 2,
                        init_screen_state: 1,
                        subscriptions: 2
    ```

    Do NOT remove or modify any existing callback. Do NOT change `local_state()` or `message()` typespec aliases. Do NOT introduce a default implementation.

    After editing, run `rtk mix format lib/foglet_bbs/tui/screen.ex` to keep formatting consistent.

    Now remove the `@tag :phase39_target` annotation on the screen_test.exs pin added in Plan 39-01 (the test for "lists subscriptions/2 in @optional_callbacks"). After removing the tag, the test runs in the default suite and must pass — that is the fail-loud signal that confirms this task succeeded.
  </action>
  <verify>
    <automated>rtk mix compile --warnings-as-errors &amp;&amp; rtk mix test test/foglet_bbs/tui/screen_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'subscriptions: 2' lib/foglet_bbs/tui/screen.ex` returns at least 1.
    - `grep -c '@callback subscriptions' lib/foglet_bbs/tui/screen.ex` returns 1.
    - The existing six `@optional_callbacks` entries (`init: 1`, `update: 3`, `render: 2`, `render: 1`, `handle_key: 2`, `init_screen_state: 1`) all still appear in the file: `for entry in 'init: 1' 'update: 3' 'render: 2' 'render: 1' 'handle_key: 2' 'init_screen_state: 1'; do grep -q "$entry" lib/foglet_bbs/tui/screen.ex || exit 1; done` exits 0.
    - The corresponding `screen_test.exs` pin no longer has `@tag :phase39_target` immediately preceding the `test "lists subscriptions/2 in @optional_callbacks"` line.
    - `rtk mix test test/foglet_bbs/tui/screen_test.exs` passes (default exclusion in effect; the pin runs in the default suite now and passes).
    - `rtk mix compile --warnings-as-errors` succeeds.
    - `rtk mix precommit` exits 0.
  </acceptance_criteria>
  <done>
    `Foglet.TUI.Screen` declares `subscriptions/2` as an optional callback; behaviour_info reflects it; the formerly-tagged pin test in `screen_test.exs` is now part of the default suite and passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none net-new) | Adding an optional callback declaration does not introduce any new input path or trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | T (tampering) | Screen behaviour declaration | accept | Behaviour modules are compile-time contracts. Tampering would be caught by `mix compile --warnings-as-errors` and the screen_test.exs behaviour-info pin. |
</threat_model>

<verification>
- `Foglet.TUI.Screen.behaviour_info(:optional_callbacks)` includes `{:subscriptions, 2}`.
- No transitional callback was removed.
- `rtk mix precommit` and `rtk mix test` both green.
</verification>

<success_criteria>
- [ ] `subscriptions/2` declared and listed under `@optional_callbacks` in `lib/foglet_bbs/tui/screen.ex`.
- [ ] `screen_test.exs` pin runs in the default suite and passes.
- [ ] `rtk mix precommit` exits 0.
- [ ] No existing callback was deleted or changed.
</success_criteria>

<output>
After completion, create `.planning/phases/39-app-shell-simplification/39-02-SUMMARY.md` recording the exact line range of the declaration insertion and the updated `@optional_callbacks` list, so downstream implementers can `@impl true` confidently.
</output>
