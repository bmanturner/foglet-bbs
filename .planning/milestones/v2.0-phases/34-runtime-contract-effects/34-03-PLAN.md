---
phase: 34-runtime-contract-effects
plan: "03"
type: execute
wave: 3
depends_on:
  - "34-01"
  - "34-02"
files_modified:
  - lib/foglet_bbs/tui/screen.ex
  - docs/ARCHITECTURE.md
  - test/foglet_bbs/tui/screen_test.exs
  - test/foglet_bbs/tui/app_test.exs
  - test/foglet_bbs/tui/layout_smoke_test.exs
autonomous: true
requirements:
  - STATE-01
  - RUNTIME-01
must_haves:
  truths:
    - "D-04: No runtime old-screen vs new-screen fallback detector or compatibility layer is added."
    - "D-05: Untouched old screen paths may remain, but the new foundation does not depend on an adapter that hides incomplete migration."
    - "D-06: Full production screen migrations remain deferred to phases 35-38; final central App cleanup remains deferred to phase 39."
    - "D-17: Stateful screens use first-class state structs with new/1 or a documented local state type."
    - "D-18: Stateless screens explicitly declare that they do not store local state."
    - "D-19: Tests prove reducer/effect contracts and generic App helpers, not brittle text-only visual assertions."
    - "D-20: Existing App init/update/view behavior, modal precedence, SizeGate behavior, session hooks, command delivery, and at least one current render smoke path remain green."
  artifacts:
    - "Foglet.TUI.Screen documentation describes stateful and stateless screen conventions."
    - "docs/ARCHITECTURE.md documents the new Context/Effect/init-update-render runtime foundation."
    - "Existing App and layout smoke tests pass after the foundation lands."
  key_links:
    - "State convention documentation connects Screen.init/1 to first-class state structs."
    - "Preservation tests connect the new runtime foundation to existing App init/update/view behavior."
    - "Deferred migration notes connect Phase 34 boundaries to phases 35-39."
---

<objective>
Lock the state convention and preservation boundary for the runtime foundation.

Purpose: Phase 34 must leave future screen migrations with clear local-state
rules while proving existing TUI behavior remains green.
Output: Documentation in code/docs, focused convention tests, and preservation
checks over existing App and render smoke paths.
</objective>

<execution_context>
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brendan.turner/Dev/personal/foglet_bbs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-runtime-contract-effects/34-SPEC.md
@.planning/phases/34-runtime-contract-effects/34-CONTEXT.md
@.planning/phases/34-runtime-contract-effects/34-RESEARCH.md
@.planning/phases/34-runtime-contract-effects/34-01-SUMMARY.md
@.planning/phases/34-runtime-contract-effects/34-02-SUMMARY.md
@lib/foglet_bbs/tui/screen.ex
@lib/foglet_bbs/tui/screens/board_list/state.ex
@lib/foglet_bbs/tui/screens/post_reader/state.ex
@docs/ARCHITECTURE.md
@test/foglet_bbs/tui/app_test.exs
@test/foglet_bbs/tui/layout_smoke_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document stateful and stateless screen conventions</name>
  <files>lib/foglet_bbs/tui/screen.ex, docs/ARCHITECTURE.md, test/foglet_bbs/tui/screen_test.exs</files>
  <read_first>lib/foglet_bbs/tui/screen.ex, docs/ARCHITECTURE.md, lib/foglet_bbs/tui/screens/board_list/state.ex, lib/foglet_bbs/tui/screens/post_reader/state.ex, .planning/phases/34-runtime-contract-effects/34-CONTEXT.md</read_first>
  <action>
Expand `Foglet.TUI.Screen` documentation with a "State conventions" section
containing these exact rules:
- "Stateful screens own a first-class state struct with `new/1` or document an explicit local state type."
- "Stateless screens explicitly return `:stateless` or `%{}` from `init/1` and do not store local state in App fields."
- "Screens do not receive `%Foglet.TUI.App{}` through `init/1`, `update/3`, or `render/2`."
- "Production screen family migrations are deferred to phases 35-38; Phase 39 removes central App screen-specific machinery."

Update `docs/ARCHITECTURE.md` §8 TUI/Raxol layer with a short paragraph naming
`Foglet.TUI.Context`, `Foglet.TUI.Effect`, and the `init/update/render`
foundation. Keep the SSH-first boundary intact and do not describe any browser
workflow.

Extend `test/foglet_bbs/tui/screen_test.exs` with one stateful sample using a
state struct with `new/1` and one stateless sample returning `:stateless`.
  </action>
  <verify>rtk mix test test/foglet_bbs/tui/screen_test.exs</verify>
  <acceptance_criteria>
    - `lib/foglet_bbs/tui/screen.ex` contains `State conventions`.
    - `lib/foglet_bbs/tui/screen.ex` contains `Stateful screens own a first-class state struct`.
    - `lib/foglet_bbs/tui/screen.ex` contains `Stateless screens explicitly return`.
    - `docs/ARCHITECTURE.md` contains `Foglet.TUI.Context`.
    - `docs/ARCHITECTURE.md` contains `Foglet.TUI.Effect`.
    - `test/foglet_bbs/tui/screen_test.exs` contains `defmodule StatefulSample`.
    - `test/foglet_bbs/tui/screen_test.exs` contains `defmodule StatelessSample`.
    - `rtk mix test test/foglet_bbs/tui/screen_test.exs` exits 0.
  </acceptance_criteria>
  <done>Stateful/stateless conventions are explicit in docs and proven by sample tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add preservation checks for current App behavior and smoke rendering</name>
  <files>test/foglet_bbs/tui/app_test.exs, test/foglet_bbs/tui/layout_smoke_test.exs</files>
  <read_first>test/foglet_bbs/tui/app_test.exs, test/foglet_bbs/tui/layout_smoke_test.exs, lib/foglet_bbs/tui/app.ex, .planning/phases/34-runtime-contract-effects/34-SPEC.md</read_first>
  <action>
Add or preserve targeted checks proving the Phase 34 foundation did not break
existing App behavior:
- `App.init(%{})` still returns `current_screen == :login`.
- Authenticated user init still routes to `:main_menu`.
- Modal precedence still prevents screen key handling while `state.modal != nil`.
- `SizeGate.too_small?/1` behavior still causes `App.view/1` to render the
  too-small path without mutating `current_screen`, `screen_state`, or `modal`.
- At least one existing render smoke path in `layout_smoke_test.exs` remains
  covered; do not add a brittle text-only assertion that merely checks the
  existence of arbitrary copy.

Prefer extending existing describes in `app_test.exs` only where coverage is
missing. Do not rewrite unrelated tests.
  </action>
  <verify>rtk mix test test/foglet_bbs/tui/app_test.exs test/foglet_bbs/tui/layout_smoke_test.exs</verify>
  <acceptance_criteria>
    - `rtk mix test test/foglet_bbs/tui/app_test.exs test/foglet_bbs/tui/layout_smoke_test.exs` exits 0.
    - `test/foglet_bbs/tui/app_test.exs` contains `current_screen == :login`.
    - `test/foglet_bbs/tui/app_test.exs` contains `current_screen == :main_menu`.
    - `test/foglet_bbs/tui/app_test.exs` contains `modal`.
    - `test/foglet_bbs/tui/layout_smoke_test.exs` remains present and runnable.
  </acceptance_criteria>
  <done>Existing App and render smoke behavior is explicitly protected while the new foundation lands.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Phase 34 targeted suite and compile gate</name>
  <files>lib/foglet_bbs/tui/screen.ex, lib/foglet_bbs/tui/context.ex, lib/foglet_bbs/tui/effect.ex, lib/foglet_bbs/tui/app.ex, docs/ARCHITECTURE.md</files>
  <read_first>.planning/phases/34-runtime-contract-effects/34-VALIDATION.md, mix.exs</read_first>
  <action>
Run the targeted Phase 34 suite exactly:
`rtk mix test test/foglet_bbs/tui/screen_test.exs test/foglet_bbs/tui/context_test.exs test/foglet_bbs/tui/effect_test.exs test/foglet_bbs/tui/app_runtime_contract_test.exs test/foglet_bbs/tui/app_test.exs test/foglet_bbs/tui/command_test.exs test/foglet_bbs/tui/layout_smoke_test.exs`.

Then run `rtk mix compile --warnings-as-errors`. Fix any failures caused by
Phase 34 edits. If a pre-existing unrelated failure blocks verification,
document the exact command and failure in `34-03-SUMMARY.md`.
  </action>
  <verify>rtk mix test test/foglet_bbs/tui/screen_test.exs test/foglet_bbs/tui/context_test.exs test/foglet_bbs/tui/effect_test.exs test/foglet_bbs/tui/app_runtime_contract_test.exs test/foglet_bbs/tui/app_test.exs test/foglet_bbs/tui/command_test.exs test/foglet_bbs/tui/layout_smoke_test.exs && rtk mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - Targeted Phase 34 test command exits 0 or `34-03-SUMMARY.md` documents a pre-existing unrelated blocker.
    - `rtk mix compile --warnings-as-errors` exits 0 or `34-03-SUMMARY.md` documents a pre-existing unrelated blocker.
  </acceptance_criteria>
  <done>Phase 34 foundation is validated against the targeted suite and compile gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> executor behavior | Plan and docs define how later agents migrate screens without fallback shortcuts. |
| Runtime foundation -> existing screens | New helpers coexist with old screen paths until later migration phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Tampering | Screen migration boundary | mitigate | Documentation and tests must state no old/new runtime fallback detector is introduced. |
| T-34-08 | Information Disclosure | State conventions | mitigate | Context exclusion tests from Plan 01 remain part of the targeted suite. |
| T-34-09 | Denial of Service | Existing App/render paths | mitigate | Existing App and layout smoke tests run after the new foundation lands. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `rtk mix test test/foglet_bbs/tui/screen_test.exs test/foglet_bbs/tui/context_test.exs test/foglet_bbs/tui/effect_test.exs test/foglet_bbs/tui/app_runtime_contract_test.exs test/foglet_bbs/tui/app_test.exs test/foglet_bbs/tui/command_test.exs test/foglet_bbs/tui/layout_smoke_test.exs`
- [ ] `rtk mix compile --warnings-as-errors`
</verification>

<success_criteria>
- The state struct convention is documented and tested.
- Phase 34 explicitly defers full screen family migration and App cleanup.
- Existing App behavior and at least one render smoke path remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-runtime-contract-effects/34-03-SUMMARY.md`.
</output>
