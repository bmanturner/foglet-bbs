---
phase: 40-verification-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/40-verification-documentation/40-SUMMARY.md
  - lib/foglet_bbs/tui/app.ex
  - lib/foglet_bbs/tui/screens/main_menu.ex
  - lib/foglet_bbs/tui/screens/board_list.ex
  - lib/foglet_bbs/tui/screens/sysop.ex
  - test/foglet_bbs/tui/screens/account_test.exs
autonomous: true
requirements: [VERIFY-01, VERIFY-05]
must_haves:
  truths:
    - "D-01: every Phase 39 carry-forward item from deferred-items.md, 39-SUMMARY.md, and 39-REVIEW-FIX.md is listed in 40-SUMMARY.md with a disposition column."
    - "D-02: the two Account/MainMenu doomed-submit failures and the two remaining Dialyzer warnings are treated as in-scope close-gate work."
    - "D-05: doomed-submit failures preserve or replay Modal.Form.submit_state so async failures leave the modal form in {:error, _}; tests are not deleted or weakened."
  artifacts:
    - path: ".planning/phases/40-verification-documentation/40-SUMMARY.md"
      provides: "carry-forward disposition register initialized before final gate evidence is appended"
      contains: "## Carry-Forward Disposition Register"
---

<objective>
Initialize the Phase 40 closure register and fix the two known final-gate blockers: BL-01 doomed modal-submit recovery and the Phase 39 deferred Dialyzer warnings. This plan makes later verification meaningful by proving the known failures are no longer baseline noise.
</objective>

<context>
@.planning/phases/40-verification-documentation/40-CONTEXT.md
@.planning/phases/40-verification-documentation/40-SPEC.md
@.planning/phases/40-verification-documentation/40-RESEARCH.md
@.planning/phases/40-verification-documentation/40-PATTERNS.md
@.planning/phases/39-app-shell-simplification/deferred-items.md
@.planning/phases/39-app-shell-simplification/39-SUMMARY.md
@.planning/phases/39-app-shell-simplification/39-REVIEW-FIX.md
@AGENTS.md
</context>

<tasks>
<task type="execute">
  <name>Task 1: Create carry-forward disposition register</name>
  <files>.planning/phases/40-verification-documentation/40-SUMMARY.md</files>
  <read_first>
    .planning/phases/39-app-shell-simplification/deferred-items.md
    .planning/phases/39-app-shell-simplification/39-SUMMARY.md
    .planning/phases/39-app-shell-simplification/39-REVIEW-FIX.md
    .planning/phases/40-verification-documentation/40-RESEARCH.md
  </read_first>
  <action>
    Create `40-SUMMARY.md` with a `## Carry-Forward Disposition Register` table. Include rows for: BL-01 doomed oneliner submit; BL-01 doomed hide-oneliner submit; `board_list.ex:161` Dialyzer `pattern_match_cov`; `sysop.ex:823` Dialyzer `pattern_match`; transitional callbacks; remaining breadcrumb migration; WR-02; WR-04; IN-02; IN-03; IN-04. Use columns `Source`, `Item`, `Disposition`, `Evidence`, and `Plan`.
  </action>
  <acceptance_criteria>
    - `rtk rg -n "Carry-Forward Disposition Register|BL-01|board_list\\.ex:161|sysop\\.ex:823|WR-02|WR-04|IN-02|IN-03|IN-04" .planning/phases/40-verification-documentation/40-SUMMARY.md` exits 0.
    - `40-SUMMARY.md` contains the literal strings `Disposition`, `Evidence`, and `Plan`.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Fix BL-01 modal submit error recovery</name>
  <files>
    lib/foglet_bbs/tui/app.ex
    lib/foglet_bbs/tui/screens/main_menu.ex
    test/foglet_bbs/tui/screens/account_test.exs
  </files>
  <read_first>
    test/foglet_bbs/tui/screens/account_test.exs (BL-01 tests around doomed oneliner and hide-oneliner submits)
    lib/foglet_bbs/tui/app.ex (modal key handling and screen task result routing)
    lib/foglet_bbs/tui/screens/main_menu.ex (modal submit and task-result clauses)
    lib/foglet_bbs/tui/widgets/modal/form.ex (`set_submit_state/2` and submit_state state machine)
  </read_first>
  <action>
    Preserve the existing BL-01 tests. Update the modal submit failure path so `{:screen_task_result, :main_menu, :submit_oneliner, {:ok, {:error, reason}}}` and `{:screen_task_result, :main_menu, :submit_hide_oneliner, {:ok, {:error, reason}}}` leave `state.modal.message.submit_state` as `{:error, reason_or_message}` rather than `:submitting`. Use `Foglet.TUI.Widgets.Modal.Form.set_submit_state/2` when updating a retained `%Modal.Form{}`. Ensure `%{key: :escape}` dismisses the form after the error path.
  </action>
  <acceptance_criteria>
    - `rtk mix test test/foglet_bbs/tui/screens/account_test.exs` exits 0.
    - `rtk rg -n "doomed oneliner submit leaves form in \\{:error, _\\}|doomed hide-oneliner submit leaves form in \\{:error, _\\}|after doomed oneliner error, %\\{key: :escape\\} dismisses the modal|after doomed hide-oneliner error" test/foglet_bbs/tui/screens/account_test.exs` exits 0.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Resolve deferred Dialyzer warnings</name>
  <files>
    lib/foglet_bbs/tui/screens/board_list.ex
    lib/foglet_bbs/tui/screens/sysop.ex
    .planning/phases/40-verification-documentation/40-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/39-app-shell-simplification/deferred-items.md
    lib/foglet_bbs/tui/screens/board_list.ex (around update/3 subscription task-result clauses)
    lib/foglet_bbs/tui/screens/sysop.ex (around apply_update_submodule_result/5)
  </read_first>
  <action>
    Remove the unreachable `_other` branch in `board_list.ex` or adjust preceding patterns so Dialyzer no longer reports `pattern_match_cov` at the deferred line. In `sysop.ex`, replace the impossible `{_ss, _effects}` match shape with clauses that match the actual reducer return type. Add the exact removed-warning notes to `40-SUMMARY.md` under the matching disposition rows.
  </action>
  <acceptance_criteria>
    - `rtk mix dialyzer` exits 0.
    - `rtk rg -n "board_list\\.ex:161|sysop\\.ex:823|Dialyzer" .planning/phases/40-verification-documentation/40-SUMMARY.md` exits 0.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run `rtk mix test test/foglet_bbs/tui/screens/account_test.exs` and `rtk mix dialyzer`.
</verification>

<success_criteria>
- Known Phase 39 deferred test and Dialyzer blockers are closed or documented with exact evidence.
- `40-SUMMARY.md` starts the carry-forward register consumed by final verification.
</success_criteria>
