---
phase: 40-verification-documentation
plan: 05
type: execute
wave: 4
depends_on: [40-01, 40-02, 40-03, 40-04]
files_modified:
  - lib/foglet_bbs/tui/SCREEN_CONTRACT.md
  - lib/foglet_bbs/tui/widgets/README.md
  - .planning/phases/40-verification-documentation/40-SUMMARY.md
autonomous: false
requirements: [VERIFY-01, VERIFY-04, VERIFY-05]
must_haves:
  truths:
    - "D-10: screen-contract guidance is a TUI-adjacent developer doc linked from lib/foglet_bbs/tui/widgets/README.md or the nearest TUI README surface."
    - "The doc includes practical examples for state ownership, effects, tasks, route params, PubSub subscriptions, modal requests, and rendering."
    - "Final evidence records rtk mix test, targeted rtk mix foglet.tui.render commands at supported sizes, and rtk mix precommit."
  artifacts:
    - path: "lib/foglet_bbs/tui/SCREEN_CONTRACT.md"
      provides: "developer guide for adding or migrating screens under Context/Effect/update/render"
      contains: "## Checklist"
    - path: ".planning/phases/40-verification-documentation/40-SUMMARY.md"
      provides: "final Phase 40 evidence and gate results"
      contains: "## Final Gate Evidence"
---

<objective>
Write the TUI screen contract guide, link it from the TUI/widget documentation surface, run final render/test/precommit gates, and complete the Phase 40 evidence summary.
</objective>

<context>
@.planning/phases/40-verification-documentation/40-CONTEXT.md
@.planning/phases/40-verification-documentation/40-SPEC.md
@.planning/phases/40-verification-documentation/40-RESEARCH.md
@.planning/phases/40-verification-documentation/40-VALIDATION.md
@AGENTS.md
</context>

<tasks>
<task type="execute">
  <name>Task 1: Write and link screen contract guidance</name>
  <files>
    lib/foglet_bbs/tui/SCREEN_CONTRACT.md
    lib/foglet_bbs/tui/widgets/README.md
  </files>
  <read_first>
    lib/foglet_bbs/tui/screen.ex
    lib/foglet_bbs/tui/context.ex
    lib/foglet_bbs/tui/effect.ex
    lib/foglet_bbs/tui/app.ex
    lib/foglet_bbs/tui/widgets/README.md
  </read_first>
  <action>
    Create `lib/foglet_bbs/tui/SCREEN_CONTRACT.md`. Include sections for state ownership, `init/1`, `update/3`, `render/2`, `Foglet.TUI.Context`, `Foglet.TUI.Effect`, task effects and `{:task_result, op, result}`, route params, optional `subscriptions/2`, modal requests, render fixtures, and `rtk mix foglet.tui.render`. Add `## Checklist` with separate checklists for new screens and migrated screens. Add a link to the guide from `lib/foglet_bbs/tui/widgets/README.md` under Further reading or a TUI-screen guidance paragraph.
  </action>
  <acceptance_criteria>
    - `rtk rg -n "Foglet\\.TUI\\.Context|Foglet\\.TUI\\.Effect|init/1|update/3|render/2|subscriptions/2|modal|route params|task_result|rtk mix foglet\\.tui\\.render|## Checklist" lib/foglet_bbs/tui/SCREEN_CONTRACT.md` exits 0.
    - `rtk rg -n "SCREEN_CONTRACT|Screen Contract|screen contract" lib/foglet_bbs/tui/widgets/README.md` exits 0.
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: Run render smoke evidence commands</name>
  <files>.planning/phases/40-verification-documentation/40-SUMMARY.md</files>
  <read_first>
    test/foglet_bbs/tui/layout_smoke_test.exs
    lib/foglet_bbs/tui/render_fixtures.ex
    .planning/phases/40-verification-documentation/40-VALIDATION.md
  </read_first>
  <action>
    Run and record the exact result for these commands in `40-SUMMARY.md` under `## Render Smoke Evidence`:
    - `rtk mix foglet.tui.render login --width 64 --height 22`
    - `rtk mix foglet.tui.render main_menu --width 80 --height 24`
    - `rtk mix foglet.tui.render board_list --width 132 --height 50`
    - `rtk mix foglet.tui.render post_reader --width 80 --height 24`
    - `rtk mix test test/foglet_bbs/tui/layout_smoke_test.exs`
    For each render command, record `screen`, `size`, `exit code`, and whether any delta is expected and deliberate.
  </action>
  <acceptance_criteria>
    - `rtk rg -n "Render Smoke Evidence|login.*64.*22|main_menu.*80.*24|board_list.*132.*50|post_reader.*80.*24|layout_smoke_test" .planning/phases/40-verification-documentation/40-SUMMARY.md` exits 0.
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 3: Run final gates and complete summary</name>
  <files>.planning/phases/40-verification-documentation/40-SUMMARY.md</files>
  <read_first>
    .planning/phases/40-verification-documentation/40-SUMMARY.md
    .planning/phases/40-verification-documentation/40-VALIDATION.md
    .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
    Run `rtk mix test` and `rtk mix precommit`. Record exact commands, exit codes, and final result under `## Final Gate Evidence` in `40-SUMMARY.md`. If either command fails, record the blocker as pre-existing/non-regressed only if evidence proves it; include owner/approval status. Update every row in the carry-forward disposition register to `Fixed`, `Excluded`, or `Blocking` with an evidence link or command.
  </action>
  <acceptance_criteria>
    - `rtk mix test` exits 0.
    - `rtk mix precommit` exits 0, or `40-SUMMARY.md` contains `pre-existing`, `non-regressed`, `owner`, and `approved as not blocking`.
    - `rtk rg -n "Final Gate Evidence|rtk mix test|rtk mix precommit|Fixed|Excluded|Blocking" .planning/phases/40-verification-documentation/40-SUMMARY.md` exits 0.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run docs grep checks, render smoke commands, `rtk mix test`, and `rtk mix precommit`.
</verification>

<success_criteria>
- TUI screen contract guidance exists and is linked from the TUI/widget documentation surface.
- Render smoke evidence, full test evidence, and precommit evidence are recorded.
- Every Phase 39 carry-forward item has a final disposition in `40-SUMMARY.md`.
</success_criteria>
